#pragma clang diagnostic ignored "-Wmissing-prototypes"

#include <metal_stdlib>
#include <simd/simd.h>

using namespace metal;

const device float* thread * constant _9 = {};

static inline __attribute__((always_inline))
void _10(const device float* thread * const thread & _11)
{
}

vertex void main0()
{
    const device float* thread * _14 = _9;
    _10(_14);
}

