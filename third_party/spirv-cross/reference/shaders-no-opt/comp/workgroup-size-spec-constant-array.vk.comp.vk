#version 450
layout(local_size_x_id = 0, local_size_y = 1, local_size_z = 1) in;

layout(constant_id = 0) const int A = 2;
const uint _20 = (uint(A) + 0u);
const uint _25 = gl_WorkGroupSize.x;
const uint _26 = (_20 * _25);
const uint _30 = (uint(A) + 0u);
const uint _31 = gl_WorkGroupSize.x;
const uint _32 = (_30 * _31);

layout(set = 0, binding = 0, std430) buffer SSBO
{
    int I;
    float V;
} _48;

float D[_32];

void main()
{
    for (int i = 0; uint(i) < _26; i++)
    {
        D[i] = 1.0 + float(i);
    }
    _48.V = D[_48.I] + D[_48.I ^ 1];
}

