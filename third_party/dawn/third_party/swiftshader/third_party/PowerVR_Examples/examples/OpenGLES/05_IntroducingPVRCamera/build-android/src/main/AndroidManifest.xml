<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.powervr.OpenGLESIntroducingPVRCamera"
        android:versionCode="1"
        android:versionName="1.0">

    <uses-feature android:glEsVersion="0x00020000"/>

    <!-- 
        The INTERNET permission is included to allow PVRTrace and PVRScope to use 
        their renderstate override/remote capabilities with this application 
    -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- 
        The WRITE_EXTERNAL_STORAGE permission is included so PVRTrace 
        can write trace files to external storage when recording this application
    --> 
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <!-- 
        The CAMERA permission is included for access to the camera
    -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />
    <application
            android:allowBackup="true"
            android:icon="@drawable/ic_launcher_icon"
            android:label="@string/app_name" >
        <activity android:name="OpenGLESIntroducingPVRCamera"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen"
            android:launchMode="singleTask"
            android:configChanges="orientation|keyboardHidden"
            android:screenOrientation="landscape"
            android:clearTaskOnLaunch="true">
            <meta-data android:name="android.app.lib_name" android:value="OpenGLESIntroducingPVRCamera" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>