if(PVR_BUILD_OPENGLES_EXAMPLES)
	if(EXISTS "${CMAKE_CURRENT_LIST_DIR}/OpenGLES/CMakeLists.txt")
		add_subdirectory("${CMAKE_CURRENT_LIST_DIR}/OpenGLES")
	endif()
endif()

if(((${MOLTENVK_FOUND}) OR (NOT APPLE)) AND NOT CMAKE_SYSTEM_NAME MATCHES "QNX")
	if(PVR_BUILD_VULKAN_EXAMPLES)
		if(EXISTS ${CMAKE_CURRENT_LIST_DIR}/Vulkan/CMakeLists.txt)
			add_subdirectory("${CMAKE_CURRENT_LIST_DIR}/Vulkan")
		endif()
	endif()
endif()

if(NOT APPLE)
	if(PVR_BUILD_OPENCL_EXAMPLES)
		if(EXISTS "${CMAKE_CURRENT_LIST_DIR}/OpenCL/CMakeLists.txt")
			add_subdirectory("${CMAKE_CURRENT_LIST_DIR}/OpenCL")
		endif()
		if(EXISTS "${CMAKE_CURRENT_LIST_DIR}/OpenGLES/OpenCLExample/CMakeLists.txt" AND NOT TARGET OpenGLESOpenCLExample)
			add_subdirectory("${CMAKE_CURRENT_LIST_DIR}/OpenGLES/OpenCLExample")
		endif()
	endif()
endif()