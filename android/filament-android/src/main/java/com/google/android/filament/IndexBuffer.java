/*
 * Copyright (C) 2026 The Android Open Source Project
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *      http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

// THIS FILE IS GENERATED BY APIGEN AND MUST NOT BE HAND-MODIFIED.

package com.google.android.filament;

import java.lang.IllegalStateException;

import java.nio.Buffer;
import java.nio.BufferOverflowException;

import androidx.annotation.IntRange;
import androidx.annotation.NonNull;
import androidx.annotation.Nullable;
import androidx.annotation.RestrictTo;

/**
 * A buffer containing vertex indices into a VertexBuffer.
 *
 * <p>Indices can be 16 or 32 bit.
 * The buffer itself is a GPU resource, therefore mutating the data can be relatively slow.
 * Typically these buffers are constant.</p>
 *
 * <p>It is possible, and even encouraged, to use a single index buffer for several Renderables.</p>
 *
 * @see VertexBuffer
 * @see RenderableManager
 */
public class IndexBuffer {
    private long mNativeObject;

    /**
     * Outcome of an asynchronous operation, reported to its completion callback.
     *
     * <p>A completion callback that cannot say why it fired is ambiguous: chaining another operation from
     * a callback that fired because the operation was canceled would proceed on a resource that was
     * never populated. The caller cannot reconstruct the answer out of band either, because an
     * operation can be canceled without anyone asking for it (the driver dropping queued work while
     * shutting down).</p>
     *
     * @see AsyncCallback
     * @see #cancelAsyncJob
     */
    public enum AsyncCallStatus {
        /** The operation ran to completion. */
        COMPLETED,
        /** The operation never ran: it was canceled, or dropped because the driver is shutting down. */
        CANCELED;

        /** @return the value of this enum constant as used by the native Filament engine. */
        public int toFilamentNative() { return ordinal(); }
    }

    /** Type of the index buffer */
    public enum IndexType {
        /** 16-bit indices */
        USHORT(12),
        /** 32-bit indices */
        UINT(17);

        private final int mValue;

        IndexType(int value) {
            mValue = value;
        }

        /** @return the value of this enum constant as used by the native Filament engine. */
        public int toFilamentNative() { return mValue; }

        public static IndexType from(int value) {
            switch (value) {
                case 12: return USHORT;
                case 17: return UINT;
                default: throw new IllegalArgumentException("Unknown IndexType value: " + value);
            }
        }
    }

    IndexBuffer(long nativeObject) {
        mNativeObject = nativeObject;
    }

    @NonNull
    @RestrictTo(RestrictTo.Scope.LIBRARY_GROUP)
    public static IndexBuffer wrap(long nativeObject) {
        return new IndexBuffer(nativeObject);
    }

    public static class Builder {
        @SuppressWarnings({"FieldCanBeLocal", "UnusedDeclaration"}) // Keep to finalize native resources
        private final BuilderFinalizer mFinalizer;
        private final long mNativeBuilder;

        /** Type of the index buffer */
        public enum IndexType {
            /** 16-bit indices */
            USHORT(12),
            /** 32-bit indices */
            UINT(17);

            private final int mValue;

            IndexType(int value) {
                mValue = value;
            }

            /** @return the value of this enum constant as used by the native Filament engine. */
            public int toFilamentNative() { return mValue; }

            public static IndexType from(int value) {
                switch (value) {
                    case 12: return USHORT;
                    case 17: return UINT;
                    default: throw new IllegalArgumentException("Unknown IndexType value: " + value);
                }
            }
        }

        public Builder() {
            mNativeBuilder = nCreateBuilder();
            mFinalizer = new BuilderFinalizer(mNativeBuilder);
        }

        long getNativeBuilder() {
            return mNativeBuilder;
        }

        /**
         * Size of the index buffer in elements.
         *
         * @param indexCount Number of indices the IndexBuffer can hold.
         *
         * @return A reference to this Builder for chaining calls.
         */
        @NonNull
        public Builder indexCount(@IntRange(from = 0) int indexCount) {
            nBuilderIndexCount(mNativeBuilder, indexCount);
            return this;
        }

        /**
         * Type of the index buffer, 16-bit or 32-bit.
         *
         * @param indexType Type of indices stored in the IndexBuffer.
         *
         * @return A reference to this Builder for chaining calls.
         */
        @NonNull
        public Builder bufferType(@NonNull IndexType indexType) {
            nBuilderBufferType(mNativeBuilder, indexType.toFilamentNative());
            return this;
        }

        /**
         * Associate an optional name with this IndexBuffer for debugging purposes.
         *
         * <p>This method should be avoided in C++ in favor of the {@code StaticString} overload.
         * It is provided primarily for bindings to other languages.</p>
         *
         * @param name A string to identify this IndexBuffer
         *
         * @return This Builder, for chaining calls.
         */
        @NonNull
        public Builder name(@NonNull String name) {
            nBuilderName(mNativeBuilder, name);
            return this;
        }

        /**
         * Creates the IndexBuffer object and returns a pointer to it.
         *
         * <p>After creation, the index
         * buffer is uninitialized. Use IndexBuffer::setBuffer() to initialize the IndexBuffer.</p>
         *
         * @param engine Reference to the filament::Engine to associate this IndexBuffer with.
         *
         * @return pointer to the newly created object.
         *
         * @see IndexBuffer#setBuffer
         */
        @NonNull
        public IndexBuffer build(@NonNull Engine engine) {
            long nativeIndexBuffer = nBuilderBuild(mNativeBuilder, engine.getNativeObject());
            if (nativeIndexBuffer == 0) throw new IllegalStateException("Couldn't create IndexBuffer");
            return new IndexBuffer(nativeIndexBuffer);
        }

        private static class BuilderFinalizer {
            private final long mNativeObject;

            BuilderFinalizer(long nativeObject) { mNativeObject = nativeObject; }

            @Override
            public void finalize() {
                try {
                    super.finalize();
                } catch (Throwable t) { // Ignore
                } finally {
                    nDestroyBuilder(mNativeObject);
                }
            }
        }
    }

    /**
     * Copy-initializes a region of this IndexBuffer from the data provided.
     *
     * @param engine Reference to the filament::Engine to associate this IndexBuffer with.
     * @param buffer A BufferDescriptor representing the data used to initialize the IndexBuffer.
     *               BufferDescriptor points to raw, untyped data that will be interpreted as
     *               either 16-bit or 32-bits indices based on the Type of this IndexBuffer.
     */
    public void setBuffer(@NonNull Engine engine, @NonNull Buffer buffer) {
        setBuffer(engine, buffer, 0, 0, null, null);
    }

    /**
     * Copy-initializes a region of this IndexBuffer from the data provided.
     *
     * @param engine Reference to the filament::Engine to associate this IndexBuffer with.
     * @param buffer A BufferDescriptor representing the data used to initialize the IndexBuffer.
     *               BufferDescriptor points to raw, untyped data that will be interpreted as
     *               either 16-bit or 32-bits indices based on the Type of this IndexBuffer.
     * @param destOffsetInBytes Offset in <i>bytes</i> into the IndexBuffer. Must be multiple of 4.
     * @param count number of bytes to copy
     */
    public void setBuffer(@NonNull Engine engine, @NonNull Buffer buffer,
            @IntRange(from = 0) int destOffsetInBytes, @IntRange(from = 0) int count) {
        setBuffer(engine, buffer, destOffsetInBytes, count, null, null);
    }

    /**
     * Copy-initializes a region of this IndexBuffer from the data provided.
     *
     * @param engine Reference to the filament::Engine to associate this IndexBuffer with.
     * @param buffer A BufferDescriptor representing the data used to initialize the IndexBuffer.
     *               BufferDescriptor points to raw, untyped data that will be interpreted as
     *               either 16-bit or 32-bits indices based on the Type of this IndexBuffer.
     * @param destOffsetInBytes Offset in <i>bytes</i> into the IndexBuffer. Must be multiple of 4.
     * @param count number of bytes to copy
     * @param handler handler to dispatch the callback or null for the default handler
     * @param callback runnable called upon completion of the operation
     */
    public void setBuffer(@NonNull Engine engine, @NonNull Buffer buffer,
            @IntRange(from = 0) int destOffsetInBytes, @IntRange(from = 0) int count,
            @Nullable Object handler, @Nullable Runnable callback) {
        int result = nSetBuffer(getNativeObject(), engine.getNativeObject(), buffer, buffer.remaining(),
                destOffsetInBytes, count == 0 ? buffer.remaining() : count, handler, callback);
        if (result < 0) {
            throw new BufferOverflowException();
        }
    }

    /**
     * Returns the size of this IndexBuffer in elements.
     *
     * @return The number of indices the IndexBuffer holds.
     */
    @IntRange(from = 0)
    public int getIndexCount() {
        return nGetIndexCount(getNativeObject());
    }

    /**
     * This non-blocking method checks if the resource has finished creation <i>successfully</i>.
     *
     * <p>If the
     * resource creation was initiated asynchronously, it will return true only after all related
     * asynchronous tasks are complete, and only if none of them was canceled. If the resource was
     * created normally without using async method, it will always return true.</p>
     *
     * <p>A canceled asynchronous creation never populates the resource, so this method keeps returning
     * false for it. The object itself remains valid and must still be destroyed as usual.</p>
     *
     * @return Whether the resource is created and usable.
     *
     * @see Builder#async
     */
    public boolean isCreationComplete() {
        return nIsCreationComplete(getNativeObject());
    }

    public long getNativeObject() {
        if (mNativeObject == 0) {
            throw new IllegalStateException("Calling method on destroyed IndexBuffer");
        }
        return mNativeObject;
    }

    void clearNativeObject() {
        mNativeObject = 0;
    }

    private static native int nSetBuffer(long nativeIndexBuffer, long nativeEngine, Buffer buffer, int remaining, int destOffsetInBytes, int count, Object handler, Runnable callback);
    @IntRange(from = 0)
    private static native int nGetIndexCount(long nativeIndexBuffer);
    private static native boolean nIsCreationComplete(long nativeIndexBuffer);
    private static native long nCreateBuilder();
    private static native void nDestroyBuilder(long nativeBuilder);
    private static native void nBuilderIndexCount(long nativeBuilder, int indexCount);
    private static native void nBuilderBufferType(long nativeBuilder, int indexType);
    private static native void nBuilderName(long nativeBuilder, String name);
    private static native long nBuilderBuild(long nativeBuilder, long nativeEngine);
}

// THIS FILE IS GENERATED BY APIGEN AND MUST NOT BE HAND-MODIFIED.
