/*
 * Copyright (C) 2026 The Android Open Source Project
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *      http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

// THIS FILE IS GENERATED BY APIGEN AND MUST NOT BE HAND-MODIFIED.

package com.google.android.filament;

import java.lang.IllegalStateException;

import java.nio.Buffer;
import java.nio.BufferOverflowException;

import androidx.annotation.IntRange;
import androidx.annotation.NonNull;
import androidx.annotation.Nullable;
import androidx.annotation.RestrictTo;

/**
 * Holds a set of buffers that define the geometry of a Renderable.
 *
 * <p>The geometry of the Renderable itself is defined by a set of vertex attributes such as
 * position, color, normals, tangents, etc...</p>
 *
 * <p>There is no need to have a 1-to-1 mapping between attributes and buffer. A buffer can hold the
 * data of several attributes -- attributes are then referred as being "interleaved".</p>
 *
 * <p>The buffers themselves are GPU resources, therefore mutating their data can be relatively slow.
 * For this reason, it is best to separate the constant data from the dynamic data into multiple
 * buffers.</p>
 *
 * <p>It is possible, and even encouraged, to use a single vertex buffer for several Renderables.</p>
 *
 * @see IndexBuffer
 * @see RenderableManager
 */
public class VertexBuffer {
    private long mNativeObject;

    /** Vertex attribute types */
    public enum VertexAttribute {
        /** XYZ position (float3) */
        POSITION(0),
        /** tangent, bitangent and normal, encoded as a quaternion (float4) */
        TANGENTS(1),
        /** vertex color (float4) */
        COLOR(2),
        /** texture coordinates (float2) */
        UV0(3),
        /** texture coordinates (float2) */
        UV1(4),
        /** indices of 4 bones, as unsigned integers (uvec4) */
        BONE_INDICES(5),
        /** weights of the 4 bones (normalized float4) */
        BONE_WEIGHTS(6),
        CUSTOM0(8),
        CUSTOM1(9),
        CUSTOM2(10),
        CUSTOM3(11),
        CUSTOM4(12),
        CUSTOM5(13),
        CUSTOM6(14),
        CUSTOM7(15),
        MORPH_POSITION_0(8),
        MORPH_POSITION_1(9),
        MORPH_POSITION_2(10),
        MORPH_POSITION_3(11),
        MORPH_TANGENTS_0(12),
        MORPH_TANGENTS_1(13),
        MORPH_TANGENTS_2(14),
        MORPH_TANGENTS_3(15);

        private final int mValue;

        VertexAttribute(int value) {
            mValue = value;
        }

        /** @return the value of this enum constant as used by the native Filament engine. */
        public int toFilamentNative() { return mValue; }

        public static VertexAttribute from(int value) {
            switch (value) {
                case 0: return POSITION;
                case 1: return TANGENTS;
                case 2: return COLOR;
                case 3: return UV0;
                case 4: return UV1;
                case 5: return BONE_INDICES;
                case 6: return BONE_WEIGHTS;
                case 8: return CUSTOM0;
                case 9: return CUSTOM1;
                case 10: return CUSTOM2;
                case 11: return CUSTOM3;
                case 12: return CUSTOM4;
                case 13: return CUSTOM5;
                case 14: return CUSTOM6;
                case 15: return CUSTOM7;
                default: throw new IllegalArgumentException("Unknown VertexAttribute value: " + value);
            }
        }
    }

    /** Supported element types */
    public enum AttributeType {
        BYTE,
        BYTE2,
        BYTE3,
        BYTE4,
        UBYTE,
        UBYTE2,
        UBYTE3,
        UBYTE4,
        SHORT,
        SHORT2,
        SHORT3,
        SHORT4,
        USHORT,
        USHORT2,
        USHORT3,
        USHORT4,
        INT,
        UINT,
        FLOAT,
        FLOAT2,
        FLOAT3,
        FLOAT4,
        HALF,
        HALF2,
        HALF3,
        HALF4;

        /** @return the value of this enum constant as used by the native Filament engine. */
        public int toFilamentNative() { return ordinal(); }
    }

    /**
     * Outcome of an asynchronous operation, reported to its completion callback.
     *
     * <p>A completion callback that cannot say why it fired is ambiguous: chaining another operation from
     * a callback that fired because the operation was canceled would proceed on a resource that was
     * never populated. The caller cannot reconstruct the answer out of band either, because an
     * operation can be canceled without anyone asking for it (the driver dropping queued work while
     * shutting down).</p>
     *
     * @see AsyncCallback
     * @see #cancelAsyncJob
     */
    public enum AsyncCallStatus {
        /** The operation ran to completion. */
        COMPLETED,
        /** The operation never ran: it was canceled, or dropped because the driver is shutting down. */
        CANCELED;

        /** @return the value of this enum constant as used by the native Filament engine. */
        public int toFilamentNative() { return ordinal(); }
    }

    VertexBuffer(long nativeObject) {
        mNativeObject = nativeObject;
    }

    @NonNull
    @RestrictTo(RestrictTo.Scope.LIBRARY_GROUP)
    public static VertexBuffer wrap(long nativeObject) {
        return new VertexBuffer(nativeObject);
    }

    public static class Builder {
        @SuppressWarnings({"FieldCanBeLocal", "UnusedDeclaration"}) // Keep to finalize native resources
        private final BuilderFinalizer mFinalizer;
        private final long mNativeBuilder;

        public Builder() {
            mNativeBuilder = nCreateBuilder();
            mFinalizer = new BuilderFinalizer(mNativeBuilder);
        }

        long getNativeBuilder() {
            return mNativeBuilder;
        }

        /**
         * Defines how many buffers will be created in this vertex buffer set.
         *
         * <p>These buffers are
         * later referenced by index from 0 to {@code bufferCount} - 1.</p>
         *
         * <p>This call is mandatory. The default is 0.</p>
         *
         * @param bufferCount Number of buffers in this vertex buffer set. The maximum value is 8.
         *
         * @return A reference to this Builder for chaining calls.
         */
        @NonNull
        public Builder bufferCount(@IntRange(from = 0) int bufferCount) {
            nBuilderBufferCount(mNativeBuilder, bufferCount);
            return this;
        }

        /**
         * Size of each buffer in the set in vertex.
         *
         * @param vertexCount Number of vertices in each buffer in this set.
         *
         * @return A reference to this Builder for chaining calls.
         */
        @NonNull
        public Builder vertexCount(@IntRange(from = 0) int vertexCount) {
            nBuilderVertexCount(mNativeBuilder, vertexCount);
            return this;
        }

        /**
         * Allows buffers to be swapped out and shared using BufferObject.
         *
         * <p>If buffer objects mode is enabled, clients must call setBufferObjectAt rather than
         * setBufferAt. This allows sharing of data between VertexBuffer objects, but it may
         * slightly increase the memory footprint of Filament's internal bookkeeping.</p>
         */
        @NonNull
        public Builder enableBufferObjects() {
            return enableBufferObjects(true);
        }

        /**
         * Allows buffers to be swapped out and shared using BufferObject.
         *
         * <p>If buffer objects mode is enabled, clients must call setBufferObjectAt rather than
         * setBufferAt. This allows sharing of data between VertexBuffer objects, but it may
         * slightly increase the memory footprint of Filament's internal bookkeeping.</p>
         *
         * @param enabled If true, enables buffer object mode.  False by default.
         */
        @NonNull
        public Builder enableBufferObjects(boolean enabled) {
            nBuilderEnableBufferObjects(mNativeBuilder, enabled);
            return this;
        }

        /**
         * Sets up an attribute for this vertex buffer set.
         *
         * <p>Using {@code byteOffset} and {@code byteStride}, attributes can be interleaved in the same buffer.</p>
         *
         * <p>VertexAttribute::TANGENTS must be specified as a quaternion and is how normals
         *          are specified.</p>
         *
         * <p>Not all backends support 3-component attributes that are not floats. For help
         *          with conversion, see geometry::Transcoder.</p>
         *
         * @param attribute The attribute to set up.
         * @param bufferIndex The index of the buffer containing the data for this attribute. Must
         *                     be between 0 and bufferCount() - 1.
         * @param attributeType The type of the attribute data (e.g. byte, float3, etc...)
         *
         * @return <p>A reference to this Builder for chaining calls.</p>
         *
         * <p>This is a no-op if the {@code attribute} is an invalid enum.
         * This is a no-op if the {@code bufferIndex} is out of bounds.</p>
         *
         * @see VertexAttribute
         */
        @NonNull
        public Builder attribute(@NonNull VertexAttribute attribute, @IntRange(from = 0) int bufferIndex, @NonNull AttributeType attributeType) {
            return attribute(attribute, bufferIndex, attributeType, 0, 0);
        }

        /**
         * Sets up an attribute for this vertex buffer set.
         *
         * <p>Using {@code byteOffset} and {@code byteStride}, attributes can be interleaved in the same buffer.</p>
         *
         * <p>VertexAttribute::TANGENTS must be specified as a quaternion and is how normals
         *          are specified.</p>
         *
         * <p>Not all backends support 3-component attributes that are not floats. For help
         *          with conversion, see geometry::Transcoder.</p>
         *
         * @param attribute The attribute to set up.
         * @param bufferIndex The index of the buffer containing the data for this attribute. Must
         *                     be between 0 and bufferCount() - 1.
         * @param attributeType The type of the attribute data (e.g. byte, float3, etc...)
         * @param byteOffset Offset in <i>bytes</i> into the buffer {@code bufferIndex}
         *
         * @return <p>A reference to this Builder for chaining calls.</p>
         *
         * <p>This is a no-op if the {@code attribute} is an invalid enum.
         * This is a no-op if the {@code bufferIndex} is out of bounds.</p>
         *
         * @see VertexAttribute
         */
        @NonNull
        public Builder attribute(@NonNull VertexAttribute attribute, @IntRange(from = 0) int bufferIndex, @NonNull AttributeType attributeType, @IntRange(from = 0) int byteOffset) {
            return attribute(attribute, bufferIndex, attributeType, byteOffset, 0);
        }

        /**
         * Sets up an attribute for this vertex buffer set.
         *
         * <p>Using {@code byteOffset} and {@code byteStride}, attributes can be interleaved in the same buffer.</p>
         *
         * <p>VertexAttribute::TANGENTS must be specified as a quaternion and is how normals
         *          are specified.</p>
         *
         * <p>Not all backends support 3-component attributes that are not floats. For help
         *          with conversion, see geometry::Transcoder.</p>
         *
         * @param attribute The attribute to set up.
         * @param bufferIndex The index of the buffer containing the data for this attribute. Must
         *                     be between 0 and bufferCount() - 1.
         * @param attributeType The type of the attribute data (e.g. byte, float3, etc...)
         * @param byteOffset Offset in <i>bytes</i> into the buffer {@code bufferIndex}
         * @param byteStride Stride in <i>bytes</i> to the next element of this attribute. When set to
         *                   zero the attribute size, as defined by {@code attributeType} is used.
         *
         * @return <p>A reference to this Builder for chaining calls.</p>
         *
         * <p>This is a no-op if the {@code attribute} is an invalid enum.
         * This is a no-op if the {@code bufferIndex} is out of bounds.</p>
         *
         * @see VertexAttribute
         */
        @NonNull
        public Builder attribute(@NonNull VertexAttribute attribute, @IntRange(from = 0) int bufferIndex, @NonNull AttributeType attributeType, @IntRange(from = 0) int byteOffset, @IntRange(from = 0) int byteStride) {
            nBuilderAttribute(mNativeBuilder, attribute.toFilamentNative(), bufferIndex, attributeType.toFilamentNative(), byteOffset, byteStride);
            return this;
        }

        /**
         * Sets whether a given attribute should be normalized.
         *
         * <p>By default attributes are not
         * normalized. A normalized attribute is mapped between 0 and 1 in the shader. This applies
         * only to integer types.</p>
         *
         * @param attribute Enum of the attribute to set the normalization flag to.
         *
         * @return <p>A reference to this Builder for chaining calls.</p>
         *
         * <p>This is a no-op if the {@code attribute} is an invalid enum.</p>
         */
        @NonNull
        public Builder normalized(@NonNull VertexAttribute attribute) {
            return normalized(attribute, true);
        }

        /**
         * Sets whether a given attribute should be normalized.
         *
         * <p>By default attributes are not
         * normalized. A normalized attribute is mapped between 0 and 1 in the shader. This applies
         * only to integer types.</p>
         *
         * @param attribute Enum of the attribute to set the normalization flag to.
         * @param normalized true to automatically normalize the given attribute.
         *
         * @return <p>A reference to this Builder for chaining calls.</p>
         *
         * <p>This is a no-op if the {@code attribute} is an invalid enum.</p>
         */
        @NonNull
        public Builder normalized(@NonNull VertexAttribute attribute, boolean normalized) {
            nBuilderNormalized(mNativeBuilder, attribute.toFilamentNative(), normalized);
            return this;
        }

        /**
         * Sets advanced skinning mode.
         *
         * <p>Bone data, indices and weights will be
         * set in RenderableManager:Builder:boneIndicesAndWeights methods.
         * Works with or without buffer objects.</p>
         *
         * @param enabled If true, enables advanced skinning mode. False by default.
         *
         * @return A reference to this Builder for chaining calls.
         *
         * @see RenderableManager.Builder#boneIndicesAndWeights
         */
        @NonNull
        public Builder advancedSkinning(boolean enabled) {
            nBuilderAdvancedSkinning(mNativeBuilder, enabled);
            return this;
        }

        /**
         * Associate an optional name with this VertexBuffer for debugging purposes.
         *
         * <p>This method should be avoided in C++ in favor of the {@code StaticString} overload.
         * It is provided primarily for bindings to other languages.</p>
         *
         * @param name A string to identify this VertexBuffer
         *
         * @return This Builder, for chaining calls.
         */
        @NonNull
        public Builder name(@NonNull String name) {
            nBuilderName(mNativeBuilder, name);
            return this;
        }

        /**
         * Creates the VertexBuffer object and returns a pointer to it.
         *
         * @param engine Reference to the filament::Engine to associate this VertexBuffer with.
         *
         * @return pointer to the newly created object.
         */
        @NonNull
        public VertexBuffer build(@NonNull Engine engine) {
            long nativeVertexBuffer = nBuilderBuild(mNativeBuilder, engine.getNativeObject());
            if (nativeVertexBuffer == 0) throw new IllegalStateException("Couldn't create VertexBuffer");
            return new VertexBuffer(nativeVertexBuffer);
        }

        private static class BuilderFinalizer {
            private final long mNativeObject;

            BuilderFinalizer(long nativeObject) { mNativeObject = nativeObject; }

            @Override
            public void finalize() {
                try {
                    super.finalize();
                } catch (Throwable t) { // Ignore
                } finally {
                    nDestroyBuilder(mNativeObject);
                }
            }
        }
    }

    /**
     * Returns the vertex count.
     *
     * @return Number of vertices in this vertex buffer set.
     */
    @IntRange(from = 0)
    public int getVertexCount() {
        return nGetVertexCount(getNativeObject());
    }

    /**
     * copy-initializes the specified buffer from the given buffer data.
     *
     * <p>Do not use this if you called enableBufferObjects() on the Builder.</p>
     *
     * @param engine Reference to the filament::Engine to associate this VertexBuffer with.
     * @param bufferIndex Index of the buffer to initialize. Must be between 0
     *                    and Builder::bufferCount() - 1.
     * @param buffer A BufferDescriptor representing the data used to initialize the buffer at
     *               index {@code bufferIndex}. BufferDescriptor points to raw, untyped data that will
     *               be copied as-is into the buffer.
     */
    public void setBufferAt(@NonNull Engine engine, @IntRange(from = 0) int bufferIndex, @NonNull Buffer buffer) {
        setBufferAt(engine, bufferIndex, buffer, 0, 0, null, null);
    }

    /**
     * copy-initializes the specified buffer from the given buffer data.
     *
     * <p>Do not use this if you called enableBufferObjects() on the Builder.</p>
     *
     * @param engine Reference to the filament::Engine to associate this VertexBuffer with.
     * @param bufferIndex Index of the buffer to initialize. Must be between 0
     *                    and Builder::bufferCount() - 1.
     * @param buffer A BufferDescriptor representing the data used to initialize the buffer at
     *               index {@code bufferIndex}. BufferDescriptor points to raw, untyped data that will
     *               be copied as-is into the buffer.
     * @param destOffsetInBytes Offset in <i>bytes</i> into the buffer at index {@code bufferIndex} of this vertex
     *                   buffer set.  Must be multiple of 4.
     * @param count number of bytes to copy
     */
    public void setBufferAt(@NonNull Engine engine, @IntRange(from = 0) int bufferIndex, @NonNull Buffer buffer,
            @IntRange(from = 0) int destOffsetInBytes, @IntRange(from = 0) int count) {
        setBufferAt(engine, bufferIndex, buffer, destOffsetInBytes, count, null, null);
    }

    /**
     * copy-initializes the specified buffer from the given buffer data.
     *
     * <p>Do not use this if you called enableBufferObjects() on the Builder.</p>
     *
     * @param engine Reference to the filament::Engine to associate this VertexBuffer with.
     * @param bufferIndex Index of the buffer to initialize. Must be between 0
     *                    and Builder::bufferCount() - 1.
     * @param buffer A BufferDescriptor representing the data used to initialize the buffer at
     *               index {@code bufferIndex}. BufferDescriptor points to raw, untyped data that will
     *               be copied as-is into the buffer.
     * @param destOffsetInBytes Offset in <i>bytes</i> into the buffer at index {@code bufferIndex} of this vertex
     *                   buffer set.  Must be multiple of 4.
     * @param count number of bytes to copy
     * @param handler handler to dispatch the callback or null for the default handler
     * @param callback runnable called upon completion of the operation
     */
    public void setBufferAt(@NonNull Engine engine, @IntRange(from = 0) int bufferIndex, @NonNull Buffer buffer,
            @IntRange(from = 0) int destOffsetInBytes, @IntRange(from = 0) int count,
            @Nullable Object handler, @Nullable Runnable callback) {
        int result = nSetBufferAt(getNativeObject(), engine.getNativeObject(), bufferIndex, buffer, buffer.remaining(),
                destOffsetInBytes, count == 0 ? buffer.remaining() : count, handler, callback);
        if (result < 0) {
            throw new BufferOverflowException();
        }
    }

    /**
     * Swaps in the given buffer object.
     *
     * <p>To use this, you must first call enableBufferObjects() on the Builder.</p>
     *
     * @param engine Reference to the filament::Engine to associate this VertexBuffer with.
     * @param bufferIndex Index of the buffer to initialize. Must be between 0
     *                    and Builder::bufferCount() - 1.
     * @param bufferObject The handle to the GPU data that will be used in this buffer slot.
     */
    public void setBufferObjectAt(@NonNull Engine engine, @IntRange(from = 0) int bufferIndex, @NonNull BufferObject bufferObject) {
        nSetBufferObjectAt(getNativeObject(), engine.getNativeObject(), bufferIndex, bufferObject.getNativeObject());
    }

    /**
     * This non-blocking method checks if the resource has finished creation <i>successfully</i>.
     *
     * <p>If the
     * resource creation was initiated asynchronously, it will return true only after all related
     * asynchronous tasks are complete, and only if none of them was canceled. If the resource was
     * created normally without using async method, it will always return true.</p>
     *
     * <p>A canceled asynchronous creation never populates the resource, so this method keeps returning
     * false for it. The object itself remains valid and must still be destroyed as usual.</p>
     *
     * @return Whether the resource is created and usable.
     *
     * @see Builder#async
     */
    public boolean isCreationComplete() {
        return nIsCreationComplete(getNativeObject());
    }

    public long getNativeObject() {
        if (mNativeObject == 0) {
            throw new IllegalStateException("Calling method on destroyed VertexBuffer");
        }
        return mNativeObject;
    }

    void clearNativeObject() {
        mNativeObject = 0;
    }

    @IntRange(from = 0)
    private static native int nGetVertexCount(long nativeVertexBuffer);
    private static native int nSetBufferAt(long nativeVertexBuffer, long nativeEngine, int bufferIndex, Buffer buffer, int remaining, int destOffsetInBytes, int count, Object handler, Runnable callback);
    private static native void nSetBufferObjectAt(long nativeVertexBuffer, long nativeEngine, @IntRange(from = 0) int bufferIndex, long nativeBufferObject);
    private static native boolean nIsCreationComplete(long nativeVertexBuffer);
    private static native long nCreateBuilder();
    private static native void nDestroyBuilder(long nativeBuilder);
    private static native void nBuilderBufferCount(long nativeBuilder, int bufferCount);
    private static native void nBuilderVertexCount(long nativeBuilder, int vertexCount);
    private static native void nBuilderEnableBufferObjects(long nativeBuilder, boolean enabled);
    private static native void nBuilderAttribute(long nativeBuilder, int attribute, int bufferIndex, int attributeType, int byteOffset, int byteStride);
    private static native void nBuilderNormalized(long nativeBuilder, int attribute, boolean normalized);
    private static native void nBuilderAdvancedSkinning(long nativeBuilder, boolean enabled);
    private static native void nBuilderName(long nativeBuilder, String name);
    private static native long nBuilderBuild(long nativeBuilder, long nativeEngine);
}

// THIS FILE IS GENERATED BY APIGEN AND MUST NOT BE HAND-MODIFIED.
