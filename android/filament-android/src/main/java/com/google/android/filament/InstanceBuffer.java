/*
 * Copyright (C) 2026 The Android Open Source Project
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *      http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

// THIS FILE IS GENERATED BY APIGEN AND MUST NOT BE HAND-MODIFIED.

package com.google.android.filament;

import java.lang.IllegalStateException;

import androidx.annotation.IntRange;
import androidx.annotation.NonNull;
import androidx.annotation.Nullable;
import androidx.annotation.RestrictTo;
import androidx.annotation.Size;

/**
 * InstanceBuffer holds draw (GPU) instance transforms.
 *
 * <p>These can be provided to a renderable to
 * "offset" each draw instance.</p>
 *
 * @see RenderableManager.Builder#instances(int, InstanceBuffer)
 */
public class InstanceBuffer {
    private long mNativeObject;

    InstanceBuffer(long nativeObject) {
        mNativeObject = nativeObject;
    }

    @NonNull
    @RestrictTo(RestrictTo.Scope.LIBRARY_GROUP)
    public static InstanceBuffer wrap(long nativeObject) {
        return new InstanceBuffer(nativeObject);
    }

    public static class Builder {
        @SuppressWarnings({"FieldCanBeLocal", "UnusedDeclaration"}) // Keep to finalize native resources
        private final BuilderFinalizer mFinalizer;
        private final long mNativeBuilder;

        /**
         * @param instanceCount The number of instances this InstanceBuffer will support, must be
         *                      >= 1 and <= \c Engine::getMaxAutomaticInstances()
         *
         * @see Engine#getMaxAutomaticInstances
         */
        public Builder(@IntRange(from = 0) int instanceCount) {
            mNativeBuilder = nCreateBuilder(instanceCount);
            mFinalizer = new BuilderFinalizer(mNativeBuilder);
        }

        long getNativeBuilder() {
            return mNativeBuilder;
        }

        /**
         * Provide an initial local transform for each instance. Each local transform is relative to
         *
         * <p>the transform of the associated renderable. This forms a parent-child relationship
         * between the renderable and its instances, so adjusting the renderable's transform will</p>
         *
         * <ul>
         *   <li>* affect all instances.
         * The array of math::mat4f must have length instanceCount, provided when constructing this
         * Builder.</li>
         * </ul>
         *
         * @param localTransforms an array of math::mat4f with length instanceCount, must remain
         *                        valid until after build() is called
         */
        @NonNull
        public Builder localTransforms(@Nullable @Size(min = 16) float[] localTransforms) {
            nBuilderLocalTransforms(mNativeBuilder, localTransforms);
            return this;
        }

        /**
         * Associate an optional name with this InstanceBuffer for debugging purposes.
         *
         * <p>This method should be avoided in C++ in favor of the {@code StaticString} overload.
         * It is provided primarily for bindings to other languages.</p>
         *
         * @param name A string to identify this InstanceBuffer
         *
         * @return This Builder, for chaining calls.
         */
        @NonNull
        public Builder name(@NonNull String name) {
            nBuilderName(mNativeBuilder, name);
            return this;
        }

        /** Creates the InstanceBuffer object and returns a pointer to it. */
        @NonNull
        public InstanceBuffer build(@NonNull Engine engine) {
            long nativeInstanceBuffer = nBuilderBuild(mNativeBuilder, engine.getNativeObject());
            if (nativeInstanceBuffer == 0) throw new IllegalStateException("Couldn't create InstanceBuffer");
            return new InstanceBuffer(nativeInstanceBuffer);
        }

        private static class BuilderFinalizer {
            private final long mNativeObject;

            BuilderFinalizer(long nativeObject) { mNativeObject = nativeObject; }

            @Override
            public void finalize() {
                try {
                    super.finalize();
                } catch (Throwable t) { // Ignore
                } finally {
                    nDestroyBuilder(mNativeObject);
                }
            }
        }
    }

    /** Returns the instance count specified when building this InstanceBuffer. */
    @IntRange(from = 0)
    public int getInstanceCount() {
        return nGetInstanceCount(getNativeObject());
    }

    /**
     * Sets the local transform for each instance.
     *
     * <p>Each local transform is relative to the transform
     * of the associated renderable. This forms a parent-child relationship between the renderable
     * and its instances, so adjusting the renderable's transform will affect all instances.</p>
     *
     * @param localTransforms an array of math::mat4f with length count, need not outlive this call
     * @param count the number of local transforms
     */
    public void setLocalTransforms(@NonNull @Size(min = 16) float[] localTransforms, @IntRange(from = 0) int count) {
        setLocalTransforms(localTransforms, count, 0);
    }

    /**
     * Sets the local transform for each instance.
     *
     * <p>Each local transform is relative to the transform
     * of the associated renderable. This forms a parent-child relationship between the renderable
     * and its instances, so adjusting the renderable's transform will affect all instances.</p>
     *
     * @param localTransforms an array of math::mat4f with length count, need not outlive this call
     * @param count the number of local transforms
     * @param offset index of the first instance to set local transforms
     */
    public void setLocalTransforms(@NonNull @Size(min = 16) float[] localTransforms, @IntRange(from = 0) int count, @IntRange(from = 0) int offset) {
        Asserts.assertMat4fIn(localTransforms);
        nSetLocalTransforms(getNativeObject(), localTransforms, count, offset);
    }

    /**
     * Returns the local transform for a given instance.
     *
     * @param index The index of the instance.
     *
     * @return The local transform of the instance.
     */
    @NonNull @Size(min = 16)
    public float[] getLocalTransform(@IntRange(from = 0) int index, @Nullable @Size(min = 16) float[] out) {
        out = Asserts.assertMat4f(out);
        nGetLocalTransform(getNativeObject(), index, out);
        return out;
    }

    public long getNativeObject() {
        if (mNativeObject == 0) {
            throw new IllegalStateException("Calling method on destroyed InstanceBuffer");
        }
        return mNativeObject;
    }

    void clearNativeObject() {
        mNativeObject = 0;
    }

    @IntRange(from = 0)
    private static native int nGetInstanceCount(long nativeInstanceBuffer);
    private static native void nSetLocalTransforms(long nativeInstanceBuffer, @NonNull @Size(min = 16) float[] localTransforms, @IntRange(from = 0) int count, @IntRange(from = 0) int offset);
    private static native void nGetLocalTransform(long nativeInstanceBuffer, @IntRange(from = 0) int index, @Size(min = 16) float[] out);
    private static native long nCreateBuilder(int instanceCount);
    private static native void nDestroyBuilder(long nativeBuilder);
    private static native void nBuilderLocalTransforms(long nativeBuilder, float[] localTransforms);
    private static native void nBuilderName(long nativeBuilder, String name);
    private static native long nBuilderBuild(long nativeBuilder, long nativeEngine);
}

// THIS FILE IS GENERATED BY APIGEN AND MUST NOT BE HAND-MODIFIED.
