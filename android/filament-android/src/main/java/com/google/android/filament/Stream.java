/*
 * Copyright (C) 2026 The Android Open Source Project
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *      http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

// THIS FILE IS GENERATED BY APIGEN AND MUST NOT BE HAND-MODIFIED.

package com.google.android.filament;

import java.lang.IllegalStateException;

import androidx.annotation.IntRange;
import androidx.annotation.NonNull;
import androidx.annotation.RestrictTo;

/**
 * Stream is used to attach a video stream to a Filament {@code Texture}.
 *
 * <p>Note that the {@code Stream} class is fairly Android centric. It supports two different
 * configurations:</p>
 *
 * <ul>
 *   <li>ACQUIRED.....connects to an Android AHardwareBuffer</li>
 *   <li>NATIVE.......connects to an Android SurfaceTexture
 * Before explaining these different configurations, let's review the high-level structure of an AR
 * or video application that uses Filament:</li>
 * </ul>
 *
 * <pre>{@code
 * while (true) {
 *
 *     // Misc application work occurs here, such as:
 *     // - Writing the image data for a video frame into a Stream
 *     // - Moving the Filament Camera
 *
 *     if (renderer->beginFrame(swapChain)) {
 *         renderer->render(view);
 *         renderer->endFrame();
 *     }
 * }
 *
 * }</pre>
 *
 * <p>Let's say that the video image data at the time of a particular invocation of {@code beginFrame}
 * becomes visible to users at time A. The 3D scene state (including the camera) at the time of
 * that same invocation becomes apparent to users at time B.</p>
 *
 * <ul>
 *   <li>If time A matches time B, we say that the stream is \em{synchronized}.</li>
 *   <li>Filament invokes low-level graphics commands on the \em{driver thread}.</li>
 *   <li>The thread that calls {@code beginFrame} is called the \em{main thread}.
 * For ACQUIRED streams, there is no need to perform the copy because Filament explicitly acquires
 * the stream, then releases it later via a callback function. This configuration is especially
 * useful when the Vulkan backend is enabled.
 * NATIVE streams are deprecated because they are backend specific and do not make any synchronization guarantee.
 * Please see {@code sample-stream-test} and {@code sample-hello-camera} for usage examples.</li>
 * </ul>
 *
 * @see backend.StreamType
 * @see Texture#setExternalStream
 * @see Engine#destroyStream
 */
public class Stream {
    static final class EnumCache {
        static final StreamType[] sStreamTypeValues = StreamType.values();
    }

    private long mNativeObject;

    /** Stream for external textures */
    public enum StreamType {
        /**
         * Not synchronized but copy-free.
         *
         * <p>Good for video.</p>
         */
        NATIVE,
        /**
         * Synchronized, copy-free, and take a release callback.
         *
         * <p>Good for AR but requires API 26+.</p>
         */
        ACQUIRED;

        /** @return the value of this enum constant as used by the native Filament engine. */
        public int toFilamentNative() { return ordinal(); }
    }

    Stream(long nativeObject) {
        mNativeObject = nativeObject;
    }

    @NonNull
    @RestrictTo(RestrictTo.Scope.LIBRARY_GROUP)
    public static Stream wrap(long nativeObject) {
        return new Stream(nativeObject);
    }

    /**
     * Constructs a Stream object instance.
     *
     * <p>By default, Stream objects are ACQUIRED and must have external images pushed to them via
     * <pre>Stream::setAcquiredImage</pre>.</p>
     *
     * <p>To create a NATIVE stream, call the <pre>stream</pre> method on the builder.</p>
     */
    public static class Builder {
        @SuppressWarnings({"FieldCanBeLocal", "UnusedDeclaration"}) // Keep to finalize native resources
        private final BuilderFinalizer mFinalizer;
        private final long mNativeBuilder;

        public Builder() {
            mNativeBuilder = nCreateBuilder();
            mFinalizer = new BuilderFinalizer(mNativeBuilder);
        }

        long getNativeBuilder() {
            return mNativeBuilder;
        }

        /**
         * @param width initial width of the incoming stream. Whether this value is used is
         *              stream dependent. On Android, it must be set when using
         *              Builder::stream.
         *
         * @return This Builder, for chaining calls.
         */
        @NonNull
        public Builder width(@IntRange(from = 0) int width) {
            nBuilderWidth(mNativeBuilder, width);
            return this;
        }

        /**
         * @param height initial height of the incoming stream. Whether this value is used is
         *              stream dependent. On Android, it must be set when using
         *              Builder::stream.
         *
         * @return This Builder, for chaining calls.
         */
        @NonNull
        public Builder height(@IntRange(from = 0) int height) {
            nBuilderHeight(mNativeBuilder, height);
            return this;
        }

        /**
         * Associate an optional name with this Stream for debugging purposes.
         *
         * <p>This method should be avoided in C++ in favor of the {@code StaticString} overload.
         *       It exists primarily for language bindings and dynamic string generation.</p>
         *
         * @param name A string to identify this Stream
         *
         * @return This Builder, for chaining calls.
         */
        @NonNull
        public Builder name(@NonNull String name) {
            nBuilderName(mNativeBuilder, name);
            return this;
        }

        /**
         * Creates the Stream object and returns a pointer to it.
         *
         * @param engine Reference to the filament::Engine to associate this Stream with.
         *
         * @return pointer to the newly created object.
         */
        @NonNull
        public Stream build(@NonNull Engine engine) {
            long nativeStream = nBuilderBuild(mNativeBuilder, engine.getNativeObject());
            if (nativeStream == 0) throw new IllegalStateException("Couldn't create Stream");
            return new Stream(nativeStream);
        }

        private static class BuilderFinalizer {
            private final long mNativeObject;

            BuilderFinalizer(long nativeObject) { mNativeObject = nativeObject; }

            @Override
            public void finalize() {
                try {
                    super.finalize();
                } catch (Throwable t) { // Ignore
                } finally {
                    nDestroyBuilder(mNativeObject);
                }
            }
        }
    }

    /** Indicates whether this stream is a NATIVE stream or ACQUIRED stream. */
    public StreamType getStreamType() {
        return EnumCache.sStreamTypeValues[nGetStreamType(getNativeObject())];
    }

    /**
     * Updates the size of the incoming stream.
     *
     * <p>Whether this value is used is
     *              stream dependent. On Android, it must be set when using
     *              Builder::stream.</p>
     *
     * @param width new width of the incoming stream
     * @param height new height of the incoming stream
     */
    public void setDimensions(@IntRange(from = 0) int width, @IntRange(from = 0) int height) {
        nSetDimensions(getNativeObject(), width, height);
    }

    /**
     * Returns the presentation time of the currently displayed frame in nanosecond.
     *
     * <p>This value can change at any time.</p>
     *
     * @return timestamp in nanosecond.
     */
    public long getTimestamp() {
        return nGetTimestamp(getNativeObject());
    }

    public long getNativeObject() {
        if (mNativeObject == 0) {
            throw new IllegalStateException("Calling method on destroyed Stream");
        }
        return mNativeObject;
    }

    void clearNativeObject() {
        mNativeObject = 0;
    }

    private static native int nGetStreamType(long nativeStream);
    private static native void nSetDimensions(long nativeStream, @IntRange(from = 0) int width, @IntRange(from = 0) int height);
    private static native long nGetTimestamp(long nativeStream);
    private static native long nCreateBuilder();
    private static native void nDestroyBuilder(long nativeBuilder);
    private static native void nBuilderWidth(long nativeBuilder, int width);
    private static native void nBuilderHeight(long nativeBuilder, int height);
    private static native void nBuilderName(long nativeBuilder, String name);
    private static native long nBuilderBuild(long nativeBuilder, long nativeEngine);
}

// THIS FILE IS GENERATED BY APIGEN AND MUST NOT BE HAND-MODIFIED.
