/*
 * Copyright (C) 2026 The Android Open Source Project
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *      http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

// THIS FILE IS GENERATED BY APIGEN AND MUST NOT BE HAND-MODIFIED.

package com.google.android.filament;

import java.lang.IllegalStateException;

import androidx.annotation.IntRange;
import androidx.annotation.NonNull;
import androidx.annotation.Nullable;
import androidx.annotation.RestrictTo;

/**
 * An offscreen render target that can be associated with a View and contains weak references to a set of attached Texture objects.
 *
 * <p>RenderTarget is intended to be used with the View's post-processing disabled for the most part.
 * especially when a DEPTH attachment is also used (see Builder::texture()).</p>
 *
 * <p>Custom RenderTarget are ultimately intended to render into textures that might be used during
 * the main render pass.</p>
 *
 * <p>Clients are responsible for the lifetime of all associated Texture attachments.</p>
 *
 * @see View
 */
public class RenderTarget {
    static final class EnumCache {
        static final CubemapFace[] sCubemapFaceValues = CubemapFace.values();
    }

    private long mNativeObject;

    /** Texture Cubemap Face */
    public enum CubemapFace {
        /** +x face */
        POSITIVE_X,
        /** -x face */
        NEGATIVE_X,
        /** +y face */
        POSITIVE_Y,
        /** -y face */
        NEGATIVE_Y,
        /** +z face */
        POSITIVE_Z,
        /** -z face */
        NEGATIVE_Z;

        /** @return the value of this enum constant as used by the native Filament engine. */
        public int toFilamentNative() { return ordinal(); }
    }

    /** Attachment identifiers */
    public enum AttachmentPoint {
        /** identifies the 1st color attachment */
        COLOR0(0),
        /** identifies the 2nd color attachment */
        COLOR1(1),
        /** identifies the 3rd color attachment */
        COLOR2(2),
        /** identifies the 4th color attachment */
        COLOR3(3),
        /** identifies the 5th color attachment */
        COLOR4(4),
        /** identifies the 6th color attachment */
        COLOR5(5),
        /** identifies the 7th color attachment */
        COLOR6(6),
        /** identifies the 8th color attachment */
        COLOR7(7),
        /** identifies the depth attachment */
        DEPTH(8),
        /** identifies the 1st color attachment */
        COLOR(0);

        private final int mValue;

        AttachmentPoint(int value) {
            mValue = value;
        }

        /** @return the value of this enum constant as used by the native Filament engine. */
        public int toFilamentNative() { return mValue; }

        public static AttachmentPoint from(int value) {
            switch (value) {
                case 0: return COLOR0;
                case 1: return COLOR1;
                case 2: return COLOR2;
                case 3: return COLOR3;
                case 4: return COLOR4;
                case 5: return COLOR5;
                case 6: return COLOR6;
                case 7: return COLOR7;
                case 8: return DEPTH;
                default: throw new IllegalArgumentException("Unknown AttachmentPoint value: " + value);
            }
        }
    }

    /** Minimum number of color attachment supported */
    public static final int MIN_SUPPORTED_COLOR_ATTACHMENTS_COUNT = 4;
    /** Maximum number of color attachment supported */
    public static final int MAX_SUPPORTED_COLOR_ATTACHMENTS_COUNT = 8;

    RenderTarget(long nativeObject) {
        mNativeObject = nativeObject;
    }

    @NonNull
    @RestrictTo(RestrictTo.Scope.LIBRARY_GROUP)
    public static RenderTarget wrap(long nativeObject) {
        return new RenderTarget(nativeObject);
    }

    /** Use Builder to construct a RenderTarget object instance */
    public static class Builder {
        @SuppressWarnings({"FieldCanBeLocal", "UnusedDeclaration"}) // Keep to finalize native resources
        private final BuilderFinalizer mFinalizer;
        private final long mNativeBuilder;

        public Builder() {
            mNativeBuilder = nCreateBuilder();
            mFinalizer = new BuilderFinalizer(mNativeBuilder);
        }

        long getNativeBuilder() {
            return mNativeBuilder;
        }

        /**
         * Sets a texture to a given attachment point.
         *
         * <p>When using a DEPTH attachment, it is important to always disable post-processing
         * in the View. Failing to do so will cause the DEPTH attachment to be ignored in most
         * cases.</p>
         *
         * <p>When the intention is to keep the content of the DEPTH attachment after rendering,
         * Usage::SAMPLEABLE must be set on the DEPTH attachment, otherwise the content of the
         * DEPTH buffer may be discarded.</p>
         *
         * @param attachment The attachment point of the texture.
         * @param texture The associated texture object.
         *
         * @return A reference to this Builder for chaining calls.
         */
        @NonNull
        public Builder texture(@NonNull AttachmentPoint attachment, @Nullable Texture texture) {
            nBuilderTexture(mNativeBuilder, attachment.toFilamentNative(), texture != null ? texture.getNativeObject() : 0);
            return this;
        }

        /**
         * Sets the mipmap level for a given attachment point.
         *
         * @param attachment The attachment point of the texture.
         * @param level The associated mipmap level, 0 by default.
         *
         * @return A reference to this Builder for chaining calls.
         */
        @NonNull
        public Builder mipLevel(@NonNull AttachmentPoint attachment, @IntRange(from = 0) int level) {
            nBuilderMipLevel(mNativeBuilder, attachment.toFilamentNative(), level);
            return this;
        }

        /**
         * Sets the face for cubemap textures at the given attachment point.
         *
         * @param attachment The attachment point.
         * @param face The associated cubemap face.
         *
         * @return A reference to this Builder for chaining calls.
         */
        @NonNull
        public Builder face(@NonNull AttachmentPoint attachment, @NonNull CubemapFace face) {
            nBuilderFace(mNativeBuilder, attachment.toFilamentNative(), face.toFilamentNative());
            return this;
        }

        /**
         * Sets an index of a single layer for 2d array, cubemap array, and 3d textures at the given
         *
         * <p>attachment point.</p>
         *
         * <p>For cubemap array textures, layer is translated into an array index and face according to</p>
         *
         * <ul>
         *   <li>index: layer / 6</li>
         *   <li>face: layer % 6</li>
         * </ul>
         *
         * @param attachment The attachment point.
         * @param layer The associated cubemap layer.
         *
         * @return A reference to this Builder for chaining calls.
         */
        @NonNull
        public Builder layer(@NonNull AttachmentPoint attachment, @IntRange(from = 0) int layer) {
            nBuilderLayer(mNativeBuilder, attachment.toFilamentNative(), layer);
            return this;
        }

        /**
         * Sets the starting index of the 2d array textures for multiview at the given attachment point.
         *
         * <p>This requires COLOR and DEPTH attachments (if set) to be of 2D array textures.</p>
         *
         * @param attachment The attachment point.
         * @param layerCount The number of layers used for multiview, starting from baseLayer.
         *
         * @return A reference to this Builder for chaining calls.
         */
        @NonNull
        public Builder multiview(@NonNull AttachmentPoint attachment, @IntRange(from = 0) int layerCount) {
            return multiview(attachment, layerCount, 0);
        }

        /**
         * Sets the starting index of the 2d array textures for multiview at the given attachment point.
         *
         * <p>This requires COLOR and DEPTH attachments (if set) to be of 2D array textures.</p>
         *
         * @param attachment The attachment point.
         * @param layerCount The number of layers used for multiview, starting from baseLayer.
         * @param baseLayer The starting index of the 2d array texture.
         *
         * @return A reference to this Builder for chaining calls.
         */
        @NonNull
        public Builder multiview(@NonNull AttachmentPoint attachment, @IntRange(from = 0) int layerCount, @IntRange(from = 0) int baseLayer) {
            nBuilderMultiview(mNativeBuilder, attachment.toFilamentNative(), layerCount, baseLayer);
            return this;
        }

        /**
         * Sets the number of samples used for MSAA (Multisample Anti-Aliasing).
         *
         * @param samples The number of samples used for multisampling.
         *
         * @return A reference to this Builder for chaining calls.
         */
        @NonNull
        public Builder samples(@IntRange(from = 0) int samples) {
            nBuilderSamples(mNativeBuilder, samples);
            return this;
        }

        /**
         * Creates the RenderTarget object and returns a pointer to it.
         *
         * @return pointer to the newly created object.
         */
        @NonNull
        public RenderTarget build(@NonNull Engine engine) {
            long nativeRenderTarget = nBuilderBuild(mNativeBuilder, engine.getNativeObject());
            if (nativeRenderTarget == 0) throw new IllegalStateException("Couldn't create RenderTarget");
            return new RenderTarget(nativeRenderTarget);
        }

        private static class BuilderFinalizer {
            private final long mNativeObject;

            BuilderFinalizer(long nativeObject) { mNativeObject = nativeObject; }

            @Override
            public void finalize() {
                try {
                    super.finalize();
                } catch (Throwable t) { // Ignore
                } finally {
                    nDestroyBuilder(mNativeObject);
                }
            }
        }
    }

    /**
     * Gets the texture set on the given attachment point
     *
     * @param attachment Attachment point
     *
     * @return A Texture object or nullptr if no texture is set for this attachment point
     */
    public Texture getTexture(@NonNull AttachmentPoint attachment) {
        long nativeTexture = nGetTexture(getNativeObject(), attachment.toFilamentNative());
        return nativeTexture == 0 ? null : new Texture(nativeTexture);
    }

    /**
     * Returns the mipmap level set on the given attachment point
     *
     * @param attachment Attachment point
     *
     * @return the mipmap level set on the given attachment point
     */
    @IntRange(from = 0)
    public int getMipLevel(@NonNull AttachmentPoint attachment) {
        return nGetMipLevel(getNativeObject(), attachment.toFilamentNative());
    }

    /**
     * Returns the face of a cubemap set on the given attachment point
     *
     * @param attachment Attachment point
     *
     * @return A cubemap face identifier. This is only relevant if the attachment's texture is
     * a cubemap.
     */
    public CubemapFace getFace(@NonNull AttachmentPoint attachment) {
        return EnumCache.sCubemapFaceValues[nGetFace(getNativeObject(), attachment.toFilamentNative())];
    }

    /**
     * Returns the texture-layer set on the given attachment point
     *
     * @param attachment Attachment point
     *
     * @return A texture layer. This is only relevant if the attachment's texture is a 3D texture.
     */
    @IntRange(from = 0)
    public int getLayer(@NonNull AttachmentPoint attachment) {
        return nGetLayer(getNativeObject(), attachment.toFilamentNative());
    }

    /**
     * Returns the number of color attachments usable by this instance of Engine.
     *
     * <p>This method is
     * guaranteed to return at least MIN_SUPPORTED_COLOR_ATTACHMENTS_COUNT and at most
     * MAX_SUPPORTED_COLOR_ATTACHMENTS_COUNT.</p>
     *
     * @return Number of color attachments usable in a render target.
     */
    @IntRange(from = 0)
    public int getSupportedColorAttachmentsCount() {
        return nGetSupportedColorAttachmentsCount(getNativeObject());
    }

    public long getNativeObject() {
        if (mNativeObject == 0) {
            throw new IllegalStateException("Calling method on destroyed RenderTarget");
        }
        return mNativeObject;
    }

    void clearNativeObject() {
        mNativeObject = 0;
    }

    private static native long nGetTexture(long nativeRenderTarget, int attachment);
    @IntRange(from = 0)
    private static native int nGetMipLevel(long nativeRenderTarget, int attachment);
    private static native int nGetFace(long nativeRenderTarget, int attachment);
    @IntRange(from = 0)
    private static native int nGetLayer(long nativeRenderTarget, int attachment);
    @IntRange(from = 0)
    private static native int nGetSupportedColorAttachmentsCount(long nativeRenderTarget);
    private static native long nCreateBuilder();
    private static native void nDestroyBuilder(long nativeBuilder);
    private static native void nBuilderTexture(long nativeBuilder, int attachment, long texture);
    private static native void nBuilderMipLevel(long nativeBuilder, int attachment, int level);
    private static native void nBuilderFace(long nativeBuilder, int attachment, int face);
    private static native void nBuilderLayer(long nativeBuilder, int attachment, int layer);
    private static native void nBuilderMultiview(long nativeBuilder, int attachment, int layerCount, int baseLayer);
    private static native void nBuilderSamples(long nativeBuilder, int samples);
    private static native long nBuilderBuild(long nativeBuilder, long nativeEngine);
}

// THIS FILE IS GENERATED BY APIGEN AND MUST NOT BE HAND-MODIFIED.
