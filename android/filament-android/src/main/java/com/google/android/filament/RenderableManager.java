/*
 * Copyright (C) 2026 The Android Open Source Project
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *      http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

// THIS FILE IS GENERATED BY APIGEN AND MUST NOT BE HAND-MODIFIED.

package com.google.android.filament;

import java.lang.IllegalStateException;

import java.nio.Buffer;
import java.nio.BufferOverflowException;

import java.util.Collections;
import java.util.EnumSet;
import java.util.Set;

import androidx.annotation.IntRange;
import androidx.annotation.NonNull;
import androidx.annotation.Nullable;
import androidx.annotation.RestrictTo;
import androidx.annotation.Size;

/**
 * Factory and manager for \em renderables, which are entities that can be drawn.
 *
 * <p>Renderables are bundles of \em primitives, each of which has its own geometry and material. All
 * primitives in a particular renderable share a set of rendering attributes, such as whether they
 * cast shadows or use vertex skinning.</p>
 *
 * <p>Usage example:</p>
 *
 * <pre>{@code
 * auto renderable = utils::EntityManager::get().create();
 *
 * RenderableManager::Builder(1)
 *         .boundingBox({{ -1, -1, -1 }, { 1, 1, 1 }})
 *         .material(0, matInstance)
 *         .geometry(0, RenderableManager::PrimitiveType::TRIANGLES, vertBuffer, indBuffer, 0, 3)
 *         .receiveShadows(false)
 *         .build(engine, renderable);
 *
 * scene->addEntity(renderable);
 *
 * }</pre>
 *
 * <p>To modify the state of an existing renderable, clients should first use RenderableManager
 * to get a temporary handle called an \em instance. The instance can then be used to get or set
 * the renderable's state. Please note that instances are ephemeral; clients should store entities,
 * not instances.</p>
 *
 * <ul>
 *   <li>For details about constructing renderables, see RenderableManager::Builder.</li>
 *   <li>To associate a 4x4 transform with an entity, see TransformManager.</li>
 *   <li>To associate a human-readable label with an entity, see utils::NameComponentManager.</li>
 * </ul>
 */
public class RenderableManager {
    private static final VertexBuffer.VertexAttribute[] sVertexAttributeValues =
            VertexBuffer.VertexAttribute.values();

    private static Set<VertexBuffer.VertexAttribute> getAttributes(int bitSet) {
        Set<VertexBuffer.VertexAttribute> set = EnumSet.noneOf(VertexBuffer.VertexAttribute.class);
        VertexBuffer.VertexAttribute[] values = sVertexAttributeValues;
        for (int i = 0, n = values.length; i < n; i++) {
            if ((bitSet & (1 << i)) != 0) {
                set.add(values[i]);
            }
        }
        return Collections.unmodifiableSet(set);
    }

    private long mNativeObject;

    /** Primitive types */
    public enum PrimitiveType {
        /** points */
        POINTS(0),
        /** lines */
        LINES(1),
        /** line strip */
        LINE_STRIP(3),
        /** triangles */
        TRIANGLES(4),
        /** triangle strip */
        TRIANGLE_STRIP(5);

        private final int mValue;

        PrimitiveType(int value) {
            mValue = value;
        }

        /** @return the value of this enum constant as used by the native Filament engine. */
        public int toFilamentNative() { return mValue; }

        public static PrimitiveType from(int value) {
            switch (value) {
                case 0: return POINTS;
                case 1: return LINES;
                case 3: return LINE_STRIP;
                case 4: return TRIANGLES;
                case 5: return TRIANGLE_STRIP;
                default: throw new IllegalArgumentException("Unknown PrimitiveType value: " + value);
            }
        }
    }

    RenderableManager(long nativeObject) {
        mNativeObject = nativeObject;
    }

    @NonNull
    @RestrictTo(RestrictTo.Scope.LIBRARY_GROUP)
    public static RenderableManager wrap(long nativeObject) {
        return new RenderableManager(nativeObject);
    }

    /** Adds renderable components to entities using a builder pattern. */
    public static class Builder {
        @SuppressWarnings({"FieldCanBeLocal", "UnusedDeclaration"}) // Keep to finalize native resources
        private final BuilderFinalizer mFinalizer;
        private final long mNativeBuilder;

        public enum Result {
            Error(-1),
            Success(0);

            private final int mValue;

            Result(int value) {
                mValue = value;
            }

            /** @return the value of this enum constant as used by the native Filament engine. */
            public int toFilamentNative() { return mValue; }

            public static Result from(int value) {
                switch (value) {
                    case -1: return Error;
                    case 0: return Success;
                    default: throw new IllegalArgumentException("Unknown Result value: " + value);
                }
            }
        }

        /** Type of geometry for a Renderable */
        public enum GeometryType {
            /** dynamic gemoetry has no restriction */
            DYNAMIC,
            /** bounds and world space transform are immutable */
            STATIC_BOUNDS,
            /** skinning/morphing not allowed and Vertex/IndexBuffer immutables */
            STATIC;

            /** @return the value of this enum constant as used by the native Filament engine. */
            public int toFilamentNative() { return ordinal(); }
        }

        /**
         * Type of morphing for a Renderable.
         *
         * <p>This usually acts as a bitmask of multiple types.</p>
         */
        public enum MorphType {
            NONE(0),
            POSITION(1),
            TANGENT(2),
            CUSTOM(4);

            private final int mValue;

            MorphType(int value) {
                mValue = value;
            }

            /** @return the value of this enum constant as used by the native Filament engine. */
            public int toFilamentNative() { return mValue; }

            public static MorphType from(int value) {
                switch (value) {
                    case 0: return NONE;
                    case 1: return POSITION;
                    case 2: return TANGENT;
                    case 4: return CUSTOM;
                    default: throw new IllegalArgumentException("Unknown MorphType value: " + value);
                }
            }
        }

        /**
         * Creates a builder for renderable components.
         *
         * @param count <p>the number of primitives that will be supplied to the builder</p>
         *
         * <p>Note that builders typically do not have a long lifetime since clients should discard
         * them after calling build(). For a usage example, see RenderableManager.</p>
         */
        public Builder(@IntRange(from = 0) int count) {
            mNativeBuilder = nCreateBuilder(count);
            mFinalizer = new BuilderFinalizer(mNativeBuilder);
        }

        long getNativeBuilder() {
            return mNativeBuilder;
        }

        /**
         * Specifies the geometry data for a primitive.
         *
         * <p>Associates a vertex buffer and an index buffer with a primitive. Typically, each
         * primitive is specified with a pair of daisy-chained calls: \c geometry(...) and \c
         * material(...).</p>
         *
         * @param index zero-based index of the primitive, must be less than the count passed to Builder constructor
         * @param type specifies the topology of the primitive (e.g., \c RenderableManager::PrimitiveType::TRIANGLES)
         * @param vertices specifies the vertex buffer, which in turn specifies a set of attributes
         * @param indices specifies the index buffer (either u16 or u32)
         * @param offset specifies where in the index buffer to start reading (expressed as a number of indices)
         * @param minIndex specifies the minimum index contained in the index buffer
         * @param maxIndex specifies the maximum index contained in the index buffer
         * @param count number of indices to read (for triangles, this should be a multiple of 3)
         */
        @NonNull
        public Builder geometry(@IntRange(from = 0) int index, @NonNull PrimitiveType type, @NonNull VertexBuffer vertices, @NonNull IndexBuffer indices, @IntRange(from = 0) int offset, @IntRange(from = 0) int minIndex, @IntRange(from = 0) int maxIndex, @IntRange(from = 0) int count) {
            nBuilderGeometry(mNativeBuilder, index, type.toFilamentNative(), vertices.getNativeObject(), indices.getNativeObject(), offset, minIndex, maxIndex, count);
            return this;
        }

        @NonNull
        public Builder geometry(@IntRange(from = 0) int index, @NonNull PrimitiveType type, @NonNull VertexBuffer vertices, @NonNull IndexBuffer indices, @IntRange(from = 0) int offset, @IntRange(from = 0) int count) {
            nBuilderGeometry(mNativeBuilder, index, type.toFilamentNative(), vertices.getNativeObject(), indices.getNativeObject(), offset, count);
            return this;
        }

        @NonNull
        public Builder geometry(@IntRange(from = 0) int index, @NonNull PrimitiveType type, @NonNull VertexBuffer vertices, @NonNull IndexBuffer indices) {
            nBuilderGeometry(mNativeBuilder, index, type.toFilamentNative(), vertices.getNativeObject(), indices.getNativeObject());
            return this;
        }

        /**
         * Specifies the geometry data for a primitive. (non-indexed version)
         *
         * <p>Filament primitives normally have an associated vertex buffer and index buffer. Typically,
         * each primitive is specified with a pair of daisy-chained calls: \c geometry(...) and
         * \c material(...).</p>
         *
         * <p>Non-indexed rendering: when {@code indices} is not provided, the primitive is treated as a
         * non-indexed draw and {@code offset} / {@code count} refer to vertex offset and vertex count
         * respectively.</p>
         *
         * <p>Attribute-less rendering: This can be used for procedural rendering, where the vertex
         * shader generates positions, UVs, etc procedurally, typically from \c gl_VertexIndex /
         * \c gl_VertexID / and \c [[vertex_id]], which can be accessed by calling {@code getVertexIndex()}
         * in vertex shader. The associated VertexBuffer may have \c bufferCount == 0 with
         * no declared attributes (see \c VertexBuffer::Builder). Attribute-less rendering requires
         * \c FEATURE_LEVEL_1 or higher as GLES2 has no {@code gl_VertexID} and is incompatible with
         * skinning and morphing.</p>
         *
         * @param index zero-based index of the primitive, must be less than the count passed to Builder constructor
         * @param type specifies the topology of the primitive (e.g., \c RenderableManager::PrimitiveType::TRIANGLES)
         * @param vertices specifies the vertex buffer, which in turn specifies a set of attributes
         * @param offset specifies where in the vertex buffer to start reading (expressed as a number of vertices)
         * @param count number of vertices to read (for triangles, this should be a multiple of 3)
         */
        @NonNull
        public Builder geometry(@IntRange(from = 0) int index, @NonNull PrimitiveType type, @NonNull VertexBuffer vertices, @IntRange(from = 0) int offset, @IntRange(from = 0) int count) {
            nBuilderGeometry(mNativeBuilder, index, type.toFilamentNative(), vertices.getNativeObject(), offset, count);
            return this;
        }

        @NonNull
        public Builder geometry(@IntRange(from = 0) int index, @NonNull PrimitiveType type, @NonNull VertexBuffer vertices) {
            nBuilderGeometry(mNativeBuilder, index, type.toFilamentNative(), vertices.getNativeObject());
            return this;
        }

        /**
         * Specify the type of geometry for this renderable.
         *
         * <p>DYNAMIC geometry has no restriction,
         * STATIC_BOUNDS geometry means that both the bounds and the world-space transform of the
         * renderable are immutable.
         * STATIC geometry has the same restrictions as STATIC_BOUNDS, but in addition disallows
         * skinning, morphing and changing the VertexBuffer or IndexBuffer in any way.</p>
         *
         * @param type type of geometry.
         */
        @NonNull
        public Builder geometryType(@NonNull GeometryType type) {
            nBuilderGeometryType(mNativeBuilder, type.toFilamentNative());
            return this;
        }

        /**
         * Binds a material instance to the specified primitive.
         *
         * <p>If no material is specified for a given primitive, Filament will fall back to a basic
         * default material.</p>
         *
         * <p>The MaterialInstance's material must have a feature level equal or lower to the engine's
         * selected feature level.</p>
         *
         * @param index zero-based index of the primitive, must be less than the count passed to
         * Builder constructor
         * @param materialInstance the material to bind
         *
         * @see Engine#setActiveFeatureLevel
         */
        @NonNull
        public Builder material(@IntRange(from = 0) int index, @NonNull MaterialInstance materialInstance) {
            nBuilderMaterial(mNativeBuilder, index, materialInstance.getNativeObject());
            return this;
        }

        /**
         * The axis-aligned bounding box of the renderable.
         *
         * <p>This is an object-space AABB used for frustum culling. For skinning and morphing, this
         * should encompass all possible vertex positions. It is mandatory unless culling is
         * disabled for the renderable.</p>
         *
         * @see #computeAABB
         */
        @NonNull
        public Builder boundingBox(@NonNull Box axisAlignedBoundingBox) {
            nBuilderBoundingBox(mNativeBuilder, axisAlignedBoundingBox.getCenterX(), axisAlignedBoundingBox.getCenterY(), axisAlignedBoundingBox.getCenterZ(), axisAlignedBoundingBox.getHalfExtentX(), axisAlignedBoundingBox.getHalfExtentY(), axisAlignedBoundingBox.getHalfExtentZ());
            return this;
        }

        /**
         * Sets bits in a visibility mask.
         *
         * <p>By default, this is 0x1.</p>
         *
         * <p>This feature provides a simple mechanism for hiding and showing groups of renderables
         * in a Scene. See View::setVisibleLayers().</p>
         *
         * <p>For example, to set bit 1 and reset bits 0 and 2 while leaving all other bits unaffected,
         * do: {@code builder.layerMask(7, 2)}.</p>
         *
         * <p>To change this at run time, see RenderableManager::setLayerMask.</p>
         *
         * @param select the set of bits to affect
         * @param values the replacement values for the affected bits
         */
        @NonNull
        public Builder layerMask(@IntRange(from = 0) int select, @IntRange(from = 0) int values) {
            nBuilderLayerMask(mNativeBuilder, select, values);
            return this;
        }

        /**
         * Provides coarse-grained control over draw order.
         *
         * <p>In general Filament reserves the right to re-order renderables to allow for efficient
         * rendering. However clients can control ordering at a coarse level using \em priority.
         * The priority is applied separately for opaque and translucent objects, that is, opaque
         * objects are always drawn before translucent objects regardless of the priority.</p>
         *
         * <p>For example, this could be used to draw a semitransparent HUD on top of everything,
         * without using a separate View. Note that priority is completely orthogonal to
         * Builder::layerMask, which merely controls visibility.</p>
         *
         * <p>The Skybox always using the lowest priority, so it's drawn last, which may improve
         * performance.</p>
         *
         * @param priority clamped to the range [0..7], defaults to 4; 7 is lowest priority
         *                 (rendered last).
         *
         * @return Builder reference for chaining calls.
         *
         * @see Builder#blendOrder
         * @see Builder#channel
         * @see RenderableManager#setPriority
         * @see RenderableManager#setBlendOrderAt
         */
        @NonNull
        public Builder priority(@IntRange(from = 0) int priority) {
            nBuilderPriority(mNativeBuilder, priority);
            return this;
        }

        /**
         * Set the channel this renderable is associated to.
         *
         * <p>There can be 8 channels.
         * All renderables in a given channel are rendered together, regardless of anything else.
         * They are sorted as usual within a channel.
         * Channels work similarly to priorities, except that they enforce the strongest ordering.</p>
         *
         * <p>Channels 0 and 1 may not have render primitives using a material with {@code refractionType}
         * set to {@code screenspace}.</p>
         *
         * @param channel clamped to the range [0..7], defaults to 2.
         *
         * @return Builder reference for chaining calls.
         *
         * @see Builder#blendOrder
         * @see Builder#priority
         * @see RenderableManager#setBlendOrderAt
         * @see RenderableManager#getChannel
         */
        @NonNull
        public Builder channel(@IntRange(from = 0) int channel) {
            nBuilderChannel(mNativeBuilder, channel);
            return this;
        }

        /**
         * Controls frustum culling, true by default.
         *
         * <p>Do not confuse frustum culling with backface culling. The latter is controlled via
         * the material.</p>
         */
        @NonNull
        public Builder culling(boolean enable) {
            nBuilderCulling(mNativeBuilder, enable);
            return this;
        }

        /**
         * Enables or disables a light channel.
         *
         * <p>Light channel 0 is enabled by default.</p>
         *
         * @param channel Light channel to enable or disable, between 0 and 7.
         */
        @NonNull
        public Builder lightChannel(@IntRange(from = 0) int channel) {
            return lightChannel(channel, true);
        }

        /**
         * Enables or disables a light channel.
         *
         * <p>Light channel 0 is enabled by default.</p>
         *
         * @param channel Light channel to enable or disable, between 0 and 7.
         * @param enable Whether to enable or disable the light channel.
         */
        @NonNull
        public Builder lightChannel(@IntRange(from = 0) int channel, boolean enable) {
            nBuilderLightChannel(mNativeBuilder, channel, enable);
            return this;
        }

        /**
         * Controls if this renderable casts shadows, false by default.
         *
         * <p>If the View's shadow type is set to ShadowType::VSM, castShadows should only be disabled
         * if either is true:</p>
         *
         * <ul>
         *   <li>receiveShadows is also disabled</li>
         *   <li>the object is guaranteed to not cast shadows on itself or other objects (for example,
         * a ground plane)</li>
         * </ul>
         */
        @NonNull
        public Builder castShadows(boolean enable) {
            nBuilderCastShadows(mNativeBuilder, enable);
            return this;
        }

        /** Controls if this renderable receives shadows, true by default. */
        @NonNull
        public Builder receiveShadows(boolean enable) {
            nBuilderReceiveShadows(mNativeBuilder, enable);
            return this;
        }

        /**
         * Controls if this renderable uses screen-space contact shadows.
         *
         * <p>This is more
         * expensive but can improve the quality of shadows, especially in large scenes.
         * (off by default).</p>
         */
        @NonNull
        public Builder screenSpaceContactShadows(boolean enable) {
            nBuilderScreenSpaceContactShadows(mNativeBuilder, enable);
            return this;
        }

        /**
         * Allows bones to be swapped out and shared using SkinningBuffer.
         *
         * <p>If skinning buffer mode is enabled, clients must call setSkinningBuffer() rather than
         * setBones(). This allows sharing of data between renderables.</p>
         */
        @NonNull
        public Builder enableSkinningBuffers() {
            return enableSkinningBuffers(true);
        }

        /**
         * Allows bones to be swapped out and shared using SkinningBuffer.
         *
         * <p>If skinning buffer mode is enabled, clients must call setSkinningBuffer() rather than
         * setBones(). This allows sharing of data between renderables.</p>
         *
         * @param enabled If true, enables buffer object mode.  False by default.
         */
        @NonNull
        public Builder enableSkinningBuffers(boolean enabled) {
            nBuilderEnableSkinningBuffers(mNativeBuilder, enabled);
            return this;
        }

        /**
         * Controls if this renderable is affected by the large-scale fog.
         *
         * @return A reference to this Builder for chaining calls.
         */
        @NonNull
        public Builder fog() {
            return fog(true);
        }

        /**
         * Controls if this renderable is affected by the large-scale fog.
         *
         * @param enabled If true, enables large-scale fog on this object. Disables it otherwise.
         *                True by default.
         *
         * @return A reference to this Builder for chaining calls.
         */
        @NonNull
        public Builder fog(boolean enabled) {
            nBuilderFog(mNativeBuilder, enabled);
            return this;
        }

        /**
         * Enables GPU vertex skinning for up to 255 bones, 0 by default.
         *
         * <p>Skinning Buffer mode must be enabled.</p>
         *
         * <p>Each vertex can be affected by up to 4 bones simultaneously. The attached
         * VertexBuffer must provide data in the \c BONE_INDICES slot (uvec4) and the
         * \c BONE_WEIGHTS slot (float4).</p>
         *
         * <p>See also RenderableManager::setSkinningBuffer() or SkinningBuffer::setBones(),
         * which can be called on a per-frame basis to advance the animation.</p>
         *
         * @param skinningBuffer nullptr to disable, otherwise the SkinningBuffer to use
         * @param count 0 to disable, otherwise the number of bone transforms (up to 255)
         * @param offset offset in the SkinningBuffer
         */
        @NonNull
        public Builder skinning(@NonNull SkinningBuffer skinningBuffer, @IntRange(from = 0) int count, @IntRange(from = 0) int offset) {
            nBuilderSkinning(mNativeBuilder, skinningBuffer.getNativeObject(), count, offset);
            return this;
        }

        /**
         * Enables GPU vertex skinning for up to 255 bones, 0 by default.
         *
         * <p>Skinning Buffer mode must be disabled.</p>
         *
         * <p>Each vertex can be affected by up to 4 bones simultaneously. The attached
         * VertexBuffer must provide data in the \c BONE_INDICES slot (uvec4) and the
         * \c BONE_WEIGHTS slot (float4).</p>
         *
         * <p>See also RenderableManager::setBones(), which can be called on a per-frame basis
         * to advance the animation.</p>
         *
         * @param boneCount number of elements (structured element count) in <code>transforms</code>
         * @param transforms the initial set of transforms (one for each bone)
         */
        @NonNull
        public Builder skinningAsMatrices(@IntRange(from = 0) int boneCount, @NonNull Buffer transforms) {
            int result = nBuilderSkinningAsMatrices(mNativeBuilder, boneCount, transforms, transforms.remaining());
            if (result < 0) {
                throw new BufferOverflowException();
            }
            return this;
        }

        /**
         * Enables GPU vertex skinning for up to 255 bones, 0 by default.
         *
         * <p>Skinning Buffer mode must be disabled.</p>
         *
         * <p>Each vertex can be affected by up to 4 bones simultaneously. The attached
         * VertexBuffer must provide data in the \c BONE_INDICES slot (uvec4) and the
         * \c BONE_WEIGHTS slot (float4).</p>
         *
         * <p>See also RenderableManager::setBones(), which can be called on a per-frame basis
         * to advance the animation.</p>
         *
         * @param transforms the initial set of transforms (one for each bone)
         * @param offset offset in elements (structured element count) in <code>transforms</code> to skip
         * @param boneCount number of elements (structured element count) in <code>transforms</code>
         */
        @NonNull
        public Builder skinningAsMatrices(@NonNull @Size(min = 16) float[] transforms, @IntRange(from = 0) int offset, @IntRange(from = 0) int boneCount) {
            if (transforms.length < (offset + boneCount) * 16) {
                throw new ArrayIndexOutOfBoundsException("Array length must be at least " + ((offset + boneCount) * 16));
            }
            nBuilderSkinningAsMatrices(mNativeBuilder, transforms, boneCount, offset);
            return this;
        }

        /**
         * Enables GPU vertex skinning for up to 255 bones, 0 by default.
         *
         * <p>Skinning Buffer mode must be disabled.</p>
         *
         * <p>Each vertex can be affected by up to 4 bones simultaneously. The attached
         * VertexBuffer must provide data in the \c BONE_INDICES slot (uvec4) and the
         * \c BONE_WEIGHTS slot (float4).</p>
         *
         * <p>See also RenderableManager::setBones(), which can be called on a per-frame basis
         * to advance the animation.</p>
         *
         * @param transforms the initial set of transforms (one for each bone)
         * @param boneCount number of elements (structured element count) in <code>transforms</code>
         */
        @NonNull
        public Builder skinningAsMatrices(@NonNull @Size(min = 16) float[] transforms, @IntRange(from = 0) int boneCount) {
            return skinningAsMatrices(transforms, 0, boneCount);
        }

        /**
         * Enables GPU vertex skinning for up to 255 bones, 0 by default.
         *
         * <p>Skinning Buffer mode must be disabled.</p>
         *
         * <p>Each vertex can be affected by up to 4 bones simultaneously. The attached
         * VertexBuffer must provide data in the \c BONE_INDICES slot (uvec4) and the
         * \c BONE_WEIGHTS slot (float4).</p>
         *
         * <p>See also RenderableManager::setBones(), which can be called on a per-frame basis
         * to advance the animation.</p>
         *
         * @param transforms the initial set of transforms (one for each bone)
         */
        @NonNull
        public Builder skinningAsMatrices(@NonNull @Size(min = 16) float[] transforms) {
            return skinningAsMatrices(transforms, 0, transforms.length / 16);
        }

        /**
         * @param boneCount number of elements (structured element count) in <code>bones</code>
         * @param bones buffer containing bones data
         */
        @NonNull
        public Builder skinning(@IntRange(from = 0) int boneCount, @NonNull Buffer bones) {
            int result = nBuilderSkinning(mNativeBuilder, boneCount, bones, bones.remaining());
            if (result < 0) {
                throw new BufferOverflowException();
            }
            return this;
        }

        /**
         * @param bones array containing bones data
         * @param offset offset in elements (structured element count) in <code>bones</code> to skip
         * @param boneCount number of elements (structured element count) in <code>bones</code>
         */
        @NonNull
        public Builder skinning(@NonNull @Size(min = 8) float[] bones, @IntRange(from = 0) int offset, @IntRange(from = 0) int boneCount) {
            if (bones.length < (offset + boneCount) * 8) {
                throw new ArrayIndexOutOfBoundsException("Array length must be at least " + ((offset + boneCount) * 8));
            }
            nBuilderSkinning(mNativeBuilder, bones, boneCount, offset);
            return this;
        }

        /**
         * @param bones array containing bones data
         * @param boneCount number of elements (structured element count) in <code>bones</code>
         */
        @NonNull
        public Builder skinning(@NonNull @Size(min = 8) float[] bones, @IntRange(from = 0) int boneCount) {
            return skinning(bones, 0, boneCount);
        }

        /** @param bones array containing bones data */
        @NonNull
        public Builder skinning(@NonNull @Size(min = 8) float[] bones) {
            return skinning(bones, 0, bones.length / 8);
        }

        @NonNull
        public Builder skinning(@IntRange(from = 0) int boneCount) {
            nBuilderSkinning(mNativeBuilder, boneCount);
            return this;
        }

        /**
         * Define bone indices and weights "pairs" for vertex skinning as a float2.
         *
         * <p>The unsigned int(pair.x) defines index of the bone and pair.y is the bone weight.
         * The pairs substitute \c BONE_INDICES and the \c BONE_WEIGHTS defined in the VertexBuffer.
         * Both ways of indices and weights definition must not be combined in one primitive.
         * Number of pairs per vertex bonesPerVertex is not limited to 4 bones.
         * Vertex buffer used for \c primitiveIndex must be set for advance skinning.
         * All bone weights of one vertex should sum to one. Otherwise they will be normalized.
         * Data must be rectangular and number of bone pairs must be same for all vertices of this
         * primitive.
         * The data is arranged sequentially, all bone pairs for the first vertex, then for the
         * second vertex, and so on.</p>
         *
         * @param primitiveIndex zero-based index of the primitive, must be less than the primitive
         *                       count passed to Builder constructor
         * @param indicesAndWeights pairs of bone index and bone weight for all vertices
         *                          sequentially
         * @param count number of all pairs, must be a multiple of vertexCount of the primitive
         *                          count = vertexCount * bonesPerVertex
         * @param bonesPerVertex number of bone pairs, same for all vertices of the primitive
         *
         * @return Builder reference for chaining calls.
         *
         * @see VertexBuffer.Builder#advancedSkinning
         */
        @NonNull
        public Builder boneIndicesAndWeights(@IntRange(from = 0) int primitiveIndex, @NonNull @Size(min = 2) float[] indicesAndWeights, @IntRange(from = 0) int count, @IntRange(from = 0) int bonesPerVertex) {
            nBuilderBoneIndicesAndWeights(mNativeBuilder, primitiveIndex, indicesAndWeights, count, bonesPerVertex);
            return this;
        }

        /**
         * Controls if the renderable has legacy vertex morphing targets, zero by default.
         *
         * <p>This is
         * required to enable GPU morphing.</p>
         *
         * <p>For legacy morphing, the attached VertexBuffer must provide data in the
         * appropriate VertexAttribute slots (\c MORPH_POSITION_0 etc). Legacy morphing only
         * supports up to 4 morph targets and will be deprecated in the future. Legacy morphing must
         * be enabled on the material definition: either via the legacyMorphing material attribute
         * or by calling filamat::MaterialBuilder::useLegacyMorphing().</p>
         *
         * <p>See also RenderableManager::setMorphWeights(), which can be called on a per-frame basis
         * to advance the animation.</p>
         */
        @NonNull
        public Builder morphing(@IntRange(from = 0) int targetCount) {
            nBuilderMorphing(mNativeBuilder, targetCount);
            return this;
        }

        /**
         * Controls if the renderable has vertex morphing targets, zero by default.
         *
         * <p>This is
         * required to enable GPU morphing.</p>
         *
         * <p>Filament supports two morphing modes: standard (default) and legacy.</p>
         *
         * <p>For standard morphing, A MorphTargetBuffer must be provided.
         * Standard morphing supports up to \c CONFIG_MAX_MORPH_TARGET_COUNT morph targets.</p>
         *
         * <p>See also RenderableManager::setMorphWeights(), which can be called on a per-frame basis
         * to advance the animation.</p>
         */
        @NonNull
        public Builder morphing(@NonNull MorphTargetBuffer morphTargetBuffer) {
            nBuilderMorphing(mNativeBuilder, morphTargetBuffer.getNativeObject());
            return this;
        }

        /**
         * Specifies the the range of the MorphTargetBuffer to use with this primitive.
         *
         * @param level the level of detail (lod), only 0 can be specified
         * @param primitiveIndex zero-based index of the primitive, must be less than the count passed to Builder constructor
         * @param offset specifies where in the morph target buffer to start reading (expressed as a number of vertices)
         */
        @NonNull
        public Builder morphing(@IntRange(from = 0) int level, @IntRange(from = 0) int primitiveIndex, @IntRange(from = 0) int offset) {
            nBuilderMorphing(mNativeBuilder, level, primitiveIndex, offset);
            return this;
        }

        /**
         * Sets the drawing order for blended primitives.
         *
         * <p>The drawing order is either global or
         * local (default) to this Renderable. In either case, the Renderable priority takes
         * precedence.</p>
         *
         * @param primitiveIndex the primitive of interest
         * @param blendOrder draw order number (0 by default). Only the lowest 15 bits are used.
         *
         * @return Builder reference for chaining calls.
         *
         * @see #globalBlendOrderEnabled
         */
        @NonNull
        public Builder blendOrder(@IntRange(from = 0) int primitiveIndex, @IntRange(from = 0) int blendOrder) {
            nBuilderBlendOrder(mNativeBuilder, primitiveIndex, blendOrder);
            return this;
        }

        /**
         * Sets whether the blend order is global or local to this Renderable (by default).
         *
         * @param primitiveIndex the primitive of interest
         * @param enabled true for global, false for local blend ordering.
         *
         * @return Builder reference for chaining calls.
         *
         * @see #blendOrder
         */
        @NonNull
        public Builder globalBlendOrderEnabled(@IntRange(from = 0) int primitiveIndex, boolean enabled) {
            nBuilderGlobalBlendOrderEnabled(mNativeBuilder, primitiveIndex, enabled);
            return this;
        }

        /**
         * Specifies the number of draw instances of this renderable.
         *
         * <p>The default is 1 instance and
         * the maximum number of instances allowed is 32767. 0 is invalid.</p>
         *
         * <p>All instances are culled using the same bounding box, so care must be taken to make
         * sure all instances render inside the specified bounding box.</p>
         *
         * <p>The material must set its {@code instanced} parameter to {@code true} in order to use
         * getInstanceIndex() in the vertex or fragment shader to get the instance index and
         * possibly adjust the position or transform.</p>
         *
         * @param instanceCount the number of instances silently clamped between 1 and 32767.
         */
        @NonNull
        public Builder instances(@IntRange(from = 0) int instanceCount) {
            nBuilderInstances(mNativeBuilder, instanceCount);
            return this;
        }

        /**
         * Specifies the number of draw instances of this renderable and an \c InstanceBuffer containing their local transforms.
         *
         * <p>The default is 1 instance and the maximum number of
         * instances allowed when supplying transforms is given by
         * \c Engine::getMaxAutomaticInstances (64 on most platforms). 0 is invalid. The
         * \c InstanceBuffer must not be destroyed before this renderable.</p>
         *
         * <p>All instances are culled using the same bounding box, so care must be taken to make
         * sure all instances render inside the specified bounding box.</p>
         *
         * <p>The material must set its {@code instanced} parameter to {@code true} in order to use
         * \c getInstanceIndex() in the vertex or fragment shader to get the instance index.</p>
         *
         * <p>Only the \c VERTEX_DOMAIN_OBJECT vertex domain is supported.</p>
         *
         * <p>The local transforms of each instance can be updated with
         * \c InstanceBuffer::setLocalTransforms.</p>
         *
         * @param instanceCount the number of instances, silently clamped between 1 and
         *                      the result of Engine::getMaxAutomaticInstances().
         * @param instanceBuffer an InstanceBuffer containing at least instanceCount transforms
         *
         * @see InstanceBuffer
         * @see #instances(int, mat4f)
         */
        @NonNull
        public Builder instances(@IntRange(from = 0) int instanceCount, @NonNull InstanceBuffer instanceBuffer) {
            nBuilderInstances(mNativeBuilder, instanceCount, instanceBuffer.getNativeObject());
            return this;
        }

        /**
         * Adds the Renderable component to an entity.
         *
         * @param engine Reference to the filament::Engine to associate this Renderable with.
         * @param entity Entity to add the Renderable component to.
         *
         * @return <p>Success if the component was created successfully, Error otherwise.</p>
         *
         * <p>If exceptions are disabled and an error occurs, this function is a no-op.
         *        Success can be checked by looking at the return value.</p>
         *
         * <p>If this component already exists on the given entity and the construction is successful,
         * it is first destroyed as if destroy(utils::Entity e) was called. In case of error,
         * the existing component is unmodified.</p>
         */
        public void build(@NonNull Engine engine, @Entity int entity) {
            if (!nBuilderBuild(mNativeBuilder, engine.getNativeObject(), entity)) {
                throw new IllegalStateException("Couldn't create RenderableManager");
            }
        }

        private static class BuilderFinalizer {
            private final long mNativeObject;

            BuilderFinalizer(long nativeObject) { mNativeObject = nativeObject; }

            @Override
            public void finalize() {
                try {
                    super.finalize();
                } catch (Throwable t) { // Ignore
                } finally {
                    nDestroyBuilder(mNativeObject);
                }
            }
        }
    }

    /** Checks if the given entity already has a renderable component. */
    public boolean hasComponent(@Entity int e) {
        return nHasComponent(getNativeObject(), e);
    }

    /**
     * Gets a temporary handle that can be used to access the renderable state.
     *
     * @return Non-zero handle if the entity has a renderable component, 0 otherwise.
     */
    @EntityInstance
    public int getInstance(@Entity int e) {
        return nGetInstance(getNativeObject(), e);
    }

    /** @return the number of Components */
    @IntRange(from = 0)
    public int getComponentCount() {
        return nGetComponentCount(getNativeObject());
    }

    /** @return true if the this manager has no components */
    public boolean empty() {
        return nEmpty(getNativeObject());
    }

    /**
     * Retrieve the {@code Entity} of the component from its {@code Instance}.
     *
     * @param i Instance of the component obtained from getInstance()
     */
    @Entity
    public int getEntity(@EntityInstance int i) {
        return nGetEntity(getNativeObject(), i);
    }

    /**
     * Retrieve the Entities of all the components of this manager.
     *
     * @param out pre-allocated array to receive the items, or null.
     *
     * @return A list, in no particular order, of all the entities managed by this manager.
     */
    @NonNull
    public @Entity int[] getAllEntities(@Nullable @Entity int[] out) {
        return nGetAllEntities(getNativeObject(), out);
    }

    @NonNull
    public @Entity int[] getAllEntities() {
        return getAllEntities(null);
    }

    /** Destroys the renderable component in the given entity. */
    public void destroy(@Entity int e) {
        nDestroy(getNativeObject(), e);
    }

    /**
     * Changes the bounding box used for frustum culling.
     *
     * <p>The renderable must not have staticGeometry enabled.</p>
     *
     * @see Builder#boundingBox
     * @see RenderableManager#getAxisAlignedBoundingBox
     */
    public void setAxisAlignedBoundingBox(@EntityInstance int instance, @NonNull Box aabb) {
        nSetAxisAlignedBoundingBox(getNativeObject(), instance, aabb.getCenterX(), aabb.getCenterY(), aabb.getCenterZ(), aabb.getHalfExtentX(), aabb.getHalfExtentY(), aabb.getHalfExtentZ());
    }

    /**
     * Gets the bounding box used for frustum culling.
     *
     * @param out pre-allocated Box to receive the data, or null.
     *
     * @see Builder#boundingBox
     * @see RenderableManager#setAxisAlignedBoundingBox
     */
    @NonNull
    public Box getAxisAlignedBoundingBox(@EntityInstance int instance, @Nullable Box out) {
        if (out == null) {
            out = new Box();
        }
        nGetAxisAlignedBoundingBox(getNativeObject(), instance, out);
        return out;
    }

    @NonNull
    public Box getAxisAlignedBoundingBox(@EntityInstance int instance) {
        return getAxisAlignedBoundingBox(instance, null);
    }

    /**
     * Changes the visibility bits.
     *
     * @see Builder#layerMask
     * @see View#setVisibleLayers
     * @see RenderableManager#getLayerMask
     */
    public void setLayerMask(@EntityInstance int instance, @IntRange(from = 0) int select, @IntRange(from = 0) int values) {
        nSetLayerMask(getNativeObject(), instance, select, values);
    }

    /**
     * Get the visibility bits.
     *
     * @see Builder#layerMask
     * @see View#setVisibleLayers
     * @see RenderableManager#getLayerMask
     */
    @IntRange(from = 0)
    public int getLayerMask(@EntityInstance int instance) {
        return nGetLayerMask(getNativeObject(), instance);
    }

    /**
     * Changes the coarse-level draw ordering.
     *
     * @see Builder#priority
     */
    public void setPriority(@EntityInstance int instance, @IntRange(from = 0) int priority) {
        nSetPriority(getNativeObject(), instance, priority);
    }

    /**
     * Get the coarse-level draw ordering.
     *
     * @see Builder#priority
     */
    @IntRange(from = 0)
    public int getPriority(@EntityInstance int instance) {
        return nGetPriority(getNativeObject(), instance);
    }

    /**
     * Changes the channel a renderable is associated to.
     *
     * @see Builder#channel
     */
    public void setChannel(@EntityInstance int instance, @IntRange(from = 0) int channel) {
        nSetChannel(getNativeObject(), instance, channel);
    }

    /**
     * Get the channel a renderable is associated to.
     *
     * @see Builder#channel
     */
    @IntRange(from = 0)
    public int getChannel(@EntityInstance int instance) {
        return nGetChannel(getNativeObject(), instance);
    }

    /**
     * Changes whether or not frustum culling is on.
     *
     * @see Builder#culling
     */
    public void setCulling(@EntityInstance int instance, boolean enable) {
        nSetCulling(getNativeObject(), instance, enable);
    }

    /**
     * Get whether or not frustum culling is on.
     *
     * @see Builder#culling
     */
    public boolean isCullingEnabled(@EntityInstance int instance) {
        return nIsCullingEnabled(getNativeObject(), instance);
    }

    /**
     * Changes whether or not the large-scale fog is applied to this renderable
     *
     * @see Builder#fog
     */
    public void setFogEnabled(@EntityInstance int instance, boolean enable) {
        nSetFogEnabled(getNativeObject(), instance, enable);
    }

    /**
     * Returns whether large-scale fog is enabled for this renderable.
     *
     * @return True if fog is enabled for this renderable.
     *
     * @see Builder#fog
     */
    public boolean getFogEnabled(@EntityInstance int instance) {
        return nGetFogEnabled(getNativeObject(), instance);
    }

    /**
     * Enables or disables a light channel.
     *
     * <p>Light channel 0 is enabled by default.</p>
     *
     * @see Builder#lightChannel
     */
    public void setLightChannel(@EntityInstance int instance, @IntRange(from = 0) int channel, boolean enable) {
        nSetLightChannel(getNativeObject(), instance, channel, enable);
    }

    /**
     * Returns whether a light channel is enabled on a specified renderable.
     *
     * @param instance Instance of the component obtained from getInstance().
     * @param channel Light channel to query
     *
     * @return true if the light channel is enabled, false otherwise
     */
    public boolean getLightChannel(@EntityInstance int instance, @IntRange(from = 0) int channel) {
        return nGetLightChannel(getNativeObject(), instance, channel);
    }

    /**
     * Changes whether or not the renderable casts shadows.
     *
     * @see Builder#castShadows
     */
    public void setCastShadows(@EntityInstance int instance, boolean enable) {
        nSetCastShadows(getNativeObject(), instance, enable);
    }

    /**
     * Changes whether or not the renderable can receive shadows.
     *
     * @see Builder#receiveShadows
     */
    public void setReceiveShadows(@EntityInstance int instance, boolean enable) {
        nSetReceiveShadows(getNativeObject(), instance, enable);
    }

    /**
     * Changes whether or not the renderable can use screen-space contact shadows.
     *
     * @see Builder#screenSpaceContactShadows
     */
    public void setScreenSpaceContactShadows(@EntityInstance int instance, boolean enable) {
        nSetScreenSpaceContactShadows(getNativeObject(), instance, enable);
    }

    /**
     * Checks if the renderable can cast shadows.
     *
     * @see Builder#castShadows
     */
    public boolean isShadowCaster(@EntityInstance int instance) {
        return nIsShadowCaster(getNativeObject(), instance);
    }

    /**
     * Checks if the renderable can receive shadows.
     *
     * @see Builder#receiveShadows
     */
    public boolean isShadowReceiver(@EntityInstance int instance) {
        return nIsShadowReceiver(getNativeObject(), instance);
    }

    /**
     * Checks if the renderable can use screen-space contact shadows.
     *
     * @see Builder#screenSpaceContactShadows
     */
    public boolean isScreenSpaceContactShadowsEnabled(@EntityInstance int instance) {
        return nIsScreenSpaceContactShadowsEnabled(getNativeObject(), instance);
    }

    /**
     * Updates the bone transforms in the range [offset, offset + count).
     *
     * <p>The bones must be pre-allocated using Builder::skinning().</p>
     *
     * @param instance instance of the component obtained from getInstance()
     * @param transforms buffer containing transforms data
     * @param count number of elements (structured element count) in <code>transforms</code>
     * @param offset offset in elements (structured element count) in the destination buffer or component
     */
    public void setBonesAsQuaternions(@EntityInstance int instance, @NonNull Buffer transforms, @IntRange(from = 0) int count, @IntRange(from = 0) int offset) {
        int result = nSetBonesAsQuaternions(getNativeObject(), instance, transforms, transforms.remaining(), count, offset);
        if (result < 0) {
            throw new BufferOverflowException();
        }
    }

    /**
     * Updates the bone transforms in the range [0, count).
     *
     * <p>The bones must be pre-allocated using Builder::skinning().</p>
     *
     * @param instance instance of the component obtained from getInstance()
     * @param transforms buffer containing transforms data
     * @param count number of elements (structured element count) in <code>transforms</code>
     */
    public void setBonesAsQuaternions(@EntityInstance int instance, @NonNull Buffer transforms, @IntRange(from = 0) int count) {
        setBonesAsQuaternions(instance, transforms, count, 0);
    }

    /**
     * Updates the bone transforms in the range [offset, offset + count).
     *
     * <p>The bones must be pre-allocated using Builder::skinning().</p>
     *
     * @param instance instance of the component obtained from getInstance()
     * @param transforms array containing transforms data
     * @param arrayOffset offset in elements (structured element count) in <code>transforms</code> to skip
     * @param count number of elements (structured element count) in <code>transforms</code>
     * @param offset offset in elements (structured element count) in the destination buffer or component
     */
    public void setBonesAsQuaternions(@EntityInstance int instance, @NonNull @Size(min = 8) float[] transforms, @IntRange(from = 0) int arrayOffset, @IntRange(from = 0) int count, @IntRange(from = 0) int offset) {
        if (transforms.length < (arrayOffset + count) * 8) {
            throw new ArrayIndexOutOfBoundsException("Array length must be at least " + ((arrayOffset + count) * 8));
        }
        nSetBonesAsQuaternions(getNativeObject(), instance, transforms, count, offset, arrayOffset);
    }

    /**
     * Updates the bone transforms in the range [offset, offset + count).
     *
     * <p>The bones must be pre-allocated using Builder::skinning().</p>
     *
     * @param instance instance of the component obtained from getInstance()
     * @param transforms array containing transforms data
     * @param count number of elements (structured element count) in <code>transforms</code>
     * @param offset offset in elements (structured element count) in the destination buffer or component
     */
    public void setBonesAsQuaternions(@EntityInstance int instance, @NonNull @Size(min = 8) float[] transforms, @IntRange(from = 0) int count, @IntRange(from = 0) int offset) {
        setBonesAsQuaternions(instance, transforms, 0, count, offset);
    }

    /**
     * Updates the bone transforms in the range [0, count).
     *
     * <p>The bones must be pre-allocated using Builder::skinning().</p>
     *
     * @param instance instance of the component obtained from getInstance()
     * @param transforms array containing transforms data
     * @param count number of elements (structured element count) in <code>transforms</code>
     */
    public void setBonesAsQuaternions(@EntityInstance int instance, @NonNull @Size(min = 8) float[] transforms, @IntRange(from = 0) int count) {
        setBonesAsQuaternions(instance, transforms, 0, count, 0);
    }

    /**
     * Updates the bone transforms in the range [0, transforms.length / 8).
     *
     * <p>The bones must be pre-allocated using Builder::skinning().</p>
     *
     * @param instance instance of the component obtained from getInstance()
     * @param transforms array containing transforms data
     */
    public void setBonesAsQuaternions(@EntityInstance int instance, @NonNull @Size(min = 8) float[] transforms) {
        setBonesAsQuaternions(instance, transforms, 0, transforms.length / 8, 0);
    }

    /**
     * @param instance instance of the component obtained from getInstance()
     * @param transforms buffer containing transforms data
     * @param count number of elements (structured element count) in <code>transforms</code>
     * @param offset offset in elements (structured element count) in the destination buffer or component
     */
    public void setBonesAsMatrices(@EntityInstance int instance, @NonNull Buffer transforms, @IntRange(from = 0) int count, @IntRange(from = 0) int offset) {
        int result = nSetBonesAsMatrices(getNativeObject(), instance, transforms, transforms.remaining(), count, offset);
        if (result < 0) {
            throw new BufferOverflowException();
        }
    }

    /**
     * @param instance instance of the component obtained from getInstance()
     * @param transforms buffer containing transforms data
     * @param count number of elements (structured element count) in <code>transforms</code>
     */
    public void setBonesAsMatrices(@EntityInstance int instance, @NonNull Buffer transforms, @IntRange(from = 0) int count) {
        setBonesAsMatrices(instance, transforms, count, 0);
    }

    /**
     * @param instance instance of the component obtained from getInstance()
     * @param transforms array containing transforms data
     * @param arrayOffset offset in elements (structured element count) in <code>transforms</code> to skip
     * @param count number of elements (structured element count) in <code>transforms</code>
     * @param offset offset in elements (structured element count) in the destination buffer or component
     */
    public void setBonesAsMatrices(@EntityInstance int instance, @NonNull @Size(min = 16) float[] transforms, @IntRange(from = 0) int arrayOffset, @IntRange(from = 0) int count, @IntRange(from = 0) int offset) {
        if (transforms.length < (arrayOffset + count) * 16) {
            throw new ArrayIndexOutOfBoundsException("Array length must be at least " + ((arrayOffset + count) * 16));
        }
        nSetBonesAsMatrices(getNativeObject(), instance, transforms, count, offset, arrayOffset);
    }

    /**
     * @param instance instance of the component obtained from getInstance()
     * @param transforms array containing transforms data
     * @param count number of elements (structured element count) in <code>transforms</code>
     * @param offset offset in elements (structured element count) in the destination buffer or component
     */
    public void setBonesAsMatrices(@EntityInstance int instance, @NonNull @Size(min = 16) float[] transforms, @IntRange(from = 0) int count, @IntRange(from = 0) int offset) {
        setBonesAsMatrices(instance, transforms, 0, count, offset);
    }

    /**
     * @param instance instance of the component obtained from getInstance()
     * @param transforms array containing transforms data
     * @param count number of elements (structured element count) in <code>transforms</code>
     */
    public void setBonesAsMatrices(@EntityInstance int instance, @NonNull @Size(min = 16) float[] transforms, @IntRange(from = 0) int count) {
        setBonesAsMatrices(instance, transforms, 0, count, 0);
    }

    /**
     * @param instance instance of the component obtained from getInstance()
     * @param transforms array containing transforms data
     */
    public void setBonesAsMatrices(@EntityInstance int instance, @NonNull @Size(min = 16) float[] transforms) {
        setBonesAsMatrices(instance, transforms, 0, transforms.length / 16, 0);
    }

    /**
     * Associates a region of a SkinningBuffer to a renderable instance
     *
     * <p>Note: due to hardware limitations offset + 256 must be smaller or equal to
     *       skinningBuffer->getBoneCount()</p>
     *
     * @param instance Instance of the component obtained from getInstance().
     * @param skinningBuffer skinning buffer to associate to the instance
     * @param count Size of the region in bones, must be smaller or equal to 256.
     * @param offset Start offset of the region in bones
     */
    public void setSkinningBuffer(@EntityInstance int instance, @NonNull SkinningBuffer skinningBuffer, @IntRange(from = 0) int count, @IntRange(from = 0) int offset) {
        nSetSkinningBuffer(getNativeObject(), instance, skinningBuffer.getNativeObject(), count, offset);
    }

    /**
     * Updates the vertex morphing weights on a renderable, all zeroes by default.
     *
     * <p>The renderable must be built with morphing enabled, see Builder::morphing(). In legacy
     * morphing mode, only the first 4 weights are considered.</p>
     *
     * @param instance Instance of the component obtained from getInstance().
     * @param weights Morph target weights to be updated.
     */
    public void setMorphWeights(@EntityInstance int instance, @NonNull float[] weights) {
        setMorphWeights(instance, weights, 0);
    }

    /**
     * Updates the vertex morphing weights on a renderable, all zeroes by default.
     *
     * <p>The renderable must be built with morphing enabled, see Builder::morphing(). In legacy
     * morphing mode, only the first 4 weights are considered.</p>
     *
     * @param instance Instance of the component obtained from getInstance().
     * @param weights Morph target weights to be updated.
     * @param offset Index of the first morph target weight to set at instance.
     */
    public void setMorphWeights(@EntityInstance int instance, @NonNull float[] weights, @IntRange(from = 0) int offset) {
        nSetMorphWeights(getNativeObject(), instance, weights, weights.length, offset);
    }

    /** Associates a MorphTargetBuffer to the given primitive. */
    public void setMorphTargetBufferOffsetAt(@EntityInstance int instance, @IntRange(from = 0) int level, @IntRange(from = 0) int primitiveIndex, @IntRange(from = 0) int offset) {
        nSetMorphTargetBufferOffsetAt(getNativeObject(), instance, level, primitiveIndex, offset);
    }

    /** Get a MorphTargetBuffer to the given renderable or null if it doesn't exist. */
    public MorphTargetBuffer getMorphTargetBuffer(@EntityInstance int instance) {
        long nativeMorphTargetBuffer = nGetMorphTargetBuffer(getNativeObject(), instance);
        return nativeMorphTargetBuffer == 0 ? null : new MorphTargetBuffer(nativeMorphTargetBuffer);
    }

    /**
     * Gets the number of morphing in the given entity.
     *
     * @see Builder#morphing
     */
    @IntRange(from = 0)
    public int getMorphTargetCount(@EntityInstance int instance) {
        return nGetMorphTargetCount(getNativeObject(), instance);
    }

    /**
     * Gets the immutable number of primitives in the given renderable.
     *
     * @see Builder#Builder(int)
     */
    @IntRange(from = 0)
    public int getPrimitiveCount(@EntityInstance int instance) {
        return nGetPrimitiveCount(getNativeObject(), instance);
    }

    /**
     * Returns the number of instances for this renderable.
     *
     * @param instance Instance of the component obtained from getInstance().
     *
     * @return The number of instances.
     *
     * @see Builder#instances
     */
    @IntRange(from = 0)
    public int getInstanceCount(@EntityInstance int instance) {
        return nGetInstanceCount(getNativeObject(), instance);
    }

    /**
     * Changes the material instance binding for the given primitive.
     *
     * <p>The MaterialInstance's material must have a feature level equal or lower to the engine's
     * selected feature level.</p>
     *
     * @see Builder#material
     * @see Engine#setActiveFeatureLevel
     */
    public void setMaterialInstanceAt(@EntityInstance int instance, @IntRange(from = 0) int primitiveIndex, @NonNull MaterialInstance materialInstance) {
        nSetMaterialInstanceAt(getNativeObject(), instance, primitiveIndex, materialInstance.getNativeObject());
    }

    /**
     * Clear the MaterialInstance for the given primitive.
     *
     * @param instance Renderable's instance
     * @param primitiveIndex Primitive index
     */
    public void clearMaterialInstanceAt(@EntityInstance int instance, @IntRange(from = 0) int primitiveIndex) {
        nClearMaterialInstanceAt(getNativeObject(), instance, primitiveIndex);
    }

    /** Retrieves the material instance that is bound to the given primitive. */
    public MaterialInstance getMaterialInstanceAt(@EntityInstance int instance, @IntRange(from = 0) int primitiveIndex) {
        long nativeMaterialInstance = nGetMaterialInstanceAt(getNativeObject(), instance, primitiveIndex);
        return nativeMaterialInstance == 0 ? null : new MaterialInstance(nativeMaterialInstance);
    }

    /**
     * Changes the geometry for the given primitive.
     *
     * @param instance Renderable's instance
     * @param primitiveIndex Primitive index
     * @param type Specifies the topology of the primitive
     * @param vertices Specifies the vertex buffer
     * @param indices Specifies the index buffer
     *
     * @see Builder#geometry
     */
    public void setGeometryAt(@EntityInstance int instance, @IntRange(from = 0) int primitiveIndex, @NonNull PrimitiveType type, @NonNull VertexBuffer vertices, @NonNull IndexBuffer indices) {
        nSetGeometryAt(getNativeObject(), instance, primitiveIndex, type.toFilamentNative(), vertices.getNativeObject(), indices.getNativeObject());
    }

    /**
     * Changes the geometry for the given primitive.
     *
     * @param instance Renderable's instance
     * @param primitiveIndex Primitive index
     * @param type Specifies the topology of the primitive
     * @param vertices Specifies the vertex buffer
     * @param indices Specifies the index buffer
     * @param offset Specifies where in the index buffer to start reading (expressed as a number of indices)
     * @param count Number of indices to read
     *
     * @see Builder#geometry
     */
    public void setGeometryAt(@EntityInstance int instance, @IntRange(from = 0) int primitiveIndex, @NonNull PrimitiveType type, @NonNull VertexBuffer vertices, @NonNull IndexBuffer indices, @IntRange(from = 0) int offset, @IntRange(from = 0) int count) {
        nSetGeometryAt(getNativeObject(), instance, primitiveIndex, type.toFilamentNative(), vertices.getNativeObject(), indices.getNativeObject(), offset, count);
    }

    /**
     * Changes the geometry for the given primitive. (non-indexed version)
     *
     * @param instance Renderable's instance
     * @param primitiveIndex Primitive index
     * @param type Specifies the topology of the primitive
     * @param vertices Specifies the vertex buffer
     *
     * @see Builder#geometry
     */
    public void setGeometryAt(@EntityInstance int instance, @IntRange(from = 0) int primitiveIndex, @NonNull PrimitiveType type, @NonNull VertexBuffer vertices) {
        nSetGeometryAt(getNativeObject(), instance, primitiveIndex, type.toFilamentNative(), vertices.getNativeObject());
    }

    /**
     * Changes the geometry for the given primitive. (non-indexed version)
     *
     * @param instance Renderable's instance
     * @param primitiveIndex Primitive index
     * @param type Specifies the topology of the primitive
     * @param vertices Specifies the vertex buffer
     * @param offset Specifies where in the vertex buffer to start reading (expressed as a number of vertices)
     * @param count Number of vertices to read
     *
     * @see Builder#geometry
     */
    public void setGeometryAt(@EntityInstance int instance, @IntRange(from = 0) int primitiveIndex, @NonNull PrimitiveType type, @NonNull VertexBuffer vertices, @IntRange(from = 0) int offset, @IntRange(from = 0) int count) {
        nSetGeometryAt(getNativeObject(), instance, primitiveIndex, type.toFilamentNative(), vertices.getNativeObject(), offset, count);
    }

    /**
     * Changes the drawing order for blended primitives.
     *
     * <p>The drawing order is either global or
     * local (default) to this Renderable. In either case, the Renderable priority takes precedence.</p>
     *
     * @param instance the renderable of interest
     * @param primitiveIndex the primitive of interest
     * @param order draw order number (0 by default). Only the lowest 15 bits are used.
     *
     * @see Builder#blendOrder
     * @see #setGlobalBlendOrderEnabledAt
     */
    public void setBlendOrderAt(@EntityInstance int instance, @IntRange(from = 0) int primitiveIndex, @IntRange(from = 0) int order) {
        nSetBlendOrderAt(getNativeObject(), instance, primitiveIndex, order);
    }

    /**
     * Get the drawing order for blended primitives.
     *
     * @param instance the renderable of interest
     * @param primitiveIndex the primitive of interest
     *
     * @see Builder#blendOrder
     * @see #setGlobalBlendOrderEnabledAt
     */
    @IntRange(from = 0)
    public int getBlendOrderAt(@EntityInstance int instance, @IntRange(from = 0) int primitiveIndex) {
        return nGetBlendOrderAt(getNativeObject(), instance, primitiveIndex);
    }

    /**
     * Changes whether the blend order is global or local to this Renderable (by default).
     *
     * @param instance the renderable of interest
     * @param primitiveIndex the primitive of interest
     * @param enabled true for global, false for local blend ordering.
     *
     * @see Builder#globalBlendOrderEnabled
     * @see #setBlendOrderAt
     */
    public void setGlobalBlendOrderEnabledAt(@EntityInstance int instance, @IntRange(from = 0) int primitiveIndex, boolean enabled) {
        nSetGlobalBlendOrderEnabledAt(getNativeObject(), instance, primitiveIndex, enabled);
    }

    /**
     * Get whether the blend order is global or local to this Renderable (by default).
     *
     * @param instance the renderable of interest
     * @param primitiveIndex the primitive of interest
     *
     * @see Builder#globalBlendOrderEnabled
     * @see #setBlendOrderAt
     */
    public boolean isGlobalBlendOrderEnabledAt(@EntityInstance int instance, @IntRange(from = 0) int primitiveIndex) {
        return nIsGlobalBlendOrderEnabledAt(getNativeObject(), instance, primitiveIndex);
    }

    /** Retrieves the set of enabled attribute slots in the given primitive's VertexBuffer. */
    @NonNull
    public Set<VertexBuffer.VertexAttribute> getEnabledAttributesAt(@EntityInstance int instance, @IntRange(from = 0) int primitiveIndex) {
        return getAttributes(nGetEnabledAttributesAt(getNativeObject(), instance, primitiveIndex));
    }

    public long getNativeObject() {
        if (mNativeObject == 0) {
            throw new IllegalStateException("Calling method on destroyed RenderableManager");
        }
        return mNativeObject;
    }

    void clearNativeObject() {
        mNativeObject = 0;
    }

    private static native boolean nHasComponent(long nativeRenderableManager, @Entity int e);
    @EntityInstance
    private static native int nGetInstance(long nativeRenderableManager, @Entity int e);
    @IntRange(from = 0)
    private static native int nGetComponentCount(long nativeRenderableManager);
    private static native boolean nEmpty(long nativeRenderableManager);
    @Entity
    private static native int nGetEntity(long nativeRenderableManager, @EntityInstance int i);
    @NonNull
    private static native int[] nGetAllEntities(long nativeRenderableManager, int[] out);
    private static native void nDestroy(long nativeRenderableManager, @Entity int e);
    private static native void nSetAxisAlignedBoundingBox(long nativeRenderableManager, @EntityInstance int instance, float aabbCenterX, float aabbCenterY, float aabbCenterZ, float aabbHalfExtentX, float aabbHalfExtentY, float aabbHalfExtentZ);
    private static native void nGetAxisAlignedBoundingBox(long nativeRenderableManager, @EntityInstance int instance, @NonNull Box out);
    private static native void nSetLayerMask(long nativeRenderableManager, @EntityInstance int instance, @IntRange(from = 0) int select, @IntRange(from = 0) int values);
    @IntRange(from = 0)
    private static native int nGetLayerMask(long nativeRenderableManager, @EntityInstance int instance);
    private static native void nSetPriority(long nativeRenderableManager, @EntityInstance int instance, @IntRange(from = 0) int priority);
    @IntRange(from = 0)
    private static native int nGetPriority(long nativeRenderableManager, @EntityInstance int instance);
    private static native void nSetChannel(long nativeRenderableManager, @EntityInstance int instance, @IntRange(from = 0) int channel);
    @IntRange(from = 0)
    private static native int nGetChannel(long nativeRenderableManager, @EntityInstance int instance);
    private static native void nSetCulling(long nativeRenderableManager, @EntityInstance int instance, boolean enable);
    private static native boolean nIsCullingEnabled(long nativeRenderableManager, @EntityInstance int instance);
    private static native void nSetFogEnabled(long nativeRenderableManager, @EntityInstance int instance, boolean enable);
    private static native boolean nGetFogEnabled(long nativeRenderableManager, @EntityInstance int instance);
    private static native void nSetLightChannel(long nativeRenderableManager, @EntityInstance int instance, @IntRange(from = 0) int channel, boolean enable);
    private static native boolean nGetLightChannel(long nativeRenderableManager, @EntityInstance int instance, @IntRange(from = 0) int channel);
    private static native void nSetCastShadows(long nativeRenderableManager, @EntityInstance int instance, boolean enable);
    private static native void nSetReceiveShadows(long nativeRenderableManager, @EntityInstance int instance, boolean enable);
    private static native void nSetScreenSpaceContactShadows(long nativeRenderableManager, @EntityInstance int instance, boolean enable);
    private static native boolean nIsShadowCaster(long nativeRenderableManager, @EntityInstance int instance);
    private static native boolean nIsShadowReceiver(long nativeRenderableManager, @EntityInstance int instance);
    private static native boolean nIsScreenSpaceContactShadowsEnabled(long nativeRenderableManager, @EntityInstance int instance);
    private static native int nSetBonesAsQuaternions(long nativeRenderableManager, int instance, @NonNull Buffer transforms, int remaining, @IntRange(from = 0) int count, @IntRange(from = 0) int offset);
    private static native void nSetBonesAsQuaternions(long nativeRenderableManager, int instance, @NonNull @Size(min = 8) float[] transforms, @IntRange(from = 0) int count, @IntRange(from = 0) int offset, @IntRange(from = 0) int arrayOffset);
    private static native int nSetBonesAsMatrices(long nativeRenderableManager, int instance, @NonNull Buffer transforms, int remaining, @IntRange(from = 0) int count, @IntRange(from = 0) int offset);
    private static native void nSetBonesAsMatrices(long nativeRenderableManager, int instance, @NonNull @Size(min = 16) float[] transforms, @IntRange(from = 0) int count, @IntRange(from = 0) int offset, @IntRange(from = 0) int arrayOffset);
    private static native void nSetSkinningBuffer(long nativeRenderableManager, @EntityInstance int instance, long nativeSkinningBuffer, @IntRange(from = 0) int count, @IntRange(from = 0) int offset);
    private static native void nSetMorphWeights(long nativeRenderableManager, @EntityInstance int instance, @NonNull float[] weights, int weightsCount, @IntRange(from = 0) int offset);
    private static native void nSetMorphTargetBufferOffsetAt(long nativeRenderableManager, @EntityInstance int instance, @IntRange(from = 0) int level, @IntRange(from = 0) int primitiveIndex, @IntRange(from = 0) int offset);
    private static native long nGetMorphTargetBuffer(long nativeRenderableManager, @EntityInstance int instance);
    @IntRange(from = 0)
    private static native int nGetMorphTargetCount(long nativeRenderableManager, @EntityInstance int instance);
    @IntRange(from = 0)
    private static native int nGetPrimitiveCount(long nativeRenderableManager, @EntityInstance int instance);
    @IntRange(from = 0)
    private static native int nGetInstanceCount(long nativeRenderableManager, @EntityInstance int instance);
    private static native void nSetMaterialInstanceAt(long nativeRenderableManager, @EntityInstance int instance, @IntRange(from = 0) int primitiveIndex, long nativeMaterialInstance);
    private static native void nClearMaterialInstanceAt(long nativeRenderableManager, @EntityInstance int instance, @IntRange(from = 0) int primitiveIndex);
    private static native long nGetMaterialInstanceAt(long nativeRenderableManager, @EntityInstance int instance, @IntRange(from = 0) int primitiveIndex);
    private static native void nSetGeometryAt(long nativeRenderableManager, @EntityInstance int instance, @IntRange(from = 0) int primitiveIndex, int type, long nativeVertices, long nativeIndices);
    private static native void nSetGeometryAt(long nativeRenderableManager, @EntityInstance int instance, @IntRange(from = 0) int primitiveIndex, int type, long nativeVertices, long nativeIndices, @IntRange(from = 0) int offset, @IntRange(from = 0) int count);
    private static native void nSetGeometryAt(long nativeRenderableManager, @EntityInstance int instance, @IntRange(from = 0) int primitiveIndex, int type, long nativeVertices);
    private static native void nSetGeometryAt(long nativeRenderableManager, @EntityInstance int instance, @IntRange(from = 0) int primitiveIndex, int type, long nativeVertices, @IntRange(from = 0) int offset, @IntRange(from = 0) int count);
    private static native void nSetBlendOrderAt(long nativeRenderableManager, @EntityInstance int instance, @IntRange(from = 0) int primitiveIndex, @IntRange(from = 0) int order);
    @IntRange(from = 0)
    private static native int nGetBlendOrderAt(long nativeRenderableManager, @EntityInstance int instance, @IntRange(from = 0) int primitiveIndex);
    private static native void nSetGlobalBlendOrderEnabledAt(long nativeRenderableManager, @EntityInstance int instance, @IntRange(from = 0) int primitiveIndex, boolean enabled);
    private static native boolean nIsGlobalBlendOrderEnabledAt(long nativeRenderableManager, @EntityInstance int instance, @IntRange(from = 0) int primitiveIndex);
    @IntRange(from = 0)
    private static native int nGetEnabledAttributesAt(long nativeRenderableManager, @EntityInstance int instance, @IntRange(from = 0) int primitiveIndex);
    private static native long nCreateBuilder(int count);
    private static native void nDestroyBuilder(long nativeBuilder);
    private static native void nBuilderGeometry(long nativeBuilder, int index, int type, long vertices, long indices, int offset, int minIndex, int maxIndex, int count);
    private static native void nBuilderGeometry(long nativeBuilder, int index, int type, long vertices, long indices, int offset, int count);
    private static native void nBuilderGeometry(long nativeBuilder, int index, int type, long vertices, long indices);
    private static native void nBuilderGeometry(long nativeBuilder, int index, int type, long vertices, int offset, int count);
    private static native void nBuilderGeometry(long nativeBuilder, int index, int type, long vertices);
    private static native void nBuilderGeometryType(long nativeBuilder, int type);
    private static native void nBuilderMaterial(long nativeBuilder, int index, long materialInstance);
    private static native void nBuilderBoundingBox(long nativeBuilder, float centerX, float centerY, float centerZ, float halfExtentX, float halfExtentY, float halfExtentZ);
    private static native void nBuilderLayerMask(long nativeBuilder, int select, int values);
    private static native void nBuilderPriority(long nativeBuilder, int priority);
    private static native void nBuilderChannel(long nativeBuilder, int channel);
    private static native void nBuilderCulling(long nativeBuilder, boolean enable);
    private static native void nBuilderLightChannel(long nativeBuilder, int channel, boolean enable);
    private static native void nBuilderCastShadows(long nativeBuilder, boolean enable);
    private static native void nBuilderReceiveShadows(long nativeBuilder, boolean enable);
    private static native void nBuilderScreenSpaceContactShadows(long nativeBuilder, boolean enable);
    private static native void nBuilderEnableSkinningBuffers(long nativeBuilder, boolean enabled);
    private static native void nBuilderFog(long nativeBuilder, boolean enabled);
    private static native void nBuilderSkinning(long nativeBuilder, long skinningBuffer, int count, int offset);
    private static native int nBuilderSkinningAsMatrices(long nativeBuilder, int boneCount, @NonNull Buffer transforms, int remaining);
    private static native void nBuilderSkinningAsMatrices(long nativeBuilder, @NonNull @Size(min = 16) float[] transforms, int boneCount, int offset);
    private static native int nBuilderSkinning(long nativeBuilder, int boneCount, @NonNull Buffer bones, int remaining);
    private static native void nBuilderSkinning(long nativeBuilder, @NonNull @Size(min = 8) float[] bones, int boneCount, int offset);
    private static native void nBuilderSkinning(long nativeBuilder, int boneCount);
    private static native void nBuilderBoneIndicesAndWeights(long nativeBuilder, int primitiveIndex, float[] indicesAndWeights, int count, int bonesPerVertex);
    private static native void nBuilderMorphing(long nativeBuilder, int targetCount);
    private static native void nBuilderMorphing(long nativeBuilder, long morphTargetBuffer);
    private static native void nBuilderMorphing(long nativeBuilder, int level, int primitiveIndex, int offset);
    private static native void nBuilderBlendOrder(long nativeBuilder, int primitiveIndex, int blendOrder);
    private static native void nBuilderGlobalBlendOrderEnabled(long nativeBuilder, int primitiveIndex, boolean enabled);
    private static native void nBuilderInstances(long nativeBuilder, int instanceCount);
    private static native void nBuilderInstances(long nativeBuilder, int instanceCount, long instanceBuffer);
    private static native boolean nBuilderBuild(long nativeBuilder, long nativeEngine, int entity);
}

// THIS FILE IS GENERATED BY APIGEN AND MUST NOT BE HAND-MODIFIED.
