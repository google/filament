/*
 * Copyright (C) 2026 The Android Open Source Project
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *      http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

// THIS FILE IS GENERATED BY APIGEN AND MUST NOT BE HAND-MODIFIED.

package com.google.android.filament;

import java.lang.IllegalStateException;

import androidx.annotation.IntRange;
import androidx.annotation.NonNull;
import androidx.annotation.Nullable;
import androidx.annotation.RestrictTo;

/**
 * A Scene is a flat container of Renderable and Light instances.
 *
 * <p>A Scene doesn't provide a hierarchy of Renderable objects, i.e.: it's not a scene-graph.
 * However, it manages the list of objects to render and the list of lights. Renderable
 * and Light objects can be added or removed from a Scene at any time.</p>
 *
 * <p>A Renderable <i>must</i> be added to a Scene in order to be rendered, and the Scene must be
 * provided to a View.</p>
 *
 * <h1>Creation and Destruction</h1>
 *
 * <p>A Scene is created using Engine.createScene() and destroyed using
 * Engine.destroy(const Scene*).</p>
 *
 * <pre>{@code
 * #include <filament/Scene.h>
 * #include <filament/Engine.h>
 * using namespace filament;
 *
 * Engine* engine = Engine::create();
 *
 * Scene* scene = engine->createScene();
 * engine->destroy(&scene);
 *
 * }</pre>
 *
 * @see View
 * @see Renderable
 * @see Light
 */
public class Scene {
    private long mNativeObject;
    private @Nullable Skybox mSkybox;
    private @Nullable IndirectLight mIndirectLight;

    @FunctionalInterface
    public interface ForEachCallback {
        void accept(@Entity int entity);
    }

    Scene(long nativeObject) {
        mNativeObject = nativeObject;
    }

    @NonNull
    @RestrictTo(RestrictTo.Scope.LIBRARY_GROUP)
    public static Scene wrap(long nativeObject) {
        return new Scene(nativeObject);
    }

    /**
     * Sets the Skybox.
     *
     * <p>The Skybox is drawn last and covers all pixels not touched by geometry.</p>
     *
     * @param skybox The Skybox to use to fill untouched pixels, or nullptr to unset the Skybox.
     */
    public void setSkybox(@Nullable Skybox skybox) {
        mSkybox = skybox;
        nSetSkybox(getNativeObject(), skybox != null ? skybox.getNativeObject() : 0);
    }

    /**
     * Returns the Skybox associated with the Scene.
     *
     * @return The associated Skybox, or nullptr if there is none.
     */
    @Nullable
    public Skybox getSkybox() {
        return mSkybox;
    }

    /**
     * Set the IndirectLight to use when rendering the Scene.
     *
     * <p>Currently, a Scene may only have a single IndirectLight. This call replaces the current
     * IndirectLight.</p>
     *
     * @param ibl The IndirectLight to use when rendering the Scene or nullptr to unset.
     *
     * @see #getIndirectLight
     */
    public void setIndirectLight(@Nullable IndirectLight ibl) {
        mIndirectLight = ibl;
        nSetIndirectLight(getNativeObject(), ibl != null ? ibl.getNativeObject() : 0);
    }

    /**
     * Get the IndirectLight or nullptr if none is set.
     *
     * @return the the IndirectLight or nullptr if none is set
     *
     * @see #setIndirectLight
     */
    @Nullable
    public IndirectLight getIndirectLight() {
        return mIndirectLight;
    }

    /**
     * Adds an Entity to the Scene.
     *
     * @param entity <p>The entity is ignored for rendering purposes if it doesn't have a Renderable or Light component.</p>
     *
     * <p>\attention
     *  A given Entity object can only be added once to a Scene.</p>
     */
    public void addEntity(@Entity int entity) {
        nAddEntity(getNativeObject(), entity);
    }

    /**
     * Adds a list of entities to the Scene.
     *
     * @param entities Array containing entities to add to the scene.
     */
    public void addEntities(@NonNull @Entity int[] entities) {
        nAddEntities(getNativeObject(), entities, entities.length);
    }

    /**
     * Removes the Renderable from the Scene.
     *
     * @param entity The Entity to remove from the Scene. If the specified
     *                   {@code entity} doesn't exist, this call is ignored.
     */
    public void remove(@Entity int entity) {
        nRemove(getNativeObject(), entity);
    }

    /**
     * Removes a list of entities to the Scene.
     *
     * <p>This is equivalent to calling remove in a loop.
     * If any of the specified entities do not exist in the scene, they are skipped.</p>
     *
     * @param entities Array containing entities to remove from the scene.
     */
    public void removeEntities(@NonNull @Entity int[] entities) {
        nRemoveEntities(getNativeObject(), entities, entities.length);
    }

    /** Remove all entities to the Scene. */
    public void removeAllEntities() {
        nRemoveAllEntities(getNativeObject());
    }

    /**
     * Returns the total number of Entities in the Scene, whether alive or not.
     *
     * @return Total number of Entities in the Scene.
     */
    @IntRange(from = 0)
    public int getEntityCount() {
        return nGetEntityCount(getNativeObject());
    }

    /**
     * Returns the number of active (alive) Renderable objects in the Scene.
     *
     * @return The number of active (alive) Renderable objects in the Scene.
     */
    @IntRange(from = 0)
    public int getRenderableCount() {
        return nGetRenderableCount(getNativeObject());
    }

    /**
     * Returns the number of active (alive) Light objects in the Scene.
     *
     * @return The number of active (alive) Light objects in the Scene.
     */
    @IntRange(from = 0)
    public int getLightCount() {
        return nGetLightCount(getNativeObject());
    }

    /**
     * Returns true if the given entity is in the Scene.
     *
     * @return Whether the given entity is in the Scene.
     */
    public boolean hasEntity(@Entity int entity) {
        return nHasEntity(getNativeObject(), entity);
    }

    /**
     * Invokes user functor on each entity in the scene.
     *
     * <p>It is not allowed to add or remove an entity from the scene within the functor.</p>
     *
     * @param functor User provided functor called for each entity in the scene
     */
    public void forEach(@NonNull ForEachCallback functor) {
        nForEach(getNativeObject(), functor);
    }

    public long getNativeObject() {
        if (mNativeObject == 0) {
            throw new IllegalStateException("Calling method on destroyed Scene");
        }
        return mNativeObject;
    }

    void clearNativeObject() {
        mNativeObject = 0;
    }

    private static native void nSetSkybox(long nativeScene, long nativeSkybox);
    private static native void nSetIndirectLight(long nativeScene, long nativeIbl);
    private static native void nAddEntity(long nativeScene, @Entity int entity);
    private static native void nAddEntities(long nativeScene, @NonNull @Entity int[] entities, int entitiesCount);
    private static native void nRemove(long nativeScene, @Entity int entity);
    private static native void nRemoveEntities(long nativeScene, @NonNull @Entity int[] entities, int entitiesCount);
    private static native void nRemoveAllEntities(long nativeScene);
    @IntRange(from = 0)
    private static native int nGetEntityCount(long nativeScene);
    @IntRange(from = 0)
    private static native int nGetRenderableCount(long nativeScene);
    @IntRange(from = 0)
    private static native int nGetLightCount(long nativeScene);
    private static native boolean nHasEntity(long nativeScene, @Entity int entity);
    private static native void nForEach(long nativeScene, @NonNull ForEachCallback functor);
}

// THIS FILE IS GENERATED BY APIGEN AND MUST NOT BE HAND-MODIFIED.
