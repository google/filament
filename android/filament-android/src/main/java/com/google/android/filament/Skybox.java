/*
 * Copyright (C) 2026 The Android Open Source Project
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *      http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

// THIS FILE IS GENERATED BY APIGEN AND MUST NOT BE HAND-MODIFIED.

package com.google.android.filament;

import java.lang.IllegalStateException;

import androidx.annotation.IntRange;
import androidx.annotation.NonNull;
import androidx.annotation.RestrictTo;
import androidx.annotation.Size;

/**
 * Skybox
 *
 * <p>When added to a Scene, the Skybox fills all untouched pixels.</p>
 *
 * <h1>Creation and destruction</h1>
 *
 * <p>A Skybox object is created using the Skybox::Builder and destroyed by calling
 * Engine::destroy(const Skybox*).</p>
 *
 * <pre>{@code
 *  filament::Engine* engine = filament::Engine::create();
 *
 *  filament::IndirectLight* skybox = filament::Skybox::Builder()
 *              .environment(cubemap)
 *              .build(*engine);
 *
 *  engine->destroy(skybox);
 *
 * }</pre>
 *
 * <p>Currently only Texture based sky boxes are supported.</p>
 *
 * @see Scene
 * @see IndirectLight
 */
public class Skybox {
    private long mNativeObject;

    Skybox(long nativeObject) {
        mNativeObject = nativeObject;
    }

    @NonNull
    @RestrictTo(RestrictTo.Scope.LIBRARY_GROUP)
    public static Skybox wrap(long nativeObject) {
        return new Skybox(nativeObject);
    }

    /** Use Builder to construct an Skybox object instance */
    public static class Builder {
        @SuppressWarnings({"FieldCanBeLocal", "UnusedDeclaration"}) // Keep to finalize native resources
        private final BuilderFinalizer mFinalizer;
        private final long mNativeBuilder;

        public Builder() {
            mNativeBuilder = nCreateBuilder();
            mFinalizer = new BuilderFinalizer(mNativeBuilder);
        }

        long getNativeBuilder() {
            return mNativeBuilder;
        }

        /**
         * Set the environment map (i.e. the skybox content).
         *
         * <p>The Skybox is rendered as though it were an infinitely large cube with the camera
         * inside it. This means that the cubemap which is mapped onto the cube's exterior
         * will appear mirrored. This follows the OpenGL conventions.</p>
         *
         * <p>The cmgen tool generates reflection maps by default which are therefore ideal to use
         * as skyboxes.</p>
         *
         * @param cubemap This Texture must be a cube map.
         *
         * @return This Builder, for chaining calls.
         *
         * @see Texture
         */
        @NonNull
        public Builder environment(@NonNull Texture cubemap) {
            nBuilderEnvironment(mNativeBuilder, cubemap.getNativeObject());
            return this;
        }

        /**
         * Indicates whether the sun should be rendered.
         *
         * <p>The sun can only be
         * rendered if there is at least one light of type SUN in the scene.
         * The default value is false.</p>
         *
         * @param show True if the sun should be rendered, false otherwise
         *
         * @return This Builder, for chaining calls.
         */
        @NonNull
        public Builder showSun(boolean show) {
            nBuilderShowSun(mNativeBuilder, show);
            return this;
        }

        /**
         * Skybox intensity when no IndirectLight is set on the Scene.
         *
         * <p>This call is ignored when an IndirectLight is set on the Scene, and the intensity
         * of the IndirectLight is used instead.</p>
         *
         * @param envIntensity Scale factor applied to the skybox texel values such that
         *                      the result is in lux, or lumen/m^2 (default = 30000)
         *
         * @return This Builder, for chaining calls.
         *
         * @see IndirectLight.Builder#intensity
         */
        @NonNull
        public Builder intensity(float envIntensity) {
            nBuilderIntensity(mNativeBuilder, envIntensity);
            return this;
        }

        /**
         * Sets the skybox to a constant color.
         *
         * <p>Default is opaque black.</p>
         *
         * <p>Ignored if an environment is set.</p>
         *
         * @return This Builder, for chaining calls.
         */
        @NonNull
        public Builder color(float colorx, float colory, float colorz, float colorw) {
            nBuilderColor(mNativeBuilder, colorx, colory, colorz, colorw);
            return this;
        }

        /**
         * Sets the skybox to a constant color.
         *
         * <p>Default is opaque black.</p>
         *
         * <p>Ignored if an environment is set.</p>
         *
         * @param color the constant color
         *
         * @return This Builder, for chaining calls.
         */
        @NonNull
        public Builder color(@NonNull @Size(min = 4) float[] color) {
            return color(color[0], color[1], color[2], color[3]);
        }

        /**
         * Set the rendering priority of the Skybox.
         *
         * <p>By default, it is set to the lowest
         * priority (7) such that the Skybox is always rendered after the opaque objects,
         * to reduce overdraw when depth culling is enabled.</p>
         *
         * @param priority clamped to the range [0..7], defaults to 7; 7 is lowest priority
         *                 (rendered last).
         *
         * @return Builder reference for chaining calls.
         *
         * @see RenderableManager.Builder#priority
         */
        @NonNull
        public Builder priority(@IntRange(from = 0) int priority) {
            nBuilderPriority(mNativeBuilder, priority);
            return this;
        }

        /**
         * Creates the Skybox object and returns a pointer to it.
         *
         * @param engine Reference to the filament::Engine to associate this Skybox with.
         *
         * @return pointer to the newly created object.
         */
        @NonNull
        public Skybox build(@NonNull Engine engine) {
            long nativeSkybox = nBuilderBuild(mNativeBuilder, engine.getNativeObject());
            if (nativeSkybox == 0) throw new IllegalStateException("Couldn't create Skybox");
            return new Skybox(nativeSkybox);
        }

        private static class BuilderFinalizer {
            private final long mNativeObject;

            BuilderFinalizer(long nativeObject) { mNativeObject = nativeObject; }

            @Override
            public void finalize() {
                try {
                    super.finalize();
                } catch (Throwable t) { // Ignore
                } finally {
                    nDestroyBuilder(mNativeObject);
                }
            }
        }
    }

    /**
     * @param colorx (x component)
     * @param colory (y component)
     * @param colorz (z component)
     * @param colorw (w component)
     */
    public void setColor(float colorx, float colory, float colorz, float colorw) {
        nSetColor(getNativeObject(), colorx, colory, colorz, colorw);
    }

    /** @param color */
    public void setColor(@NonNull @Size(min = 4) float[] color) {
        setColor(color[0], color[1], color[2], color[3]);
    }

    /**
     * Sets bits in a visibility mask.
     *
     * <p>By default, this is 0x1.</p>
     *
     * <p>This provides a simple mechanism for hiding or showing this Skybox in a Scene.</p>
     *
     * <p>For example, to set bit 1 and reset bits 0 and 2 while leaving all other bits unaffected,
     * call: {@code setLayerMask(7, 2)}.</p>
     *
     * @param select the set of bits to affect
     * @param values the replacement values for the affected bits
     *
     * @see View#setVisibleLayers
     */
    public void setLayerMask(@IntRange(from = 0) int select, @IntRange(from = 0) int values) {
        nSetLayerMask(getNativeObject(), select, values);
    }

    /** @return the visibility mask bits */
    @IntRange(from = 0)
    public int getLayerMask() {
        return nGetLayerMask(getNativeObject());
    }

    /** Returns the skybox's intensity in lux, or lumen/m^2. */
    public float getIntensity() {
        return nGetIntensity(getNativeObject());
    }

    /** @return the associated texture */
    public Texture getTexture() {
        long nativeTexture = nGetTexture(getNativeObject());
        return nativeTexture == 0 ? null : new Texture(nativeTexture);
    }

    public long getNativeObject() {
        if (mNativeObject == 0) {
            throw new IllegalStateException("Calling method on destroyed Skybox");
        }
        return mNativeObject;
    }

    void clearNativeObject() {
        mNativeObject = 0;
    }

    private static native void nSetColor(long nativeSkybox, float colorx, float colory, float colorz, float colorw);
    private static native void nSetLayerMask(long nativeSkybox, @IntRange(from = 0) int select, @IntRange(from = 0) int values);
    @IntRange(from = 0)
    private static native int nGetLayerMask(long nativeSkybox);
    private static native float nGetIntensity(long nativeSkybox);
    private static native long nGetTexture(long nativeSkybox);
    private static native long nCreateBuilder();
    private static native void nDestroyBuilder(long nativeBuilder);
    private static native void nBuilderEnvironment(long nativeBuilder, long cubemap);
    private static native void nBuilderShowSun(long nativeBuilder, boolean show);
    private static native void nBuilderIntensity(long nativeBuilder, float envIntensity);
    private static native void nBuilderColor(long nativeBuilder, float colorx, float colory, float colorz, float colorw);
    private static native void nBuilderPriority(long nativeBuilder, int priority);
    private static native long nBuilderBuild(long nativeBuilder, long nativeEngine);
}

// THIS FILE IS GENERATED BY APIGEN AND MUST NOT BE HAND-MODIFIED.
