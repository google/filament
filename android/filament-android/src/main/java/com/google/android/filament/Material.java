/*
 * Copyright (C) 2026 The Android Open Source Project
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *      http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

// THIS FILE IS GENERATED BY APIGEN AND MUST NOT BE HAND-MODIFIED.

package com.google.android.filament;

import java.lang.IllegalStateException;

import java.nio.Buffer;

import java.util.Collections;
import java.util.EnumSet;
import java.util.Set;

import androidx.annotation.IntRange;
import androidx.annotation.NonNull;
import androidx.annotation.Nullable;
import androidx.annotation.RestrictTo;
import androidx.annotation.Size;

import com.google.android.filament.proguard.UsedByNative;

/**
 * A Material defines the visual appearance of a surface.
 *
 * <p>Materials are created from a binary blob generated by the material compiler (matc).
 * A Material is a template from which MaterialInstance objects can be created.</p>
 */
@UsedByNative
public class Material {
    static final class EnumCache {
        static final BlendingMode[] sBlendingModeValues = BlendingMode.values();
        static final CullingMode[] sCullingModeValues = CullingMode.values();
        static final Engine.FeatureLevel[] sFeatureLevelValues = Engine.FeatureLevel.values();
        static final Interpolation[] sInterpolationValues = Interpolation.values();
        static final MaterialDomain[] sMaterialDomainValues = MaterialDomain.values();
        static final ReflectionMode[] sReflectionModeValues = ReflectionMode.values();
        static final RefractionMode[] sRefractionModeValues = RefractionMode.values();
        static final RefractionType[] sRefractionTypeValues = RefractionType.values();
        static final Shading[] sShadingValues = Shading.values();
        static final TransparencyMode[] sTransparencyModeValues = TransparencyMode.values();
        static final VertexDomain[] sVertexDomainValues = VertexDomain.values();
    }

    private static final VertexBuffer.VertexAttribute[] sVertexAttributeValues =
            VertexBuffer.VertexAttribute.values();

    private static Set<VertexBuffer.VertexAttribute> getAttributes(int bitSet) {
        Set<VertexBuffer.VertexAttribute> set = EnumSet.noneOf(VertexBuffer.VertexAttribute.class);
        VertexBuffer.VertexAttribute[] values = sVertexAttributeValues;
        for (int i = 0, n = values.length; i < n; i++) {
            if ((bitSet & (1 << i)) != 0) {
                set.add(values[i]);
            }
        }
        return Collections.unmodifiableSet(set);
    }

    private long mNativeObject;
    private final MaterialInstance mDefaultInstance;

    /** Supported blending modes */
    public enum BlendingMode {
        /** material is opaque */
        OPAQUE,
        /** material is transparent and color is alpha-pre-multiplied, affects diffuse lighting only */
        TRANSPARENT,
        /** material is additive (e.g.: hologram) */
        ADD,
        /** material is masked (i.e. alpha tested) */
        MASKED,
        /** material is transparent and color is alpha-pre-multiplied, affects specular lighting when adding more entries, change the size of FRenderer::CommandKey::blending */
        FADE,
        /** material darkens what's behind it */
        MULTIPLY,
        /** material brightens what's behind it */
        SCREEN,
        /** custom blending function */
        CUSTOM;

        /** @return the value of this enum constant as used by the native Filament engine. */
        public int toFilamentNative() { return ordinal(); }
    }

    /** Supported shading models */
    public enum Shading {
        /** no lighting applied, emissive possible */
        UNLIT,
        /** default, standard lighting */
        LIT,
        /** subsurface lighting model */
        SUBSURFACE,
        /** cloth lighting model */
        CLOTH,
        /** legacy lighting model */
        SPECULAR_GLOSSINESS;

        /** @return the value of this enum constant as used by the native Filament engine. */
        public int toFilamentNative() { return ordinal(); }
    }

    /** Attribute interpolation types in the fragment shader */
    public enum Interpolation {
        /** default, smooth interpolation */
        SMOOTH,
        /** flat interpolation */
        FLAT;

        /** @return the value of this enum constant as used by the native Filament engine. */
        public int toFilamentNative() { return ordinal(); }
    }

    /** Supported types of vertex domains. */
    public enum VertexDomain {
        /** vertices are in object space, default */
        OBJECT,
        /** vertices are in world space */
        WORLD,
        /** vertices are in view space */
        VIEW,
        /** vertices are in normalized device space */
        DEVICE;

        /** @return the value of this enum constant as used by the native Filament engine. */
        public int toFilamentNative() { return ordinal(); }
    }

    /** How transparent objects are handled */
    public enum TransparencyMode {
        /** the transparent object is drawn honoring the raster state */
        DEFAULT,
        /** the transparent object is first drawn in the depth buffer, then in the color buffer, honoring the culling mode, but ignoring the depth test function */
        TWO_PASSES_ONE_SIDE,
        /**
         * the transparent object is drawn twice in the color buffer, first with back faces only, then with front faces; the culling mode is ignored.
         *
         * <p>Can be combined with two-sided lighting</p>
         */
        TWO_PASSES_TWO_SIDES;

        /** @return the value of this enum constant as used by the native Filament engine. */
        public int toFilamentNative() { return ordinal(); }
    }

    public static class UserVariantFilterBit {
        private UserVariantFilterBit() {}

        /** Directional lighting */
        public static final int DIRECTIONAL_LIGHTING = 0x1;
        /** Dynamic lighting */
        public static final int DYNAMIC_LIGHTING = 0x2;
        /** Shadow receiver */
        public static final int SHADOW_RECEIVER = 0x4;
        /** Skinning */
        public static final int SKINNING = 0x8;
        /** Fog */
        public static final int FOG = 0x10;
        /** Variance shadow maps */
        public static final int VSM = 0x20;
        /** Screen-space reflections */
        public static final int SSR = 0x40;
        /** Instanced stereo rendering */
        public static final int STE = 0x80;
        public static final int ALL = 0xFF;
    }

    /** Refraction */
    public enum RefractionMode {
        /** no refraction */
        NONE,
        /** refracted rays go to the ibl cubemap */
        CUBEMAP,
        /** refracted rays go to screen space */
        SCREEN_SPACE;

        /** @return the value of this enum constant as used by the native Filament engine. */
        public int toFilamentNative() { return ordinal(); }
    }

    /** Refraction type */
    public enum RefractionType {
        /** refraction through solid objects (e.g. a sphere) */
        SOLID,
        /** refraction through thin objects (e.g. window) */
        THIN;

        /** @return the value of this enum constant as used by the native Filament engine. */
        public int toFilamentNative() { return ordinal(); }
    }

    /** Reflection mode */
    public enum ReflectionMode {
        DEFAULT,
        /** reflections sample from the scene's IBL only */
        SCREEN_SPACE;

        /** @return the value of this enum constant as used by the native Filament engine. */
        public int toFilamentNative() { return ordinal(); }
    }

    /** Material domains */
    public enum MaterialDomain {
        /** shaders applied to renderables */
        SURFACE,
        /** shaders applied to rendered buffers */
        POST_PROCESS,
        /** compute shader */
        COMPUTE;

        /** @return the value of this enum constant as used by the native Filament engine. */
        public int toFilamentNative() { return ordinal(); }
    }

    /** Supported uniform types */
    public enum ParameterType {
        BOOL,
        BOOL2,
        BOOL3,
        BOOL4,
        FLOAT,
        FLOAT2,
        FLOAT3,
        FLOAT4,
        INT,
        INT2,
        INT3,
        INT4,
        UINT,
        UINT2,
        UINT3,
        UINT4,
        /** a 3x3 float matrix */
        MAT3,
        /** a 4x4 float matrix */
        MAT4,
        STRUCT;

        /** @return the value of this enum constant as used by the native Filament engine. */
        public int toFilamentNative() { return ordinal(); }
    }

    public enum Precision {
        LOW,
        MEDIUM,
        HIGH,
        DEFAULT;

        /** @return the value of this enum constant as used by the native Filament engine. */
        public int toFilamentNative() { return ordinal(); }
    }

    /** Texture sampler type */
    public enum SamplerType {
        /** 2D texture */
        SAMPLER_2D,
        /** 2D array texture */
        SAMPLER_2D_ARRAY,
        /** Cube map texture */
        SAMPLER_CUBEMAP,
        /** External texture */
        SAMPLER_EXTERNAL,
        /** 3D texture */
        SAMPLER_3D,
        /** Cube map array texture (feature level 2) */
        SAMPLER_CUBEMAP_ARRAY;

        /** @return the value of this enum constant as used by the native Filament engine. */
        public int toFilamentNative() { return ordinal(); }
    }

    /** Texture sampler format */
    public enum SamplerFormat {
        /** signed integer sampler */
        INT,
        /** unsigned integer sampler */
        UINT,
        /** float sampler */
        FLOAT,
        /** shadow sampler (PCF) */
        SHADOW;

        /** @return the value of this enum constant as used by the native Filament engine. */
        public int toFilamentNative() { return ordinal(); }
    }

    /** Face culling Mode */
    public enum CullingMode {
        /** No culling, front and back faces are visible */
        NONE,
        /** Front face culling, only back faces are visible */
        FRONT,
        /** Back face culling, only front faces are visible */
        BACK,
        /** Front and Back, geometry is not visible */
        FRONT_AND_BACK;

        /** @return the value of this enum constant as used by the native Filament engine. */
        public int toFilamentNative() { return ordinal(); }
    }

    /**
     * Shader model.
     *
     * <p>These enumerants are used across all backends and refer to a level of functionality and quality.</p>
     *
     * <p>For example, the OpenGL backend returns {@code MOBILE} if it supports OpenGL ES, or {@code DESKTOP} if it
     * supports Desktop OpenGL, this is later used to select the proper shader.</p>
     *
     * <p>Shader quality vs. performance is also affected by ShaderModel.</p>
     */
    public enum ShaderModel {
        /** Mobile level functionality */
        MOBILE(1),
        /** Desktop level functionality */
        DESKTOP(2);

        private final int mValue;

        ShaderModel(int value) {
            mValue = value;
        }

        /** @return the value of this enum constant as used by the native Filament engine. */
        public int toFilamentNative() { return mValue; }

        public static ShaderModel from(int value) {
            switch (value) {
                case 1: return MOBILE;
                case 2: return DESKTOP;
                default: throw new IllegalArgumentException("Unknown ShaderModel value: " + value);
            }
        }
    }

    /** Subpass type */
    public enum SubpassType {
        SUBPASS_INPUT;

        /** @return the value of this enum constant as used by the native Filament engine. */
        public int toFilamentNative() { return ordinal(); }
    }

    /** Defines whether a material instance should use UBO batching or not. */
    public enum UboBatchingMode {
        /**
         * For default, it follows the engine settings.
         *
         * <p>If UBO batching is enabled on the engine and the material domain is SURFACE, it
         * turns on the UBO batching. Otherwise, it turns off the UBO batching.</p>
         */
        DEFAULT,
        /** Disable the Ubo Batching for this material */
        DISABLED;

        /** @return the value of this enum constant as used by the native Filament engine. */
        public int toFilamentNative() { return ordinal(); }
    }

    /**
     * Shader compiler priority queue
     *
     * <p>On platforms which support parallel shader compilation, compilation requests will be processed in
     * order of priority, then insertion order. See Material::compile().</p>
     */
    public enum CompilerPriorityQueue {
        /**
         * We need this program NOW.
         *
         * <p>When passed as an argument to Material::compile(), if the platform doesn't support parallel
         * compilation, but does support amortized shader compilation, the given shader program will be
         * synchronously compiled.</p>
         */
        CRITICAL,
        /** We will need this program soon. */
        HIGH,
        /** We will need this program eventually. */
        LOW;

        /** @return the value of this enum constant as used by the native Filament engine. */
        public int toFilamentNative() { return ordinal(); }
    }

    /** Element types for float tagged array buffers. */
    public enum FloatElement {
        FLOAT,
        FLOAT2,
        FLOAT3,
        FLOAT4,
        MAT3,
        MAT4;

        /** @return the value of this enum constant as used by the native Filament engine. */
        public int toFilamentNative() { return ordinal(); }
    }

    /** Element types for int tagged array buffers. */
    public enum IntElement {
        INT,
        INT2,
        INT3,
        INT4;

        /** @return the value of this enum constant as used by the native Filament engine. */
        public int toFilamentNative() { return ordinal(); }
    }

    /** Element types for boolean tagged array buffers. */
    public enum BooleanElement {
        BOOL,
        BOOL2,
        BOOL3,
        BOOL4;

        /** @return the value of this enum constant as used by the native Filament engine. */
        public int toFilamentNative() { return ordinal(); }
    }

    Material(long nativeObject) {
        mNativeObject = nativeObject;
        mDefaultInstance = new MaterialInstance(nGetDefaultInstance(nativeObject), this);
    }

    @NonNull
    @RestrictTo(RestrictTo.Scope.LIBRARY_GROUP)
    public static Material wrap(long nativeObject) {
        return new Material(nativeObject);
    }

    public static class Builder {
        @SuppressWarnings({"FieldCanBeLocal", "UnusedDeclaration"}) // Keep to finalize native resources
        private final BuilderFinalizer mFinalizer;
        private final long mNativeBuilder;
        private final java.util.List<Object> mRetainedBuffers = new java.util.ArrayList<>();

        public enum ShadowSamplingQuality {
            HARD,
            LOW;

            /** @return the value of this enum constant as used by the native Filament engine. */
            public int toFilamentNative() { return ordinal(); }
        }

        public Builder() {
            mNativeBuilder = nCreateBuilder();
            mFinalizer = new BuilderFinalizer(mNativeBuilder);
        }

        long getNativeBuilder() {
            return mNativeBuilder;
        }

        /**
         * Specifies the material data.
         *
         * <p>The material data is a binary blob produced by
         * libfilamat or by matc.</p>
         *
         * @param payload Buffer of the material data, must stay valid until build() is called.
         */
        @NonNull
        public Builder payload(@NonNull Buffer payload, @IntRange(from = 0) int size) {
            mRetainedBuffers.add(payload);
            nBuilderPayload(mNativeBuilder, payload, size);
            return this;
        }

        @NonNull
        public Builder payload(@NonNull Buffer payload) {
            return payload(payload, payload.remaining());
        }

        /**
         * Sets the quality of the indirect lights computations.
         *
         * <p>This is only taken into account
         * if this material is lit and in the surface domain. This setting will affect the
         * IndirectLight computation if one is specified on the Scene and Spherical Harmonics
         * are used for the irradiance.</p>
         *
         * @param shBandCount Number of spherical harmonic bands. Must be 1, 2 or 3 (default).
         *
         * @return Reference to this Builder for chaining calls.
         *
         * @see IndirectLight
         */
        @NonNull
        public Builder sphericalHarmonicsBandCount(@IntRange(from = 0) int shBandCount) {
            nBuilderSphericalHarmonicsBandCount(mNativeBuilder, shBandCount);
            return this;
        }

        /**
         * Set the quality of shadow sampling.
         *
         * <p>This is only taken into account
         * if this material is lit and in the surface domain.</p>
         *
         * @param quality
         */
        @NonNull
        public Builder shadowSamplingQuality(@NonNull ShadowSamplingQuality quality) {
            nBuilderShadowSamplingQuality(mNativeBuilder, quality.toFilamentNative());
            return this;
        }

        /**
         * Set the batching mode of the instances created from this material.
         *
         * @param uboBatchingMode
         */
        @NonNull
        public Builder uboBatching(@NonNull UboBatchingMode uboBatchingMode) {
            nBuilderUboBatching(mNativeBuilder, uboBatchingMode.toFilamentNative());
            return this;
        }

        /**
         * Creates the Material object and returns a pointer to it.
         *
         * @param engine Reference to the filament::Engine to associate this Material with.
         *
         * @return pointer to the newly created object or nullptr if exceptions are disabled and
         *         an error occurred.
         */
        @NonNull
        public Material build(@NonNull Engine engine) {
            long nativeMaterial = nBuilderBuild(mNativeBuilder, engine.getNativeObject());
            if (nativeMaterial == 0) throw new IllegalStateException("Couldn't create Material");
            return new Material(nativeMaterial);
        }

        private static class BuilderFinalizer {
            private final long mNativeObject;

            BuilderFinalizer(long nativeObject) { mNativeObject = nativeObject; }

            @Override
            public void finalize() {
                try {
                    super.finalize();
                } catch (Throwable t) { // Ignore
                } finally {
                    nDestroyBuilder(mNativeObject);
                }
            }
        }
    }

    /**
     * Asynchronously ensures that a subset of this Material's variants are compiled.
     *
     * <p>After issuing
     * several Material::compile() calls in a row, it is recommended to call Engine::flush()
     * such that the backend can start the compilation work as soon as possible.
     * The provided callback is guaranteed to be called on the main thread after all specified
     * variants of the material are compiled. This can take hundreds of milliseconds.</p>
     *
     * <p>If all the material's variants are already compiled, the callback will be scheduled as
     * soon as possible, but this might take a few dozen millisecond, corresponding to how
     * many previous frames are enqueued in the backend. This also varies by backend. Therefore,
     * it is recommended to only call this method once per material shortly after creation.</p>
     *
     * <p>If the same variant is scheduled for compilation multiple times, the first scheduling
     * takes precedence; later scheduling are ignored.</p>
     *
     * <p>caveat: A consequence is that if a variant is scheduled on the low priority queue and later
     * scheduled again on the high priority queue, the later scheduling is ignored.
     * Therefore, the second callback could be called before the variant is compiled.
     * However, the first callback, if specified, will trigger as expected.</p>
     *
     * <p>The callback is guaranteed to be called. If the engine is destroyed while some material
     * variants are still compiling or in the queue, these will be discarded and the corresponding
     * callback will be called. In that case however the Material pointer passed to the callback
     * is guaranteed to be invalid (either because it's been destroyed by the user already, or,
     * because it's been cleaned-up by the Engine).</p>
     *
     * <p>UserVariantFilterMask::ALL should be used with caution. Only variants that an application
     * needs should be included in the variants argument. For example, the STE variant is only used
     * for stereoscopic rendering. If an application is not planning to render in stereo, this bit
     * should be turned off to avoid unnecessary material compilations.</p>
     *
     * <p>Note that it is possible to override specialization constants on a per-MaterialInstance basis
     * (</p>
     *
     * @param priority Which priority queue to use, LOW or HIGH.
     * @param variants Variants to include to the compile command.
     * @param handler Handler to dispatch the callback or nullptr for the default handler
     * @param callback callback called on the main thread when the compilation is done on
     *                      by backend.
     *
     * @see Material#compile
     */
    public void compile(@NonNull CompilerPriorityQueue priority, @IntRange(from = 0) int variants, @Nullable Object handler, @Nullable Runnable callback) {
        nCompile(getNativeObject(), priority.toFilamentNative(), variants, handler, callback);
    }

    /**
     * Creates a new instance of this material.
     *
     * <p>Material instances should be freed using
     * Engine::destroy(const MaterialInstance*).</p>
     *
     * @return A pointer to the new instance.
     */
    public MaterialInstance createInstance() {
        return createInstance(null);
    }

    /**
     * Creates a new instance of this material.
     *
     * <p>Material instances should be freed using
     * Engine::destroy(const MaterialInstance*).</p>
     *
     * @param name Optional name to associate with the given material instance. If this is null,
     * then the instance inherits the material's name.
     *
     * @return A pointer to the new instance.
     */
    public MaterialInstance createInstance(@Nullable String name) {
        long nativeMaterialInstance = nCreateInstance(getNativeObject(), name);
        if (nativeMaterialInstance == 0) throw new IllegalStateException("Couldn't create MaterialInstance");
        return new MaterialInstance(nativeMaterialInstance, this);
    }

    /** Returns the name of this material as a null-terminated string. */
    @NonNull
    public String getName() {
        return nGetName(getNativeObject());
    }

    /** Returns the shading model of this material. */
    public Shading getShading() {
        return EnumCache.sShadingValues[nGetShading(getNativeObject())];
    }

    /**
     * Returns the interpolation mode of this material.
     *
     * <p>This affects how variables are interpolated.</p>
     */
    public Interpolation getInterpolation() {
        return EnumCache.sInterpolationValues[nGetInterpolation(getNativeObject())];
    }

    /** Returns the blending mode of this material. */
    public BlendingMode getBlendingMode() {
        return EnumCache.sBlendingModeValues[nGetBlendingMode(getNativeObject())];
    }

    /** Returns the vertex domain of this material. */
    public VertexDomain getVertexDomain() {
        return EnumCache.sVertexDomainValues[nGetVertexDomain(getNativeObject())];
    }

    /** Returns the material's supported variants */
    @IntRange(from = 0)
    public int getSupportedVariants() {
        return nGetSupportedVariants(getNativeObject());
    }

    /**
     * Returns the material domain of this material.
     *
     * <p>The material domain determines how the material is used.</p>
     */
    public MaterialDomain getMaterialDomain() {
        return EnumCache.sMaterialDomainValues[nGetMaterialDomain(getNativeObject())];
    }

    /** Returns the default culling mode of this material. */
    public CullingMode getCullingMode() {
        return EnumCache.sCullingModeValues[nGetCullingMode(getNativeObject())];
    }

    /**
     * Returns the transparency mode of this material.
     *
     * <p>This value only makes sense when the blending mode is transparent or fade.</p>
     */
    public TransparencyMode getTransparencyMode() {
        return EnumCache.sTransparencyModeValues[nGetTransparencyMode(getNativeObject())];
    }

    /** Indicates whether instances of this material will, by default, write to the color buffer. */
    public boolean isColorWriteEnabled() {
        return nIsColorWriteEnabled(getNativeObject());
    }

    /** Indicates whether instances of this material will, by default, write to the depth buffer. */
    public boolean isDepthWriteEnabled() {
        return nIsDepthWriteEnabled(getNativeObject());
    }

    /** Indicates whether instances of this material will, by default, use depth testing. */
    public boolean isDepthCullingEnabled() {
        return nIsDepthCullingEnabled(getNativeObject());
    }

    /** Indicates whether this material is double-sided. */
    public boolean isDoubleSided() {
        return nIsDoubleSided(getNativeObject());
    }

    /** Indicates whether this material uses alpha to coverage. */
    public boolean isAlphaToCoverageEnabled() {
        return nIsAlphaToCoverageEnabled(getNativeObject());
    }

    /** Returns the alpha mask threshold used when the blending mode is set to masked. */
    public float getMaskThreshold() {
        return nGetMaskThreshold(getNativeObject());
    }

    /**
     * Indicates whether this material uses the shadowing factor as a color multiplier.
     *
     * <p>This values only makes sense when the shading mode is unlit.</p>
     */
    public boolean hasShadowMultiplier() {
        return nHasShadowMultiplier(getNativeObject());
    }

    /** Indicates whether this material has specular anti-aliasing enabled */
    public boolean hasSpecularAntiAliasing() {
        return nHasSpecularAntiAliasing(getNativeObject());
    }

    /** Returns the screen-space variance for specular-antialiasing, this value is between 0 and 1. */
    public float getSpecularAntiAliasingVariance() {
        return nGetSpecularAntiAliasingVariance(getNativeObject());
    }

    /** Returns the clamping threshold for specular-antialiasing, this value is between 0 and 1. */
    public float getSpecularAntiAliasingThreshold() {
        return nGetSpecularAntiAliasingThreshold(getNativeObject());
    }

    /** Returns the list of vertex attributes required by this material. */
    @NonNull
    public Set<VertexBuffer.VertexAttribute> getRequiredAttributes() {
        return getAttributes(nGetRequiredAttributes(getNativeObject()));
    }

    /** Returns the refraction mode used by this material. */
    public RefractionMode getRefractionMode() {
        return EnumCache.sRefractionModeValues[nGetRefractionMode(getNativeObject())];
    }

    /** Return the refraction type used by this material. */
    public RefractionType getRefractionType() {
        return EnumCache.sRefractionTypeValues[nGetRefractionType(getNativeObject())];
    }

    /** Returns the reflection mode used by this material. */
    public ReflectionMode getReflectionMode() {
        return EnumCache.sReflectionModeValues[nGetReflectionMode(getNativeObject())];
    }

    /** Returns the minimum required feature level for this material. */
    public Engine.FeatureLevel getFeatureLevel() {
        return EnumCache.sFeatureLevelValues[nGetFeatureLevel(getNativeObject())];
    }

    /**
     * Returns the number of parameters declared by this material.
     *
     * <p>The returned value can be 0.</p>
     */
    @IntRange(from = 0)
    public int getParameterCount() {
        return nGetParameterCount(getNativeObject());
    }

    /**
     * Gets information about this material's parameters.
     *
     * @return The number of parameters written to the parameters pointer.
     */
    @NonNull
    public ParameterInfo[] getParameters() {
        int count = getParameterCount();
        ParameterInfo[] parameters = new ParameterInfo[count];
        if (count > 0) {
            for (int i = 0; i < count; i++) {
                parameters[i] = new ParameterInfo();
            }
            nGetParameters(getNativeObject(), parameters, count);
        }
        return parameters;
    }

    @IntRange(from = 0)
    public int getParameters(@NonNull ParameterInfo[] parameters, @IntRange(from = 0) int count) {
        return nGetParameters(getNativeObject(), parameters, count);
    }

    /** Indicates whether a parameter of the given name exists on this material. */
    public boolean hasParameter(@NonNull String name) {
        return nHasParameter(getNativeObject(), name);
    }

    /** Indicates whether an existing parameter is a sampler or not. */
    public boolean isSampler(@NonNull String name) {
        return nIsSampler(getNativeObject(), name);
    }

    /**
     * Returns a view of the material source (.mat which is a JSON-ish file) string, if it has been set.
     *
     * <p>Otherwise, it returns a view of an empty string.
     * The lifetime of the string_view is tied to the lifetime of the Material.</p>
     */
    @NonNull
    public String getSource() {
        return nGetSource(getNativeObject());
    }

    /**
     * Gets the name of the transform field associated for the given sampler parameter.
     *
     * <p>In the case where the parameter does not have a transform name field, it will return nullptr.</p>
     *
     * @param samplerName the name of the sampler parameter to query.
     *
     * @return If exists, the transform name value otherwise returns a nullptr.
     */
    @Nullable
    public String getParameterTransformName(@NonNull String samplerName) {
        return nGetParameterTransformName(getNativeObject(), samplerName);
    }

    /**
     * Sets the value of the given parameter on this material's default instance.
     *
     * @param name The name of the material parameter
     * @param value The value of the material parameter
     *
     * @see #getDefaultInstance
     */
    public void setDefaultParameter(@NonNull String name, float value) {
        nSetDefaultParameter(getNativeObject(), name, value);
    }

    /**
     * Sets the value of the given parameter on this material's default instance.
     *
     * @param name The name of the material parameter
     * @param value The value of the material parameter
     *
     * @see #getDefaultInstance
     */
    public void setDefaultParameter(@NonNull String name, int value) {
        nSetDefaultParameter(getNativeObject(), name, value);
    }

    /**
     * Sets the value of the given parameter on this material's default instance.
     *
     * @param name The name of the material parameter
     * @param valuex (x component) The value of the material parameter
     * @param valuey (y component) The value of the material parameter
     *
     * @see #getDefaultInstance
     */
    public void setDefaultParameter(@NonNull String name, int valuex, int valuey) {
        nSetDefaultParameter(getNativeObject(), name, valuex, valuey);
    }

    /**
     * Sets the value of the given parameter on this material's default instance.
     *
     * @param name The name of the material parameter
     * @param valuex (x component) The value of the material parameter
     * @param valuey (y component) The value of the material parameter
     * @param valuez (z component) The value of the material parameter
     *
     * @see #getDefaultInstance
     */
    public void setDefaultParameter(@NonNull String name, int valuex, int valuey, int valuez) {
        nSetDefaultParameter(getNativeObject(), name, valuex, valuey, valuez);
    }

    /**
     * Sets the value of the given parameter on this material's default instance.
     *
     * @param name The name of the material parameter
     * @param valuex (x component) The value of the material parameter
     * @param valuey (y component) The value of the material parameter
     * @param valuez (z component) The value of the material parameter
     * @param valuew (w component) The value of the material parameter
     *
     * @see #getDefaultInstance
     */
    public void setDefaultParameter(@NonNull String name, int valuex, int valuey, int valuez, int valuew) {
        nSetDefaultParameter(getNativeObject(), name, valuex, valuey, valuez, valuew);
    }

    /**
     * Sets the value of the given parameter on this material's default instance.
     *
     * @param name The name of the material parameter
     * @param valuex (x component) The value of the material parameter
     * @param valuey (y component) The value of the material parameter
     *
     * @see #getDefaultInstance
     */
    public void setDefaultParameter(@NonNull String name, float valuex, float valuey) {
        nSetDefaultParameter(getNativeObject(), name, valuex, valuey);
    }

    /**
     * Sets the value of the given parameter on this material's default instance.
     *
     * @param name The name of the material parameter
     * @param valuex (x component) The value of the material parameter
     * @param valuey (y component) The value of the material parameter
     * @param valuez (z component) The value of the material parameter
     *
     * @see #getDefaultInstance
     */
    public void setDefaultParameter(@NonNull String name, float valuex, float valuey, float valuez) {
        nSetDefaultParameter(getNativeObject(), name, valuex, valuey, valuez);
    }

    /**
     * Sets the value of the given parameter on this material's default instance.
     *
     * @param name The name of the material parameter
     * @param valuex (x component) The value of the material parameter
     * @param valuey (y component) The value of the material parameter
     * @param valuez (z component) The value of the material parameter
     * @param valuew (w component) The value of the material parameter
     *
     * @see #getDefaultInstance
     */
    public void setDefaultParameter(@NonNull String name, float valuex, float valuey, float valuez, float valuew) {
        nSetDefaultParameter(getNativeObject(), name, valuex, valuey, valuez, valuew);
    }

    /**
     * Sets the value of the given parameter on this material's default instance.
     *
     * @param name The name of the material parameter
     * @param value The value of the material parameter
     *
     * @see #getDefaultInstance
     */
    public void setDefaultParameter(@NonNull String name, @NonNull @Size(min = 16) float[] value) {
        Asserts.assertMat4fIn(value);
        nSetDefaultParameter(getNativeObject(), name, value);
    }

    /**
     * Sets the value of the given parameter on this material's default instance.
     *
     * @param name The name of the material parameter
     * @param value The value of the material parameter
     *
     * @see #getDefaultInstance
     */
    public void setDefaultParameter(@NonNull String name, boolean value) {
        nSetDefaultParameter(getNativeObject(), name, value);
    }

    /**
     * Sets the value of the given parameter on this material's default instance.
     *
     * @param name The name of the material parameter
     * @param valuex (x component) The value of the material parameter
     * @param valuey (y component) The value of the material parameter
     *
     * @see #getDefaultInstance
     */
    public void setDefaultParameter(@NonNull String name, boolean valuex, boolean valuey) {
        nSetDefaultParameter(getNativeObject(), name, valuex, valuey);
    }

    /**
     * Sets the value of the given parameter on this material's default instance.
     *
     * @param name The name of the material parameter
     * @param valuex (x component) The value of the material parameter
     * @param valuey (y component) The value of the material parameter
     * @param valuez (z component) The value of the material parameter
     *
     * @see #getDefaultInstance
     */
    public void setDefaultParameter(@NonNull String name, boolean valuex, boolean valuey, boolean valuez) {
        nSetDefaultParameter(getNativeObject(), name, valuex, valuey, valuez);
    }

    /**
     * Sets the value of the given parameter on this material's default instance.
     *
     * @param name The name of the material parameter
     * @param valuex (x component) The value of the material parameter
     * @param valuey (y component) The value of the material parameter
     * @param valuez (z component) The value of the material parameter
     * @param valuew (w component) The value of the material parameter
     *
     * @see #getDefaultInstance
     */
    public void setDefaultParameter(@NonNull String name, boolean valuex, boolean valuey, boolean valuez, boolean valuew) {
        nSetDefaultParameter(getNativeObject(), name, valuex, valuey, valuez, valuew);
    }

    /**
     * Sets the value of the given parameter array on this material's default instance.
     *
     * @param name The name of the material parameter
     * @param type the number of components for each individual parameter
     * @param values An array of values
     * @param offset the number of elements in <code>values</code> to skip
     *
     * @see #getDefaultInstance
     */
    public void setDefaultParameter(@NonNull String name, @NonNull FloatElement type, @NonNull float[] values, @IntRange(from = 0) int offset, @IntRange(from = 1) int count) {
        nSetDefaultParameterFloatArray(getNativeObject(), name, type.ordinal(), values, offset, count);
    }

    /**
     * Sets the value of the given parameter array on this material's default instance.
     *
     * @param name The name of the material parameter
     * @param type the number of components for each individual parameter
     * @param values An array of values
     *
     * @see #getDefaultInstance
     */
    public void setDefaultParameter(@NonNull String name, @NonNull FloatElement type, @NonNull float[] values, @IntRange(from = 1) int count) {
        setDefaultParameter(name, type, values, 0, count);
    }

    /**
     * Sets the value of the given parameter array on this material's default instance.
     *
     * @param name The name of the material parameter
     * @param type the number of components for each individual parameter
     * @param values An array of values
     * @param offset the number of elements in <code>values</code> to skip
     *
     * @see #getDefaultInstance
     */
    public void setDefaultParameter(@NonNull String name, @NonNull IntElement type, @NonNull int[] values, @IntRange(from = 0) int offset, @IntRange(from = 1) int count) {
        nSetDefaultParameterIntArray(getNativeObject(), name, type.ordinal(), values, offset, count);
    }

    /**
     * Sets the value of the given parameter array on this material's default instance.
     *
     * @param name The name of the material parameter
     * @param type the number of components for each individual parameter
     * @param values An array of values
     *
     * @see #getDefaultInstance
     */
    public void setDefaultParameter(@NonNull String name, @NonNull IntElement type, @NonNull int[] values, @IntRange(from = 1) int count) {
        setDefaultParameter(name, type, values, 0, count);
    }

    /**
     * Sets the value of the given parameter array on this material's default instance.
     *
     * @param name The name of the material parameter
     * @param type the number of components for each individual parameter
     * @param values An array of values
     * @param offset the number of elements in <code>values</code> to skip
     *
     * @see #getDefaultInstance
     */
    public void setDefaultParameter(@NonNull String name, @NonNull BooleanElement type, @NonNull boolean[] values, @IntRange(from = 0) int offset, @IntRange(from = 1) int count) {
        nSetDefaultParameterBooleanArray(getNativeObject(), name, type.ordinal(), values, offset, count);
    }

    /**
     * Sets the value of the given parameter array on this material's default instance.
     *
     * @param name The name of the material parameter
     * @param type the number of components for each individual parameter
     * @param values An array of values
     *
     * @see #getDefaultInstance
     */
    public void setDefaultParameter(@NonNull String name, @NonNull BooleanElement type, @NonNull boolean[] values, @IntRange(from = 1) int count) {
        setDefaultParameter(name, type, values, 0, count);
    }

    /**
     * Sets a texture and sampler parameters on this material's default instance.
     *
     * @param name The name of the material texture parameter
     * @param texture The texture to set as parameter
     * @param sampler The sampler to be used with this texture
     *
     * @see #getDefaultInstance
     */
    public void setDefaultParameter(@NonNull String name, @Nullable Texture texture, @NonNull TextureSampler sampler) {
        nSetDefaultParameter(getNativeObject(), name, texture != null ? texture.getNativeObject() : 0, sampler.mSampler);
    }

    /**
     * Sets the color of the given parameter on this material's default instance.
     *
     * @param name The name of the material color parameter
     * @param type Whether the color is specified in the linear or sRGB space
     * @param colorx (x component) The color as a floating point red, green, blue tuple
     * @param colory (y component) The color as a floating point red, green, blue tuple
     * @param colorz (z component) The color as a floating point red, green, blue tuple
     *
     * @see #getDefaultInstance
     */
    public void setDefaultParameter(@NonNull String name, @NonNull Colors.RgbType type, float colorx, float colory, float colorz) {
        nSetDefaultParameter(getNativeObject(), name, type.ordinal(), colorx, colory, colorz);
    }

    /**
     * Sets the color of the given parameter on this material's default instance.
     *
     * @param name The name of the material color parameter
     * @param type Whether the color is specified in the linear or sRGB space
     * @param color The color as a floating point red, green, blue tuple
     *
     * @see #getDefaultInstance
     */
    public void setDefaultParameter(@NonNull String name, Colors.RgbType type, @NonNull @Size(min = 3) float[] color) {
        setDefaultParameter(name, type, color[0], color[1], color[2]);
    }

    /**
     * Sets the color of the given parameter on this material's default instance.
     *
     * @param name The name of the material color parameter
     * @param type Whether the color is specified in the linear or sRGB space
     * @param colorx (x component) The color as a floating point red, green, blue, alpha tuple
     * @param colory (y component) The color as a floating point red, green, blue, alpha tuple
     * @param colorz (z component) The color as a floating point red, green, blue, alpha tuple
     * @param colorw (w component) The color as a floating point red, green, blue, alpha tuple
     *
     * @see #getDefaultInstance
     */
    public void setDefaultParameter(@NonNull String name, @NonNull Colors.RgbaType type, float colorx, float colory, float colorz, float colorw) {
        nSetDefaultParameter(getNativeObject(), name, type.ordinal(), colorx, colory, colorz, colorw);
    }

    /**
     * Sets the color of the given parameter on this material's default instance.
     *
     * @param name The name of the material color parameter
     * @param type Whether the color is specified in the linear or sRGB space
     * @param color The color as a floating point red, green, blue, alpha tuple
     *
     * @see #getDefaultInstance
     */
    public void setDefaultParameter(@NonNull String name, Colors.RgbaType type, @NonNull @Size(min = 4) float[] color) {
        setDefaultParameter(name, type, color[0], color[1], color[2], color[3]);
    }

    /** Returns this material's default instance. */
    @NonNull
    public MaterialInstance getDefaultInstance() {
        return mDefaultInstance;
    }

    @UsedByNative("Engine.cpp")
    public long getNativeObject() {
        if (mNativeObject == 0) {
            throw new IllegalStateException("Calling method on destroyed Material");
        }
        return mNativeObject;
    }

    void clearNativeObject() {
        mNativeObject = 0;
    }
    /** Holds information about a material parameter. */
    public static class ParameterInfo {
        public String name;
        public boolean isSampler;
        public boolean isSubpass;
        public ParameterType type;
        public SamplerType samplerType;
        public SubpassType subpassType;
        @IntRange(from = 0)
        public int count;
        public Precision precision;

        public ParameterInfo() {
        }

        public ParameterInfo(String name, boolean isSampler, boolean isSubpass, ParameterType type, SamplerType samplerType, SubpassType subpassType, @IntRange(from = 0) int count, Precision precision) {
            setName(name);
            setIsSampler(isSampler);
            setIsSubpass(isSubpass);
            setType(type);
            setSamplerType(samplerType);
            setSubpassType(subpassType);
            setCount(count);
            setPrecision(precision);
        }

        public void setName(String name) {
            this.name = name;
        }

        public String getName() {
            return name;
        }

        public void setIsSampler(boolean isSampler) {
            this.isSampler = isSampler;
        }

        public boolean getIsSampler() {
            return isSampler;
        }

        public void setIsSubpass(boolean isSubpass) {
            this.isSubpass = isSubpass;
        }

        public boolean getIsSubpass() {
            return isSubpass;
        }

        public void setType(ParameterType type) {
            this.type = type;
        }

        public ParameterType getType() {
            return type;
        }

        public void setSamplerType(SamplerType samplerType) {
            this.samplerType = samplerType;
        }

        public SamplerType getSamplerType() {
            return samplerType;
        }

        public void setSubpassType(SubpassType subpassType) {
            this.subpassType = subpassType;
        }

        public SubpassType getSubpassType() {
            return subpassType;
        }

        public void setCount(@IntRange(from = 0) int count) {
            this.count = count;
        }

        @IntRange(from = 0) public int getCount() {
            return count;
        }

        public void setPrecision(Precision precision) {
            this.precision = precision;
        }

        public Precision getPrecision() {
            return precision;
        }

    }


    private static native void nCompile(long nativeMaterial, int priority, int variants, Object handler, Runnable callback);
    private static native long nCreateInstance(long nativeMaterial, @Nullable String name);
    @NonNull
    private static native String nGetName(long nativeMaterial);
    private static native int nGetShading(long nativeMaterial);
    private static native int nGetInterpolation(long nativeMaterial);
    private static native int nGetBlendingMode(long nativeMaterial);
    private static native int nGetVertexDomain(long nativeMaterial);
    @IntRange(from = 0)
    private static native int nGetSupportedVariants(long nativeMaterial);
    private static native int nGetMaterialDomain(long nativeMaterial);
    private static native int nGetCullingMode(long nativeMaterial);
    private static native int nGetTransparencyMode(long nativeMaterial);
    private static native boolean nIsColorWriteEnabled(long nativeMaterial);
    private static native boolean nIsDepthWriteEnabled(long nativeMaterial);
    private static native boolean nIsDepthCullingEnabled(long nativeMaterial);
    private static native boolean nIsDoubleSided(long nativeMaterial);
    private static native boolean nIsAlphaToCoverageEnabled(long nativeMaterial);
    private static native float nGetMaskThreshold(long nativeMaterial);
    private static native boolean nHasShadowMultiplier(long nativeMaterial);
    private static native boolean nHasSpecularAntiAliasing(long nativeMaterial);
    private static native float nGetSpecularAntiAliasingVariance(long nativeMaterial);
    private static native float nGetSpecularAntiAliasingThreshold(long nativeMaterial);
    @IntRange(from = 0)
    private static native int nGetRequiredAttributes(long nativeMaterial);
    private static native int nGetRefractionMode(long nativeMaterial);
    private static native int nGetRefractionType(long nativeMaterial);
    private static native int nGetReflectionMode(long nativeMaterial);
    private static native int nGetFeatureLevel(long nativeMaterial);
    @IntRange(from = 0)
    private static native int nGetParameterCount(long nativeMaterial);
    private static native int nGetParameters(long nativeMaterial, @NonNull ParameterInfo[] parameters, @IntRange(from = 0) int count);
    private static native boolean nHasParameter(long nativeMaterial, @NonNull String name);
    private static native boolean nIsSampler(long nativeMaterial, @NonNull String name);
    @NonNull
    private static native String nGetSource(long nativeMaterial);
    @Nullable
    private static native String nGetParameterTransformName(long nativeMaterial, @NonNull String samplerName);
    private static native void nSetDefaultParameter(long nativeMaterial, @NonNull String name, float value);
    private static native void nSetDefaultParameter(long nativeMaterial, @NonNull String name, int value);
    private static native void nSetDefaultParameter(long nativeMaterial, @NonNull String name, int valuex, int valuey);
    private static native void nSetDefaultParameter(long nativeMaterial, @NonNull String name, int valuex, int valuey, int valuez);
    private static native void nSetDefaultParameter(long nativeMaterial, @NonNull String name, int valuex, int valuey, int valuez, int valuew);
    private static native void nSetDefaultParameter(long nativeMaterial, @NonNull String name, float valuex, float valuey);
    private static native void nSetDefaultParameter(long nativeMaterial, @NonNull String name, float valuex, float valuey, float valuez);
    private static native void nSetDefaultParameter(long nativeMaterial, @NonNull String name, float valuex, float valuey, float valuez, float valuew);
    private static native void nSetDefaultParameter(long nativeMaterial, @NonNull String name, @NonNull @Size(min = 16) float[] value);
    private static native void nSetDefaultParameter(long nativeMaterial, @NonNull String name, boolean value);
    private static native void nSetDefaultParameter(long nativeMaterial, @NonNull String name, boolean valuex, boolean valuey);
    private static native void nSetDefaultParameter(long nativeMaterial, @NonNull String name, boolean valuex, boolean valuey, boolean valuez);
    private static native void nSetDefaultParameter(long nativeMaterial, @NonNull String name, boolean valuex, boolean valuey, boolean valuez, boolean valuew);
    private static native void nSetDefaultParameterFloatArray(long nativeMaterial, @NonNull String name, int type, @NonNull @Size(min = 1) float[] values, @IntRange(from = 0) int offset, @IntRange(from = 1) int count);
    private static native void nSetDefaultParameterIntArray(long nativeMaterial, @NonNull String name, int type, @NonNull @Size(min = 1) int[] values, @IntRange(from = 0) int offset, @IntRange(from = 1) int count);
    private static native void nSetDefaultParameterBooleanArray(long nativeMaterial, @NonNull String name, int type, @NonNull @Size(min = 1) boolean[] values, @IntRange(from = 0) int offset, @IntRange(from = 1) int count);
    private static native void nSetDefaultParameter(long nativeMaterial, @NonNull String name, long nativeTexture, int sampler);
    private static native void nSetDefaultParameter(long nativeMaterial, @NonNull String name, int type, float colorx, float colory, float colorz);
    private static native void nSetDefaultParameter(long nativeMaterial, @NonNull String name, int type, float colorx, float colory, float colorz, float colorw);
    private static native long nGetDefaultInstance(long nativeMaterial);
    private static native long nCreateBuilder();
    private static native void nDestroyBuilder(long nativeBuilder);
    private static native void nBuilderPayload(long nativeBuilder, Buffer payload, int size);
    private static native void nBuilderSphericalHarmonicsBandCount(long nativeBuilder, int shBandCount);
    private static native void nBuilderShadowSamplingQuality(long nativeBuilder, int quality);
    private static native void nBuilderUboBatching(long nativeBuilder, int uboBatchingMode);
    private static native long nBuilderBuild(long nativeBuilder, long nativeEngine);
}

// THIS FILE IS GENERATED BY APIGEN AND MUST NOT BE HAND-MODIFIED.
