/*
 * Copyright (C) 2026 The Android Open Source Project
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *      http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

// THIS FILE IS GENERATED BY APIGEN AND MUST NOT BE HAND-MODIFIED.

package com.google.android.filament;

import java.lang.IllegalStateException;

import androidx.annotation.IntRange;
import androidx.annotation.NonNull;
import androidx.annotation.Nullable;
import androidx.annotation.RestrictTo;
import androidx.annotation.Size;

import com.google.android.filament.proguard.UsedByNative;

/**
 * A MaterialInstance represents a specific instance of a Material.
 *
 * <p>While a Material defines the shader code and the set of parameters, a MaterialInstance
 * holds the specific values for those parameters.</p>
 */
@UsedByNative
public class MaterialInstance {
    static final class EnumCache {
        static final TextureSampler.CompareFunc[] sCompareFuncValues = TextureSampler.CompareFunc.values();
        static final Material.CullingMode[] sCullingModeValues = Material.CullingMode.values();
        static final Material.TransparencyMode[] sTransparencyModeValues = Material.TransparencyMode.values();
    }

    private final @Nullable Material mMaterial;
    private long mNativeObject;

    /** stencil operation */
    public enum StencilOperation {
        /** Keeps the current value. */
        KEEP,
        /** Sets the value to 0. */
        ZERO,
        /** Sets the value to the stencil reference value. */
        REPLACE,
        /**
         * Increments the current value.
         *
         * <p>Clamps to the maximum representable unsigned value.</p>
         */
        INCR_CLAMP,
        /**
         * Increments the current value.
         *
         * <p>Wraps value to zero when incrementing the maximum representable unsigned value.</p>
         */
        INCR_WRAP,
        /**
         * Decrements the current value.
         *
         * <p>Clamps to 0.</p>
         */
        DECR_CLAMP,
        /**
         * Decrements the current value.
         *
         * <p>Wraps value to the maximum representable unsigned value when decrementing a value of zero.</p>
         */
        DECR_WRAP,
        /** Bitwise inverts the current value. */
        INVERT;

        /** @return the value of this enum constant as used by the native Filament engine. */
        public int toFilamentNative() { return ordinal(); }
    }

    /** stencil faces */
    public enum StencilFace {
        /** Update stencil state for front-facing polygons. */
        FRONT(1),
        /** Update stencil state for back-facing polygons. */
        BACK(2),
        /** Update stencil state for all polygons. */
        FRONT_AND_BACK(3);

        private final int mValue;

        StencilFace(int value) {
            mValue = value;
        }

        /** @return the value of this enum constant as used by the native Filament engine. */
        public int toFilamentNative() { return mValue; }

        public static StencilFace from(int value) {
            switch (value) {
                case 1: return FRONT;
                case 2: return BACK;
                case 3: return FRONT_AND_BACK;
                default: throw new IllegalArgumentException("Unknown StencilFace value: " + value);
            }
        }
    }

    /** Element types for float tagged array buffers. */
    public enum FloatElement {
        FLOAT,
        FLOAT2,
        FLOAT3,
        FLOAT4,
        MAT3,
        MAT4;

        /** @return the value of this enum constant as used by the native Filament engine. */
        public int toFilamentNative() { return ordinal(); }
    }

    /** Element types for int tagged array buffers. */
    public enum IntElement {
        INT,
        INT2,
        INT3,
        INT4;

        /** @return the value of this enum constant as used by the native Filament engine. */
        public int toFilamentNative() { return ordinal(); }
    }

    /** Element types for boolean tagged array buffers. */
    public enum BooleanElement {
        BOOL,
        BOOL2,
        BOOL3,
        BOOL4;

        /** @return the value of this enum constant as used by the native Filament engine. */
        public int toFilamentNative() { return ordinal(); }
    }

    MaterialInstance(long nativeObject, @Nullable Material material) {
        mNativeObject = nativeObject;
        mMaterial = material;
    }

    /* package */ MaterialInstance(long nativeMaterialInstance) {
        this(nativeMaterialInstance, null);
    }

    @NonNull
    @RestrictTo(RestrictTo.Scope.LIBRARY_GROUP)
    public static MaterialInstance wrap(long nativeObject, @Nullable Material material) {
        return new MaterialInstance(nativeObject, material);
    }

    @NonNull
    @RestrictTo(RestrictTo.Scope.LIBRARY_GROUP)
    public static MaterialInstance wrap(long nativeObject) {
        return new MaterialInstance(nativeObject, null);
    }

    /**
     * Creates a new MaterialInstance using another MaterialInstance as a template for initialization.
     *
     * <p>The new MaterialInstance is an instance of the same Material of the template instance and
     * must be destroyed just like any other MaterialInstance.</p>
     *
     * @param other A MaterialInstance to use as a template for initializing a new instance
     *
     * @return A new MaterialInstance
     */
    public static MaterialInstance duplicate(@NonNull MaterialInstance other) {
        return duplicate(other, null);
    }

    /**
     * Creates a new MaterialInstance using another MaterialInstance as a template for initialization.
     *
     * <p>The new MaterialInstance is an instance of the same Material of the template instance and
     * must be destroyed just like any other MaterialInstance.</p>
     *
     * @param other A MaterialInstance to use as a template for initializing a new instance
     * @param name A name for the new MaterialInstance or nullptr to use the template's name
     *
     * @return A new MaterialInstance
     */
    public static MaterialInstance duplicate(@NonNull MaterialInstance other, @Nullable String name) {
        long nativeInstance = nDuplicate(other.getNativeObject(), name);
        if (nativeInstance == 0) throw new IllegalStateException("Couldn't duplicate MaterialInstance");
        return new MaterialInstance(nativeInstance, other.getMaterial());
    }

    /** @return the Material associated with this instance */
    public Material getMaterial() {
        return mMaterial;
    }

    /** @return the name associated with this instance */
    @NonNull
    public String getName() {
        return nGetName(getNativeObject());
    }

    /** inline helper to provide the name as a null-terminated C string */
    public void setParameter(@NonNull String name, float value) {
        nSetParameter(getNativeObject(), name, value);
    }

    /** inline helper to provide the name as a null-terminated C string */
    public void setParameter(@NonNull String name, int value) {
        nSetParameter(getNativeObject(), name, value);
    }

    /**
     * inline helper to provide the name as a null-terminated C string
     *
     * @param valuex (x component)
     * @param valuey (y component)
     */
    public void setParameter(@NonNull String name, int valuex, int valuey) {
        nSetParameter(getNativeObject(), name, valuex, valuey);
    }

    /**
     * inline helper to provide the name as a null-terminated C string
     *
     * @param valuex (x component)
     * @param valuey (y component)
     * @param valuez (z component)
     */
    public void setParameter(@NonNull String name, int valuex, int valuey, int valuez) {
        nSetParameter(getNativeObject(), name, valuex, valuey, valuez);
    }

    /**
     * inline helper to provide the name as a null-terminated C string
     *
     * @param valuex (x component)
     * @param valuey (y component)
     * @param valuez (z component)
     * @param valuew (w component)
     */
    public void setParameter(@NonNull String name, int valuex, int valuey, int valuez, int valuew) {
        nSetParameter(getNativeObject(), name, valuex, valuey, valuez, valuew);
    }

    /**
     * inline helper to provide the name as a null-terminated C string
     *
     * @param valuex (x component)
     * @param valuey (y component)
     */
    public void setParameter(@NonNull String name, float valuex, float valuey) {
        nSetParameter(getNativeObject(), name, valuex, valuey);
    }

    /**
     * inline helper to provide the name as a null-terminated C string
     *
     * @param valuex (x component)
     * @param valuey (y component)
     * @param valuez (z component)
     */
    public void setParameter(@NonNull String name, float valuex, float valuey, float valuez) {
        nSetParameter(getNativeObject(), name, valuex, valuey, valuez);
    }

    /**
     * inline helper to provide the name as a null-terminated C string
     *
     * @param valuex (x component)
     * @param valuey (y component)
     * @param valuez (z component)
     * @param valuew (w component)
     */
    public void setParameter(@NonNull String name, float valuex, float valuey, float valuez, float valuew) {
        nSetParameter(getNativeObject(), name, valuex, valuey, valuez, valuew);
    }

    /** inline helper to provide the name as a null-terminated C string */
    public void setParameter(@NonNull String name, @NonNull @Size(min = 16) float[] value) {
        Asserts.assertMat4fIn(value);
        nSetParameter(getNativeObject(), name, value);
    }

    /** inline helper to provide the name as a null-terminated C string */
    public void setParameter(@NonNull String name, boolean value) {
        nSetParameter(getNativeObject(), name, value);
    }

    /**
     * inline helper to provide the name as a null-terminated C string
     *
     * @param valuex (x component)
     * @param valuey (y component)
     */
    public void setParameter(@NonNull String name, boolean valuex, boolean valuey) {
        nSetParameter(getNativeObject(), name, valuex, valuey);
    }

    /**
     * inline helper to provide the name as a null-terminated C string
     *
     * @param valuex (x component)
     * @param valuey (y component)
     * @param valuez (z component)
     */
    public void setParameter(@NonNull String name, boolean valuex, boolean valuey, boolean valuez) {
        nSetParameter(getNativeObject(), name, valuex, valuey, valuez);
    }

    /**
     * inline helper to provide the name as a null-terminated C string
     *
     * @param valuex (x component)
     * @param valuey (y component)
     * @param valuez (z component)
     * @param valuew (w component)
     */
    public void setParameter(@NonNull String name, boolean valuex, boolean valuey, boolean valuez, boolean valuew) {
        nSetParameter(getNativeObject(), name, valuex, valuey, valuez, valuew);
    }

    /**
     * Set a uniform array by name
     *
     * @param name Name of the parameter array as defined by Material.
     * @param type the number of components for each individual parameter
     * @param values Array of values to set to the named parameter array.
     * @param offset the number of elements in <code>values</code> to skip
     *
     * @see Material#hasParameter
     */
    public void setParameter(@NonNull String name, @NonNull FloatElement type, @NonNull float[] values, @IntRange(from = 0) int offset, @IntRange(from = 1) int count) {
        nSetParameterFloatArray(getNativeObject(), name, type.ordinal(), values, offset, count);
    }

    /**
     * Set a uniform array by name
     *
     * @param name Name of the parameter array as defined by Material.
     * @param type the number of components for each individual parameter
     * @param values Array of values to set to the named parameter array.
     *
     * @see Material#hasParameter
     */
    public void setParameter(@NonNull String name, @NonNull FloatElement type, @NonNull float[] values, @IntRange(from = 1) int count) {
        setParameter(name, type, values, 0, count);
    }

    /**
     * Set a uniform array by name
     *
     * @param name Name of the parameter array as defined by Material.
     * @param type the number of components for each individual parameter
     * @param values Array of values to set to the named parameter array.
     * @param offset the number of elements in <code>values</code> to skip
     *
     * @see Material#hasParameter
     */
    public void setParameter(@NonNull String name, @NonNull IntElement type, @NonNull int[] values, @IntRange(from = 0) int offset, @IntRange(from = 1) int count) {
        nSetParameterIntArray(getNativeObject(), name, type.ordinal(), values, offset, count);
    }

    /**
     * Set a uniform array by name
     *
     * @param name Name of the parameter array as defined by Material.
     * @param type the number of components for each individual parameter
     * @param values Array of values to set to the named parameter array.
     *
     * @see Material#hasParameter
     */
    public void setParameter(@NonNull String name, @NonNull IntElement type, @NonNull int[] values, @IntRange(from = 1) int count) {
        setParameter(name, type, values, 0, count);
    }

    /**
     * Set a uniform array by name
     *
     * @param name Name of the parameter array as defined by Material.
     * @param type the number of components for each individual parameter
     * @param values Array of values to set to the named parameter array.
     * @param offset the number of elements in <code>values</code> to skip
     *
     * @see Material#hasParameter
     */
    public void setParameter(@NonNull String name, @NonNull BooleanElement type, @NonNull boolean[] values, @IntRange(from = 0) int offset, @IntRange(from = 1) int count) {
        nSetParameterBooleanArray(getNativeObject(), name, type.ordinal(), values, offset, count);
    }

    /**
     * Set a uniform array by name
     *
     * @param name Name of the parameter array as defined by Material.
     * @param type the number of components for each individual parameter
     * @param values Array of values to set to the named parameter array.
     *
     * @see Material#hasParameter
     */
    public void setParameter(@NonNull String name, @NonNull BooleanElement type, @NonNull boolean[] values, @IntRange(from = 1) int count) {
        setParameter(name, type, values, 0, count);
    }

    /** inline helper to provide the name as a null-terminated C string */
    public void setParameter(@NonNull String name, @Nullable Texture texture, @NonNull TextureSampler sampler) {
        nSetParameter(getNativeObject(), name, texture != null ? texture.getNativeObject() : 0, sampler.mSampler);
    }

    /**
     * inline helper to provide the name as a null-terminated C string
     *
     * @param colorx (x component)
     * @param colory (y component)
     * @param colorz (z component)
     */
    public void setParameter(@NonNull String name, @NonNull Colors.RgbType type, float colorx, float colory, float colorz) {
        nSetParameter(getNativeObject(), name, type.ordinal(), colorx, colory, colorz);
    }

    /**
     * inline helper to provide the name as a null-terminated C string
     *
     * @param name
     * @param type
     * @param color
     */
    public void setParameter(@NonNull String name, Colors.RgbType type, @NonNull @Size(min = 3) float[] color) {
        setParameter(name, type, color[0], color[1], color[2]);
    }

    /**
     * inline helper to provide the name as a null-terminated C string
     *
     * @param colorx (x component)
     * @param colory (y component)
     * @param colorz (z component)
     * @param colorw (w component)
     */
    public void setParameter(@NonNull String name, @NonNull Colors.RgbaType type, float colorx, float colory, float colorz, float colorw) {
        nSetParameter(getNativeObject(), name, type.ordinal(), colorx, colory, colorz, colorw);
    }

    /**
     * inline helper to provide the name as a null-terminated C string
     *
     * @param name
     * @param type
     * @param color
     */
    public void setParameter(@NonNull String name, Colors.RgbaType type, @NonNull @Size(min = 4) float[] color) {
        setParameter(name, type, color[0], color[1], color[2], color[3]);
    }

    /** inline helper to provide the name as a null-terminated C string */
    public void setConstant(@NonNull String name, int value) {
        nSetConstant(getNativeObject(), name, value);
    }

    /** inline helper to provide the name as a null-terminated C string */
    public void setConstant(@NonNull String name, float value) {
        nSetConstant(getNativeObject(), name, value);
    }

    /** inline helper to provide the name as a null-terminated C string */
    public void setConstant(@NonNull String name, boolean value) {
        nSetConstant(getNativeObject(), name, value);
    }

    /** inline helper to provide the name as a null-terminated C string */
    public int getConstantInt(@NonNull String name) {
        return nGetConstantInt(getNativeObject(), name);
    }

    /** inline helper to provide the name as a null-terminated C string */
    public float getConstantFloat(@NonNull String name) {
        return nGetConstantFloat(getNativeObject(), name);
    }

    /** inline helper to provide the name as a null-terminated C string */
    public boolean getConstantBoolean(@NonNull String name) {
        return nGetConstantBoolean(getNativeObject(), name);
    }

    /**
     * Asynchronously ensures that a subset of this MaterialInstance's variants are compiled.
     *
     * <p>This function behaves identically to Material::compile(), but takes into account the
     * specific constants overridden by setConstant().</p>
     *
     * @param priority Which priority queue to use, LOW or HIGH.
     * @param variants Variants to include to the compile command.
     * @param handler Handler to dispatch the callback or nullptr for the default handler
     * @param callback callback called on the main thread when the compilation is done on
     *                      by backend.
     *
     * @see Material#compile
     * @see #setConstant
     */
    public void compile(@NonNull Material.CompilerPriorityQueue priority, @IntRange(from = 0) int variants, @Nullable Object handler, @Nullable Runnable callback) {
        nCompile(getNativeObject(), priority.ordinal(), variants, handler, callback);
    }

    /**
     * Set-up a custom scissor rectangle; by default it is disabled.
     *
     * <p>The scissor rectangle gets clipped by the View's viewport, in other words, the scissor
     * cannot affect fragments outside of the View's Viewport.</p>
     *
     * <p>Currently the scissor is not compatible with dynamic resolution and should always be
     * disabled when dynamic resolution is used.</p>
     *
     * @param left left coordinate of the scissor box relative to the viewport
     * @param bottom bottom coordinate of the scissor box relative to the viewport
     * @param width width of the scissor box
     * @param height height of the scissor box
     *
     * @see #unsetScissor
     * @see View#setViewport
     * @see View#setDynamicResolutionOptions
     */
    public void setScissor(@IntRange(from = 0) int left, @IntRange(from = 0) int bottom, @IntRange(from = 0) int width, @IntRange(from = 0) int height) {
        nSetScissor(getNativeObject(), left, bottom, width, height);
    }

    /**
     * Returns the scissor rectangle to its default disabled setting.
     *
     * <p>Currently the scissor is not compatible with dynamic resolution and should always be
     * disabled when dynamic resolution is used.</p>
     *
     * @see View#setDynamicResolutionOptions
     */
    public void unsetScissor() {
        nUnsetScissor(getNativeObject());
    }

    /**
     * Sets a polygon offset that will be applied to all renderables drawn with this material instance.
     *
     * <p>The value of the offset is scale <i> dz + r </i> constant, where dz is the change in depth
     *  relative to the screen area of the triangle, and r is the smallest value that is guaranteed
     *  to produce a resolvable offset for a given implementation. This offset is added before the
     *  depth test.</p>
     *
     * <p>using a polygon offset other than zero has a significant negative performance
     *  impact, as most implementations have to disable early depth culling. DO NOT USE unless
     *  absolutely necessary.</p>
     *
     * @param scale scale factor used to create a variable depth offset for each triangle
     * @param constant scale factor used to create a constant depth offset for each triangle
     */
    public void setPolygonOffset(float scale, float constant) {
        nSetPolygonOffset(getNativeObject(), scale, constant);
    }

    /**
     * Overrides the minimum alpha value a fragment must have to not be discarded when the blend mode is MASKED.
     *
     * <p>Defaults to 0.4 if it has not been set in the parent Material. The specified
     * value should be between 0 and 1 and will be clamped if necessary.</p>
     */
    public void setMaskThreshold(float threshold) {
        nSetMaskThreshold(getNativeObject(), threshold);
    }

    /** Gets the minimum alpha value a fragment must have to not be discarded when the blend mode is MASKED */
    public float getMaskThreshold() {
        return nGetMaskThreshold(getNativeObject());
    }

    /**
     * Sets the screen space variance of the filter kernel used when applying specular anti-aliasing.
     *
     * <p>The default value is set to 0.15. The specified value should be between
     * 0 and 1 and will be clamped if necessary.</p>
     */
    public void setSpecularAntiAliasingVariance(float variance) {
        nSetSpecularAntiAliasingVariance(getNativeObject(), variance);
    }

    /** Gets the screen space variance of the filter kernel used when applying specular anti-aliasing. */
    public float getSpecularAntiAliasingVariance() {
        return nGetSpecularAntiAliasingVariance(getNativeObject());
    }

    /**
     * Sets the clamping threshold used to suppress estimation errors when applying specular anti-aliasing.
     *
     * <p>The default value is set to 0.2. The specified value should be between 0
     * and 1 and will be clamped if necessary.</p>
     */
    public void setSpecularAntiAliasingThreshold(float threshold) {
        nSetSpecularAntiAliasingThreshold(getNativeObject(), threshold);
    }

    /** Gets the clamping threshold used to suppress estimation errors when applying specular anti-aliasing. */
    public float getSpecularAntiAliasingThreshold() {
        return nGetSpecularAntiAliasingThreshold(getNativeObject());
    }

    /**
     * Enables or disables double-sided lighting if the parent Material has double-sided capability, otherwise prints a warning.
     *
     * <p>If double-sided lighting is enabled, backface culling is
     * automatically disabled.</p>
     */
    public void setDoubleSided(boolean doubleSided) {
        nSetDoubleSided(getNativeObject(), doubleSided);
    }

    /** Returns whether double-sided lighting is enabled when the parent Material has double-sided capability. */
    public boolean isDoubleSided() {
        return nIsDoubleSided(getNativeObject());
    }

    /** Specifies how transparent objects should be rendered (default is DEFAULT). */
    public void setTransparencyMode(@NonNull Material.TransparencyMode mode) {
        nSetTransparencyMode(getNativeObject(), mode.ordinal());
    }

    /** Returns the transparency mode. */
    public Material.TransparencyMode getTransparencyMode() {
        return EnumCache.sTransparencyModeValues[nGetTransparencyMode(getNativeObject())];
    }

    /** Overrides the default triangle culling state that was set on the material. */
    public void setCullingMode(@NonNull Material.CullingMode culling) {
        nSetCullingMode(getNativeObject(), culling.ordinal());
    }

    /** Overrides the default triangle culling state that was set on the material separately for the color and shadow passes */
    public void setCullingMode(@NonNull Material.CullingMode colorPassCullingMode, @NonNull Material.CullingMode shadowPassCullingMode) {
        nSetCullingMode(getNativeObject(), colorPassCullingMode.ordinal(), shadowPassCullingMode.ordinal());
    }

    /** Returns the face culling mode. */
    public Material.CullingMode getCullingMode() {
        return EnumCache.sCullingModeValues[nGetCullingMode(getNativeObject())];
    }

    /** Returns the face culling mode for the shadow passes. */
    public Material.CullingMode getShadowCullingMode() {
        return EnumCache.sCullingModeValues[nGetShadowCullingMode(getNativeObject())];
    }

    /** Overrides the default color-buffer write state that was set on the material. */
    public void setColorWrite(boolean enable) {
        nSetColorWrite(getNativeObject(), enable);
    }

    /** Returns whether color write is enabled. */
    public boolean isColorWriteEnabled() {
        return nIsColorWriteEnabled(getNativeObject());
    }

    /** Overrides the default depth-buffer write state that was set on the material. */
    public void setDepthWrite(boolean enable) {
        nSetDepthWrite(getNativeObject(), enable);
    }

    /** Returns whether depth write is enabled. */
    public boolean isDepthWriteEnabled() {
        return nIsDepthWriteEnabled(getNativeObject());
    }

    /** Overrides the default depth testing state that was set on the material. */
    public void setDepthCulling(boolean enable) {
        nSetDepthCulling(getNativeObject(), enable);
    }

    /** Overrides the default depth function state that was set on the material. */
    public void setDepthFunc(@NonNull TextureSampler.CompareFunc depthFunc) {
        nSetDepthFunc(getNativeObject(), depthFunc.ordinal());
    }

    /** Returns the depth function state. */
    public TextureSampler.CompareFunc getDepthFunc() {
        return EnumCache.sCompareFuncValues[nGetDepthFunc(getNativeObject())];
    }

    /** Returns whether depth culling is enabled. */
    public boolean isDepthCullingEnabled() {
        return nIsDepthCullingEnabled(getNativeObject());
    }

    /** Overrides the default stencil-buffer write state that was set on the material. */
    public void setStencilWrite(boolean enable) {
        nSetStencilWrite(getNativeObject(), enable);
    }

    /** Returns whether stencil write is enabled. */
    public boolean isStencilWriteEnabled() {
        return nIsStencilWriteEnabled(getNativeObject());
    }

    /**
     * Sets the stencil comparison function (default is StencilCompareFunc::A).
     *
     * <p>It's possible to set separate stencil comparison functions; one for front-facing polygons,
     * and one for back-facing polygons. The face parameter determines the comparison function(s)
     * updated by this call.</p>
     */
    public void setStencilCompareFunction(@NonNull TextureSampler.CompareFunc func) {
        setStencilCompareFunction(func, StencilFace.FRONT_AND_BACK);
    }

    /**
     * Sets the stencil comparison function (default is StencilCompareFunc::A).
     *
     * <p>It's possible to set separate stencil comparison functions; one for front-facing polygons,
     * and one for back-facing polygons. The face parameter determines the comparison function(s)
     * updated by this call.</p>
     */
    public void setStencilCompareFunction(@NonNull TextureSampler.CompareFunc func, @NonNull StencilFace face) {
        nSetStencilCompareFunction(getNativeObject(), func.ordinal(), face.toFilamentNative());
    }

    /**
     * Sets the stencil fail operation (default is StencilOperation::KEEP).
     *
     * <p>The stencil fail operation is performed to update values in the stencil buffer when the
     * stencil test fails.</p>
     *
     * <p>It's possible to set separate stencil fail operations; one for front-facing polygons, and one
     * for back-facing polygons. The face parameter determines the stencil fail operation(s) updated
     * by this call.</p>
     */
    public void setStencilOpStencilFail(@NonNull StencilOperation op) {
        setStencilOpStencilFail(op, StencilFace.FRONT_AND_BACK);
    }

    /**
     * Sets the stencil fail operation (default is StencilOperation::KEEP).
     *
     * <p>The stencil fail operation is performed to update values in the stencil buffer when the
     * stencil test fails.</p>
     *
     * <p>It's possible to set separate stencil fail operations; one for front-facing polygons, and one
     * for back-facing polygons. The face parameter determines the stencil fail operation(s) updated
     * by this call.</p>
     */
    public void setStencilOpStencilFail(@NonNull StencilOperation op, @NonNull StencilFace face) {
        nSetStencilOpStencilFail(getNativeObject(), op.toFilamentNative(), face.toFilamentNative());
    }

    /**
     * Sets the depth fail operation (default is StencilOperation::KEEP).
     *
     * <p>The depth fail operation is performed to update values in the stencil buffer when the depth
     * test fails.</p>
     *
     * <p>It's possible to set separate depth fail operations; one for front-facing polygons, and one
     * for back-facing polygons. The face parameter determines the depth fail operation(s) updated
     * by this call.</p>
     */
    public void setStencilOpDepthFail(@NonNull StencilOperation op) {
        setStencilOpDepthFail(op, StencilFace.FRONT_AND_BACK);
    }

    /**
     * Sets the depth fail operation (default is StencilOperation::KEEP).
     *
     * <p>The depth fail operation is performed to update values in the stencil buffer when the depth
     * test fails.</p>
     *
     * <p>It's possible to set separate depth fail operations; one for front-facing polygons, and one
     * for back-facing polygons. The face parameter determines the depth fail operation(s) updated
     * by this call.</p>
     */
    public void setStencilOpDepthFail(@NonNull StencilOperation op, @NonNull StencilFace face) {
        nSetStencilOpDepthFail(getNativeObject(), op.toFilamentNative(), face.toFilamentNative());
    }

    /**
     * Sets the depth-stencil pass operation (default is StencilOperation::KEEP).
     *
     * <p>The depth-stencil pass operation is performed to update values in the stencil buffer when
     * both the stencil test and depth test pass.</p>
     *
     * <p>It's possible to set separate depth-stencil pass operations; one for front-facing polygons,
     * and one for back-facing polygons. The face parameter determines the depth-stencil pass
     * operation(s) updated by this call.</p>
     */
    public void setStencilOpDepthStencilPass(@NonNull StencilOperation op) {
        setStencilOpDepthStencilPass(op, StencilFace.FRONT_AND_BACK);
    }

    /**
     * Sets the depth-stencil pass operation (default is StencilOperation::KEEP).
     *
     * <p>The depth-stencil pass operation is performed to update values in the stencil buffer when
     * both the stencil test and depth test pass.</p>
     *
     * <p>It's possible to set separate depth-stencil pass operations; one for front-facing polygons,
     * and one for back-facing polygons. The face parameter determines the depth-stencil pass
     * operation(s) updated by this call.</p>
     */
    public void setStencilOpDepthStencilPass(@NonNull StencilOperation op, @NonNull StencilFace face) {
        nSetStencilOpDepthStencilPass(getNativeObject(), op.toFilamentNative(), face.toFilamentNative());
    }

    /**
     * Sets the stencil reference value (default is 0).
     *
     * <p>The stencil reference value is the left-hand side for stencil comparison tests. It's also
     * used as the replacement stencil value when StencilOperation is REPLACE.</p>
     *
     * <p>It's possible to set separate stencil reference values; one for front-facing polygons, and
     * one for back-facing polygons. The face parameter determines the reference value(s) updated by
     * this call.</p>
     */
    public void setStencilReferenceValue(@IntRange(from = 0) int value) {
        setStencilReferenceValue(value, StencilFace.FRONT_AND_BACK);
    }

    /**
     * Sets the stencil reference value (default is 0).
     *
     * <p>The stencil reference value is the left-hand side for stencil comparison tests. It's also
     * used as the replacement stencil value when StencilOperation is REPLACE.</p>
     *
     * <p>It's possible to set separate stencil reference values; one for front-facing polygons, and
     * one for back-facing polygons. The face parameter determines the reference value(s) updated by
     * this call.</p>
     */
    public void setStencilReferenceValue(@IntRange(from = 0) int value, @NonNull StencilFace face) {
        nSetStencilReferenceValue(getNativeObject(), value, face.toFilamentNative());
    }

    /**
     * Sets the stencil read mask (default is 0xFF).
     *
     * <p>The stencil read mask masks the bits of the values participating in the stencil comparison
     * test- both the value read from the stencil buffer and the reference value.</p>
     *
     * <p>It's possible to set separate stencil read masks; one for front-facing polygons, and one for
     * back-facing polygons. The face parameter determines the stencil read mask(s) updated by this
     * call.</p>
     */
    public void setStencilReadMask(@IntRange(from = 0) int readMask) {
        setStencilReadMask(readMask, StencilFace.FRONT_AND_BACK);
    }

    /**
     * Sets the stencil read mask (default is 0xFF).
     *
     * <p>The stencil read mask masks the bits of the values participating in the stencil comparison
     * test- both the value read from the stencil buffer and the reference value.</p>
     *
     * <p>It's possible to set separate stencil read masks; one for front-facing polygons, and one for
     * back-facing polygons. The face parameter determines the stencil read mask(s) updated by this
     * call.</p>
     */
    public void setStencilReadMask(@IntRange(from = 0) int readMask, @NonNull StencilFace face) {
        nSetStencilReadMask(getNativeObject(), readMask, face.toFilamentNative());
    }

    /**
     * Sets the stencil write mask (default is 0xFF).
     *
     * <p>The stencil write mask masks the bits in the stencil buffer updated by stencil operations.</p>
     *
     * <p>It's possible to set separate stencil write masks; one for front-facing polygons, and one for
     * back-facing polygons. The face parameter determines the stencil write mask(s) updated by this
     * call.</p>
     */
    public void setStencilWriteMask(@IntRange(from = 0) int writeMask) {
        setStencilWriteMask(writeMask, StencilFace.FRONT_AND_BACK);
    }

    /**
     * Sets the stencil write mask (default is 0xFF).
     *
     * <p>The stencil write mask masks the bits in the stencil buffer updated by stencil operations.</p>
     *
     * <p>It's possible to set separate stencil write masks; one for front-facing polygons, and one for
     * back-facing polygons. The face parameter determines the stencil write mask(s) updated by this
     * call.</p>
     */
    public void setStencilWriteMask(@IntRange(from = 0) int writeMask, @NonNull StencilFace face) {
        nSetStencilWriteMask(getNativeObject(), writeMask, face.toFilamentNative());
    }

    /**
     * PostProcess and compute domain material instance must be commited manually.
     *
     * <p>This call has
     * no effect on surface domain materials.</p>
     *
     * @param engine Filament engine
     */
    public void commit(@NonNull Engine engine) {
        nCommit(getNativeObject(), engine.getNativeObject());
    }

    @UsedByNative("Engine.cpp")
    public long getNativeObject() {
        if (mNativeObject == 0) {
            throw new IllegalStateException("Calling method on destroyed MaterialInstance");
        }
        return mNativeObject;
    }

    void clearNativeObject() {
        mNativeObject = 0;
    }

    private static native long nDuplicate(long nativeOther, @Nullable String name);
    @NonNull
    private static native String nGetName(long nativeMaterialInstance);
    private static native void nSetParameter(long nativeMaterialInstance, @NonNull String name, float value);
    private static native void nSetParameter(long nativeMaterialInstance, @NonNull String name, int value);
    private static native void nSetParameter(long nativeMaterialInstance, @NonNull String name, int valuex, int valuey);
    private static native void nSetParameter(long nativeMaterialInstance, @NonNull String name, int valuex, int valuey, int valuez);
    private static native void nSetParameter(long nativeMaterialInstance, @NonNull String name, int valuex, int valuey, int valuez, int valuew);
    private static native void nSetParameter(long nativeMaterialInstance, @NonNull String name, float valuex, float valuey);
    private static native void nSetParameter(long nativeMaterialInstance, @NonNull String name, float valuex, float valuey, float valuez);
    private static native void nSetParameter(long nativeMaterialInstance, @NonNull String name, float valuex, float valuey, float valuez, float valuew);
    private static native void nSetParameter(long nativeMaterialInstance, @NonNull String name, @NonNull @Size(min = 16) float[] value);
    private static native void nSetParameter(long nativeMaterialInstance, @NonNull String name, boolean value);
    private static native void nSetParameter(long nativeMaterialInstance, @NonNull String name, boolean valuex, boolean valuey);
    private static native void nSetParameter(long nativeMaterialInstance, @NonNull String name, boolean valuex, boolean valuey, boolean valuez);
    private static native void nSetParameter(long nativeMaterialInstance, @NonNull String name, boolean valuex, boolean valuey, boolean valuez, boolean valuew);
    private static native void nSetParameterFloatArray(long nativeMaterialInstance, @NonNull String name, int type, @NonNull @Size(min = 1) float[] values, @IntRange(from = 0) int offset, @IntRange(from = 1) int count);
    private static native void nSetParameterIntArray(long nativeMaterialInstance, @NonNull String name, int type, @NonNull @Size(min = 1) int[] values, @IntRange(from = 0) int offset, @IntRange(from = 1) int count);
    private static native void nSetParameterBooleanArray(long nativeMaterialInstance, @NonNull String name, int type, @NonNull @Size(min = 1) boolean[] values, @IntRange(from = 0) int offset, @IntRange(from = 1) int count);
    private static native void nSetParameter(long nativeMaterialInstance, @NonNull String name, long nativeTexture, int sampler);
    private static native void nSetParameter(long nativeMaterialInstance, @NonNull String name, int type, float colorx, float colory, float colorz);
    private static native void nSetParameter(long nativeMaterialInstance, @NonNull String name, int type, float colorx, float colory, float colorz, float colorw);
    private static native void nSetConstant(long nativeMaterialInstance, @NonNull String name, int value);
    private static native void nSetConstant(long nativeMaterialInstance, @NonNull String name, float value);
    private static native void nSetConstant(long nativeMaterialInstance, @NonNull String name, boolean value);
    private static native int nGetConstantInt(long nativeMaterialInstance, @NonNull String name);
    private static native float nGetConstantFloat(long nativeMaterialInstance, @NonNull String name);
    private static native boolean nGetConstantBoolean(long nativeMaterialInstance, @NonNull String name);
    private static native void nCompile(long nativeMaterialInstance, int priority, int variants, Object handler, Runnable callback);
    private static native void nSetScissor(long nativeMaterialInstance, @IntRange(from = 0) int left, @IntRange(from = 0) int bottom, @IntRange(from = 0) int width, @IntRange(from = 0) int height);
    private static native void nUnsetScissor(long nativeMaterialInstance);
    private static native void nSetPolygonOffset(long nativeMaterialInstance, float scale, float constant);
    private static native void nSetMaskThreshold(long nativeMaterialInstance, float threshold);
    private static native float nGetMaskThreshold(long nativeMaterialInstance);
    private static native void nSetSpecularAntiAliasingVariance(long nativeMaterialInstance, float variance);
    private static native float nGetSpecularAntiAliasingVariance(long nativeMaterialInstance);
    private static native void nSetSpecularAntiAliasingThreshold(long nativeMaterialInstance, float threshold);
    private static native float nGetSpecularAntiAliasingThreshold(long nativeMaterialInstance);
    private static native void nSetDoubleSided(long nativeMaterialInstance, boolean doubleSided);
    private static native boolean nIsDoubleSided(long nativeMaterialInstance);
    private static native void nSetTransparencyMode(long nativeMaterialInstance, int mode);
    private static native int nGetTransparencyMode(long nativeMaterialInstance);
    private static native void nSetCullingMode(long nativeMaterialInstance, int culling);
    private static native void nSetCullingMode(long nativeMaterialInstance, int colorPassCullingMode, int shadowPassCullingMode);
    private static native int nGetCullingMode(long nativeMaterialInstance);
    private static native int nGetShadowCullingMode(long nativeMaterialInstance);
    private static native void nSetColorWrite(long nativeMaterialInstance, boolean enable);
    private static native boolean nIsColorWriteEnabled(long nativeMaterialInstance);
    private static native void nSetDepthWrite(long nativeMaterialInstance, boolean enable);
    private static native boolean nIsDepthWriteEnabled(long nativeMaterialInstance);
    private static native void nSetDepthCulling(long nativeMaterialInstance, boolean enable);
    private static native void nSetDepthFunc(long nativeMaterialInstance, int depthFunc);
    private static native int nGetDepthFunc(long nativeMaterialInstance);
    private static native boolean nIsDepthCullingEnabled(long nativeMaterialInstance);
    private static native void nSetStencilWrite(long nativeMaterialInstance, boolean enable);
    private static native boolean nIsStencilWriteEnabled(long nativeMaterialInstance);
    private static native void nSetStencilCompareFunction(long nativeMaterialInstance, int func, int face);
    private static native void nSetStencilOpStencilFail(long nativeMaterialInstance, int op, int face);
    private static native void nSetStencilOpDepthFail(long nativeMaterialInstance, int op, int face);
    private static native void nSetStencilOpDepthStencilPass(long nativeMaterialInstance, int op, int face);
    private static native void nSetStencilReferenceValue(long nativeMaterialInstance, @IntRange(from = 0) int value, int face);
    private static native void nSetStencilReadMask(long nativeMaterialInstance, @IntRange(from = 0) int readMask, int face);
    private static native void nSetStencilWriteMask(long nativeMaterialInstance, @IntRange(from = 0) int writeMask, int face);
    private static native void nCommit(long nativeMaterialInstance, long nativeEngine);
}

// THIS FILE IS GENERATED BY APIGEN AND MUST NOT BE HAND-MODIFIED.
