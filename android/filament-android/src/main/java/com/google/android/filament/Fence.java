/*
 * Copyright (C) 2026 The Android Open Source Project
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *      http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

// THIS FILE IS GENERATED BY APIGEN AND MUST NOT BE HAND-MODIFIED.

package com.google.android.filament;

import java.lang.IllegalStateException;

import androidx.annotation.IntRange;
import androidx.annotation.NonNull;
import androidx.annotation.RestrictTo;

/** Fence is used to synchronize the application main thread with filament's rendering thread. */
public class Fence {
    private long mNativeObject;

    /** Error codes for Fence::wait() */
    public enum FenceStatus {
        /**
         * An error occurred.
         *
         * <p>The Fence condition is not satisfied.</p>
         */
        ERROR(-1),
        /** The Fence condition is satisfied. */
        CONDITION_SATISFIED(0),
        /**
         * wait()'s timeout expired.
         *
         * <p>The Fence condition is not satisfied.</p>
         */
        TIMEOUT_EXPIRED(1);

        private final int mValue;

        FenceStatus(int value) {
            mValue = value;
        }

        /** @return the value of this enum constant as used by the native Filament engine. */
        public int toFilamentNative() { return mValue; }

        public static FenceStatus from(int value) {
            switch (value) {
                case -1: return ERROR;
                case 0: return CONDITION_SATISFIED;
                case 1: return TIMEOUT_EXPIRED;
                default: throw new IllegalArgumentException("Unknown FenceStatus value: " + value);
            }
        }
    }

    /**
     * Mode controls the behavior of the command stream when calling wait()
     *
     * <p>@attention
     * It would be unwise to call {@code wait(..., Mode::DONT_FLUSH)} from the same thread
     * the Fence was created, as it would most certainly create a dead-lock.</p>
     */
    public enum Mode {
        /** The command stream is flushed */
        FLUSH,
        /** The command stream is not flushed */
        DONT_FLUSH;

        /** @return the value of this enum constant as used by the native Filament engine. */
        public int toFilamentNative() { return ordinal(); }
    }

    /** Special {@code timeout} value to disable wait()'s timeout. */
    public static final long FENCE_WAIT_FOR_EVER = -1;
    public static final long WAIT_FOR_EVER = FENCE_WAIT_FOR_EVER;

    Fence(long nativeObject) {
        mNativeObject = nativeObject;
    }

    @NonNull
    @RestrictTo(RestrictTo.Scope.LIBRARY_GROUP)
    public static Fence wrap(long nativeObject) {
        return new Fence(nativeObject);
    }

    /**
     * Client-side wait on the Fence.
     *
     * <p>Blocks the current thread until the Fence signals.</p>
     *
     * @param mode Whether the command stream is flushed before waiting or not.
     *
     * @return FenceStatus::CONDITION_SATISFIED on success,
     *                  FenceStatus::TIMEOUT_EXPIRED if the time out expired or
     *                  FenceStatus::ERROR in other cases.
     *
     * @see #Mode
     */
    public FenceStatus wait(@NonNull Mode mode) {
        return wait(mode, FENCE_WAIT_FOR_EVER);
    }

    /**
     * Client-side wait on the Fence.
     *
     * <p>Blocks the current thread until the Fence signals.</p>
     *
     * @param mode Whether the command stream is flushed before waiting or not.
     * @param timeout Wait time out in nanoseconds. Using a {@code timeout} of 0 is a way to query the state of the fence.
     *                  A {@code timeout} value of FENCE_WAIT_FOR_EVER is used to disable the timeout.
     *
     * @return FenceStatus::CONDITION_SATISFIED on success,
     *                  FenceStatus::TIMEOUT_EXPIRED if the time out expired or
     *                  FenceStatus::ERROR in other cases.
     *
     * @see #Mode
     */
    public FenceStatus wait(@NonNull Mode mode, @IntRange(from = 0) long timeout) {
        return FenceStatus.from(nWait(getNativeObject(), mode.toFilamentNative(), timeout));
    }

    /**
     * Client-side wait on a Fence and destroy the Fence.
     *
     * @param fence Fence object to wait on.
     *
     * @return FenceStatus::CONDITION_SATISFIED on success,
     *          FenceStatus::ERROR otherwise.
     */
    public static FenceStatus waitAndDestroy(@NonNull Fence fence) {
        return waitAndDestroy(fence, Mode.FLUSH);
    }

    /**
     * Client-side wait on a Fence and destroy the Fence.
     *
     * @param fence Fence object to wait on.
     * @param mode Whether the command stream is flushed before waiting or not.
     *
     * @return FenceStatus::CONDITION_SATISFIED on success,
     *          FenceStatus::ERROR otherwise.
     */
    public static FenceStatus waitAndDestroy(@NonNull Fence fence, @NonNull Mode mode) {
        return FenceStatus.from(nWaitAndDestroy(fence.getNativeObject(), mode.toFilamentNative()));
    }

    public long getNativeObject() {
        if (mNativeObject == 0) {
            throw new IllegalStateException("Calling method on destroyed Fence");
        }
        return mNativeObject;
    }

    void clearNativeObject() {
        mNativeObject = 0;
    }

    private static native int nWait(long nativeFence, int mode, @IntRange(from = 0) long timeout);
    private static native int nWaitAndDestroy(long nativeFence, int mode);
}

// THIS FILE IS GENERATED BY APIGEN AND MUST NOT BE HAND-MODIFIED.
