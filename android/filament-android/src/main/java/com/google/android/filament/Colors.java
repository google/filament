/*
 * Copyright (C) 2026 The Android Open Source Project
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *      http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

// THIS FILE IS GENERATED BY APIGEN AND MUST NOT BE HAND-MODIFIED.

package com.google.android.filament;

import androidx.annotation.NonNull;
import androidx.annotation.Nullable;
import androidx.annotation.Size;

import java.lang.annotation.Retention;
import java.lang.annotation.Target;

import static java.lang.annotation.ElementType.FIELD;
import static java.lang.annotation.ElementType.LOCAL_VARIABLE;
import static java.lang.annotation.ElementType.METHOD;
import static java.lang.annotation.ElementType.PARAMETER;
import static java.lang.annotation.RetentionPolicy.SOURCE;

/** Utilities to manipulate and convert colors */
public class Colors {
    @Retention(SOURCE)
    @Target({PARAMETER, METHOD, LOCAL_VARIABLE, FIELD})
    public @interface LinearColor {
    }

    /** types of RGB colors */
    public enum RgbType {
        /** the color is defined in Rec.709-sRGB-D65 (sRGB) space */
        SRGB,
        /** the color is defined in Rec.709-Linear-D65 ("linear sRGB") space */
        LINEAR;

        /** @return the value of this enum constant as used by the native Filament engine. */
        public int toFilamentNative() { return ordinal(); }
    }

    /** types of RGBA colors */
    public enum RgbaType {
        /** the color is defined in Rec.709-sRGB-D65 (sRGB) space and the RGB values have not been pre-multiplied by the alpha (for instance, a 50% transparent red is <1,0,0,0.5>) */
        SRGB,
        /** the color is defined in Rec.709-Linear-D65 ("linear sRGB") space and the RGB values have not been pre-multiplied by the alpha (for instance, a 50% transparent red is <1,0,0,0.5>) */
        LINEAR,
        /** the color is defined in Rec.709-sRGB-D65 (sRGB) space and the RGB values have been pre-multiplied by the alpha (for instance, a 50% transparent red is <0.5,0,0,0.5>) */
        PREMULTIPLIED_SRGB,
        /** the color is defined in Rec.709-Linear-D65 ("linear sRGB") space and the RGB values have been pre-multiplied by the alpha (for instance, a 50% transparent red is <0.5,0,0,0.5>) */
        PREMULTIPLIED_LINEAR;

        /** @return the value of this enum constant as used by the native Filament engine. */
        public int toFilamentNative() { return ordinal(); }
    }

    /** type of color conversion to use when converting to/from sRGB and linear spaces */
    public enum ColorConversion {
        /** accurate conversion using the sRGB standard */
        ACCURATE,
        /** fast conversion using a simple gamma 2.2 curve */
        FAST;

        /** @return the value of this enum constant as used by the native Filament engine. */
        public int toFilamentNative() { return ordinal(); }
    }

    private Colors() {
    }

    /** converts an RGB color to linear space, the conversion depends on the specified type */
    @NonNull @Size(min = 3) @LinearColor
    public static float[] toLinear(@NonNull RgbType type, @NonNull @Size(min = 3) float[] color, @Nullable @Size(min = 3) float[] out) {
        return toLinear(type, color[0], color[1], color[2], out);
    }

    /**
     * converts an RGB color to linear space, the conversion depends on the specified type
     *
     * @param colorx (x component)
     * @param colory (y component)
     * @param colorz (z component)
     */
    @NonNull @Size(min = 3) @LinearColor
    public static float[] toLinear(@NonNull RgbType type, float colorx, float colory, float colorz, @Nullable @Size(min = 3) float[] out) {
        out = Asserts.assertFloat3(out);
        nToLinear(type.toFilamentNative(), colorx, colory, colorz, out);
        return out;
    }

    /** converts an RGBA color to linear space, the conversion depends on the specified type */
    @NonNull @Size(min = 4) @LinearColor
    public static float[] toLinear(@NonNull RgbaType type, @NonNull @Size(min = 4) float[] color, @Nullable @Size(min = 4) float[] out) {
        return toLinear(type, color[0], color[1], color[2], color[3], out);
    }

    /**
     * converts an RGBA color to linear space, the conversion depends on the specified type
     *
     * @param colorx (x component)
     * @param colory (y component)
     * @param colorz (z component)
     * @param colorw (w component)
     */
    @NonNull @Size(min = 4) @LinearColor
    public static float[] toLinear(@NonNull RgbaType type, float colorx, float colory, float colorz, float colorw, @Nullable @Size(min = 4) float[] out) {
        out = Asserts.assertFloat4(out);
        nToLinear(type.toFilamentNative(), colorx, colory, colorz, colorw, out);
        return out;
    }

    /** converts an RGB color in sRGB space to an RGB color in linear space */
    @NonNull @Size(min = 3) @LinearColor
    public static float[] toLinear(@NonNull @Size(min = 3) float[] color, @Nullable @Size(min = 3) float[] out) {
        return toLinear(color[0], color[1], color[2], out);
    }

    /**
     * converts an RGB color in sRGB space to an RGB color in linear space
     *
     * @param colorx (x component)
     * @param colory (y component)
     * @param colorz (z component)
     */
    @NonNull @Size(min = 3) @LinearColor
    public static float[] toLinear(float colorx, float colory, float colorz, @Nullable @Size(min = 3) float[] out) {
        out = Asserts.assertFloat3(out);
        nToLinear(colorx, colory, colorz, out);
        return out;
    }

    /** Converts an RGB color in Rec.709-Linear-D65 ("linear sRGB") space to an RGB color in Rec.709-sRGB-D65 (sRGB) space. */
    @NonNull @Size(min = 3)
    public static float[] toSRGB(@NonNull @Size(min = 3) @LinearColor float[] color, @Nullable @Size(min = 3) float[] out) {
        return toSRGB(color[0], color[1], color[2], out);
    }

    /**
     * Converts an RGB color in Rec.709-Linear-D65 ("linear sRGB") space to an RGB color in Rec.709-sRGB-D65 (sRGB) space.
     *
     * @param colorx (x component)
     * @param colory (y component)
     * @param colorz (z component)
     */
    @NonNull @Size(min = 3)
    public static float[] toSRGB(float colorx, float colory, float colorz, @Nullable @Size(min = 3) float[] out) {
        out = Asserts.assertFloat3(out);
        nToSRGB(colorx, colory, colorz, out);
        return out;
    }

    /**
     * Converts an RGBA color in Rec.709-sRGB-D65 (sRGB) space to an RGBA color in Rec.709-Linear-D65 ("linear sRGB") space the alpha component is left unmodified.
     *
     * @param colorx (x component)
     * @param colory (y component)
     * @param colorz (z component)
     * @param colorw (w component)
     */
    @NonNull @Size(min = 4) @LinearColor
    public static float[] toLinear(float colorx, float colory, float colorz, float colorw, @Nullable @Size(min = 4) float[] out) {
        out = Asserts.assertFloat4(out);
        nToLinear(colorx, colory, colorz, colorw, out);
        return out;
    }

    /**
     * Converts an RGBA color in Rec.709-Linear-D65 ("linear sRGB") space to an RGBA color in Rec.709-sRGB-D65 (sRGB) space the alpha component is left unmodified.
     *
     * @param colorx (x component)
     * @param colory (y component)
     * @param colorz (z component)
     * @param colorw (w component)
     */
    @NonNull @Size(min = 4)
    public static float[] toSRGB(float colorx, float colory, float colorz, float colorw, @Nullable @Size(min = 4) float[] out) {
        out = Asserts.assertFloat4(out);
        nToSRGB(colorx, colory, colorz, colorw, out);
        return out;
    }

    /** Converts a correlated color temperature to a linear RGB color in sRGB space the temperature must be expressed in kelvin and must be in the range 1,000K to 15,000K. */
    @NonNull @Size(min = 3) @LinearColor
    public static float[] cct(float K, @Nullable @Size(min = 3) float[] out) {
        out = Asserts.assertFloat3(out);
        nCct(K, out);
        return out;
    }

    /** Converts a CIE standard illuminant series D to a linear RGB color in sRGB space the temperature must be expressed in kelvin and must be in the range 4,000K to 25,000K */
    @NonNull @Size(min = 3) @LinearColor
    public static float[] illuminantD(float K, @Nullable @Size(min = 3) float[] out) {
        out = Asserts.assertFloat3(out);
        nIlluminantD(K, out);
        return out;
    }

    /**
     * Computes the Beer-Lambert absorption coefficients from the specified transmittance color and distance.
     *
     * <p>The computed absorption will guarantee
     * the white light will become the specified color at the specified distance.
     * The output of this function can be used as the absorption parameter of
     * materials that use refraction.</p>
     *
     * @param color the desired linear RGB color in sRGB space
     * @param distance the distance at which white light should become the specified color
     *
     * @return absorption coefficients for the Beer-Lambert law
     */
    @NonNull @Size(min = 3)
    public static float[] absorptionAtDistance(@NonNull @Size(min = 3) @LinearColor float[] color, float distance, @Nullable @Size(min = 3) float[] out) {
        return absorptionAtDistance(color[0], color[1], color[2], distance, out);
    }

    /**
     * Computes the Beer-Lambert absorption coefficients from the specified transmittance color and distance.
     *
     * <p>The computed absorption will guarantee
     * the white light will become the specified color at the specified distance.
     * The output of this function can be used as the absorption parameter of
     * materials that use refraction.</p>
     *
     * @param colorx (x component) the desired linear RGB color in sRGB space
     * @param colory (y component) the desired linear RGB color in sRGB space
     * @param colorz (z component) the desired linear RGB color in sRGB space
     * @param distance the distance at which white light should become the specified color
     *
     * @return absorption coefficients for the Beer-Lambert law
     */
    @NonNull @Size(min = 3)
    public static float[] absorptionAtDistance(float colorx, float colory, float colorz, float distance, @Nullable @Size(min = 3) float[] out) {
        out = Asserts.assertFloat3(out);
        nAbsorptionAtDistance(colorx, colory, colorz, distance, out);
        return out;
    }


    private static native void nToLinear(int type, float colorx, float colory, float colorz, @Size(min = 3) float[] out);
    private static native void nToLinear(int type, float colorx, float colory, float colorz, float colorw, @Size(min = 4) float[] out);
    private static native void nToLinear(float colorx, float colory, float colorz, @Size(min = 3) float[] out);
    private static native void nToSRGB(float colorx, float colory, float colorz, @Size(min = 3) float[] out);
    private static native void nToLinear(float colorx, float colory, float colorz, float colorw, @Size(min = 4) float[] out);
    private static native void nToSRGB(float colorx, float colory, float colorz, float colorw, @Size(min = 4) float[] out);
    private static native void nCct(float K, @Size(min = 3) float[] out);
    private static native void nIlluminantD(float K, @Size(min = 3) float[] out);
    private static native void nAbsorptionAtDistance(float colorx, float colory, float colorz, float distance, @Size(min = 3) float[] out);
}

// THIS FILE IS GENERATED BY APIGEN AND MUST NOT BE HAND-MODIFIED.
