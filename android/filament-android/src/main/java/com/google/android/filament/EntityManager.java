/*
 * Copyright (C) 2026 The Android Open Source Project
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *      http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

// THIS FILE IS GENERATED BY APIGEN AND MUST NOT BE HAND-MODIFIED.

package com.google.android.filament;

import java.lang.IllegalStateException;

import androidx.annotation.IntRange;
import androidx.annotation.NonNull;
import androidx.annotation.RestrictTo;

/**
 * Thread-safe coordinator managing the lifecycle, indices, and generation tracking for all Entity instances.
 *
 * <p>The EntityManager is the core source-of-truth for Entity identities in Filament. It coordinates the
 * creation, recycling, and safe reclamation of 32-bit Entity IDs. Due to the highly multithreaded nature of the
 * engine, the EntityManager provides <b>two distinct groups of APIs</b>:</p>
 *
 * <h2></h2>
 *
 * <h3>1. Public API (Gameplay & Application Layer)</h3>
 *
 * <p>This group is aimed at standard usage for applications, renderers, and game systems.</p>
 *
 * <ul>
 *   <li><b>Entity Creation</b>: Methods like {@code create}() and {@code create}(size_t, Entity*) allocate new or recycled Entity
 * identities.</li>
 *   <li><b>Entity Destruction</b>: Methods like {@code destroy}(Entity) and {@code destroy}(size_t, Entity*) logically kill entities,
 * instantly advancing the active timeline.</li>
 *   <li><b>Queries & Hooks</b>: Mechanisms to query logical presence ({@code isAlive}()) or subscribe to global destruction
 * notifications via {@code registerChangeCallback}().</li>
 * </ul>
 *
 * <h2></h2>
 *
 * <h3>2. Internal Component API (ECS Integration & Component Managers)</h3>
 *
 * <p>This group supports the underlying asynchronous <b>Epoch-Based Reclamation (EBR)</b> garbage collector. Designed
 * exclusively for subclasses of {@code SingleInstanceComponentManagerBase} (e.g. {@code FCameraManager}, {@code FTransformManager}),
 * these methods govern thread-consensus and physical payload purging budgets:</p>
 *
 * <ul>
 *   <li><b>Consensus Registration</b>: Methods like {@code registerWatermark}() and {@code unregisterWatermark}() allow components to
 * join or vacate the global timeline consensus.</li>
 *   <li><b>Timeline Sweeps</b>: Methods like {@code advanceEpoch}() and {@code reclaimSafeEpochs}() seal completed frames and process
 * physical reclaiming boundaries.</li>
 *   <li><b>Missed Garbage Harvests</b>: APIs like {@code getMissedGarbage}() enable Component Managers to retrieve logically dead
 * Entity bitsets precisely synced against their local watermarks in atomic, collision-free transaction blocks.</li>
 * </ul>
 */
public class EntityManager {
    private long mNativeObject;

    EntityManager(long nativeObject) {
        mNativeObject = nativeObject;
    }

    @NonNull
    @RestrictTo(RestrictTo.Scope.LIBRARY_GROUP)
    public static EntityManager wrap(long nativeObject) {
        return new EntityManager(nativeObject);
    }

    /**
     * Gets a reference to the global EntityManager singleton.
     *
     * <p>It is recommended to cache this reference locally to bypass lookups.
     * @return Reference to the thread-safe global EntityManager.</p>
     */
    public static EntityManager get() {
        long nativeEntityManager = nGet();
        return nativeEntityManager == 0 ? null : new EntityManager(nativeEntityManager);
    }

    /**
     * Flushes all pending entity lifecycle change notifications to registered callbacks.
     *
     * <p>Invoked automatically upon internal queue thresholds, or manually per frame.</p>
     */
    public void flushNotifications() {
        nFlushNotifications(getNativeObject());
    }

    /**
     * Retrieves the maximum theoretical upper bound of entities that can exist concurrently.
     *
     * <p>Factored around reserved index slots and 2^GENERATION_SHIFT limits.</p>
     *
     * @return The maximum available 32-bit Entity identity limit.
     */
    @IntRange(from = 0)
    public static int getMaxEntityCount() {
        return nGetMaxEntityCount();
    }

    /**
     * Returns the total number of currently active/alive Entities.
     *
     * @return Count of logical entities currently populated in the system.
     */
    @IntRange(from = 0)
    public int getEntityCount() {
        return nGetEntityCount(getNativeObject());
    }

    /**
     * Allocates and creates a batch of new or recycled Entities.
     *
     * @param entities Output array receiving the populated Entity IDs.
     */
    public void create(@NonNull @Entity int[] entities) {
        nCreate(getNativeObject(), entities, entities.length);
    }

    /**
     * Globally and logically destroys a batch of Entities.
     *
     * @param entities Contiguous array of Entities to logically kill.
     */
    public void destroy(@NonNull @Entity int[] entities) {
        nDestroy(getNativeObject(), entities, entities.length);
    }

    /**
     * Allocates and creates a single new or recycled Entity.
     *
     * @return The populated Entity ID, or Entity.isNull() upon allocation failure.
     */
    @Entity
    public int create() {
        return nCreate(getNativeObject());
    }

    /**
     * Globally and logically destroys a single Entity identity.
     *
     * @param e The Entity to logically kill.
     */
    public void destroy(@Entity int e) {
        nDestroy(getNativeObject(), e);
    }

    /**
     * Queries the logical lifecycle state of a given Entity.
     *
     * @param e The Entity to test for presence.
     *
     * @return True if the Entity is active and logically alive, false if dead/destroyed.
     */
    public boolean isAlive(@Entity int e) {
        return nIsAlive(getNativeObject(), e);
    }

    /**
     * Advances the timeline to the next epoch.
     *
     * <p>Increments the current epoch ID and seals the current epoch. All subsequent
     * entity destructions will be recorded in the new epoch. Automatically triggers
     * reclaimSafeEpochs() to recycle safe, completed epochs.
     * Thread-safe.</p>
     */
    public void advanceEpoch() {
        nAdvanceEpoch(getNativeObject());
    }

    /**
     * Reclaims and recycles all indices from epochs that are safe to reclaim.
     *
     * <p>This method is intended EXCLUSIVELY for testing, unit test harnesses, and micro-benchmarks
     * to trigger harvesting sweeps in isolation. In production, Filament NEVER needs to call this method,
     * as advanceEpoch() automatically and synchronously invokes the identical recycling sweep.</p>
     */
    public void reclaimSafeEpochs() {
        nReclaimSafeEpochs(getNativeObject());
    }

    /**
     * Returns the current active (unsealed) epoch ID. Thread-safe.
     *
     * @return The current epoch ID.
     */
    @IntRange(from = 0)
    public long getLatestEpochID() {
        return nGetLatestEpochID(getNativeObject());
    }

    /**
     * Use carefully, several entities can have the same index.
     *
     * @param e The entity to query.
     *
     * @return The entity index.
     */
    @IntRange(from = 0)
    public static int getIndex(@Entity int e) {
        return nGetIndex(e);
    }

    public long getNativeObject() {
        if (mNativeObject == 0) {
            throw new IllegalStateException("Calling method on destroyed EntityManager");
        }
        return mNativeObject;
    }

    void clearNativeObject() {
        mNativeObject = 0;
    }

    private static native long nGet();
    private static native void nFlushNotifications(long nativeEntityManager);
    @IntRange(from = 0)
    private static native int nGetMaxEntityCount();
    @IntRange(from = 0)
    private static native int nGetEntityCount(long nativeEntityManager);
    private static native void nCreate(long nativeEntityManager, @NonNull @Entity int[] entities, int entitiesCount);
    private static native void nDestroy(long nativeEntityManager, @NonNull @Entity int[] entities, int entitiesCount);
    @Entity
    private static native int nCreate(long nativeEntityManager);
    private static native void nDestroy(long nativeEntityManager, @Entity int e);
    private static native boolean nIsAlive(long nativeEntityManager, @Entity int e);
    private static native void nAdvanceEpoch(long nativeEntityManager);
    private static native void nReclaimSafeEpochs(long nativeEntityManager);
    @IntRange(from = 0)
    private static native long nGetLatestEpochID(long nativeEntityManager);
    @IntRange(from = 0)
    private static native int nGetIndex(@Entity int e);
}

// THIS FILE IS GENERATED BY APIGEN AND MUST NOT BE HAND-MODIFIED.
