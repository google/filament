/*
 * Copyright (C) 2026 The Android Open Source Project
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *      http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

// THIS FILE IS GENERATED BY APIGEN AND MUST NOT BE HAND-MODIFIED.

package com.google.android.filament;

import java.lang.IllegalStateException;

import androidx.annotation.IntRange;
import androidx.annotation.NonNull;
import androidx.annotation.Nullable;

import com.google.android.filament.proguard.UsedByNative;

/**
 * Engine is filament's main entry-point.
 *
 * <p>An Engine instance main function is to keep track of all resources created by the user and
 * manage the rendering thread as well as the hardware renderer.</p>
 *
 * <p>To use filament, an Engine instance must be created first:</p>
 *
 * <pre>{@code
 * #include <filament/Engine.h>
 * using namespace filament;
 *
 * Engine* engine = Engine::create();
 *
 * }</pre>
 *
 * <p>Engine essentially represents (or is associated to) a hardware context
 * (e.g. an OpenGL ES context).</p>
 *
 * <p>Rendering typically happens in an operating system's window (which can be full screen), such
 * window is managed by a filament.Renderer.</p>
 *
 * <p>A typical filament render loop looks like this:</p>
 *
 * <pre>{@code
 * #include <filament/Engine.h>
 * #include <filament/Renderer.h>
 * #include <filament/Scene.h>
 * #include <filament/View.h>
 * using namespace filament;
 *
 * Engine* engine       = Engine::create();
 * SwapChain* swapChain = engine->createSwapChain(nativeWindow);
 * Renderer* renderer   = engine->createRenderer();
 * Scene* scene         = engine->createScene();
 * View* view           = engine->createView();
 *
 * view->setScene(scene);
 *
 * do {
 *     // typically we wait for VSYNC and user input events
 *     if (renderer->beginFrame(swapChain)) {
 *         renderer->render(view);
 *         renderer->endFrame();
 *     }
 * } while (!quit);
 *
 * engine->destroy(view);
 * engine->destroy(scene);
 * engine->destroy(renderer);
 * engine->destroy(swapChain);
 * Engine::destroy(&engine); // clears engine*
 *
 * }</pre>
 *
 * <h1>Resource Tracking</h1>
 *
 * <p>Each Engine instance keeps track of all objects created by the user, such as vertex and index
 *  buffers, lights, cameras, etc...
 *  The user is expected to free those resources, however, leaked resources are freed when the
 *  engine instance is destroyed and a warning is emitted in the console.</p>
 *
 * <h1>Thread safety</h1>
 *
 * <p>An Engine instance is not thread-safe. The implementation makes no attempt to synchronize
 * calls to an Engine instance methods.
 * If multi-threading is needed, synchronization must be external.</p>
 *
 * <h1>Multi-threading</h1>
 *
 * <p>When created, the Engine instance starts a render thread as well as multiple worker threads,
 * these threads have an elevated priority appropriate for rendering, based on the platform's
 * best practices. The number of worker threads depends on the platform and is automatically
 * chosen for best performance.</p>
 *
 * <p>On platforms with asymmetric cores (e.g. ARM's Big.Little), Engine makes some educated guesses
 * as to which cores to use for the render thread and worker threads. For example, it'll try to
 * keep an OpenGL ES thread on a Big core.</p>
 *
 * <h1>Swap Chains</h1>
 *
 * <p>A swap chain represents an Operating System's <i>native</i> renderable surface. Typically it's a window
 * or a view. Because a SwapChain is initialized from a native object, it is given to filament
 * as a {@code void*}, which must be of the proper type for each platform filament is running on.</p>
 *
 * @see SwapChain
 * @see Renderer
 */
@UsedByNative
public class Engine {
    static final class EnumCache {
        static final Backend[] sBackendValues = Backend.values();
        static final FeatureLevel[] sFeatureLevelValues = FeatureLevel.values();
    }

    private long mNativeObject;
    private @Nullable Config mConfig;
    private @Nullable EntityManager mEntityManager;
    private @Nullable RenderableManager mRenderableManager;
    private @Nullable LightManager mLightManager;
    private @Nullable TransformManager mTransformManager;

    /** Selects which driver a particular Engine should use. */
    public enum Backend {
        /** Automatically selects an appropriate driver for the platform. */
        DEFAULT,
        /** Selects the OpenGL/ES driver (default on Android) */
        OPENGL,
        /** Selects the Vulkan driver if the platform supports it (default on Linux/Windows) */
        VULKAN,
        /** Selects the Metal driver if the platform supports it (default on MacOS/iOS). */
        METAL,
        /** Selects the Webgpu driver if the platform supports webgpu. */
        WEBGPU,
        /** Selects the no-op driver for testing purposes. */
        NOOP;

        /** @return the value of this enum constant as used by the native Filament engine. */
        public int toFilamentNative() { return ordinal(); }
    }

    /** Defines the backend's feature levels. */
    public enum FeatureLevel {
        /** OpenGL ES 2.0 features */
        FEATURE_LEVEL_0,
        /** OpenGL ES 3.0 features (default) */
        FEATURE_LEVEL_1,
        /** OpenGL ES 3.1 features + 16 textures units + cubemap arrays */
        FEATURE_LEVEL_2,
        /** OpenGL ES 3.1 features + 31 textures units + cubemap arrays */
        FEATURE_LEVEL_3;

        /** @return the value of this enum constant as used by the native Filament engine. */
        public int toFilamentNative() { return ordinal(); }
    }

    public enum StereoscopicType {
        /** No stereoscopic rendering */
        NONE,
        /** Stereoscopic rendering is performed using instanced rendering technique. */
        INSTANCED,
        /** Stereoscopic rendering is performed using the multiview feature from the graphics backend. */
        MULTIVIEW;

        /** @return the value of this enum constant as used by the native Filament engine. */
        public int toFilamentNative() { return ordinal(); }
    }

    /** This controls the priority level for GPU work scheduling, which helps prioritize the submitted GPU work and enables preemption. */
    public enum GpuContextPriority {
        /** Backend default GPU context priority (typically MEDIUM) */
        DEFAULT,
        /**
         * For non-interactive, deferrable workloads.
         *
         * <p>This should not interfere with standard
         * applications.</p>
         */
        LOW,
        /** The default priority level for standard applications. */
        MEDIUM,
        /** For high-priority, latency-sensitive workloads that are more important than standard applications. */
        HIGH,
        /** The highest priority, intended for system-critical, real-time applications where missing deadlines is unacceptable (e.g., VR/AR compositors or other system-critical tasks). */
        REALTIME;

        /** @return the value of this enum constant as used by the native Filament engine. */
        public int toFilamentNative() { return ordinal(); }
    }

    public enum AsynchronousMode {
        /**
         * Asynchronous operations are disabled.
         *
         * <p>This is the default.</p>
         */
        NONE,
        /**
         * Attempts to use a dedicated worker thread for asynchronous tasks.
         *
         * <p>If threading is not
         * supported by the platform, it automatically falls back to using an amortization strategy.</p>
         */
        THREAD_PREFERRED,
        /** Uses an amortization strategy, processing a small number of asynchronous tasks during each engine update cycle. */
        AMORTIZATION;

        /** @return the value of this enum constant as used by the native Filament engine. */
        public int toFilamentNative() { return ordinal(); }
    }

    /** Three-state feature state. */
    public enum FeatureState {
        DISABLED,
        ENABLED,
        INDETERMINATE;

        /** @return the value of this enum constant as used by the native Filament engine. */
        public int toFilamentNative() { return ordinal(); }
    }

    private Engine(long nativeEngine, @Nullable Config config) {
        mNativeObject = nativeEngine;
        mConfig = config;
    }

    @NonNull
    public static Engine create() {
        return new Builder().build();
    }

    @NonNull
    public static Engine create(@NonNull Backend backend) {
        return new Builder()
            .backend(backend)
            .build();
    }

    @NonNull
    public static Engine create(@NonNull Object sharedContext) {
        return new Builder()
            .sharedContext(sharedContext)
            .build();
    }

    public void destroy() {
        nDestroyEngine(getNativeObject());
        clearNativeObject();
    }

    @NonNull
    public SwapChain createSwapChain(@NonNull Object surface) {
        return createSwapChain(surface, SwapChainFlags.CONFIG_DEFAULT);
    }

    @NonNull
    public SwapChain createSwapChain(@NonNull Object surface, long flags) {
        if (Platform.get().validateSurface(surface)) {
            long nativeSwapChain = nCreateSwapChain(getNativeObject(), surface, flags);
            if (nativeSwapChain == 0) throw new IllegalStateException("Couldn't create SwapChain");
            return new SwapChain(nativeSwapChain, surface);
        }
        throw new IllegalArgumentException("Invalid surface " + surface);
    }

    @NonNull
    public SwapChain createSwapChain(int width, int height, long flags) {
        if (width >= 0 && height >= 0) {
            long nativeSwapChain =
                nCreateSwapChainHeadless(getNativeObject(), width, height, flags);
            if (nativeSwapChain == 0) throw new IllegalStateException("Couldn't create SwapChain");
            return new SwapChain(nativeSwapChain, null);
        }
        throw new IllegalArgumentException("Invalid parameters");
    }

    @NonNull
    public SwapChain createSwapChain(int width, int height) {
        return createSwapChain(width, height, SwapChainFlags.CONFIG_DEFAULT);
    }

    @NonNull
    public SwapChain createSwapChainFromNativeSurface(@NonNull NativeSurface surface, long flags) {
        long nativeSwapChain =
                nCreateSwapChainFromRawPointer(getNativeObject(), surface.getNativeObject(), flags);
        if (nativeSwapChain == 0) throw new IllegalStateException("Couldn't create SwapChain");
        return new SwapChain(nativeSwapChain, surface);
    }


    public void destroySwapChain(@NonNull SwapChain swapChain) {
        assertDestroy(nDestroySwapChain(getNativeObject(), swapChain.getNativeObject()));
        swapChain.clearNativeObject();
    }

    public void destroyView(@NonNull View view) {
        assertDestroy(nDestroyView(getNativeObject(), view.getNativeObject()));
        view.clearNativeObject();
    }

    public void destroyRenderer(@NonNull Renderer renderer) {
        assertDestroy(nDestroyRenderer(getNativeObject(), renderer.getNativeObject()));
        renderer.clearNativeObject();
    }

    public void destroyScene(@NonNull Scene scene) {
        assertDestroy(nDestroyScene(getNativeObject(), scene.getNativeObject()));
        scene.clearNativeObject();
    }

    public void destroyStream(@NonNull Stream stream) {
        assertDestroy(nDestroyStream(getNativeObject(), stream.getNativeObject()));
        stream.clearNativeObject();
    }

    public void destroyFence(@NonNull Fence fence) {
        assertDestroy(nDestroyFence(getNativeObject(), fence.getNativeObject()));
        fence.clearNativeObject();
    }

    public void destroyFramePacer(@NonNull FramePacer framePacer) {
        assertDestroy(nDestroyFramePacer(getNativeObject(), framePacer.getNativeObject()));
        framePacer.clearNativeObject();
    }

    public void destroyBufferObject(@NonNull BufferObject bufferObject) {
        assertDestroy(nDestroyBufferObject(getNativeObject(), bufferObject.getNativeObject()));
        bufferObject.clearNativeObject();
    }

    public void destroyIndexBuffer(@NonNull IndexBuffer indexBuffer) {
        assertDestroy(nDestroyIndexBuffer(getNativeObject(), indexBuffer.getNativeObject()));
        indexBuffer.clearNativeObject();
    }

    public void destroyInstanceBuffer(@NonNull InstanceBuffer instanceBuffer) {
        assertDestroy(nDestroyInstanceBuffer(getNativeObject(), instanceBuffer.getNativeObject()));
        instanceBuffer.clearNativeObject();
    }

    public void destroyVertexBuffer(@NonNull VertexBuffer vertexBuffer) {
        assertDestroy(nDestroyVertexBuffer(getNativeObject(), vertexBuffer.getNativeObject()));
        vertexBuffer.clearNativeObject();
    }

    public void destroySkinningBuffer(@NonNull SkinningBuffer skinningBuffer) {
        assertDestroy(nDestroySkinningBuffer(getNativeObject(), skinningBuffer.getNativeObject()));
        skinningBuffer.clearNativeObject();
    }

    public void destroyMorphTargetBuffer(@NonNull MorphTargetBuffer morphTargetBuffer) {
        assertDestroy(nDestroyMorphTargetBuffer(getNativeObject(), morphTargetBuffer.getNativeObject()));
        morphTargetBuffer.clearNativeObject();
    }

    public void destroyIndirectLight(@NonNull IndirectLight ibl) {
        assertDestroy(nDestroyIndirectLight(getNativeObject(), ibl.getNativeObject()));
        ibl.clearNativeObject();
    }

    public void destroyMaterial(@NonNull Material material) {
        assertDestroy(nDestroyMaterial(getNativeObject(), material.getNativeObject()));
        material.clearNativeObject();
    }

    public void destroyMaterialInstance(@NonNull MaterialInstance materialInstance) {
        assertDestroy(nDestroyMaterialInstance(getNativeObject(), materialInstance.getNativeObject()));
        materialInstance.clearNativeObject();
    }

    public void destroySkybox(@NonNull Skybox skybox) {
        assertDestroy(nDestroySkybox(getNativeObject(), skybox.getNativeObject()));
        skybox.clearNativeObject();
    }

    public void destroyColorGrading(@NonNull ColorGrading colorGrading) {
        assertDestroy(nDestroyColorGrading(getNativeObject(), colorGrading.getNativeObject()));
        colorGrading.clearNativeObject();
    }

    public void destroyTexture(@NonNull Texture texture) {
        assertDestroy(nDestroyTexture(getNativeObject(), texture.getNativeObject()));
        texture.clearNativeObject();
    }

    public void destroyRenderTarget(@NonNull RenderTarget target) {
        assertDestroy(nDestroyRenderTarget(getNativeObject(), target.getNativeObject()));
        target.clearNativeObject();
    }

    public void destroyEntity(@Entity int entity) {
        nDestroyEntity(getNativeObject(), entity);
    }

    public void destroy(@NonNull SwapChain swapChain) { destroySwapChain(swapChain); }
    public void destroy(@NonNull View view) { destroyView(view); }
    public void destroy(@NonNull Renderer renderer) { destroyRenderer(renderer); }
    public void destroy(@NonNull Scene scene) { destroyScene(scene); }
    public void destroy(@NonNull Stream stream) { destroyStream(stream); }
    public void destroy(@NonNull Fence fence) { destroyFence(fence); }
    public void destroy(@NonNull FramePacer framePacer) { destroyFramePacer(framePacer); }
    public void destroy(@NonNull BufferObject bufferObject) { destroyBufferObject(bufferObject); }
    public void destroy(@NonNull IndexBuffer indexBuffer) { destroyIndexBuffer(indexBuffer); }
    public void destroy(@NonNull InstanceBuffer instanceBuffer) { destroyInstanceBuffer(instanceBuffer); }
    public void destroy(@NonNull VertexBuffer vertexBuffer) { destroyVertexBuffer(vertexBuffer); }
    public void destroy(@NonNull SkinningBuffer skinningBuffer) { destroySkinningBuffer(skinningBuffer); }
    public void destroy(@NonNull MorphTargetBuffer morphTargetBuffer) { destroyMorphTargetBuffer(morphTargetBuffer); }
    public void destroy(@NonNull IndirectLight ibl) { destroyIndirectLight(ibl); }
    public void destroy(@NonNull Material material) { destroyMaterial(material); }
    public void destroy(@NonNull MaterialInstance materialInstance) { destroyMaterialInstance(materialInstance); }
    public void destroy(@NonNull Skybox skybox) { destroySkybox(skybox); }
    public void destroy(@NonNull ColorGrading colorGrading) { destroyColorGrading(colorGrading); }
    public void destroy(@NonNull Texture texture) { destroyTexture(texture); }
    public void destroy(@NonNull RenderTarget target) { destroyRenderTarget(target); }
    public void destroy(@Entity int entity) { destroyEntity(entity); }

    private static void assertDestroy(boolean success) {
        if (!success) {
            throw new IllegalStateException("Object could not be destroyed (or already destroyed)");
        }
    }

    /**
     * Use <code>Builder</code> to construct an <code>Engine</code> object instance.
     */
    public static class Builder {
        @SuppressWarnings({"FieldCanBeLocal", "UnusedDeclaration"})
        private final BuilderFinalizer mFinalizer;
        private final long mNativeBuilder;
        private Config mConfig;

        public Builder() {
            mNativeBuilder = nCreateBuilder();
            mFinalizer = new BuilderFinalizer(mNativeBuilder);
        }

        /**
         * Sets the {@link Backend} for the Engine.
         *
         * @param backend Driver backend to use
         * @return A reference to this Builder for chaining calls.
         */
        public Builder backend(Backend backend) {
            nSetBuilderBackend(mNativeBuilder, backend.ordinal());
            return this;
        }

        /**
         * Sets a sharedContext for the Engine.
         *
         * @param sharedContext  A platform-dependant OpenGL context used as a shared context
         *                       when creating filament's internal context. On Android this parameter
         *                       <b>must be</b> an instance of {@link android.opengl.EGLContext}.
         * @return A reference to this Builder for chaining calls.
         */
        public Builder sharedContext(Object sharedContext) {
            if (Platform.get().validateSharedContext(sharedContext)) {
                nSetBuilderSharedContext(mNativeBuilder,
                        Platform.get().getSharedContextNativeHandle(sharedContext));
                return this;
            }
            throw new IllegalArgumentException("Invalid shared context " + sharedContext);
        }

        /**
         * Configure the Engine with custom parameters.
         *
         * @param config A {@link Config} object
         * @return A reference to this Builder for chaining calls.
         */
        public Builder config(Config config) {
            mConfig = config;
            nSetBuilderConfig(mNativeBuilder, config.commandBufferSizeMB,
                    config.perRenderPassArenaSizeMB, config.driverHandleArenaSizeMB,
                    config.minCommandBufferSizeMB, config.perFrameCommandsSizeMB,
                    config.jobSystemThreadCount, config.disableParallelShaderCompile,
                    config.stereoscopicType.ordinal(), config.stereoscopicEyeCount,
                    config.resourceAllocatorCacheSizeMB, config.resourceAllocatorCacheMaxAge,
                    config.disableHandleUseAfterFreeCheck,
                    config.preferredShaderLanguage.ordinal(),
                    config.forceGLES2Context, config.assertNativeWindowIsValid,
                    config.gpuContextPriority.ordinal(),
                    config.sharedUboInitialSizeInBytes,
                    config.enableMultipleDirectionalLights);
            return this;
        }

        /**
         * Sets the initial featureLevel for the Engine.
         *
         * @param featureLevel The feature level at which initialize Filament.
         * @return A reference to this Builder for chaining calls.
         */
        public Builder featureLevel(FeatureLevel featureLevel) {
            nSetBuilderFeatureLevel(mNativeBuilder, featureLevel.ordinal());
            return this;
        }

        /**
         * Sets the initial paused state of the rendering thread.
         *
         * @param paused Whether to start the rendering thread paused.
         * @return A reference to this Builder for chaining calls.
         */
        public Builder paused(boolean paused) {
            nSetBuilderPaused(mNativeBuilder, paused);
            return this;
        }

        /**
         * Set a feature flag value. This is the only way to set constant feature flags.
         * @param name feature name
         * @param value true to enable, false to disable
         * @return A reference to this Builder for chaining calls.
         */
        public Builder feature(@NonNull String name, boolean value) {
            nSetBuilderFeature(mNativeBuilder, name, value);
            return this;
        }

        /**
         * Sets the builder used to create the default ColorGrading object.
         * @param colorGrading Builder used to create the default color grading.
         * @return A reference to this Builder for chaining calls.
         */
        @NonNull
        public Builder colorGrading(@NonNull ColorGrading.Builder colorGrading) {
            nSetBuilderColorGrading(mNativeBuilder, colorGrading.getNativeBuilder());
            return this;
        }

        /**
         * Creates an instance of Engine
         *
         * @return A newly created <code>Engine</code>, or <code>null</code> if the GPU driver couldn't
         *         be initialized, for instance if it doesn't support the right version of OpenGL or
         *         OpenGL ES.
         *
         * @throws Error if there isn't enough memory to allocate the command buffer.
         */
        public Engine build() {
            long nativeEngine = nBuilderBuild(mNativeBuilder);
            if (nativeEngine == 0) throw new IllegalStateException("Couldn't create Engine");
            return new Engine(nativeEngine, mConfig);
        }

        private static class BuilderFinalizer {
            private final long mNativeObject;

            BuilderFinalizer(long nativeObject) {
                mNativeObject = nativeObject;
            }

            @Override
            public void finalize() {
                try {
                    super.finalize();
                } catch (Throwable t) { // Ignore
                } finally {
                    nDestroyBuilder(mNativeObject);
                }
            }
        }
    }

    /**
     * Query the feature level supported by the selected backend.
     *
     * <p>A specific feature level needs to be set before the corresponding features can be used.</p>
     *
     * @return FeatureLevel supported the selected backend.
     *
     * @see #setActiveFeatureLevel
     */
    public FeatureLevel getSupportedFeatureLevel() {
        return EnumCache.sFeatureLevelValues[nGetSupportedFeatureLevel(getNativeObject())];
    }

    /**
     * Activate all features of a given feature level.
     *
     * <p>If an explicit feature level is not specified
     * at Engine initialization time via Builder::featureLevel, the default feature level is
     * FeatureLevel::FEATURE_LEVEL_0 on devices not compatible with GLES 3.0; otherwise, the default
     * is FeatureLevel::FEATURE_LEVEL_1. The selected feature level must not be higher than the
     * value returned by getActiveFeatureLevel() and it's not possible lower the active feature
     * level. Additionally, it is not possible to modify the feature level at all if the Engine was
     * initialized at FeatureLevel::FEATURE_LEVEL_0.</p>
     *
     * @param featureLevel the feature level to activate. If featureLevel is lower than
     *                     getActiveFeatureLevel(), the current (higher) feature level is kept. If
     *                     featureLevel is higher than getSupportedFeatureLevel(), or if the engine
     *                     was initialized at feature level 0, an exception is thrown, or the
     *                     program is terminated if exceptions are disabled.
     *
     * @return the active feature level.
     *
     * @see Builder#featureLevel
     * @see #getSupportedFeatureLevel
     * @see #getActiveFeatureLevel
     */
    public FeatureLevel setActiveFeatureLevel(@NonNull FeatureLevel featureLevel) {
        return EnumCache.sFeatureLevelValues[nSetActiveFeatureLevel(getNativeObject(), featureLevel.toFilamentNative())];
    }

    /**
     * Returns the currently active feature level.
     *
     * @return currently active feature level
     *
     * @see #getSupportedFeatureLevel
     * @see #setActiveFeatureLevel
     */
    public FeatureLevel getActiveFeatureLevel() {
        return EnumCache.sFeatureLevelValues[nGetActiveFeatureLevel(getNativeObject())];
    }

    /**
     * Queries the maximum number of GPU instances that Filament creates when automatic instancing is enabled.
     *
     * <p>This value is also the limit for the number of transforms that can be stored in
     * an InstanceBuffer. This value may depend on the device and platform, but will remain constant
     * during the lifetime of this Engine.</p>
     *
     * <p>This value does not apply when using the instances(size_t) method on
     * RenderableManager::Builder.</p>
     *
     * @return the number of max automatic instances
     *
     * @see #setAutomaticInstancingEnabled
     * @see RenderableManager.Builder#instances(int)
     * @see RenderableManager.Builder#instances(int, InstanceBuffer)
     */
    @IntRange(from = 0)
    public int getMaxAutomaticInstances() {
        return nGetMaxAutomaticInstances(getNativeObject());
    }

    /**
     * Queries the device and platform for support of the given stereoscopic type.
     *
     * @return true if the given stereo rendering is supported, false otherwise
     *
     * @see View#setStereoscopicOptions
     */
    public boolean isStereoSupported(@NonNull StereoscopicType stereoscopicType) {
        return nIsStereoSupported(getNativeObject(), stereoscopicType.toFilamentNative());
    }

    /**
     * Checks if the engine is set up for asynchronous operation.
     *
     * <p>If it returns true, the
     * asynchronous versions of the APIs are available for use.</p>
     *
     * @return true if the engine supports asynchronous operation.
     */
    public boolean isAsynchronousModeEnabled() {
        return nIsAsynchronousModeEnabled(getNativeObject());
    }

    /**
     * Returns whether the engine has encountered an unrecoverable failure.
     *
     * <p>If this returns true, the engine is in an unrecoverable state and further calls to
     * rendering methods will fail or be ignored. Apps can use this to check for fatal
     * errors instead of relying on exceptions.</p>
     *
     * @return true if an unrecoverable failure has occurred, false otherwise.
     */
    public boolean hasUnrecoverableFailure() {
        return nHasUnrecoverableFailure(getNativeObject());
    }

    /**
     * Retrieves the configuration settings of this Engine.
     *
     * <p>This method returns the configuration object that was supplied to the Engine's
     * Builder::config method during the creation of this Engine. If the Builder::config method was
     * not explicitly called (or called with nullptr), this method returns the default configuration
     * settings.</p>
     *
     * @return a Config object with this Engine's configuration
     *
     * @see Builder#config
     */
    @NonNull
    public Config getConfig() {
        if (mConfig == null) {
            mConfig = new Config();
        }
        return mConfig;
    }

    /**
     * Returns the maximum number of stereoscopic eyes supported by Filament.
     *
     * <p>The actual number of
     * eyes rendered is set at Engine creation time with the Engine::Config::stereoscopicEyeCount
     * setting.</p>
     *
     * @return the max number of stereoscopic eyes supported
     *
     * @see Engine.Config#stereoscopicEyeCount
     */
    @IntRange(from = 0)
    public static int getMaxStereoscopicEyes() {
        return nGetMaxStereoscopicEyes();
    }

    /** @return EntityManager used by filament */
    @NonNull
    public EntityManager getEntityManager() {
        if (mEntityManager == null) {
            long nativeEntityManager = nGetEntityManager(getNativeObject());
            if (nativeEntityManager == 0) throw new IllegalStateException("Couldn't get EntityManager");
            mEntityManager = new EntityManager(nativeEntityManager);
        }
        return mEntityManager;
    }

    /** @return RenderableManager reference */
    @NonNull
    public RenderableManager getRenderableManager() {
        if (mRenderableManager == null) {
            long nativeRenderableManager = nGetRenderableManager(getNativeObject());
            if (nativeRenderableManager == 0) throw new IllegalStateException("Couldn't get RenderableManager");
            mRenderableManager = new RenderableManager(nativeRenderableManager);
        }
        return mRenderableManager;
    }

    /** @return LightManager reference */
    @NonNull
    public LightManager getLightManager() {
        if (mLightManager == null) {
            long nativeLightManager = nGetLightManager(getNativeObject());
            if (nativeLightManager == 0) throw new IllegalStateException("Couldn't get LightManager");
            mLightManager = new LightManager(nativeLightManager);
        }
        return mLightManager;
    }

    /** @return TransformManager reference */
    @NonNull
    public TransformManager getTransformManager() {
        if (mTransformManager == null) {
            long nativeTransformManager = nGetTransformManager(getNativeObject());
            if (nativeTransformManager == 0) throw new IllegalStateException("Couldn't get TransformManager");
            mTransformManager = new TransformManager(nativeTransformManager);
        }
        return mTransformManager;
    }

    /**
     * Helper to enable accurate translations.
     *
     * <p>If you need this Engine to handle a very large world space, one way to achieve this
     * automatically is to enable accurate translations in the TransformManager. This helper
     * provides a convenient way of doing that.
     * This is typically called once just after creating the Engine.</p>
     */
    public void enableAccurateTranslations() {
        nEnableAccurateTranslations(getNativeObject());
    }

    /**
     * Enables or disables automatic instancing of render primitives.
     *
     * <p>Instancing of render
     * primitives can greatly reduce CPU overhead but requires the instanced primitives to be
     * identical (i.e. use the same geometry) and use the same MaterialInstance. If it is known
     * that the scene doesn't contain any identical primitives, automatic instancing can have some
     * overhead and it is then best to disable it.</p>
     *
     * <p>Disabled by default.</p>
     *
     * @param enable true to enable, false to disable automatic instancing.
     *
     * @see RenderableManager
     * @see MaterialInstance
     */
    public void setAutomaticInstancingEnabled(boolean enable) {
        nSetAutomaticInstancingEnabled(getNativeObject(), enable);
    }

    /**
     * @return true if automatic instancing is enabled, false otherwise.
     *
     * @see #setAutomaticInstancingEnabled
     */
    public boolean isAutomaticInstancingEnabled() {
        return nIsAutomaticInstancingEnabled(getNativeObject());
    }

    /**
     * Creates a renderer associated to this engine.
     *
     * <p>A Renderer is intended to map to a <i>window</i> on screen.</p>
     *
     * @return A pointer to the newly created Renderer.
     */
    public Renderer createRenderer() {
        long nativeRenderer = nCreateRenderer(getNativeObject());
        if (nativeRenderer == 0) throw new IllegalStateException("Couldn't create Renderer");
        return new Renderer(nativeRenderer, this);
    }

    /**
     * Creates a View.
     *
     * @return A pointer to the newly created View.
     */
    public View createView() {
        long nativeView = nCreateView(getNativeObject());
        if (nativeView == 0) throw new IllegalStateException("Couldn't create View");
        return new View(nativeView);
    }

    /**
     * Creates a Scene.
     *
     * @return A pointer to the newly created Scene.
     */
    public Scene createScene() {
        long nativeScene = nCreateScene(getNativeObject());
        if (nativeScene == 0) throw new IllegalStateException("Couldn't create Scene");
        return new Scene(nativeScene);
    }

    /**
     * Creates a Camera component.
     *
     * @param entity Entity to add the camera component to.
     *
     * @return A pointer to the newly created Camera.
     */
    public Camera createCamera(@Entity int entity) {
        long nativeCamera = nCreateCamera(getNativeObject(), entity);
        if (nativeCamera == 0) throw new IllegalStateException("Couldn't create Camera");
        return new Camera(nativeCamera);
    }

    /**
     * Returns the Camera component of the given entity.
     *
     * @param entity An entity.
     *
     * @return A pointer to the Camera component for this entity or nullptr if the entity didn't
     *         have a Camera component. The pointer is valid until destroyCameraComponent()
     *         is called or the entity itself is destroyed.
     */
    public Camera getCameraComponent(@Entity int entity) {
        long nativeCamera = nGetCameraComponent(getNativeObject(), entity);
        return nativeCamera == 0 ? null : new Camera(nativeCamera);
    }

    /**
     * Destroys the Camera component associated with the given entity.
     *
     * @param entity An entity.
     */
    public void destroyCameraComponent(@Entity int entity) {
        nDestroyCameraComponent(getNativeObject(), entity);
    }

    /**
     * Creates a Fence.
     *
     * @return A pointer to the newly created Fence.
     */
    public Fence createFence() {
        long nativeFence = nCreateFence(getNativeObject());
        if (nativeFence == 0) throw new IllegalStateException("Couldn't create Fence");
        return new Fence(nativeFence);
    }

    /**
     * Retrieve the count of each resource tracked by Engine.
     *
     * <p>This is intended for debugging.
     * @{</p>
     */
    @IntRange(from = 0)
    public int getBufferObjectCount() {
        return nGetBufferObjectCount(getNativeObject());
    }

    @IntRange(from = 0)
    public int getViewCount() {
        return nGetViewCount(getNativeObject());
    }

    @IntRange(from = 0)
    public int getSceneCount() {
        return nGetSceneCount(getNativeObject());
    }

    @IntRange(from = 0)
    public int getSwapChainCount() {
        return nGetSwapChainCount(getNativeObject());
    }

    @IntRange(from = 0)
    public int getStreamCount() {
        return nGetStreamCount(getNativeObject());
    }

    @IntRange(from = 0)
    public int getIndexBufferCount() {
        return nGetIndexBufferCount(getNativeObject());
    }

    @IntRange(from = 0)
    public int getSkinningBufferCount() {
        return nGetSkinningBufferCount(getNativeObject());
    }

    @IntRange(from = 0)
    public int getMorphTargetBufferCount() {
        return nGetMorphTargetBufferCount(getNativeObject());
    }

    @IntRange(from = 0)
    public int getInstanceBufferCount() {
        return nGetInstanceBufferCount(getNativeObject());
    }

    @IntRange(from = 0)
    public int getVertexBufferCount() {
        return nGetVertexBufferCount(getNativeObject());
    }

    @IntRange(from = 0)
    public int getIndirectLightCount() {
        return nGetIndirectLightCount(getNativeObject());
    }

    @IntRange(from = 0)
    public int getMaterialCount() {
        return nGetMaterialCount(getNativeObject());
    }

    @IntRange(from = 0)
    public int getTextureCount() {
        return nGetTextureCount(getNativeObject());
    }

    @IntRange(from = 0)
    public int getSkyboxeCount() {
        return nGetSkyboxeCount(getNativeObject());
    }

    @IntRange(from = 0)
    public int getColorGradingCount() {
        return nGetColorGradingCount(getNativeObject());
    }

    @IntRange(from = 0)
    public int getRenderTargetCount() {
        return nGetRenderTargetCount(getNativeObject());
    }

    /**
     * Cancel the pending asynchronous call pointed to by {@code id}, which is retrieved whenever you invoke a non-blocking version of method on an object, such as {@code Texture::setImageAsync} or {@code BufferObject::setBufferAsync}.
     *
     * <p>Canceling does not suppress the completion callback of the call. The callback always runs
     * exactly once, on the handler the call was given: with {@code backend::AsyncCallStatus::CANCELED} if
     * the operation never ran, and with {@code COMPLETED} if it did. So a caller counting outstanding
     * operations always balances out, and whatever the callback owns is still released.</p>
     *
     * @param id The unique identifier for the asynchronous call to be canceled.
     *
     * @return Returns true upon successful cancellation. It returns false if the asynchronous
     * operation cannot be canceled because it is currently running, has finished, or has previously
     * been canceled.
     */
    public boolean cancelAsyncCall(@IntRange(from = 0) int id) {
        return nCancelAsyncCall(getNativeObject(), id);
    }

    /**
     * Kicks the hardware thread (e.g. the OpenGL, Vulkan or Metal thread) and blocks until all commands to this point are executed.
     *
     * <p>Note that does guarantee that the
     * hardware is actually finished.</p>
     *
     * <p>This is typically used right after destroying the <code>SwapChain</code>,
     * in cases where a guarantee about the <code>SwapChain</code> destruction is needed in a
     * timely fashion, such as when responding to Android's
     * <code>android.view.SurfaceHolder.Callback.surfaceDestroyed</code></p>
     *
     * <p>If the backend thread has encountered an unrecoverable error, this function becomes a no-op.</p>
     */
    public void flushAndWait() {
        nFlushAndWait(getNativeObject());
    }

    /**
     * Kicks the hardware thread (e.g. the OpenGL, Vulkan or Metal thread) and blocks until all commands to this point are executed.
     *
     * <p>Note that does guarantee that the
     * hardware is actually finished.</p>
     *
     * <p>A timeout can be specified, if for some reason this flushAndWait doesn't complete before the timeout, it will
     * return false, true otherwise.</p>
     *
     * <p>This is typically used right after destroying the <code>SwapChain</code>,
     * in cases where a guarantee about the <code>SwapChain</code> destruction is needed in a
     * timely fashion, such as when responding to Android's
     * <code>android.view.SurfaceHolder.Callback.surfaceDestroyed</code></p>
     *
     * <p>If the backend thread has encountered an unrecoverable error, this function becomes a no-op and returns false.</p>
     *
     * @param timeout A timeout in nanoseconds
     *
     * @return true if successful, false if flushAndWait timed out, in which case it wasn't successful and commands
     * might still be executing on both the CPU and GPU sides.
     */
    public boolean flushAndWait(@IntRange(from = 0) long timeout) {
        return nFlushAndWait(getNativeObject(), timeout);
    }

    /**
     * Kicks the hardware thread (e.g. the OpenGL, Vulkan or Metal thread) but does not wait for commands to be either executed or the hardware finished.
     *
     * <p>This is typically used after creating a lot of objects to start draining the command
     * queue which has a limited size.</p>
     *
     * <p>If the backend thread has encountered an unrecoverable error, this function becomes a no-op.</p>
     */
    public void flush() {
        nFlush(getNativeObject());
    }

    /**
     * Get paused state of rendering thread.
     *
     * <p>Warning: This is an experimental API.</p>
     *
     * @see #setPaused
     */
    public boolean isPaused() {
        return nIsPaused(getNativeObject());
    }

    /**
     * Pause or resume rendering thread.
     *
     * <p>Warning: This is an experimental API. In particular, note the following caveats.</p>
     *
     * <ul><li>
     * Buffer callbacks will never be called as long as the rendering thread is paused.
     * Do not rely on a buffer callback to unpause the thread.
     * </li><li>
     * While the rendering thread is paused, rendering commands will continue to be queued until the
     * buffer limit is reached. When the limit is reached, the program will abort.
     * </li></ul>
     */
    public void setPaused(boolean paused) {
        nSetPaused(getNativeObject(), paused);
    }

    /**
     * Switch the command queue to unprotected mode.
     *
     * <p>Protected mode can be activated via
     * Renderer::beginFrame() using a protected SwapChain.</p>
     *
     * @see Renderer
     * @see SwapChain
     */
    public void unprotected() {
        nUnprotected(getNativeObject());
    }

    /** Returns the resolved backend. */
    public Backend getBackend() {
        return EnumCache.sBackendValues[nGetBackend(getNativeObject())];
    }

    /**
     * Get the current time.
     *
     * <p>This is a convenience function that simply returns the
     * time in nanosecond since epoch of std::chrono::steady_clock.
     * A possible implementation is:</p>
     *
     * <pre>{@code
     *     return std::chrono::steady_clock::now().time_since_epoch().count();
     *
     * }</pre>
     *
     * @return current time in nanosecond since epoch of std::chrono::steady_clock.
     *
     * @see Renderer#beginFrame
     */
    @IntRange(from = 0)
    public static long getSteadyClockTimeNano() {
        return nGetSteadyClockTimeNano();
    }

    /**
     * Check if a feature flag exists
     *
     * @param name name of the feature flag to check
     *
     * @return true if the feature flag exists, false otherwise
     */
    public boolean hasFeatureFlag(@NonNull String name) {
        return nHasFeatureFlag(getNativeObject(), name);
    }

    /**
     * Set the value of a non-constant feature flag.
     *
     * @param name name of the feature flag to set
     * @param value value to set
     *
     * @return true if the value was set, false if the feature flag is constant or doesn't exist.
     */
    public boolean setFeatureFlag(@NonNull String name, boolean value) {
        return nSetFeatureFlag(getNativeObject(), name, value);
    }

    /**
     * Retrieves the value of any feature flag.
     *
     * @param name name of the feature flag
     *
     * @return the value of the flag if it exists
     */
    public boolean getFeatureFlag(@NonNull String name) {
        if (!hasFeatureFlag(name)) throw new IllegalArgumentException("Feature flag " + name + " does not exist");
        return nGetFeatureFlag(getNativeObject(), name);
    }

    /**
     * Asynchronously ensures that the variants of the specified Material needed to render it in the provided View are compiled.
     *
     * @param priority Which priority queue to use, LOW or HIGH.
     * @param material The Material to compile.
     * @param view The View in which the material will be rendered.
     * @param shadowReceiver Indicates whether to compile the shadow-receiving variants.
     *                       Pass {@code FeatureState::INDETERMINATE} to compile both permutations.
     * @param skinning Indicates whether to compile the skinning variants.
     *                       Pass {@code FeatureState::INDETERMINATE} to compile both permutations.
     * @param handler Handler to dispatch the callback or nullptr for the default handler.
     * @param callback Callback called on the main thread when the compilation is done
     *                       by the backend.
     *
     * @see Material#compile
     */
    public void compile(@NonNull Material.CompilerPriorityQueue priority, @NonNull Material material, @NonNull View view, @NonNull FeatureState shadowReceiver, @NonNull FeatureState skinning, @Nullable Object handler, @Nullable Runnable callback) {
        nCompile(getNativeObject(), priority.ordinal(), material.getNativeObject(), view.getNativeObject(), shadowReceiver.toFilamentNative(), skinning.toFilamentNative(), handler, callback);
    }

    @UsedByNative("Engine.cpp")
    public long getNativeObject() {
        if (mNativeObject == 0) {
            throw new IllegalStateException("Calling method on destroyed Engine");
        }
        return mNativeObject;
    }

    void clearNativeObject() {
        mNativeObject = 0;
    }
    /**
     * Config is used to define the memory footprint used by the engine, such as the command buffer size.
     *
     * <p>Config can be used to customize engine requirements based
     * on the applications needs.</p>
     *
     * <p>.perRenderPassArenaSizeMB (default: 3 MiB)
     *   +--------------------------+
     *   |                          |
     *   | .perFrameCommandsSizeMB  |
     *   |    (default 2 MiB)       |
     *   |                          |
     *   +--------------------------+
     *   |  (froxel, etc...)        |
     *   +--------------------------+</p>
     *
     * <p>.commandBufferSizeMB (default 3MiB)
     *   +--------------------------+
     *   | .minCommandBufferSizeMB  |
     *   +--------------------------+
     *   | .minCommandBufferSizeMB  |
     *   +--------------------------+
     *   | .minCommandBufferSizeMB  |
     *   +--------------------------+
     *   :                          :
     *   :                          :</p>
     */
    public static class Config {
        // #defines in Engine.h
        private static final long FILAMENT_PER_RENDER_PASS_ARENA_SIZE_IN_MB = 3;
        private static final long FILAMENT_PER_FRAME_COMMANDS_SIZE_IN_MB = 2;
        private static final long FILAMENT_MIN_COMMAND_BUFFERS_SIZE_IN_MB = 1;
        private static final long FILAMENT_COMMAND_BUFFER_SIZE_IN_MB =
                FILAMENT_MIN_COMMAND_BUFFERS_SIZE_IN_MB * 3;
    
        /**
         * Special value for jobSystemThreadCount, forcing the JobSystem to be single-threaded.
         */
        public static final long SINGLE_THREADED = 0xFFFFFFFFL;
    
        public enum ShaderLanguage {
            DEFAULT,
            MSL,
            METAL_LIBRARY;

            public int toFilamentNative() { return ordinal(); }
        }

        /**
         * Size in MiB of the low-level command buffer arena.
         *
         * <p>Each new command buffer is allocated from here. If this buffer is too small the program
         * might terminate or rendering errors might occur.</p>
         *
         * <p>This is typically set to minCommandBufferSizeMB * 3, so that up to 3 frames can be
         * batched-up at once.</p>
         *
         * <p>This value affects the application's memory usage.</p>
         */
        public long commandBufferSizeMB = FILAMENT_COMMAND_BUFFER_SIZE_IN_MB;
        /**
         * Size in MiB of the per-frame data arena.
         *
         * <p>This is the main arena used for allocations when preparing a frame.
         * e.g.: Froxel data and high-level commands are allocated from this arena.</p>
         *
         * <p>If this size is too small, the program will abort on debug builds and have undefined
         * behavior otherwise.</p>
         *
         * <p>This value affects the application's memory usage.</p>
         */
        public long perRenderPassArenaSizeMB = FILAMENT_PER_RENDER_PASS_ARENA_SIZE_IN_MB;
        /**
         * Size in MiB of the backend's handle arena.
         *
         * <p>Backends will fallback to slower heap-based allocations when running out of space and
         * log this condition.</p>
         *
         * <p>If 0, then the default value for the given platform is used</p>
         *
         * <p>This value affects the application's memory usage.</p>
         */
        public long driverHandleArenaSizeMB = 0;
        /**
         * Minimum size in MiB of a low-level command buffer.
         *
         * <p>This is how much space is guaranteed to be available for low-level commands when a new
         * buffer is allocated. If this is too small, the engine might have to stall to wait for
         * more space to become available, this situation is logged.</p>
         *
         * <p>This value does not affect the application's memory usage.</p>
         */
        public long minCommandBufferSizeMB = FILAMENT_MIN_COMMAND_BUFFERS_SIZE_IN_MB;
        /**
         * Size in MiB of the per-frame high level command buffer.
         *
         * <p>This buffer is related to the number of draw calls achievable within a frame, if it is
         * too small, the program will abort on debug builds and have undefined behavior otherwise.</p>
         *
         * <p>It is allocated from the 'per-render-pass arena' above. Make sure that at least 1 MiB is
         * left in the per-render-pass arena when deciding the size of this buffer.</p>
         *
         * <p>This value does not affect the application's memory usage.</p>
         */
        public long perFrameCommandsSizeMB = FILAMENT_PER_FRAME_COMMANDS_SIZE_IN_MB;
        /**
         * Number of threads to use in Engine's JobSystem.
         *
         * <p>Engine uses a utils::JobSystem to carry out parallelization of Engine workloads. This
         * value sets the number of threads allocated for JobSystem. Configuring this value can be
         * helpful in CPU-constrained environments where too many threads can cause contention of
         * CPU and reduce performance.</p>
         *
         * <p>The default value is 0, which implies that the Engine will use a heuristic to determine
         * the number of threads to use.</p>
         *
         * <p>The special value SINGLE_THREADED forces the JobSystem to be single-threaded and not use
         * a thread pool (jobs are executed on the calling thread).</p>
         */
        public long jobSystemThreadCount = 0;
        /**
         * When uploading vertex or index data, the Filament Metal backend copies data into a shared staging area before transferring it to the GPU.
         *
         * <p>This setting controls
         * the total size of the buffer used to perform these allocations.</p>
         *
         * <p>Higher values can improve performance when performing many uploads across a small
         * number of frames.</p>
         *
         * <p>This buffer remains alive throughout the lifetime of the Engine, so this size adds to the
         * memory footprint of the app and should be set as conservative as possible.</p>
         *
         * <p>A value of 0 disables the shared staging buffer entirely; uploads will acquire an
         * individual buffer from a pool of shared buffers.</p>
         *
         * <p>Only respected by the Metal backend.</p>
         */
        public long metalUploadBufferSizeBytes = 512 * 1024;
        /**
         * The action to take if a Drawable cannot be acquired.
         *
         * <p>Each frame rendered requires a CAMetalDrawable texture, which is
         * presented on-screen at the completion of each frame. These are
         * limited and provided round-robin style by the system.</p>
         */
        public boolean metalDisablePanicOnDrawableFailure = false;
        /**
         * Set to {@code true} to forcibly disable parallel shader compilation in the backend.
         *
         * <p>Currently only honored by the GL and Metal backends.</p>
         *
         * @deprecated use "backend.disable_parallel_shader_compile" feature flag instead
         */
        public boolean disableParallelShaderCompile = false;
        public StereoscopicType stereoscopicType = StereoscopicType.NONE;
        public long stereoscopicEyeCount = 2;
        public long resourceAllocatorCacheSizeMB = 64;
        public long resourceAllocatorCacheMaxAge = 1;
        public boolean disableHandleUseAfterFreeCheck = false;
        public Config.ShaderLanguage preferredShaderLanguage = ShaderLanguage.DEFAULT;
        public boolean forceGLES2Context = false;
        /**
         * Assert the native window associated to a SwapChain is valid when calling makeCurrent().
         *
         * <p>This is only supported for:</p>
         *
         * <ul>
         *   <li>PlatformEGLAndroid</li>
         * </ul>
         *
         * @deprecated use "backend.opengl.assert_native_window_is_valid" feature flag instead
         */
        public boolean assertNativeWindowIsValid = false;
        /**
         * GPU context priority level.
         *
         * <p>Controls GPU work scheduling and preemption.</p>
         */
        public GpuContextPriority gpuContextPriority = GpuContextPriority.DEFAULT;
        /**
         * The initial size in bytes of the shared uniform buffer used for material instance batching.
         *
         * <p>If the buffer runs out of space during a frame, it will be automatically reallocated
         * with a larger capacity. Setting an appropriate initial size can help avoid runtime
         * reallocations, which can cause a minor performance stutter, at the cost of higher
         * initial memory usage.</p>
         */
        public long sharedUboInitialSizeInBytes = 256 * 64;
        /**
         * Asynchronous mode for the engine.
         *
         * <p>Defines how asynchronous operations are handled.
         * Note that selecting a non-NONE mode does not guarantee asynchronous methods are
         * supported, as the underlying backend or the feature flag may override this configuration.
         * Always validate availability via Engine::isAsynchronousModeEnabled() before
         * invoking asynchronous methods.</p>
         */
        public AsynchronousMode asynchronousMode = AsynchronousMode.NONE;
        /**
         * Capacity of the LRU cache for material definitions.
         *
         * <p>A value of 0 indicates that definitions will be destroyed immediately when they are no
         * longer referenced by any material instances or scenes. A value greater than 0 defines
         * the maximum number of unreferenced definitions to keep alive to avoid re-compilation.</p>
         */
        public long materialCacheCapacity = 0;
        /**
         * Capacity of the LRU cache for program specializations.
         *
         * <p>Similar to materialCacheCapacity, but applies to the underlying shader programs generated
         * for materials. A value of 0 means immediate destruction of unreferenced programs. A
         * positive value caches up to that number of programs.</p>
         */
        public long programCacheCapacity = 0;
        /**
         * Whether a scene can contain more than one directional light.
         *
         * <p>By default, and historically, only the dominant directional light (the one with the
         * highest intensity) of a scene is evaluated. When this is enabled, up to four
         * additional directional lights contribute lighting; they don't cast shadows and don't
         * draw a sun's disk. Scenes with a single directional light are unaffected either way.</p>
         *
         * @see LightManager
         */
        public boolean enableMultipleDirectionalLights = false;
    }


    private static native int nGetSupportedFeatureLevel(long nativeEngine);
    private static native int nSetActiveFeatureLevel(long nativeEngine, int featureLevel);
    private static native int nGetActiveFeatureLevel(long nativeEngine);
    @IntRange(from = 0)
    private static native int nGetMaxAutomaticInstances(long nativeEngine);
    private static native boolean nIsStereoSupported(long nativeEngine, int stereoscopicType);
    private static native boolean nIsAsynchronousModeEnabled(long nativeEngine);
    private static native boolean nHasUnrecoverableFailure(long nativeEngine);
    @IntRange(from = 0)
    private static native int nGetMaxStereoscopicEyes();
    private static native long nGetEntityManager(long nativeEngine);
    private static native long nGetRenderableManager(long nativeEngine);
    private static native long nGetLightManager(long nativeEngine);
    private static native long nGetTransformManager(long nativeEngine);
    private static native void nEnableAccurateTranslations(long nativeEngine);
    private static native void nSetAutomaticInstancingEnabled(long nativeEngine, boolean enable);
    private static native boolean nIsAutomaticInstancingEnabled(long nativeEngine);
    private static native long nCreateRenderer(long nativeEngine);
    private static native long nCreateView(long nativeEngine);
    private static native long nCreateScene(long nativeEngine);
    private static native long nCreateCamera(long nativeEngine, @Entity int entity);
    private static native long nGetCameraComponent(long nativeEngine, @Entity int entity);
    private static native void nDestroyCameraComponent(long nativeEngine, @Entity int entity);
    private static native long nCreateFence(long nativeEngine);
    @IntRange(from = 0)
    private static native int nGetBufferObjectCount(long nativeEngine);
    @IntRange(from = 0)
    private static native int nGetViewCount(long nativeEngine);
    @IntRange(from = 0)
    private static native int nGetSceneCount(long nativeEngine);
    @IntRange(from = 0)
    private static native int nGetSwapChainCount(long nativeEngine);
    @IntRange(from = 0)
    private static native int nGetStreamCount(long nativeEngine);
    @IntRange(from = 0)
    private static native int nGetIndexBufferCount(long nativeEngine);
    @IntRange(from = 0)
    private static native int nGetSkinningBufferCount(long nativeEngine);
    @IntRange(from = 0)
    private static native int nGetMorphTargetBufferCount(long nativeEngine);
    @IntRange(from = 0)
    private static native int nGetInstanceBufferCount(long nativeEngine);
    @IntRange(from = 0)
    private static native int nGetVertexBufferCount(long nativeEngine);
    @IntRange(from = 0)
    private static native int nGetIndirectLightCount(long nativeEngine);
    @IntRange(from = 0)
    private static native int nGetMaterialCount(long nativeEngine);
    @IntRange(from = 0)
    private static native int nGetTextureCount(long nativeEngine);
    @IntRange(from = 0)
    private static native int nGetSkyboxeCount(long nativeEngine);
    @IntRange(from = 0)
    private static native int nGetColorGradingCount(long nativeEngine);
    @IntRange(from = 0)
    private static native int nGetRenderTargetCount(long nativeEngine);
    private static native boolean nCancelAsyncCall(long nativeEngine, @IntRange(from = 0) int id);
    private static native void nFlushAndWait(long nativeEngine);
    private static native boolean nFlushAndWait(long nativeEngine, @IntRange(from = 0) long timeout);
    private static native void nFlush(long nativeEngine);
    private static native boolean nIsPaused(long nativeEngine);
    private static native void nSetPaused(long nativeEngine, boolean paused);
    private static native void nUnprotected(long nativeEngine);
    private static native int nGetBackend(long nativeEngine);
    @IntRange(from = 0)
    private static native long nGetSteadyClockTimeNano();
    private static native boolean nHasFeatureFlag(long nativeEngine, @NonNull String name);
    private static native boolean nSetFeatureFlag(long nativeEngine, @NonNull String name, boolean value);
    private static native boolean nGetFeatureFlag(long nativeEngine, @NonNull String name);
    private static native void nCompile(long nativeEngine, int priority, long material, long view, int shadowReceiver, int skinning, Object handler, Runnable callback);
    private static native void nDestroyEngine(long nativeEngine);
    private static native long nCreateSwapChain(long nativeEngine, Object nativeWindow, long flags);
    private static native long nCreateSwapChainHeadless(long nativeEngine, int width, int height, long flags);
    private static native long nCreateSwapChainFromRawPointer(long nativeEngine, long pointer, long flags);
    private static native boolean nDestroyBufferObject(long nativeEngine, long nativeBufferObject);
    private static native boolean nDestroyColorGrading(long nativeEngine, long nativeColorGrading);
    private static native boolean nDestroyFence(long nativeEngine, long nativeFence);
    private static native boolean nDestroyFramePacer(long nativeEngine, long nativeFramePacer);
    private static native boolean nDestroyIndexBuffer(long nativeEngine, long nativeIndexBuffer);
    private static native boolean nDestroyIndirectLight(long nativeEngine, long nativeIndirectLight);
    private static native boolean nDestroyInstanceBuffer(long nativeEngine, long nativeInstanceBuffer);
    private static native boolean nDestroyMaterial(long nativeEngine, long nativeMaterial);
    private static native boolean nDestroyMaterialInstance(long nativeEngine, long nativeMaterialInstance);
    private static native boolean nDestroyMorphTargetBuffer(long nativeEngine, long nativeMorphTargetBuffer);
    private static native boolean nDestroyRenderTarget(long nativeEngine, long nativeTarget);
    private static native boolean nDestroyRenderer(long nativeEngine, long nativeRenderer);
    private static native boolean nDestroyScene(long nativeEngine, long nativeScene);
    private static native boolean nDestroySkinningBuffer(long nativeEngine, long nativeSkinningBuffer);
    private static native boolean nDestroySkybox(long nativeEngine, long nativeSkybox);
    private static native boolean nDestroyStream(long nativeEngine, long nativeStream);
    private static native boolean nDestroySwapChain(long nativeEngine, long nativeSwapChain);
    private static native boolean nDestroyTexture(long nativeEngine, long nativeTexture);
    private static native boolean nDestroyVertexBuffer(long nativeEngine, long nativeVertexBuffer);
    private static native boolean nDestroyView(long nativeEngine, long nativeView);
    private static native void nDestroyEntity(long nativeEngine, int entity);
    private static native long nCreateBuilder();
    private static native void nDestroyBuilder(long nativeBuilder);
    private static native void nSetBuilderBackend(long nativeBuilder, long backend);
    private static native void nSetBuilderConfig(long nativeBuilder, long commandBufferSizeMB,
            long perRenderPassArenaSizeMB, long driverHandleArenaSizeMB,
            long minCommandBufferSizeMB, long perFrameCommandsSizeMB, long jobSystemThreadCount,
            boolean disableParallelShaderCompile, int stereoscopicType, long stereoscopicEyeCount,
            long resourceAllocatorCacheSizeMB, long resourceAllocatorCacheMaxAge,
            boolean disableHandleUseAfterFreeCheck,
            int preferredShaderLanguage,
            boolean forceGLES2Context, boolean assertNativeWindowIsValid,
            int gpuContextPriority,
            long sharedUboInitialSizeInBytes,
            boolean enableMultipleDirectionalLights);
    private static native void nSetBuilderFeatureLevel(long nativeBuilder, int ordinal);
    private static native void nSetBuilderSharedContext(long nativeBuilder, long sharedContext);
    private static native void nSetBuilderPaused(long nativeBuilder, boolean paused);
    private static native void nSetBuilderFeature(long nativeBuilder, String name, boolean value);
    private static native void nSetBuilderColorGrading(long nativeBuilder, long nativeColorGradingBuilder);
    private static native long nBuilderBuild(long nativeBuilder);
}

// THIS FILE IS GENERATED BY APIGEN AND MUST NOT BE HAND-MODIFIED.
