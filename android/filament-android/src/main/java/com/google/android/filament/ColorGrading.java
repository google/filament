/*
 * Copyright (C) 2026 The Android Open Source Project
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *      http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

// THIS FILE IS GENERATED BY APIGEN AND MUST NOT BE HAND-MODIFIED.

package com.google.android.filament;

import java.lang.IllegalStateException;

import java.nio.Buffer;

import androidx.annotation.IntRange;
import androidx.annotation.NonNull;
import androidx.annotation.Nullable;
import androidx.annotation.RestrictTo;
import androidx.annotation.Size;

/**
 * ColorGrading is used to transform (either to modify or correct) the colors of the HDR buffer
 *
 * <p>rendered by Filament. Color grading transforms are applied after lighting, and after any lens
 * effects (bloom for instance), and include tone mapping.</p>
 *
 * <h1>Creation, usage and destruction</h1>
 *
 * <p>A ColorGrading object is created using the ColorGrading::Builder and destroyed by calling
 * Engine::destroy(const ColorGrading*). A ColorGrading object is meant to be set on a View.</p>
 *
 * <pre>{@code
 *  filament::Engine* engine = filament::Engine::create();
 *
 *  filament::ColorGrading* colorGrading = filament::ColorGrading::Builder()
 *              .toneMapping(filament::ColorGrading::ToneMapping::ACES)
 *              .build(*engine);
 *
 *  myView->setColorGrading(colorGrading);
 *
 *  engine->destroy(colorGrading);
 *
 * }</pre>
 *
 * <h1>Performance</h1>
 *
 * <p>Creating a new ColorGrading object may be more expensive than other Filament objects as a LUT may
 * need to be generated. The generation of this LUT, if necessary, may happen on the CPU.</p>
 *
 * <h1>Ordering</h1>
 *
 * <p>The various transforms held by ColorGrading are applied in the following order:</p>
 *
 * <ul>
 *   <li>Exposure</li>
 *   <li>Night adaptation</li>
 *   <li>White balance</li>
 *   <li>Channel mixer</li>
 *   <li>Shadows/mid-tones/highlights</li>
 *   <li>Slope/offset/power (CDL)</li>
 *   <li>Contrast</li>
 *   <li>Vibrance</li>
 *   <li>Saturation</li>
 *   <li>Curves</li>
 *   <li>Tone mapping</li>
 *   <li>Luminance scaling</li>
 *   <li>Gamut mapping</li>
 * </ul>
 *
 * <h1>Defaults</h1>
 *
 * <p>Here are the default color grading options:</p>
 *
 * <ul>
 *   <li>Exposure: 0.0</li>
 *   <li>Night adaptation: 0.0</li>
 *   <li>White balance: temperature 0, and tint 0</li>
 *   <li>Channel mixer: red {1,0,0}, green {0,1,0}, blue {0,0,1}</li>
 *   <li>Shadows/mid-tones/highlights: shadows {1,1,1,0}, mid-tones {1,1,1,0}, highlights {1,1,1,0},
 * ranges {0,0.333,0.550,1}</li>
 *   <li>Slope/offset/power: slope 1.0, offset 0.0, and power 1.0</li>
 *   <li>Contrast: 1.0</li>
 *   <li>Vibrance: 1.0</li>
 *   <li>Saturation: 1.0</li>
 *   <li>Curves: gamma {1,1,1}, midPoint {1,1,1}, and scale {1,1,1}</li>
 *   <li>Tone mapping: ACESLegacyToneMapper</li>
 *   <li>Luminance scaling: false</li>
 *   <li>Gamut mapping: false</li>
 *   <li>Output color space: Rec709-sRGB-D65</li>
 * </ul>
 *
 * @see View
 */
public class ColorGrading {
    private long mNativeObject;

    public enum QualityLevel {
        LOW,
        MEDIUM,
        HIGH,
        ULTRA;

        /** @return the value of this enum constant as used by the native Filament engine. */
        public int toFilamentNative() { return ordinal(); }
    }

    public enum LutFormat {
        /** 10 bits per component */
        INTEGER,
        /** 16 bits per component (10 bits mantissa precision) */
        FLOAT;

        /** @return the value of this enum constant as used by the native Filament engine. */
        public int toFilamentNative() { return ordinal(); }
    }

    /**
     * List of available tone-mapping operators.
     *
     * @deprecated Use Builder::toneMapper(ToneMapper*) instead
     */
    public enum ToneMapping {
        /** Linear tone mapping (i.e. no tone mapping) */
        LINEAR,
        /** ACES tone mapping, with a brightness modifier to match Filament's legacy tone mapper */
        ACES_LEGACY,
        /** ACES tone mapping */
        ACES,
        /** Filmic tone mapping, modelled after ACES but applied in sRGB space */
        FILMIC,
        /** Tone mapping used to validate/debug scene exposure */
        DISPLAY_RANGE;

        /** @return the value of this enum constant as used by the native Filament engine. */
        public int toFilamentNative() { return ordinal(); }
    }

    ColorGrading(long nativeObject) {
        mNativeObject = nativeObject;
    }

    @NonNull
    @RestrictTo(RestrictTo.Scope.LIBRARY_GROUP)
    public static ColorGrading wrap(long nativeObject) {
        return new ColorGrading(nativeObject);
    }

    /** Use Builder to construct a ColorGrading object instance */
    public static class Builder {
        @SuppressWarnings({"FieldCanBeLocal", "UnusedDeclaration"}) // Keep to finalize native resources
        private final BuilderFinalizer mFinalizer;
        private final long mNativeBuilder;

        public Builder() {
            mNativeBuilder = nCreateBuilder();
            mFinalizer = new BuilderFinalizer(mNativeBuilder);
        }

        long getNativeBuilder() {
            return mNativeBuilder;
        }

        /**
         * Sets the quality level of the color grading.
         *
         * <p>When color grading is implemented using
         * a 3D LUT, the quality level may impact the resolution and bit depth of the backing
         * 3D texture. For instance, a low quality level will use a 16x16x16 10 bit LUT, a medium
         * quality level will use a 32x32x32 10 bit LUT, a high quality will use a 32x32x32 16 bit
         * LUT, and a ultra quality will use a 64x64x64 16 bit LUT.</p>
         *
         * <p>This setting has no effect if generating a 1D LUT.</p>
         *
         * <p>This overrides the values set by format() and dimensions().</p>
         *
         * <p>The default quality is medium.</p>
         *
         * @param qualityLevel The desired quality of the color grading process
         *
         * @return This Builder, for chaining calls
         */
        @NonNull
        public Builder quality(@NonNull QualityLevel qualityLevel) {
            nBuilderQuality(mNativeBuilder, qualityLevel.toFilamentNative());
            return this;
        }

        /**
         * When color grading is implemented using a 3D LUT, this sets the texture format of of the LUT.
         *
         * <p>This overrides the value set by quality().</p>
         *
         * <p>This setting has no effect if generating a 1D LUT.</p>
         *
         * <p>The default is INTEGER</p>
         *
         * @param format The desired format of the 3D LUT.
         *
         * @return This Builder, for chaining calls
         */
        @NonNull
        public Builder format(@NonNull LutFormat format) {
            nBuilderFormat(mNativeBuilder, format.toFilamentNative());
            return this;
        }

        /**
         * When color grading is implemented using a 3D LUT, this sets the dimension of the LUT.
         *
         * <p>This overrides the value set by quality().</p>
         *
         * <p>This setting has no effect if generating a 1D LUT.</p>
         *
         * <p>The default is 32</p>
         *
         * @param dim The desired dimension of the LUT. Between 16 and 64.
         *
         * @return This Builder, for chaining calls
         */
        @NonNull
        public Builder dimensions(@IntRange(from = 0) int dim) {
            nBuilderDimensions(mNativeBuilder, dim);
            return this;
        }

        /**
         * Selects the tone mapping operator to apply to the HDR color buffer as the last operation of the color grading post-processing step.
         *
         * <p>The default tone mapping operator is ACESLegacyToneMapper.</p>
         *
         * <p>The specified tone mapper must have a lifecycle that exceeds the lifetime of
         * this builder. Since the build(Engine&) method is synchronous, it is safe to
         * delete the tone mapper object after that finishes executing.</p>
         *
         * @param toneMapper The tone mapping operator to apply to the HDR color buffer
         *
         * @return This Builder, for chaining calls
         */
        @NonNull
        public Builder toneMapper(@Nullable ToneMapper toneMapper) {
            nBuilderToneMapper(mNativeBuilder, toneMapper != null ? toneMapper.getNativeObject() : 0);
            return this;
        }

        /**
         * Selects the tone mapping operator to apply to the HDR color buffer as the last operation of the color grading post-processing step.
         *
         * <p>The default tone mapping operator is ACES_LEGACY.</p>
         *
         * @param toneMapping The tone mapping operator to apply to the HDR color buffer
         *
         * @return This Builder, for chaining calls
         *
         * @deprecated Use toneMapper(ToneMapper*) instead
         */
        @NonNull
        public Builder toneMapping(@NonNull ToneMapping toneMapping) {
            nBuilderToneMapping(mNativeBuilder, toneMapping.toFilamentNative());
            return this;
        }

        /**
         * Enables or disables the luminance scaling component (LICH) from the exposure value invariant luminance system (EVILS).
         *
         * <p>When this setting is enabled, pixels with high
         * chromatic values will roll-off to white to offer a more natural rendering. This step
         * also helps avoid undesirable hue skews caused by out of gamut colors clipped
         * to the destination color gamut.</p>
         *
         * <p>When luminance scaling is enabled, tone mapping is performed on the luminance of each
         * pixel instead of per-channel.</p>
         *
         * @param luminanceScaling Enables or disables luminance scaling post-tone mapping
         *
         * @return This Builder, for chaining calls
         */
        @NonNull
        public Builder luminanceScaling(boolean luminanceScaling) {
            nBuilderLuminanceScaling(mNativeBuilder, luminanceScaling);
            return this;
        }

        /**
         * Enables or disables gamut mapping to the destination color space's gamut.
         *
         * <p>When gamut
         * mapping is turned off, out-of-gamut colors are clipped to the destination's gamut,
         * which may produce hue skews (blue skewing to purple, green to yellow, etc.). When
         * gamut mapping is enabled, out-of-gamut colors are brought back in gamut by trying to
         * preserve the perceived chroma and lightness of the original values.</p>
         *
         * @param gamutMapping Enables or disables gamut mapping
         *
         * @return This Builder, for chaining calls
         */
        @NonNull
        public Builder gamutMapping(boolean gamutMapping) {
            nBuilderGamutMapping(mNativeBuilder, gamutMapping);
            return this;
        }

        /**
         * Adjusts the exposure of this image.
         *
         * <p>The exposure is specified in stops:
         * each stop brightens (positive values) or darkens (negative values) the image by
         * a factor of 2. This means that an exposure of 3 will brighten the image 8 times
         * more than an exposure of 0 (2^3 = 8 and 2^0 = 1). Contrary to the camera's exposure,
         * this setting is applied after all post-processing (bloom, etc.) are applied.</p>
         *
         * @param exposure Value in EV stops. Can be negative, 0, or positive.
         *
         * @return This Builder, for chaining calls
         */
        @NonNull
        public Builder exposure(float exposure) {
            nBuilderExposure(mNativeBuilder, exposure);
            return this;
        }

        /**
         * Controls the amount of night adaptation to replicate a more natural representation of low-light conditions as perceived by the human vision system.
         *
         * <p>In low-light conditions,
         * peak luminance sensitivity of the eye shifts toward the blue end of the color spectrum:
         * darker tones appear brighter, reducing contrast, and colors are blue shifted (the darker
         * the more intense the effect).</p>
         *
         * @param adaptation Amount of adaptation, between 0 (no adaptation) and 1 (full adaptation).
         *
         * @return This Builder, for chaining calls
         */
        @NonNull
        public Builder nightAdaptation(float adaptation) {
            nBuilderNightAdaptation(mNativeBuilder, adaptation);
            return this;
        }

        /**
         * Adjusts the while balance of the image. This can be used to remove color casts
         *
         * <p>and correct the appearance of the white point in the scene, or to alter the
         * overall chromaticity of the image for artistic reasons (to make the image appear
         * cooler or warmer for instance).</p>
         *
         * <p>The while balance adjustment is defined with two values:</p>
         *
         * <ul>
         *   <li>Temperature, to modify the color temperature. This value will modify the colors
         * on a blue/yellow axis. Lower values apply a cool color temperature, and higher
         * values apply a warm color temperature. The lowest value, -1.0f, is equivalent to
         * a temperature of 50,000K. The highest value, 1.0f, is equivalent to a temperature
         * of 2,000K.</li>
         *   <li>Tint, to modify the colors on a green/magenta axis. The lowest value, -1.0f, will
         * apply a strong green cast, and the highest value, 1.0f, will apply a strong magenta
         * cast.
         * Both values are expected to be in the range [-1.0..+1.0]. Values outside of that
         * range will be clipped to that range.</li>
         * </ul>
         *
         * @param temperature Modification on the blue/yellow axis, as a value between -1.0 and +1.0.
         * @param tint Modification on the green/magenta axis, as a value between -1.0 and +1.0.
         *
         * @return This Builder, for chaining calls
         */
        @NonNull
        public Builder whiteBalance(float temperature, float tint) {
            nBuilderWhiteBalance(mNativeBuilder, temperature, tint);
            return this;
        }

        /**
         * The channel mixer adjustment modifies each output color channel using the specified
         *
         * <p>mix of the source color channels.</p>
         *
         * <p>By default each output color channel is set to use 100% of the corresponding source
         * channel and 0% of the other channels. For instance, the output red channel is set to
         * {1.0, 0.0, 1.0} or 100% red, 0% green and 0% blue.</p>
         *
         * <p>Each output channel can add or subtract data from the source channel by using values
         * in the range [-2.0..+2.0]. Values outside of that range will be clipped to that range.</p>
         *
         * <p>Using the channel mixer adjustment you can for instance create a monochrome output
         * by setting all 3 output channels to the same mix. For instance: {0.4, 0.4, 0.2} for
         * all 3 output channels(40% red, 40% green and 20% blue).</p>
         *
         * <p>More complex mixes can be used to create more complex effects. For instance, here is
         * a mix that creates a sepia tone effect:</p>
         *
         * <ul>
         *   <li>outRed   = {0.255, 0.858, 0.087}</li>
         *   <li>outGreen = {0.213, 0.715, 0.072}</li>
         *   <li>outBlue  = {0.170, 0.572, 0.058}</li>
         * </ul>
         *
         * @return This Builder, for chaining calls
         */
        @NonNull
        public Builder channelMixer(float outRedx, float outRedy, float outRedz, float outGreenx, float outGreeny, float outGreenz, float outBluex, float outBluey, float outBluez) {
            nBuilderChannelMixer(mNativeBuilder, outRedx, outRedy, outRedz, outGreenx, outGreeny, outGreenz, outBluex, outBluey, outBluez);
            return this;
        }

        /**
         * The channel mixer adjustment modifies each output color channel using the specified
         *
         * <p>mix of the source color channels.</p>
         *
         * <p>By default each output color channel is set to use 100% of the corresponding source
         * channel and 0% of the other channels. For instance, the output red channel is set to
         * {1.0, 0.0, 1.0} or 100% red, 0% green and 0% blue.</p>
         *
         * <p>Each output channel can add or subtract data from the source channel by using values
         * in the range [-2.0..+2.0]. Values outside of that range will be clipped to that range.</p>
         *
         * <p>Using the channel mixer adjustment you can for instance create a monochrome output
         * by setting all 3 output channels to the same mix. For instance: {0.4, 0.4, 0.2} for
         * all 3 output channels(40% red, 40% green and 20% blue).</p>
         *
         * <p>More complex mixes can be used to create more complex effects. For instance, here is
         * a mix that creates a sepia tone effect:</p>
         *
         * <ul>
         *   <li>outRed   = {0.255, 0.858, 0.087}</li>
         *   <li>outGreen = {0.213, 0.715, 0.072}</li>
         *   <li>outBlue  = {0.170, 0.572, 0.058}</li>
         * </ul>
         *
         * @param outRed The mix of source RGB for the output red channel, between -2.0 and +2.0
         * @param outGreen The mix of source RGB for the output green channel, between -2.0 and +2.0
         * @param outBlue The mix of source RGB for the output blue channel, between -2.0 and +2.0
         *
         * @return This Builder, for chaining calls
         */
        @NonNull
        public Builder channelMixer(@NonNull @Size(min = 3) float[] outRed, @NonNull @Size(min = 3) float[] outGreen, @NonNull @Size(min = 3) float[] outBlue) {
            return channelMixer(outRed[0], outRed[1], outRed[2], outGreen[0], outGreen[1], outGreen[2], outBlue[0], outBlue[1], outBlue[2]);
        }

        /**
         * Adjusts the colors separately in 3 distinct tonal ranges or zones: shadows, mid-tones, and highlights.
         *
         * <p>The tonal zones are by the ranges parameter: the x and y components define the beginning
         * and end of the transition from shadows to mid-tones, and the z and w components define
         * the beginning and end of the transition from mid-tones to highlights.</p>
         *
         * <p>A smooth transition is applied between the zones which means for instance that the
         * correction color of the shadows range will partially apply to the mid-tones, and the
         * other way around. This ensure smooth visual transitions in the final image.</p>
         *
         * <p>Each correction color is defined as a linear RGB color and a weight. The weight is a
         * value (which may be positive or negative) that is added to the linear RGB color before
         * mixing. This can be used to darken or brighten the selected tonal range.</p>
         *
         * <p>Shadows/mid-tones/highlights adjustment are performed linear space.</p>
         *
         * @return This Builder, for chaining calls
         */
        @NonNull
        public Builder shadowsMidtonesHighlights(float shadowsx, float shadowsy, float shadowsz, float shadowsw, float midtonesx, float midtonesy, float midtonesz, float midtonesw, float highlightsx, float highlightsy, float highlightsz, float highlightsw, float rangesx, float rangesy, float rangesz, float rangesw) {
            nBuilderShadowsMidtonesHighlights(mNativeBuilder, shadowsx, shadowsy, shadowsz, shadowsw, midtonesx, midtonesy, midtonesz, midtonesw, highlightsx, highlightsy, highlightsz, highlightsw, rangesx, rangesy, rangesz, rangesw);
            return this;
        }

        /**
         * Adjusts the colors separately in 3 distinct tonal ranges or zones: shadows, mid-tones, and highlights.
         *
         * <p>The tonal zones are by the ranges parameter: the x and y components define the beginning
         * and end of the transition from shadows to mid-tones, and the z and w components define
         * the beginning and end of the transition from mid-tones to highlights.</p>
         *
         * <p>A smooth transition is applied between the zones which means for instance that the
         * correction color of the shadows range will partially apply to the mid-tones, and the
         * other way around. This ensure smooth visual transitions in the final image.</p>
         *
         * <p>Each correction color is defined as a linear RGB color and a weight. The weight is a
         * value (which may be positive or negative) that is added to the linear RGB color before
         * mixing. This can be used to darken or brighten the selected tonal range.</p>
         *
         * <p>Shadows/mid-tones/highlights adjustment are performed linear space.</p>
         *
         * @param shadows Linear RGB color (.rgb) and weight (.w) to apply to the shadows
         * @param midtones Linear RGB color (.rgb) and weight (.w) to apply to the mid-tones
         * @param highlights Linear RGB color (.rgb) and weight (.w) to apply to the highlights
         * @param ranges Range of the shadows (x and y), and range of the highlights (z and w)
         *
         * @return This Builder, for chaining calls
         */
        @NonNull
        public Builder shadowsMidtonesHighlights(@NonNull @Size(min = 4) float[] shadows, @NonNull @Size(min = 4) float[] midtones, @NonNull @Size(min = 4) float[] highlights, @NonNull @Size(min = 4) float[] ranges) {
            return shadowsMidtonesHighlights(shadows[0], shadows[1], shadows[2], shadows[3], midtones[0], midtones[1], midtones[2], midtones[3], highlights[0], highlights[1], highlights[2], highlights[3], ranges[0], ranges[1], ranges[2], ranges[3]);
        }

        /**
         * Applies a slope, offset, and power, as defined by the ASC CDL (American Society of Cinematographers Color Decision List) to the image.
         *
         * <p>The CDL can be used to adjust the
         * colors of different tonal ranges in the image.</p>
         *
         * <p>The ASC CDL is similar to the lift/gamma/gain controls found in many color grading tools.
         * Lift is equivalent to a combination of offset and slope, gain is equivalent to slope,
         * and gamma is equivalent to power.</p>
         *
         * <p>The slope and power values must be strictly positive. Values less than or equal to 0 will
         * be clamped to a small positive value, offset can be any positive or negative value.</p>
         *
         * <p>Version 1.2 of the ASC CDL adds saturation control, which is here provided as a separate
         * API. See the saturation() method for more information.</p>
         *
         * <p>Slope/offset/power adjustments are performed in log space.</p>
         *
         * @return This Builder, for chaining calls
         */
        @NonNull
        public Builder slopeOffsetPower(float slopex, float slopey, float slopez, float offsetx, float offsety, float offsetz, float powerx, float powery, float powerz) {
            nBuilderSlopeOffsetPower(mNativeBuilder, slopex, slopey, slopez, offsetx, offsety, offsetz, powerx, powery, powerz);
            return this;
        }

        /**
         * Applies a slope, offset, and power, as defined by the ASC CDL (American Society of Cinematographers Color Decision List) to the image.
         *
         * <p>The CDL can be used to adjust the
         * colors of different tonal ranges in the image.</p>
         *
         * <p>The ASC CDL is similar to the lift/gamma/gain controls found in many color grading tools.
         * Lift is equivalent to a combination of offset and slope, gain is equivalent to slope,
         * and gamma is equivalent to power.</p>
         *
         * <p>The slope and power values must be strictly positive. Values less than or equal to 0 will
         * be clamped to a small positive value, offset can be any positive or negative value.</p>
         *
         * <p>Version 1.2 of the ASC CDL adds saturation control, which is here provided as a separate
         * API. See the saturation() method for more information.</p>
         *
         * <p>Slope/offset/power adjustments are performed in log space.</p>
         *
         * @param slope Multiplier of the input color, must be a strictly positive number
         * @param offset Added to the input color, can be a negative or positive number, including 0
         * @param power Power exponent of the input color, must be a strictly positive number
         *
         * @return This Builder, for chaining calls
         */
        @NonNull
        public Builder slopeOffsetPower(@NonNull @Size(min = 3) float[] slope, @NonNull @Size(min = 3) float[] offset, @NonNull @Size(min = 3) float[] power) {
            return slopeOffsetPower(slope[0], slope[1], slope[2], offset[0], offset[1], offset[2], power[0], power[1], power[2]);
        }

        /**
         * Adjusts the contrast of the image.
         *
         * <p>Lower values decrease the contrast of the image
         * (the tonal range is narrowed), and higher values increase the contrast of the image
         * (the tonal range is widened). A value of 1.0 has no effect.</p>
         *
         * <p>The contrast is defined as a value in the range [0.0...2.0]. Values outside of that
         * range will be clipped to that range.</p>
         *
         * <p>Contrast adjustment is performed in log space.</p>
         *
         * @param contrast Contrast expansion, between 0.0 and 2.0. 1.0 leaves contrast unaffected
         *
         * @return This Builder, for chaining calls
         */
        @NonNull
        public Builder contrast(float contrast) {
            nBuilderContrast(mNativeBuilder, contrast);
            return this;
        }

        /**
         * Adjusts the saturation of the image based on the input color's saturation level.
         *
         * <p>Colors with a high level of saturation are less affected than colors with low saturation
         * levels.</p>
         *
         * <p>Lower vibrance values decrease intensity of the colors present in the image, and
         * higher values increase the intensity of the colors in the image. A value of 1.0 has
         * no effect.</p>
         *
         * <p>The vibrance is defined as a value in the range [0.0...2.0]. Values outside of that
         * range will be clipped to that range.</p>
         *
         * <p>Vibrance adjustment is performed in linear space.</p>
         *
         * @param vibrance Vibrance, between 0.0 and 2.0. 1.0 leaves vibrance unaffected
         *
         * @return This Builder, for chaining calls
         */
        @NonNull
        public Builder vibrance(float vibrance) {
            nBuilderVibrance(mNativeBuilder, vibrance);
            return this;
        }

        /**
         * Adjusts the saturation of the image.
         *
         * <p>Lower values decrease intensity of the colors
         * present in the image, and higher values increase the intensity of the colors in the
         * image. A value of 1.0 has no effect.</p>
         *
         * <p>The saturation is defined as a value in the range [0.0...2.0]. Values outside of that
         * range will be clipped to that range.</p>
         *
         * <p>Saturation adjustment is performed in linear space.</p>
         *
         * @param saturation Saturation, between 0.0 and 2.0. 1.0 leaves saturation unaffected
         *
         * @return This Builder, for chaining calls
         */
        @NonNull
        public Builder saturation(float saturation) {
            nBuilderSaturation(mNativeBuilder, saturation);
            return this;
        }

        /**
         * Applies a curve to each RGB channel of the image.
         *
         * <p>Each curve is defined by 3 values:
         * a gamma value applied to the shadows only, a mid-point indicating where shadows stop
         * and highlights start, and a scale factor for the highlights.</p>
         *
         * <p>The gamma and mid-point must be strictly positive values. If they are not, they will be
         * clamped to a small positive value. The scale can be any negative of positive value.</p>
         *
         * <p>Curves are applied in linear space.</p>
         *
         * @return This Builder, for chaining calls
         */
        @NonNull
        public Builder curves(float shadowGammax, float shadowGammay, float shadowGammaz, float midPointx, float midPointy, float midPointz, float highlightScalex, float highlightScaley, float highlightScalez) {
            nBuilderCurves(mNativeBuilder, shadowGammax, shadowGammay, shadowGammaz, midPointx, midPointy, midPointz, highlightScalex, highlightScaley, highlightScalez);
            return this;
        }

        /**
         * Applies a curve to each RGB channel of the image.
         *
         * <p>Each curve is defined by 3 values:
         * a gamma value applied to the shadows only, a mid-point indicating where shadows stop
         * and highlights start, and a scale factor for the highlights.</p>
         *
         * <p>The gamma and mid-point must be strictly positive values. If they are not, they will be
         * clamped to a small positive value. The scale can be any negative of positive value.</p>
         *
         * <p>Curves are applied in linear space.</p>
         *
         * @param shadowGamma Power value to apply to the shadows, must be strictly positive
         * @param midPoint Mid-point defining where shadows stop and highlights start, must be strictly positive
         * @param highlightScale Scale factor for the highlights, can be any negative or positive value
         *
         * @return This Builder, for chaining calls
         */
        @NonNull
        public Builder curves(@NonNull @Size(min = 3) float[] shadowGamma, @NonNull @Size(min = 3) float[] midPoint, @NonNull @Size(min = 3) float[] highlightScale) {
            return curves(shadowGamma[0], shadowGamma[1], shadowGamma[2], midPoint[0], midPoint[1], midPoint[2], highlightScale[0], highlightScale[1], highlightScale[2]);
        }

        /**
         * Specifies a custom 3D color grading LUT to map the final sRGB color.
         *
         * <p>The LUT is applied after post-processing and in LDR (sRGB space).
         * The data must be a 3D array of float3 (RGB) values.
         * The dimension does not need to be a power of two, but must be non-zero.
         * The values are always interpolated (trilinear) because the input color from previous steps is continuous.
         * The dimension doesn't need to match dimensions().
         * If the dimension is 0 or the data is empty, the custom LUT is skipped (ignored).</p>
         *
         * @param data FixedCapacityVector containing the custom LUT data (3D array of float3).
         * @param dimension Dimension of the custom LUT.
         *
         * @return This Builder, for chaining calls
         */
        @NonNull
        public Builder customLut(Buffer data, @IntRange(from = 0) int dimension) {
            nBuilderCustomLut(mNativeBuilder, data, dimension);
            return this;
        }

        /**
         * Hints whether the engine is permitted to use fast mathematical approximations (such as SIMD polynomial transcendentals) during LUT generation when eligible.
         *
         * <p>Setting fastMath to false forces exact C++ scalar libm calculations.
         * The default is true.</p>
         *
         * @param fastMath true to allow fast mathematical approximations, false otherwise.
         *
         * @return This Builder, for chaining calls.
         */
        @NonNull
        public Builder fastMath(boolean fastMath) {
            nBuilderFastMath(mNativeBuilder, fastMath);
            return this;
        }

        /**
         * Creates the ColorGrading object and returns a pointer to it.
         *
         * @param engine Reference to the filament::Engine to associate this ColorGrading with.
         *
         * @return pointer to the newly created object.
         */
        @NonNull
        public ColorGrading build(@NonNull Engine engine) {
            long nativeColorGrading = nBuilderBuild(mNativeBuilder, engine.getNativeObject());
            if (nativeColorGrading == 0) throw new IllegalStateException("Couldn't create ColorGrading");
            return new ColorGrading(nativeColorGrading);
        }

        private static class BuilderFinalizer {
            private final long mNativeObject;

            BuilderFinalizer(long nativeObject) { mNativeObject = nativeObject; }

            @Override
            public void finalize() {
                try {
                    super.finalize();
                } catch (Throwable t) { // Ignore
                } finally {
                    nDestroyBuilder(mNativeObject);
                }
            }
        }
    }

    public long getNativeObject() {
        if (mNativeObject == 0) {
            throw new IllegalStateException("Calling method on destroyed ColorGrading");
        }
        return mNativeObject;
    }

    void clearNativeObject() {
        mNativeObject = 0;
    }

    private static native long nCreateBuilder();
    private static native void nDestroyBuilder(long nativeBuilder);
    private static native void nBuilderQuality(long nativeBuilder, int qualityLevel);
    private static native void nBuilderFormat(long nativeBuilder, int format);
    private static native void nBuilderDimensions(long nativeBuilder, int dim);
    private static native void nBuilderToneMapper(long nativeBuilder, long toneMapper);
    private static native void nBuilderToneMapping(long nativeBuilder, int toneMapping);
    private static native void nBuilderLuminanceScaling(long nativeBuilder, boolean luminanceScaling);
    private static native void nBuilderGamutMapping(long nativeBuilder, boolean gamutMapping);
    private static native void nBuilderExposure(long nativeBuilder, float exposure);
    private static native void nBuilderNightAdaptation(long nativeBuilder, float adaptation);
    private static native void nBuilderWhiteBalance(long nativeBuilder, float temperature, float tint);
    private static native void nBuilderChannelMixer(long nativeBuilder, float outRedx, float outRedy, float outRedz, float outGreenx, float outGreeny, float outGreenz, float outBluex, float outBluey, float outBluez);
    private static native void nBuilderShadowsMidtonesHighlights(long nativeBuilder, float shadowsx, float shadowsy, float shadowsz, float shadowsw, float midtonesx, float midtonesy, float midtonesz, float midtonesw, float highlightsx, float highlightsy, float highlightsz, float highlightsw, float rangesx, float rangesy, float rangesz, float rangesw);
    private static native void nBuilderSlopeOffsetPower(long nativeBuilder, float slopex, float slopey, float slopez, float offsetx, float offsety, float offsetz, float powerx, float powery, float powerz);
    private static native void nBuilderContrast(long nativeBuilder, float contrast);
    private static native void nBuilderVibrance(long nativeBuilder, float vibrance);
    private static native void nBuilderSaturation(long nativeBuilder, float saturation);
    private static native void nBuilderCurves(long nativeBuilder, float shadowGammax, float shadowGammay, float shadowGammaz, float midPointx, float midPointy, float midPointz, float highlightScalex, float highlightScaley, float highlightScalez);
    private static native void nBuilderCustomLut(long nativeBuilder, Buffer data, int dimension);
    private static native void nBuilderFastMath(long nativeBuilder, boolean fastMath);
    private static native long nBuilderBuild(long nativeBuilder, long nativeEngine);
}

// THIS FILE IS GENERATED BY APIGEN AND MUST NOT BE HAND-MODIFIED.
