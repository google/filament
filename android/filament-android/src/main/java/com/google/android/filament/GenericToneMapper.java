/*
 * Copyright (C) 2026 The Android Open Source Project
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *      http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

// THIS FILE IS GENERATED BY APIGEN AND MUST NOT BE HAND-MODIFIED.

package com.google.android.filament;

import java.lang.IllegalStateException;

/**
 * Generic tone mapping operator that gives control over the tone mapping
 *
 * <p>curve. This operator can be used to control the aesthetics of the final
 * image. This operator also allows to control the dynamic range of the
 * scene referred values.</p>
 *
 * <p>The tone mapping curve is defined by 5 parameters:</p>
 *
 * <ul>
 *   <li>contrast: controls the contrast of the curve</li>
 *   <li>midGrayIn: sets the input middle gray</li>
 *   <li>midGrayOut: sets the output middle gray</li>
 *   <li>hdrMax: defines the maximum input value that will be mapped to
 * output white</li>
 * </ul>
 */
public class GenericToneMapper extends ToneMapper {
    /**
     * Builds a new generic tone mapper.
     *
     * <p>The default values of the
     * constructor parameters approximate an ACES tone mapping curve
     * and the maximum input value is set to 10.0.</p>
     */
    public GenericToneMapper() {
        this(1.55f, 0.18f, 0.215f, 10.0f);
    }

    /**
     * Builds a new generic tone mapper.
     *
     * <p>The default values of the
     * constructor parameters approximate an ACES tone mapping curve
     * and the maximum input value is set to 10.0.</p>
     *
     * @param contrast controls the contrast of the curve, must be > 0.0, values
     *                 in the range 0.5..2.0 are recommended.
     */
    public GenericToneMapper(float contrast) {
        this(contrast, 0.18f, 0.215f, 10.0f);
    }

    /**
     * Builds a new generic tone mapper.
     *
     * <p>The default values of the
     * constructor parameters approximate an ACES tone mapping curve
     * and the maximum input value is set to 10.0.</p>
     *
     * @param contrast controls the contrast of the curve, must be > 0.0, values
     *                 in the range 0.5..2.0 are recommended.
     * @param midGrayIn sets the input middle gray, between 0.0 and 1.0.
     */
    public GenericToneMapper(float contrast, float midGrayIn) {
        this(contrast, midGrayIn, 0.215f, 10.0f);
    }

    /**
     * Builds a new generic tone mapper.
     *
     * <p>The default values of the
     * constructor parameters approximate an ACES tone mapping curve
     * and the maximum input value is set to 10.0.</p>
     *
     * @param contrast controls the contrast of the curve, must be > 0.0, values
     *                 in the range 0.5..2.0 are recommended.
     * @param midGrayIn sets the input middle gray, between 0.0 and 1.0.
     * @param midGrayOut sets the output middle gray, between 0.0 and 1.0.
     */
    public GenericToneMapper(float contrast, float midGrayIn, float midGrayOut) {
        this(contrast, midGrayIn, midGrayOut, 10.0f);
    }

    /**
     * Builds a new generic tone mapper.
     *
     * <p>The default values of the
     * constructor parameters approximate an ACES tone mapping curve
     * and the maximum input value is set to 10.0.</p>
     *
     * @param contrast controls the contrast of the curve, must be > 0.0, values
     *                 in the range 0.5..2.0 are recommended.
     * @param midGrayIn sets the input middle gray, between 0.0 and 1.0.
     * @param midGrayOut sets the output middle gray, between 0.0 and 1.0.
     * @param hdrMax defines the maximum input value that will be mapped to
     *               output white. Must be >= 1.0.
     */
    public GenericToneMapper(float contrast, float midGrayIn, float midGrayOut, float hdrMax) {
        super(nCreateGenericToneMapperFloatFloatFloatFloat(contrast, midGrayIn, midGrayOut, hdrMax));
    }

    public boolean isOneDimensional() {
        return nIsOneDimensional(getNativeObject());
    }

    public boolean isLDR() {
        return nIsLDR(getNativeObject());
    }

    /** Returns the contrast of the curve as a strictly positive value. */
    public float getContrast() {
        return nGetContrast(getNativeObject());
    }

    /** Returns the middle gray point for input values as a value between 0.0 and 1.0. */
    public float getMidGrayIn() {
        return nGetMidGrayIn(getNativeObject());
    }

    /** Returns the middle gray point for output values as a value between 0.0 and 1.0. */
    public float getMidGrayOut() {
        return nGetMidGrayOut(getNativeObject());
    }

    /** Returns the maximum input value that will map to output white, as a value >= 1.0. */
    public float getHdrMax() {
        return nGetHdrMax(getNativeObject());
    }

    /** Sets the contrast of the curve, must be > 0.0, values in the range 0.5..2.0 are recommended. */
    public void setContrast(float contrast) {
        nSetContrast(getNativeObject(), contrast);
    }

    /** Sets the input middle gray, between 0.0 and 1.0. */
    public void setMidGrayIn(float midGrayIn) {
        nSetMidGrayIn(getNativeObject(), midGrayIn);
    }

    /** Sets the output middle gray, between 0.0 and 1.0. */
    public void setMidGrayOut(float midGrayOut) {
        nSetMidGrayOut(getNativeObject(), midGrayOut);
    }

    /**
     * Defines the maximum input value that will be mapped to output white.
     *
     * <p>Must be >= 1.0.</p>
     */
    public void setHdrMax(float hdrMax) {
        nSetHdrMax(getNativeObject(), hdrMax);
    }


    private static native long nCreateGenericToneMapperFloatFloatFloatFloat(float contrast, float midGrayIn, float midGrayOut, float hdrMax);
    private static native boolean nIsOneDimensional(long nativeGenericToneMapper);
    private static native boolean nIsLDR(long nativeGenericToneMapper);
    private static native float nGetContrast(long nativeGenericToneMapper);
    private static native float nGetMidGrayIn(long nativeGenericToneMapper);
    private static native float nGetMidGrayOut(long nativeGenericToneMapper);
    private static native float nGetHdrMax(long nativeGenericToneMapper);
    private static native void nSetContrast(long nativeGenericToneMapper, float contrast);
    private static native void nSetMidGrayIn(long nativeGenericToneMapper, float midGrayIn);
    private static native void nSetMidGrayOut(long nativeGenericToneMapper, float midGrayOut);
    private static native void nSetHdrMax(long nativeGenericToneMapper, float hdrMax);
}

// THIS FILE IS GENERATED BY APIGEN AND MUST NOT BE HAND-MODIFIED.
