/*
 * Copyright (C) 2026 The Android Open Source Project
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *      http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

// THIS FILE IS GENERATED BY APIGEN AND MUST NOT BE HAND-MODIFIED.

package com.google.android.filament;

import androidx.annotation.NonNull;
import androidx.annotation.Nullable;
import androidx.annotation.Size;

/** An axis aligned 3D box represented by its center and half-extent. */
public class Box {
    private float mCenterX;
    private float mCenterY;
    private float mCenterZ;
    private float mHalfExtentX;
    private float mHalfExtentY;
    private float mHalfExtentZ;

    public Box() {
    }

    public Box(float centerX, float centerY, float centerZ, float halfExtentX, float halfExtentY, float halfExtentZ) {
        setCenter(centerX, centerY, centerZ);
        setHalfExtent(halfExtentX, halfExtentY, halfExtentZ);
    }

    public Box(@NonNull @Size(min = 3) float[] center, @NonNull @Size(min = 3) float[] halfExtent) {
        setCenter(center);
        setHalfExtent(halfExtent);
    }

    public void setCenter(float centerX, float centerY, float centerZ) {
        mCenterX = centerX;
        mCenterY = centerY;
        mCenterZ = centerZ;
    }

    public void setCenter(@NonNull @Size(min = 3) float[] center) {
        mCenterX = center[0];
        mCenterY = center[1];
        mCenterZ = center[2];
    }

    @NonNull @Size(min = 3)
    public float[] getCenter(@Nullable @Size(min = 3) float[] out) {
        if (out == null) {
            out = new float[3];
        }
        out[0] = mCenterX;
        out[1] = mCenterY;
        out[2] = mCenterZ;
        return out;
    }

    @NonNull @Size(min = 3)
    public float[] getCenter() {
        return getCenter(null);
    }

    public float getCenterX() {
        return mCenterX;
    }

    public void setCenterX(float centerX) {
        mCenterX = centerX;
    }

    public float getCenterY() {
        return mCenterY;
    }

    public void setCenterY(float centerY) {
        mCenterY = centerY;
    }

    public float getCenterZ() {
        return mCenterZ;
    }

    public void setCenterZ(float centerZ) {
        mCenterZ = centerZ;
    }

    public void setHalfExtent(float halfExtentX, float halfExtentY, float halfExtentZ) {
        mHalfExtentX = halfExtentX;
        mHalfExtentY = halfExtentY;
        mHalfExtentZ = halfExtentZ;
    }

    public void setHalfExtent(@NonNull @Size(min = 3) float[] halfExtent) {
        mHalfExtentX = halfExtent[0];
        mHalfExtentY = halfExtent[1];
        mHalfExtentZ = halfExtent[2];
    }

    @NonNull @Size(min = 3)
    public float[] getHalfExtent(@Nullable @Size(min = 3) float[] out) {
        if (out == null) {
            out = new float[3];
        }
        out[0] = mHalfExtentX;
        out[1] = mHalfExtentY;
        out[2] = mHalfExtentZ;
        return out;
    }

    @NonNull @Size(min = 3)
    public float[] getHalfExtent() {
        return getHalfExtent(null);
    }

    public float getHalfExtentX() {
        return mHalfExtentX;
    }

    public void setHalfExtentX(float halfExtentX) {
        mHalfExtentX = halfExtentX;
    }

    public float getHalfExtentY() {
        return mHalfExtentY;
    }

    public void setHalfExtentY(float halfExtentY) {
        mHalfExtentY = halfExtentY;
    }

    public float getHalfExtentZ() {
        return mHalfExtentZ;
    }

    public void setHalfExtentZ(float halfExtentZ) {
        mHalfExtentZ = halfExtentZ;
    }

    /**
     * Whether the box is empty, i.e.: its extents are zero.
     *
     * @return true if the extents of the box are zero
     */
    public boolean isEmpty() {
        return nIsEmpty(mCenterX, mCenterY, mCenterZ, mHalfExtentX, mHalfExtentY, mHalfExtentZ);
    }

    /**
     * Computes the lowest coordinates corner of the box.
     *
     * @return center - halfExtent
     */
    @NonNull @Size(min = 3)
    public float[] getMin(@Nullable @Size(min = 3) float[] out) {
        out = Asserts.assertFloat3(out);
        nGetMin(mCenterX, mCenterY, mCenterZ, mHalfExtentX, mHalfExtentY, mHalfExtentZ, out);
        return out;
    }

    /**
     * Computes the largest coordinates corner of the box.
     *
     * @return center + halfExtent
     */
    @NonNull @Size(min = 3)
    public float[] getMax(@Nullable @Size(min = 3) float[] out) {
        out = Asserts.assertFloat3(out);
        nGetMax(mCenterX, mCenterY, mCenterZ, mHalfExtentX, mHalfExtentY, mHalfExtentZ, out);
        return out;
    }

    /**
     * Initializes the 3D box from its min / max coordinates on each axis
     *
     * @param minx (x component) lowest coordinates corner of the box
     * @param miny (y component) lowest coordinates corner of the box
     * @param minz (z component) lowest coordinates corner of the box
     * @param maxx (x component) largest coordinates corner of the box
     * @param maxy (y component) largest coordinates corner of the box
     * @param maxz (z component) largest coordinates corner of the box
     *
     * @return This bounding box
     */
    public Box set(float minx, float miny, float minz, float maxx, float maxy, float maxz) {
        return nSet(mCenterX, mCenterY, mCenterZ, mHalfExtentX, mHalfExtentY, mHalfExtentZ, minx, miny, minz, maxx, maxy, maxz);
    }

    /**
     * Initializes the 3D box from its min / max coordinates on each axis
     *
     * @param min lowest coordinates corner of the box
     * @param max largest coordinates corner of the box
     *
     * @return This bounding box
     */
    public Box set(@NonNull @Size(min = 3) float[] min, @NonNull @Size(min = 3) float[] max) {
        return set(min[0], min[1], min[2], max[0], max[1], max[2]);
    }

    /**
     * Computes the bounding box of the union of two boxes
     *
     * @param box The box to be combined with
     *
     * @return The bounding box of the union of *this and box
     */
    public Box unionSelf(@NonNull Box box) {
        return nUnionSelf(mCenterX, mCenterY, mCenterZ, mHalfExtentX, mHalfExtentY, mHalfExtentZ, box.getCenterX(), box.getCenterY(), box.getCenterZ(), box.getHalfExtentX(), box.getHalfExtentY(), box.getHalfExtentZ());
    }

    /**
     * Translates the box <i>to</i> a given center position
     *
     * @param trx (x component) position to translate the box to
     * @param try_ (y component) position to translate the box to
     * @param trz (z component) position to translate the box to
     *
     * @return A box centered in {@code tr} with the same extent than *this
     */
    public Box translateTo(float trx, float try_, float trz) {
        return nTranslateTo(mCenterX, mCenterY, mCenterZ, mHalfExtentX, mHalfExtentY, mHalfExtentZ, trx, try_, trz);
    }

    /**
     * Translates the box <i>to</i> a given center position
     *
     * @param tr position to translate the box to
     *
     * @return A box centered in {@code tr} with the same extent than *this
     */
    public Box translateTo(@NonNull @Size(min = 3) float[] tr) {
        return translateTo(tr[0], tr[1], tr[2]);
    }

    /**
     * Computes the smallest bounding sphere of the box.
     *
     * @return The smallest sphere defined by its center (.xyz) and radius (.w) that contains *this
     */
    @NonNull @Size(min = 4)
    public float[] getBoundingSphere(@Nullable @Size(min = 4) float[] out) {
        out = Asserts.assertFloat4(out);
        nGetBoundingSphere(mCenterX, mCenterY, mCenterZ, mHalfExtentX, mHalfExtentY, mHalfExtentZ, out);
        return out;
    }

    /**
     * Transform a Box by a linear transform and a translation.
     *
     * @param m a 3x3 matrix, the linear transform
     * @param tx (x component) a float3, the translation
     * @param ty (y component) a float3, the translation
     * @param tz (z component) a float3, the translation
     * @param box the box to transform
     *
     * @return the bounding box of the transformed box
     */
    public static Box transform(@NonNull @Size(min = 9) float[] m, float tx, float ty, float tz, @NonNull Box box) {
        Asserts.assertMat3fIn(m);
        return nTransform(m, tx, ty, tz, box.getCenterX(), box.getCenterY(), box.getCenterZ(), box.getHalfExtentX(), box.getHalfExtentY(), box.getHalfExtentZ());
    }

    /**
     * Transform a Box by a linear transform and a translation.
     *
     * @param m a 3x3 matrix, the linear transform
     * @param t a float3, the translation
     * @param box the box to transform
     *
     * @return the bounding box of the transformed box
     */
    public static Box transform(float[] m, @NonNull @Size(min = 3) float[] t, Box box) {
        return transform(m, t[0], t[1], t[2], box);
    }


    private static native boolean nIsEmpty(float centerX, float centerY, float centerZ, float halfExtentX, float halfExtentY, float halfExtentZ);
    private static native void nGetMin(float centerX, float centerY, float centerZ, float halfExtentX, float halfExtentY, float halfExtentZ, @Size(min = 3) float[] out);
    private static native void nGetMax(float centerX, float centerY, float centerZ, float halfExtentX, float halfExtentY, float halfExtentZ, @Size(min = 3) float[] out);
    private static native Box nSet(float centerX, float centerY, float centerZ, float halfExtentX, float halfExtentY, float halfExtentZ, float minx, float miny, float minz, float maxx, float maxy, float maxz);
    private static native Box nUnionSelf(float centerX, float centerY, float centerZ, float halfExtentX, float halfExtentY, float halfExtentZ, float boxCenterX, float boxCenterY, float boxCenterZ, float boxHalfExtentX, float boxHalfExtentY, float boxHalfExtentZ);
    private static native Box nTranslateTo(float centerX, float centerY, float centerZ, float halfExtentX, float halfExtentY, float halfExtentZ, float trx, float try_, float trz);
    private static native void nGetBoundingSphere(float centerX, float centerY, float centerZ, float halfExtentX, float halfExtentY, float halfExtentZ, @Size(min = 4) float[] out);
    private static native Box nTransform(@NonNull @Size(min = 9) float[] m, float tx, float ty, float tz, float boxCenterX, float boxCenterY, float boxCenterZ, float boxHalfExtentX, float boxHalfExtentY, float boxHalfExtentZ);
}

// THIS FILE IS GENERATED BY APIGEN AND MUST NOT BE HAND-MODIFIED.
