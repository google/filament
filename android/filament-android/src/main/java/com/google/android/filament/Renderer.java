/*
 * Copyright (C) 2026 The Android Open Source Project
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *      http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

// THIS FILE IS GENERATED BY APIGEN AND MUST NOT BE HAND-MODIFIED.

package com.google.android.filament;

import java.lang.IllegalStateException;

import java.nio.Buffer;
import java.nio.BufferOverflowException;
import java.nio.ReadOnlyBufferException;

import androidx.annotation.IntRange;
import androidx.annotation.NonNull;
import androidx.annotation.Nullable;
import androidx.annotation.RestrictTo;
import androidx.annotation.Size;

/**
 * A Renderer instance represents an operating system's window.
 *
 * <p>Typically, applications create a Renderer per window. The Renderer generates drawing commands
 * for the render thread and manages frame latency.</p>
 *
 * <p>A Renderer generates drawing commands from a View, itself containing a Scene description.</p>
 *
 * <h1>Creation and Destruction</h1>
 *
 * <p>A Renderer is created using Engine.createRenderer() and destroyed using
 * Engine.destroy(const Renderer*).</p>
 *
 * <pre>{@code
 * #include <filament/Renderer.h>
 * #include <filament/Engine.h>
 * using namespace filament;
 *
 * Engine* engine = Engine::create();
 *
 * Renderer* renderer = engine->createRenderer();
 * engine->destroy(&renderer);
 *
 * }</pre>
 *
 * @see Engine
 * @see View
 */
public class Renderer {
    private final Engine mEngine;
    private long mNativeObject;
    private @Nullable DisplayInfo mDisplayInfo;
    private @Nullable FrameRateOptions mFrameRateOptions;
    private @Nullable ClearOptions mClearOptions;

    /**
     * Indicates that the dstSwapChain passed into copyFrame() should be committed after the frame has been copied.
     *
     * @see #copyFrame
     */
    public static final int COMMIT = 0x1;
    /**
     * Indicates that the presentation time should be set on the dstSwapChain passed into copyFrame to the monotonic clock time when the frame is copied.
     *
     * @see #copyFrame
     */
    public static final int SET_PRESENTATION_TIME = 0x2;
    /**
     * Indicates that the dstSwapChain passed into copyFrame() should be cleared to black before the frame is copied into the specified viewport.
     *
     * @see #copyFrame
     */
    public static final int CLEAR = 0x4;

    Renderer(long nativeObject, @NonNull Engine engine) {
        mNativeObject = nativeObject;
        mEngine = engine;
    }

    @NonNull
    @RestrictTo(RestrictTo.Scope.LIBRARY_GROUP)
    public static Renderer wrap(long nativeObject, @NonNull Engine engine) {
        return new Renderer(nativeObject, engine);
    }

    /**
     * Retrieve a history of frame timing information.
     *
     * <p>The maximum frame history size is
     * given by getMaxFrameHistorySize().
     * All or part of the history can be lost when using a different SwapChain in beginFrame().</p>
     *
     * @param outHistory pre-allocated array of FrameInfo.
     *
     * @return A vector of FrameInfo.
     *
     * @see #beginFrame
     */
    public int getFrameInfoHistory(@NonNull FrameInfo[] outHistory) {
        return nGetFrameInfoHistory(getNativeObject(), outHistory);
    }

    /**
     * @return the maximum supported frame history size.
     *
     * @see #getFrameInfoHistory
     */
    @IntRange(from = 0)
    public int getMaxFrameHistorySize() {
        return nGetMaxFrameHistorySize(getNativeObject());
    }

    /**
     * Information about the display this Renderer is associated to.
     *
     * <p>This information is needed
     * to accurately compute dynamic-resolution scaling and for frame-pacing.</p>
     */
    public void setDisplayInfo(@NonNull DisplayInfo info) {
        mDisplayInfo = info;
        nSetDisplayInfo(getNativeObject(), info.getRefreshRate(), info.getPresentationDeadlineNanos(), info.getVsyncOffsetNanos());
    }

    /** Set options controlling the desired frame-rate. */
    public void setFrameRateOptions(@NonNull FrameRateOptions options) {
        mFrameRateOptions = options;
        nSetFrameRateOptions(getNativeObject(), options.getHeadRoomRatio(), options.getScaleRate(), options.getHistory(), options.getInterval());
    }

    /** Set ClearOptions which are used at the beginning of a frame to clear or retain the SwapChain content. */
    public void setClearOptions(@NonNull ClearOptions options) {
        mClearOptions = options;
        nSetClearOptions(getNativeObject(), options.getClearColorX(), options.getClearColorY(), options.getClearColorZ(), options.getClearColorW(), options.getClearStencil(), options.getClear(), options.getDiscard());
    }

    /**
     * Returns the ClearOptions currently set.
     *
     * @return A reference to a ClearOptions structure.
     */
    @NonNull
    public ClearOptions getClearOptions() {
        if (mClearOptions == null) {
            mClearOptions = new ClearOptions();
        }
        return mClearOptions;
    }

    /**
     * Get the Engine that created this Renderer.
     *
     * @return A pointer to the Engine instance this Renderer is associated to.
     */
    @NonNull
    public Engine getEngine() {
        return mEngine;
    }

    /**
     * The use of this method is optional.
     *
     * <p>It sets the VSYNC time expressed as the duration in
     * nanosecond since epoch of std::chrono::steady_clock.
     * If called, passing 0 to vsyncSteadyClockTimeNano in Renderer::beginFrame will use this
     * time instead.</p>
     *
     * @param steadyClockTimeNano duration in nanosecond since epoch of std::chrono::steady_clock
     *
     * @see Engine#getSteadyClockTimeNano
     * @see Renderer#beginFrame
     */
    public void setVsyncTime(@IntRange(from = 0) long steadyClockTimeNano) {
        nSetVsyncTime(getNativeObject(), steadyClockTimeNano);
    }

    /** Call skipFrame when momentarily skipping frames, for instance if the content of the scene doesn't change. */
    public void skipFrame() {
        skipFrame(0);
    }

    /**
     * Call skipFrame when momentarily skipping frames, for instance if the content of the scene doesn't change.
     *
     * @param vsyncSteadyClockTimeNano
     */
    public void skipFrame(@IntRange(from = 0) long vsyncSteadyClockTimeNano) {
        nSkipFrame(getNativeObject(), vsyncSteadyClockTimeNano);
    }

    /**
     * Returns true if the current frame should be rendered.
     *
     * <p>This is a convenience method that returns the same value as beginFrame().</p>
     *
     * <p>This method will return false once a backend exception has been delivered to the main thread.</p>
     *
     * @return <i>false</i> the current frame should be skipped, or an unrecoverable backend exception has occurred.
     *      <i>true</i> the current frame can be rendered
     *
     * @see #beginFrame
     */
    public boolean shouldRenderFrame() {
        return nShouldRenderFrame(getNativeObject());
    }

    /**
     * Set up a frame for this Renderer.
     *
     * <p>beginFrame() manages frame-pacing, and returns whether a frame should be drawn. The
     * goal of this is to skip frames when the GPU falls behind in order to keep the frame
     * latency low.</p>
     *
     * <p>If a given frame takes too much time in the GPU, the CPU will get ahead of the GPU. The
     * display will draw the same frame twice producing a stutter. At this point, the CPU is
     * ahead of the GPU and depending on how many frames are buffered, latency increases.</p>
     *
     * <p>beginFrame() attempts to detect this situation and returns false in that case, indicating
     * to the caller to skip the current frame.</p>
     *
     * <p>When beginFrame() returns true, it is mandatory to render the frame and call endFrame().
     * However, when beginFrame() returns false, the caller has the choice to either skip the
     * frame and not call endFrame(), or proceed as though true was returned.</p>
     *
     * <p>All calls to render() must happen <i>after</i> beginFrame().
     * It is recommended to use the same swapChain for every call to beginFrame, failing to do
     * so can result is losing all or part of the FrameInfo history.</p>
     *
     * <p>This method will return false if called again after a backend exception was already thrown and delivered to the main thread.</p>
     *
     * @param swapChain A pointer to the SwapChain instance to use.
     *
     * @return <p><i>false</i> the current frame should be skipped,
     *      <i>true</i> the current frame must be drawn and endFrame() must be called.</p>
     *
     * <p>@remark
     * When skipping a frame, the whole frame is canceled, and endFrame() must not be called.</p>
     *
     * @see #endFrame
     */
    public boolean beginFrame(@NonNull SwapChain swapChain) {
        return beginFrame(swapChain, 0);
    }

    /**
     * Set up a frame for this Renderer.
     *
     * <p>beginFrame() manages frame-pacing, and returns whether a frame should be drawn. The
     * goal of this is to skip frames when the GPU falls behind in order to keep the frame
     * latency low.</p>
     *
     * <p>If a given frame takes too much time in the GPU, the CPU will get ahead of the GPU. The
     * display will draw the same frame twice producing a stutter. At this point, the CPU is
     * ahead of the GPU and depending on how many frames are buffered, latency increases.</p>
     *
     * <p>beginFrame() attempts to detect this situation and returns false in that case, indicating
     * to the caller to skip the current frame.</p>
     *
     * <p>When beginFrame() returns true, it is mandatory to render the frame and call endFrame().
     * However, when beginFrame() returns false, the caller has the choice to either skip the
     * frame and not call endFrame(), or proceed as though true was returned.</p>
     *
     * <p>All calls to render() must happen <i>after</i> beginFrame().
     * It is recommended to use the same swapChain for every call to beginFrame, failing to do
     * so can result is losing all or part of the FrameInfo history.</p>
     *
     * <p>This method will return false if called again after a backend exception was already thrown and delivered to the main thread.</p>
     *
     * @param swapChain A pointer to the SwapChain instance to use.
     * @param vsyncSteadyClockTimeNano The time in nanosecond of when the current frame started,
     *                                 or 0 if unknown. This value should be the timestamp of
     *                                 the last h/w vsync. It is expressed in the
     *                                 std::chrono::steady_clock time base.
     *                                 On Android this should be the frame time received from
     *                                 a Choreographer.
     *
     * @return <p><i>false</i> the current frame should be skipped,
     *      <i>true</i> the current frame must be drawn and endFrame() must be called.</p>
     *
     * <p>@remark
     * When skipping a frame, the whole frame is canceled, and endFrame() must not be called.</p>
     *
     * @see #endFrame
     */
    public boolean beginFrame(@NonNull SwapChain swapChain, @IntRange(from = 0) long vsyncSteadyClockTimeNano) {
        return nBeginFrame(getNativeObject(), swapChain.getNativeObject(), vsyncSteadyClockTimeNano);
    }

    /**
     * Set the time at which the frame must be presented to the display hardware.
     *
     * <p>This value is used to configure the hardware and must typically be strictly smaller than the
     * desired presentation time (i.e. it must include some headroom but not too much). For instance,
     * on Android, it is typically set to desired_presentation_time - vsync_period / 2. This behavior
     * can vary on other platforms.</p>
     *
     * <p>This must be called before endFrame().</p>
     *
     * @param monotonic_clock_ns the presentation configuration timestamp in nanoseconds on the steady clock.
     */
    public void setPresentationTime(long monotonic_clock_ns) {
        nSetPresentationTime(getNativeObject(), monotonic_clock_ns);
    }

    /**
     * Set the real desired presentation time targeted for this frame.
     *
     * <p>Unlike setPresentationTime(), which configures hardware headroom, this is the exact target
     * presentation time and is used for FrameInfo frame history reporting.</p>
     *
     * <p>This must be called before endFrame().</p>
     *
     * @param monotonic_clock_ns the desired presentation timestamp in nanoseconds on the steady clock.
     */
    public void setDesiredPresentationTime(long monotonic_clock_ns) {
        nSetDesiredPresentationTime(getNativeObject(), monotonic_clock_ns);
    }

    /**
     * Set the deadline time point on the steady clock by which CPU and GPU rendering must complete for the buffer to meet its target display latching window.
     *
     * <p>This must be called before endFrame().</p>
     *
     * @param monotonic_clock_ns the deadline timestamp in nanoseconds on the steady clock.
     */
    public void setRenderingDeadline(long monotonic_clock_ns) {
        nSetRenderingDeadline(getNativeObject(), monotonic_clock_ns);
    }

    /**
     * Render a View into this renderer's window.
     *
     * <p>This is filament main rendering method, most of the CPU-side heavy lifting is performed
     * here. render() main function is to generate render commands which are asynchronously
     * executed by the Engine's render thread.</p>
     *
     * <p>render() generates commands for each of the following stages:</p>
     *
     * <ol>
     *   <li>Shadow map passes, if needed.</li>
     *   <li>Depth pre-pass.</li>
     *   <li>Color pass.</li>
     *   <li>Post-processing pass.
     * A typical render loop looks like this:</li>
     * </ol>
     *
     * <pre>{@code
     * #include <filament/Renderer.h>
     * #include <filament/View.h>
     * using namespace filament;
     *
     * void renderLoop(Renderer* renderer, SwapChain* swapChain) {
     *     do {
     *         // typically we wait for VSYNC and user input events
     *         if (renderer->beginFrame(swapChain)) {
     *             renderer->render(mView);
     *             renderer->endFrame();
     *         }
     *     } while (!quit());
     * }
     *
     * }</pre>
     *
     * <p>render() must be called from the Engine's main thread (or external synchronization
     * must be provided). In particular, calls to render() on different Renderer instances
     * <b>must</b> be synchronized.</p>
     *
     * @param view <p>A pointer to the view to render.</p>
     *
     * <p>@attention
     * render() must be called <i>after</i> beginFrame() and <i>before</i> endFrame().</p>
     *
     * <p>@remark
     * render() perform potentially heavy computations and cannot be multi-threaded. However,
     * internally, render() is highly multi-threaded to both improve performance in mitigate
     * the call's latency.</p>
     *
     * <p>@remark
     * render() is typically called once per frame (but not necessarily).</p>
     *
     * @see #beginFrame
     * @see #endFrame
     * @see View
     */
    public void render(@NonNull View view) {
        nRender(getNativeObject(), view.getNativeObject());
    }

    /**
     * Copy the currently rendered view to the indicated swap chain, using the indicated source and destination rectangle.
     *
     * @param dstSwapChain The swap chain into which the frame should be copied.
     * @param dstViewport The destination rectangle in which to draw the view.
     * @param srcViewport The source rectangle to be copied.
     */
    public void copyFrame(@NonNull SwapChain dstSwapChain, @NonNull Viewport dstViewport, @NonNull Viewport srcViewport) {
        copyFrame(dstSwapChain, dstViewport, srcViewport, 0);
    }

    /**
     * Copy the currently rendered view to the indicated swap chain, using the indicated source and destination rectangle.
     *
     * @param dstSwapChain The swap chain into which the frame should be copied.
     * @param dstViewport The destination rectangle in which to draw the view.
     * @param srcViewport The source rectangle to be copied.
     * @param flags <p>One or more CopyFrameFlag behavior configuration flags.</p>
     *
     * <p>@remark
     * copyFrame() should be called after a frame is rendered using render()
     * but before endFrame() is called.</p>
     */
    public void copyFrame(@NonNull SwapChain dstSwapChain, @NonNull Viewport dstViewport, @NonNull Viewport srcViewport, @IntRange(from = 0) int flags) {
        nCopyFrame(getNativeObject(), dstSwapChain.getNativeObject(), dstViewport.getLeft(), dstViewport.getBottom(), dstViewport.getWidth(), dstViewport.getHeight(), srcViewport.getLeft(), srcViewport.getBottom(), srcViewport.getWidth(), srcViewport.getHeight(), flags);
    }

    /**
     * Reads back the content of the SwapChain associated with this Renderer.
     *
     * @param xoffset Left offset of the sub-region to read back.
     * @param yoffset Bottom offset of the sub-region to read back.
     * @param width Width of the sub-region to read back.
     * @param height Height of the sub-region to read back.
     * @param buffer <p>Client-side buffer where the read-back will be written.</p>
     *
     * <p>The following formats are always supported:</p>
     *
     * <ul>
     *   <li>PixelBufferDescriptor::PixelDataFormat::RGBA</li>
     *   <li>PixelBufferDescriptor::PixelDataFormat::RGBA_INTEGER
     * The following types are always supported:</li>
     *   <li>PixelBufferDescriptor::PixelDataType::UBYTE</li>
     *   <li>PixelBufferDescriptor::PixelDataType::UINT</li>
     *   <li>PixelBufferDescriptor::PixelDataType::INT</li>
     *   <li>PixelBufferDescriptor::PixelDataType::FLOAT
     * Other combinations of format/type may be supported. If a combination is
     * not supported, this operation may fail silently. Use a DEBUG build
     * to get some logs about the failure.
     * Framebuffer as seen on User buffer (PixelBufferDescriptor&)
     * screen
     * +--------------------+</li>
     * </ul>
     *
     * <table summary="">
     *   <tr>
     *     <th style="text-align: left"></th>
     *     <th style="text-align: left"></th>
     *     <th style="text-align: left">.stride         .alignment</th>
     *   </tr>
     *   <tr>
     *     <td style="text-align: left"></td>
     *     <td style="text-align: left">O----------------------+--+   low addresses</td>
     *   </tr>
     *   <tr>
     *     <td style="text-align: left"></td>
     *     <td style="text-align: left"></td>
     *     <td style="text-align: left"></td>
     *     <td></td>
     *     <td></td>
     *   </tr>
     *   <tr>
     *     <td style="text-align: left">w</td>
     *     <td style="text-align: left"></td>
     *     <td style="text-align: left"></td>
     *     <td>.top</td>
     *     <td></td>
     *   </tr>
     *   <tr>
     *     <td style="text-align: left"><---------></td>
     *     <td style="text-align: left"></td>
     *     <td style="text-align: left">V</td>
     *     <td></td>
     *   </tr>
     *   <tr>
     *     <td style="text-align: left">+---------+</td>
     *     <td style="text-align: left"></td>
     *     <td style="text-align: left">+---------+</td>
     *     <td></td>
     *   </tr>
     *   <tr>
     *     <td style="text-align: left"></td>
     *     <td style="text-align: left">^</td>
     *     <td style="text-align: left"></td>
     *     <td>======></td>
     *     <td></td>
     *     <td></td>
     *     <td></td>
     *     <td></td>
     *   </tr>
     *   <tr>
     *     <td style="text-align: left">x</td>
     *     <td style="text-align: left">h</td>
     *     <td style="text-align: left"></td>
     *     <td></td>
     *     <td></td>
     *     <td>.left</td>
     *     <td></td>
     *     <td></td>
     *     <td></td>
     *   </tr>
     *   <tr>
     *     <td style="text-align: left">+------></td>
     *     <td style="text-align: left">v</td>
     *     <td style="text-align: left"></td>
     *     <td>+----></td>
     *     <td></td>
     *     <td></td>
     *     <td></td>
     *   </tr>
     *   <tr>
     *     <td style="text-align: left">+.........+</td>
     *     <td style="text-align: left"></td>
     *     <td style="text-align: left">+.........+</td>
     *     <td></td>
     *   </tr>
     *   <tr>
     *     <td style="text-align: left">^</td>
     *     <td style="text-align: left"></td>
     *     <td style="text-align: left"></td>
     *     <td></td>
     *   </tr>
     *   <tr>
     *     <td style="text-align: left">y</td>
     *     <td style="text-align: left"></td>
     *     <td style="text-align: left">+----------------------+--+  high addresses</td>
     *   </tr>
     *   <tr>
     *     <td style="text-align: left">O------------+-------+</td>
     *   </tr>
     * </table>
     *
     * <p>readPixels() must be called within a frame, meaning after beginFrame() and before endFrame().
     * Typically, readPixels() will be called after render().</p>
     *
     * <p>After issuing this method, the callback associated with {@code buffer} will be invoked on the
     * main thread, indicating that the read-back has completed. Typically, this will happen
     * after multiple calls to beginFrame(), render(), endFrame().</p>
     *
     * <p>It is also possible to use a Fence to wait for the read-back.</p>
     *
     * <p>@remark
     * readPixels() is intended for debugging and testing. It will impact performance significantly.</p>
     */
    public void readPixels(@IntRange(from = 0) int xoffset, @IntRange(from = 0) int yoffset, @IntRange(from = 0) int width, @IntRange(from = 0) int height, @NonNull PixelBufferDescriptor buffer) {
        if (buffer.storage.isReadOnly()) {
            throw new ReadOnlyBufferException();
        }
        int type = (buffer.type != null) ? buffer.type.ordinal() : 0;
        int format = (buffer.type == Texture.Type.COMPRESSED) ?
                (buffer.compressedFormat != null ? buffer.compressedFormat.ordinal() : 0) :
                (buffer.format != null ? buffer.format.ordinal() : 0);
        int stride = (buffer.type == Texture.Type.COMPRESSED) ? buffer.compressedSizeInBytes : buffer.stride;
        nReadPixels(getNativeObject(), xoffset, yoffset, width, height, buffer.storage, buffer.storage.remaining(),
                buffer.left, buffer.top, type, buffer.alignment,
                stride, format,
                buffer.handler, buffer.callback);
    }

    /**
     * Finishes the current frame and schedules it for display.
     *
     * <p>endFrame() schedules the current frame to be displayed on the Renderer's window.</p>
     *
     * <p>All calls to render() must happen <i>before</i> endFrame(). endFrame() must be called if
     * beginFrame() returned true, otherwise, endFrame() must not be called unless the caller
     * ignored beginFrame()'s return value.</p>
     *
     * @see #beginFrame
     */
    public void endFrame() {
        nEndFrame(getNativeObject());
    }

    /**
     * Reads back the content of the provided RenderTarget.
     *
     * @param renderTarget RenderTarget to read back from.
     * @param xoffset Left offset of the sub-region to read back.
     * @param yoffset Bottom offset of the sub-region to read back.
     * @param width Width of the sub-region to read back.
     * @param height Height of the sub-region to read back.
     * @param buffer <p>Client-side buffer where the read-back will be written.</p>
     *
     * <p>The following formats are always supported:</p>
     *
     * <ul>
     *   <li>PixelBufferDescriptor::PixelDataFormat::RGBA</li>
     *   <li>PixelBufferDescriptor::PixelDataFormat::RGBA_INTEGER
     * The following types are always supported:</li>
     *   <li>PixelBufferDescriptor::PixelDataType::UBYTE</li>
     *   <li>PixelBufferDescriptor::PixelDataType::UINT</li>
     *   <li>PixelBufferDescriptor::PixelDataType::INT</li>
     *   <li>PixelBufferDescriptor::PixelDataType::FLOAT
     * Other combinations of format/type may be supported. If a combination is
     * not supported, this operation may fail silently. Use a DEBUG build
     * to get some logs about the failure.
     * Framebuffer as seen on User buffer (PixelBufferDescriptor&)
     * screen
     * +--------------------+</li>
     * </ul>
     *
     * <table summary="">
     *   <tr>
     *     <th style="text-align: left"></th>
     *     <th style="text-align: left"></th>
     *     <th style="text-align: left">.stride         .alignment</th>
     *   </tr>
     *   <tr>
     *     <td style="text-align: left"></td>
     *     <td style="text-align: left">O----------------------+--+   low addresses</td>
     *   </tr>
     *   <tr>
     *     <td style="text-align: left"></td>
     *     <td style="text-align: left"></td>
     *     <td style="text-align: left"></td>
     *     <td></td>
     *     <td></td>
     *   </tr>
     *   <tr>
     *     <td style="text-align: left">w</td>
     *     <td style="text-align: left"></td>
     *     <td style="text-align: left"></td>
     *     <td>.top</td>
     *     <td></td>
     *   </tr>
     *   <tr>
     *     <td style="text-align: left"><---------></td>
     *     <td style="text-align: left"></td>
     *     <td style="text-align: left">V</td>
     *     <td></td>
     *   </tr>
     *   <tr>
     *     <td style="text-align: left">+---------+</td>
     *     <td style="text-align: left"></td>
     *     <td style="text-align: left">+---------+</td>
     *     <td></td>
     *   </tr>
     *   <tr>
     *     <td style="text-align: left"></td>
     *     <td style="text-align: left">^</td>
     *     <td style="text-align: left"></td>
     *     <td>======></td>
     *     <td></td>
     *     <td></td>
     *     <td></td>
     *     <td></td>
     *   </tr>
     *   <tr>
     *     <td style="text-align: left">x</td>
     *     <td style="text-align: left">h</td>
     *     <td style="text-align: left"></td>
     *     <td></td>
     *     <td></td>
     *     <td>.left</td>
     *     <td></td>
     *     <td></td>
     *     <td></td>
     *   </tr>
     *   <tr>
     *     <td style="text-align: left">+------></td>
     *     <td style="text-align: left">v</td>
     *     <td style="text-align: left"></td>
     *     <td>+----></td>
     *     <td></td>
     *     <td></td>
     *     <td></td>
     *   </tr>
     *   <tr>
     *     <td style="text-align: left">+.........+</td>
     *     <td style="text-align: left"></td>
     *     <td style="text-align: left">+.........+</td>
     *     <td></td>
     *   </tr>
     *   <tr>
     *     <td style="text-align: left">^</td>
     *     <td style="text-align: left"></td>
     *     <td style="text-align: left"></td>
     *     <td></td>
     *   </tr>
     *   <tr>
     *     <td style="text-align: left">y</td>
     *     <td style="text-align: left"></td>
     *     <td style="text-align: left">+----------------------+--+  high addresses</td>
     *   </tr>
     *   <tr>
     *     <td style="text-align: left">O------------+-------+</td>
     *   </tr>
     * </table>
     *
     * <p>Typically readPixels() will be called after render() and before endFrame().</p>
     *
     * <p>After issuing this method, the callback associated with {@code buffer} will be invoked on the
     * main thread, indicating that the read-back has completed. Typically, this will happen
     * after multiple calls to beginFrame(), render(), endFrame().</p>
     *
     * <p>It is also possible to use a Fence to wait for the read-back.</p>
     *
     * <p>OpenGL only: if issuing a readPixels on a RenderTarget backed by a Texture that had data
     * uploaded to it via setImage, the data returned from readPixels will be y-flipped with respect
     * to the setImage call.</p>
     *
     * <p>Note: the texture that backs the COLOR attachment for {@code renderTarget} must have
     * TextureUsage::BLIT_SRC as part of its usage.</p>
     *
     * <p>@remark
     * readPixels() is intended for debugging and testing. It will impact performance significantly.</p>
     */
    public void readPixels(@NonNull RenderTarget renderTarget, @IntRange(from = 0) int xoffset, @IntRange(from = 0) int yoffset, @IntRange(from = 0) int width, @IntRange(from = 0) int height, @NonNull PixelBufferDescriptor buffer) {
        if (buffer.storage.isReadOnly()) {
            throw new ReadOnlyBufferException();
        }
        int type = (buffer.type != null) ? buffer.type.ordinal() : 0;
        int format = (buffer.type == Texture.Type.COMPRESSED) ?
                (buffer.compressedFormat != null ? buffer.compressedFormat.ordinal() : 0) :
                (buffer.format != null ? buffer.format.ordinal() : 0);
        int stride = (buffer.type == Texture.Type.COMPRESSED) ? buffer.compressedSizeInBytes : buffer.stride;
        nReadPixels(getNativeObject(), renderTarget.getNativeObject(), xoffset, yoffset, width, height, buffer.storage, buffer.storage.remaining(),
                buffer.left, buffer.top, type, buffer.alignment,
                stride, format,
                buffer.handler, buffer.callback);
    }

    /**
     * Render a standalone View into its associated RenderTarget
     *
     * <p>This call is mostly equivalent to calling render(View*) inside a
     * beginFrame / endFrame block, but incurs less overhead. It can be used
     * as a poor man's compute API.</p>
     *
     * <p>renderStandaloneView() must be called from the Engine's main thread
     * (or external synchronization must be provided). In particular, calls to
     * renderStandaloneView() on different Renderer instances <b>must</b> be synchronized.</p>
     *
     * @param view <p>A pointer to the view to render. This View must have a RenderTarget associated
     *             to it.</p>
     *
     * <p>@attention
     * renderStandaloneView() must be called outside of beginFrame() / endFrame().</p>
     *
     * <p>@remark
     * renderStandaloneView() perform potentially heavy computations and cannot be multi-threaded.
     * However, internally, renderStandaloneView() is highly multi-threaded to both improve
     * performance in mitigate the call's latency.</p>
     */
    public void renderStandaloneView(@NonNull View view) {
        nRenderStandaloneView(getNativeObject(), view.getNativeObject());
    }

    /**
     * Returns the material time in seconds evaluated for the current frame.
     *
     * <p>This value is constant for all
     * views rendered during a frame. When available, this time is projected forward to the predicted
     * presentation time on the display; otherwise, it evaluates at the vsync time of beginFrame().
     * The epoch is set with setMaterialTimeEpoch().</p>
     *
     * <p>In materials, this value can be queried using {@code vec4 getUserTime()}. The value returned
     * is a highp vec4 encoded as follows:</p>
     *
     * <p>time.x = (float)Renderer.getMaterialTime();
     *      time.y = Renderer.getMaterialTime() - time.x;</p>
     *
     * <p>It follows that the following invariants are true:</p>
     *
     * <p>(double)time.x + (double)time.y == Renderer.getMaterialTime()
     *      time.x == (float)Renderer.getMaterialTime()</p>
     *
     * <p>This "float-float" encoding allows the shader code to perform high precision (i.e. double) time
     * calculations when needed despite the lack of double precision in the shader (e.g. using Dekker's
     * algorithms). For example, to compute (double)time * vertex in the material, use the following construct:</p>
     *
     * <p>vec3 result = time.x <i> vertex + time.y </i> vertex;</p>
     *
     * <p>Most of the time, high precision computations are not required, but be aware that the
     * precision of time.x rapidly diminishes as time passes:</p>
     *
     * <p>time    | precision
     *          --------+----------</p>
     *
     * <ol>
     *   <li>7s   |    us
     * 4h39    |    ms
     * 77h      |   1/60s
     * In other words, it is only possible to get microsecond accuracy for about 16s or millisecond
     * accuracy for just under 5h.
     * This problem can be mitigated by calling setMaterialTimeEpoch(), or using high precision time as
     * described above.</li>
     * </ol>
     *
     * @return The time in seconds since setMaterialTimeEpoch() was last called.
     *
     * @see #setMaterialTimeEpoch
     */
    public double getMaterialTime() {
        return nGetMaterialTime(getNativeObject());
    }

    /**
     * Backward compatibility helper for getUserTime().
     *
     * @deprecated Use getMaterialTime() instead.
     */
    public double getUserTime() {
        return nGetUserTime(getNativeObject());
    }

    /**
     * Sets the material time epoch to the specified steady clock timestamp in nanoseconds, i.e. resets the material time to zero relative to that time.
     *
     * <p>Use this method to keep the precision of time high in materials, in practice it should
     * be called at least when the application is paused, e.g. Activity.onPause() in Android.</p>
     *
     * @param monotonic_clock_ns the steady clock timestamp in nanoseconds to set as the material time epoch.
     *
     * @see #getMaterialTime
     */
    public void setMaterialTimeEpoch(long monotonic_clock_ns) {
        nSetMaterialTimeEpoch(getNativeObject(), monotonic_clock_ns);
    }

    /**
     * Backward compatibility helper for resetUserTime().
     *
     * @deprecated Use setMaterialTimeEpoch() instead.
     */
    public void resetUserTime() {
        nResetUserTime(getNativeObject());
    }

    /**
     * Requests the next frameCount frames to be skipped.
     *
     * <p>For Debugging.</p>
     *
     * @param frameCount number of frames to skip.
     */
    public void skipNextFrames(@IntRange(from = 0) int frameCount) {
        nSkipNextFrames(getNativeObject(), frameCount);
    }

    /**
     * Remainder count of frame to be skipped
     *
     * @return remaining frames to be skipped
     */
    @IntRange(from = 0)
    public int getFrameToSkipCount() {
        return nGetFrameToSkipCount(getNativeObject());
    }

    /**
     * Queries whether the GPU execution has fallen behind the CPU rendering execution.
     *
     * <p>This is highly useful when managing the application's presentation loop manually (e.g.
     * with the {@code FramePacer}), allowing the client to proactively detect and react to a latency build-up
     * before continuing with frame execution.</p>
     *
     * @return true if the GPU pipeline is delayed, false if ready.
     */
    public boolean hasGpuFallenBehind() {
        return nHasGpuFallenBehind(getNativeObject());
    }

    /**
     * Sets the physical clock time when the frame scheduling callback was entered.
     *
     * <p>This is used by the frame pacer and pipeline estimator to accurately
     * measure the active CPU duration (including app logic running before beginFrame).</p>
     *
     * @param time Monotonic steady clock time_point.
     */
    public void setFrameScheduleTime(@IntRange(from = 0) long time) {
        nSetFrameScheduleTime(getNativeObject(), time);
    }

    /**
     * Stalls the render thread (GPU submission pipeline) for the given duration in nanoseconds.
     *
     * <p>This is useful for simulating long rendering frames (e.g. testing buffer stuffing recovery)
     * without blocking the application's main event loop thread.</p>
     *
     * @param duration_ns the duration to pause the render thread in nanoseconds.
     */
    public void pauseRenderThread(@IntRange(from = 0) long duration_ns) {
        nPauseRenderThread(getNativeObject(), duration_ns);
    }

    @NonNull
    public DisplayInfo getDisplayInfo() {
        if (mDisplayInfo == null) {
            mDisplayInfo = new DisplayInfo();
        }
        return mDisplayInfo;
    }

    @NonNull
    public FrameRateOptions getFrameRateOptions() {
        if (mFrameRateOptions == null) {
            mFrameRateOptions = new FrameRateOptions();
        }
        return mFrameRateOptions;
    }

    public long getNativeObject() {
        if (mNativeObject == 0) {
            throw new IllegalStateException("Calling method on destroyed Renderer");
        }
        return mNativeObject;
    }

    void clearNativeObject() {
        mNativeObject = 0;
    }
    /**
     * Use DisplayInfo to set important Display properties.
     *
     * <p>This is used to achieve correct
     * frame pacing and dynamic resolution scaling.</p>
     */
    public static class DisplayInfo {
        public float refreshRate;
        @IntRange(from = 0)
        public long presentationDeadlineNanos;
        @IntRange(from = 0)
        public long vsyncOffsetNanos;

        public DisplayInfo() {
        }

        public DisplayInfo(float refreshRate, @IntRange(from = 0) long presentationDeadlineNanos, @IntRange(from = 0) long vsyncOffsetNanos) {
            setRefreshRate(refreshRate);
            setPresentationDeadlineNanos(presentationDeadlineNanos);
            setVsyncOffsetNanos(vsyncOffsetNanos);
        }

        public void setRefreshRate(float refreshRate) {
            this.refreshRate = refreshRate;
        }

        public float getRefreshRate() {
            return refreshRate;
        }

        public void setPresentationDeadlineNanos(@IntRange(from = 0) long presentationDeadlineNanos) {
            this.presentationDeadlineNanos = presentationDeadlineNanos;
        }

        @IntRange(from = 0) public long getPresentationDeadlineNanos() {
            return presentationDeadlineNanos;
        }

        public void setVsyncOffsetNanos(@IntRange(from = 0) long vsyncOffsetNanos) {
            this.vsyncOffsetNanos = vsyncOffsetNanos;
        }

        @IntRange(from = 0) public long getVsyncOffsetNanos() {
            return vsyncOffsetNanos;
        }

    }

    /**
     * Timing information about a frame
     *
     * @see #getFrameInfoHistory
     */
    public static class FrameInfo {
        /** value not supported */
        public static final long INVALID = - 1;
        /** value not yet available */
        public static final long PENDING = - 2;

        @IntRange(from = 0)
        public int frameId;
        public long gpuFrameDuration;
        public long denoisedGpuFrameDuration;
        public long beginFrame;
        public long endFrame;
        public long backendBeginFrame;
        public long backendEndFrame;
        public long gpuFrameComplete;
        public long vsync;
        public long displayPresent;
        public long presentDeadline;
        public long displayPresentInterval;
        public long compositionToPresentLatency;
        public long expectedPresentLatency;
        public long frameScheduleTime;

        public FrameInfo() {
        }

        public FrameInfo(@IntRange(from = 0) int frameId, long gpuFrameDuration, long denoisedGpuFrameDuration, long beginFrame, long endFrame, long backendBeginFrame, long backendEndFrame, long gpuFrameComplete, long vsync, long displayPresent, long presentDeadline, long displayPresentInterval, long compositionToPresentLatency, long expectedPresentLatency, long frameScheduleTime) {
            setFrameId(frameId);
            setGpuFrameDuration(gpuFrameDuration);
            setDenoisedGpuFrameDuration(denoisedGpuFrameDuration);
            setBeginFrame(beginFrame);
            setEndFrame(endFrame);
            setBackendBeginFrame(backendBeginFrame);
            setBackendEndFrame(backendEndFrame);
            setGpuFrameComplete(gpuFrameComplete);
            setVsync(vsync);
            setDisplayPresent(displayPresent);
            setPresentDeadline(presentDeadline);
            setDisplayPresentInterval(displayPresentInterval);
            setCompositionToPresentLatency(compositionToPresentLatency);
            setExpectedPresentLatency(expectedPresentLatency);
            setFrameScheduleTime(frameScheduleTime);
        }

        public void setFrameId(@IntRange(from = 0) int frameId) {
            this.frameId = frameId;
        }

        @IntRange(from = 0) public int getFrameId() {
            return frameId;
        }

        public void setGpuFrameDuration(long gpuFrameDuration) {
            this.gpuFrameDuration = gpuFrameDuration;
        }

        public long getGpuFrameDuration() {
            return gpuFrameDuration;
        }

        public void setDenoisedGpuFrameDuration(long denoisedGpuFrameDuration) {
            this.denoisedGpuFrameDuration = denoisedGpuFrameDuration;
        }

        public long getDenoisedGpuFrameDuration() {
            return denoisedGpuFrameDuration;
        }

        public void setBeginFrame(long beginFrame) {
            this.beginFrame = beginFrame;
        }

        public long getBeginFrame() {
            return beginFrame;
        }

        public void setEndFrame(long endFrame) {
            this.endFrame = endFrame;
        }

        public long getEndFrame() {
            return endFrame;
        }

        public void setBackendBeginFrame(long backendBeginFrame) {
            this.backendBeginFrame = backendBeginFrame;
        }

        public long getBackendBeginFrame() {
            return backendBeginFrame;
        }

        public void setBackendEndFrame(long backendEndFrame) {
            this.backendEndFrame = backendEndFrame;
        }

        public long getBackendEndFrame() {
            return backendEndFrame;
        }

        public void setGpuFrameComplete(long gpuFrameComplete) {
            this.gpuFrameComplete = gpuFrameComplete;
        }

        public long getGpuFrameComplete() {
            return gpuFrameComplete;
        }

        public void setVsync(long vsync) {
            this.vsync = vsync;
        }

        public long getVsync() {
            return vsync;
        }

        public void setDisplayPresent(long displayPresent) {
            this.displayPresent = displayPresent;
        }

        public long getDisplayPresent() {
            return displayPresent;
        }

        public void setPresentDeadline(long presentDeadline) {
            this.presentDeadline = presentDeadline;
        }

        public long getPresentDeadline() {
            return presentDeadline;
        }

        public void setDisplayPresentInterval(long displayPresentInterval) {
            this.displayPresentInterval = displayPresentInterval;
        }

        public long getDisplayPresentInterval() {
            return displayPresentInterval;
        }

        public void setCompositionToPresentLatency(long compositionToPresentLatency) {
            this.compositionToPresentLatency = compositionToPresentLatency;
        }

        public long getCompositionToPresentLatency() {
            return compositionToPresentLatency;
        }

        public void setExpectedPresentLatency(long expectedPresentLatency) {
            this.expectedPresentLatency = expectedPresentLatency;
        }

        public long getExpectedPresentLatency() {
            return expectedPresentLatency;
        }

        public void setFrameScheduleTime(long frameScheduleTime) {
            this.frameScheduleTime = frameScheduleTime;
        }

        public long getFrameScheduleTime() {
            return frameScheduleTime;
        }

    }

    /**
     * Use FrameRateOptions to set the desired frame rate and control how quickly the system reacts to GPU load changes.
     *
     * <p>interval: desired frame interval in multiple of the refresh period, set in DisplayInfo
     *           (as 1 / DisplayInfo::refreshRate)</p>
     *
     * <p>The parameters below are relevant when some Views are using dynamic resolution scaling:</p>
     *
     * <p>headRoomRatio: additional headroom for the GPU as a ratio of the targetFrameTime.
     *                Useful for taking into account constant costs like post-processing or
     *                GPU drivers on different platforms.
     * history:   History size. higher values, tend to filter more (clamped to 31)
     * scaleRate: rate at which the gpu load is adjusted to reach the target frame rate
     *            This value can be computed as 1 / N, where N is the number of frames
     *            needed to reach 64% of the target scale factor.
     *            Higher values make the dynamic resolution react faster.</p>
     *
     * @see View.DynamicResolutionOptions
     * @see Renderer.DisplayInfo
     */
    public static class FrameRateOptions {
        public float headRoomRatio;
        public float scaleRate;
        @IntRange(from = 0)
        public int history;
        @IntRange(from = 0)
        public int interval;

        public FrameRateOptions() {
        }

        public FrameRateOptions(float headRoomRatio, float scaleRate, @IntRange(from = 0) int history, @IntRange(from = 0) int interval) {
            setHeadRoomRatio(headRoomRatio);
            setScaleRate(scaleRate);
            setHistory(history);
            setInterval(interval);
        }

        public void setHeadRoomRatio(float headRoomRatio) {
            this.headRoomRatio = headRoomRatio;
        }

        public float getHeadRoomRatio() {
            return headRoomRatio;
        }

        public void setScaleRate(float scaleRate) {
            this.scaleRate = scaleRate;
        }

        public float getScaleRate() {
            return scaleRate;
        }

        public void setHistory(@IntRange(from = 0) int history) {
            this.history = history;
        }

        @IntRange(from = 0) public int getHistory() {
            return history;
        }

        public void setInterval(@IntRange(from = 0) int interval) {
            this.interval = interval;
        }

        @IntRange(from = 0) public int getInterval() {
            return interval;
        }

    }

    /** ClearOptions are used at the beginning of a frame to clear or retain the SwapChain content. */
    public static class ClearOptions {
        private double mClearColorX;
        private double mClearColorY;
        private double mClearColorZ;
        private double mClearColorW;
        @IntRange(from = 0)
        public int clearStencil;
        public boolean clear;
        public boolean discard;

        public ClearOptions() {
        }

        public ClearOptions(double clearColorX, double clearColorY, double clearColorZ, double clearColorW, @IntRange(from = 0) int clearStencil, boolean clear, boolean discard) {
            setClearColor(clearColorX, clearColorY, clearColorZ, clearColorW);
            setClearStencil(clearStencil);
            setClear(clear);
            setDiscard(discard);
        }

        public ClearOptions(@NonNull @Size(min = 4) double[] clearColor, @IntRange(from = 0) int clearStencil, boolean clear, boolean discard) {
            setClearColor(clearColor);
            setClearStencil(clearStencil);
            setClear(clear);
            setDiscard(discard);
        }

        public void setClearColor(double clearColorX, double clearColorY, double clearColorZ, double clearColorW) {
            mClearColorX = clearColorX;
            mClearColorY = clearColorY;
            mClearColorZ = clearColorZ;
            mClearColorW = clearColorW;
        }

        public void setClearColor(@NonNull @Size(min = 4) double[] clearColor) {
            mClearColorX = clearColor[0];
            mClearColorY = clearColor[1];
            mClearColorZ = clearColor[2];
            mClearColorW = clearColor[3];
        }

        @NonNull @Size(min = 4)
        public double[] getClearColor(@Nullable @Size(min = 4) double[] out) {
            if (out == null) {
                out = new double[4];
            }
            out[0] = mClearColorX;
            out[1] = mClearColorY;
            out[2] = mClearColorZ;
            out[3] = mClearColorW;
            return out;
        }

        @NonNull @Size(min = 4)
        public double[] getClearColor() {
            return getClearColor(null);
        }

        public double getClearColorX() {
            return mClearColorX;
        }

        public void setClearColorX(double clearColorX) {
            mClearColorX = clearColorX;
        }

        public double getClearColorY() {
            return mClearColorY;
        }

        public void setClearColorY(double clearColorY) {
            mClearColorY = clearColorY;
        }

        public double getClearColorZ() {
            return mClearColorZ;
        }

        public void setClearColorZ(double clearColorZ) {
            mClearColorZ = clearColorZ;
        }

        public double getClearColorW() {
            return mClearColorW;
        }

        public void setClearColorW(double clearColorW) {
            mClearColorW = clearColorW;
        }

        public void setClearStencil(@IntRange(from = 0) int clearStencil) {
            this.clearStencil = clearStencil;
        }

        @IntRange(from = 0) public int getClearStencil() {
            return clearStencil;
        }

        public void setClear(boolean clear) {
            this.clear = clear;
        }

        public boolean getClear() {
            return clear;
        }

        public void setDiscard(boolean discard) {
            this.discard = discard;
        }

        public boolean getDiscard() {
            return discard;
        }

    }


    private static native int nGetFrameInfoHistory(long nativeRenderer, FrameInfo[] outHistory);
    @IntRange(from = 0)
    private static native int nGetMaxFrameHistorySize(long nativeRenderer);
    private static native void nSetDisplayInfo(long nativeRenderer, float infoRefreshRate, long infoPresentationDeadlineNanos, long infoVsyncOffsetNanos);
    private static native void nSetFrameRateOptions(long nativeRenderer, float optionsHeadRoomRatio, float optionsScaleRate, int optionsHistory, int optionsInterval);
    private static native void nSetClearOptions(long nativeRenderer, double optionsClearColorX, double optionsClearColorY, double optionsClearColorZ, double optionsClearColorW, int optionsClearStencil, boolean optionsClear, boolean optionsDiscard);
    private static native void nSetVsyncTime(long nativeRenderer, @IntRange(from = 0) long steadyClockTimeNano);
    private static native void nSkipFrame(long nativeRenderer, @IntRange(from = 0) long vsyncSteadyClockTimeNano);
    private static native boolean nShouldRenderFrame(long nativeRenderer);
    private static native boolean nBeginFrame(long nativeRenderer, long nativeSwapChain, @IntRange(from = 0) long vsyncSteadyClockTimeNano);
    private static native void nSetPresentationTime(long nativeRenderer, long monotonic_clock_ns);
    private static native void nSetDesiredPresentationTime(long nativeRenderer, long monotonic_clock_ns);
    private static native void nSetRenderingDeadline(long nativeRenderer, long monotonic_clock_ns);
    private static native void nRender(long nativeRenderer, long nativeView);
    private static native void nCopyFrame(long nativeRenderer, long nativeDstSwapChain, int dstViewportLeft, int dstViewportBottom, int dstViewportWidth, int dstViewportHeight, int srcViewportLeft, int srcViewportBottom, int srcViewportWidth, int srcViewportHeight, @IntRange(from = 0) int flags);
    private static native void nReadPixels(long nativeRenderer, int xoffset, int yoffset, int width, int height, Buffer storage, int remaining, int left, int top, int type, int alignment, int stride, int format, Object handler, Runnable callback);
    private static native void nEndFrame(long nativeRenderer);
    private static native void nReadPixels(long nativeRenderer, long nativeRenderTarget, int xoffset, int yoffset, int width, int height, Buffer storage, int remaining, int left, int top, int type, int alignment, int stride, int format, Object handler, Runnable callback);
    private static native void nRenderStandaloneView(long nativeRenderer, long nativeView);
    private static native double nGetMaterialTime(long nativeRenderer);
    private static native double nGetUserTime(long nativeRenderer);
    private static native void nSetMaterialTimeEpoch(long nativeRenderer, long monotonic_clock_ns);
    private static native void nResetUserTime(long nativeRenderer);
    private static native void nSkipNextFrames(long nativeRenderer, @IntRange(from = 0) int frameCount);
    @IntRange(from = 0)
    private static native int nGetFrameToSkipCount(long nativeRenderer);
    private static native boolean nHasGpuFallenBehind(long nativeRenderer);
    private static native void nSetFrameScheduleTime(long nativeRenderer, @IntRange(from = 0) long time);
    private static native void nPauseRenderThread(long nativeRenderer, @IntRange(from = 0) long duration_ns);
}

// THIS FILE IS GENERATED BY APIGEN AND MUST NOT BE HAND-MODIFIED.
