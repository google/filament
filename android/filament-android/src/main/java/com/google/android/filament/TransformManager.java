/*
 * Copyright (C) 2026 The Android Open Source Project
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *      http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

// THIS FILE IS GENERATED BY APIGEN AND MUST NOT BE HAND-MODIFIED.

package com.google.android.filament;

import java.lang.IllegalStateException;

import androidx.annotation.IntRange;
import androidx.annotation.NonNull;
import androidx.annotation.Nullable;
import androidx.annotation.RestrictTo;
import androidx.annotation.Size;

/**
 * TransformManager is used to add transform components to entities.
 *
 * <p>A Transform component gives an entity a position and orientation in space in the coordinate
 * space of its parent transform. The TransformManager takes care of computing the world-space
 * transform of each component (i.e. its transform relative to the root).</p>
 *
 * <h1>Creation and destruction</h1>
 *
 * <p>A transform component is created using TransformManager::create() and destroyed by calling
 * TransformManager::destroy().</p>
 *
 * <pre>{@code
 *  filament::Engine* engine = filament::Engine::create();
 *  utils::Entity object = utils::EntityManager.get().create();
 *
 *  auto& tcm = engine->getTransformManager();
 *
 *  // create the transform component
 *  tcm.create(object);
 *
 *  // set its transform
 *  auto i = tcm.getInstance(object);
 *  tcm.setTransform(i, mat4f::translation({ 0, 0, -1 }));
 *
 *  // destroy the transform component
 *  tcm.destroy(object);
 *
 * }</pre>
 */
public class TransformManager {
    private long mNativeObject;

    TransformManager(long nativeObject) {
        mNativeObject = nativeObject;
    }

    @NonNull
    @RestrictTo(RestrictTo.Scope.LIBRARY_GROUP)
    public static TransformManager wrap(long nativeObject) {
        return new TransformManager(nativeObject);
    }

    /**
     * Returns whether a particular Entity is associated with a component of this TransformManager
     *
     * @param e An Entity.
     *
     * @return true if this Entity has a component associated with this manager.
     */
    public boolean hasComponent(@Entity int e) {
        return nHasComponent(getNativeObject(), e);
    }

    /**
     * Gets an Instance representing the transform component associated with the given Entity.
     *
     * <p>Use Instance::isValid() to make sure the component exists.</p>
     *
     * @param e An Entity.
     *
     * @return An Instance object, which represents the transform component associated with the Entity e.
     *
     * @see #hasComponent
     */
    @EntityInstance
    public int getInstance(@Entity int e) {
        return nGetInstance(getNativeObject(), e);
    }

    /** @return the number of Components */
    @IntRange(from = 0)
    public int getComponentCount() {
        return nGetComponentCount(getNativeObject());
    }

    /** @return true if the this manager has no components */
    public boolean empty() {
        return nEmpty(getNativeObject());
    }

    /**
     * Retrieve the {@code Entity} of the component from its {@code Instance}.
     *
     * @param i Instance of the component obtained from getInstance()
     */
    @Entity
    public int getEntity(@EntityInstance int i) {
        return nGetEntity(getNativeObject(), i);
    }

    /**
     * Retrieve the Entities of all the components of this manager.
     *
     * @param out pre-allocated array to receive the items, or null.
     *
     * @return A list, in no particular order, of all the entities managed by this manager.
     */
    @NonNull
    public @Entity int[] getAllEntities(@Nullable @Entity int[] out) {
        return nGetAllEntities(getNativeObject(), out);
    }

    @NonNull
    public @Entity int[] getAllEntities() {
        return getAllEntities(null);
    }

    /**
     * Enables or disable the accurate translation mode.
     *
     * <p>Disabled by default.</p>
     *
     * <p>When accurate translation mode is active, the translation component of all transforms is
     * maintained at double precision. This is only useful if the mat4 version of setTransform()
     * is used, as well as getTransformAccurate().</p>
     *
     * @param enable true to enable the accurate translation mode, false to disable.
     *
     * @see #isAccurateTranslationsEnabled
     * @see #create(int, int, float[])
     * @see #setTransform(int, float[])
     * @see #getTransformAccurate
     * @see #getWorldTransformAccurate
     */
    public void setAccurateTranslationsEnabled(boolean enable) {
        nSetAccurateTranslationsEnabled(getNativeObject(), enable);
    }

    /**
     * Returns whether the high precision translation mode is active.
     *
     * @return true if accurate translations mode is active, false otherwise
     *
     * @see #setAccurateTranslationsEnabled
     */
    public boolean isAccurateTranslationsEnabled() {
        return nIsAccurateTranslationsEnabled(getNativeObject());
    }

    /**
     * Creates a transform component and associate it with the given entity.
     *
     * @param entity An Entity to associate a transform component to.
     * @param parent The Instance of the parent transform, or Instance{} if no parent.
     * @param localTransform <p>The transform to initialize the transform component with.
     *                          This is always relative to the parent.</p>
     *
     * <p>If this component already exists on the given entity, it is first destroyed as if
     * destroy(utils::Entity e) was called.</p>
     *
     * @see #destroy
     */
    public void create(@Entity int entity, @EntityInstance int parent, @NonNull @Size(min = 16) float[] localTransform) {
        Asserts.assertMat4fIn(localTransform);
        nCreate(getNativeObject(), entity, parent, localTransform);
    }

    public void create(@Entity int entity, @EntityInstance int parent, @NonNull @Size(min = 16) double[] localTransform) {
        Asserts.assertMat4dIn(localTransform);
        nCreate(getNativeObject(), entity, parent, localTransform);
    }

    public void create(@Entity int entity) {
        create(entity, 0);
    }

    public void create(@Entity int entity, @EntityInstance int parent) {
        nCreate(getNativeObject(), entity, parent);
    }

    /**
     * Destroys this component from the given entity, children are orphaned.
     *
     * <p>If this transform had children, these are orphaned, which means their local
     * transform becomes a world transform. Usually it's nonsensical. It's recommended to make
     * sure that a destroyed transform doesn't have children.</p>
     *
     * @param e An entity.
     *
     * @see #create
     */
    public void destroy(@Entity int e) {
        nDestroy(getNativeObject(), e);
    }

    /**
     * Re-parents an entity to a new one.
     *
     * @param i The instance of the transform component to re-parent
     * @param newParent The instance of the new parent transform
     * @attention It is an error to re-parent an entity to a descendant and will cause undefined behaviour.
     *
     * @see #getInstance
     */
    public void setParent(@EntityInstance int i, @EntityInstance int newParent) {
        nSetParent(getNativeObject(), i, newParent);
    }

    /**
     * Returns the parent of a transform component, or the null entity if it is a root.
     *
     * @param i The instance of the transform component to query.
     */
    @Entity
    public int getParent(@EntityInstance int i) {
        return nGetParent(getNativeObject(), i);
    }

    /**
     * Returns the number of children of a transform component.
     *
     * @param i The instance of the transform component to query.
     *
     * @return The number of children of the queried component.
     */
    @IntRange(from = 0)
    public int getChildCount(@EntityInstance int i) {
        return nGetChildCount(getNativeObject(), i);
    }

    /**
     * Gets a list of children for a transform component.
     *
     * @param i The instance of the transform component to query.
     * @param children Pointer to array-of-Entity. The array must have at least "count" elements.
     * @param count The maximum number of children to retrieve.
     *
     * @return The number of children written to the pointer.
     */
    @IntRange(from = 0)
    public int getChildren(@EntityInstance int i, @NonNull @Entity int[] children, @IntRange(from = 0) int count) {
        return nGetChildren(getNativeObject(), i, children, count);
    }

    /**
     * Sets a local transform of a transform component.
     *
     * @param ci The instance of the transform component to set the local transform to.
     * @param localTransform The local transform (i.e. relative to the parent).
     * @attention This operation can be slow if the hierarchy of transform is too deep, and this
     *            will be particularly bad when updating a lot of transforms. In that case,
     *            consider using openLocalTransformTransaction() / commitLocalTransformTransaction().
     *
     * @see #getTransform
     */
    public void setTransform(@EntityInstance int ci, @NonNull @Size(min = 16) float[] localTransform) {
        Asserts.assertMat4fIn(localTransform);
        nSetTransform(getNativeObject(), ci, localTransform);
    }

    /**
     * Sets a local transform of a transform component and keeps double precision translation.
     *
     * <p>All other values of the transform are stored at single precision.</p>
     *
     * @param ci The instance of the transform component to set the local transform to.
     * @param localTransform The local transform (i.e. relative to the parent).
     * @attention This operation can be slow if the hierarchy of transform is too deep, and this
     *            will be particularly bad when updating a lot of transforms. In that case,
     *            consider using openLocalTransformTransaction() / commitLocalTransformTransaction().
     *
     * @see #getTransform
     */
    public void setTransform(@EntityInstance int ci, @NonNull @Size(min = 16) double[] localTransform) {
        Asserts.assertMat4dIn(localTransform);
        nSetTransform(getNativeObject(), ci, localTransform);
    }

    /**
     * Returns the local transform of a transform component.
     *
     * @param ci The instance of the transform component to query the local transform from.
     *
     * @return The local transform of the component (i.e. relative to the parent). This always
     *         returns the value set by setTransform().
     *
     * @see #setTransform
     */
    @NonNull @Size(min = 16)
    public float[] getTransform(@EntityInstance int ci, @Nullable @Size(min = 16) float[] out) {
        out = Asserts.assertMat4f(out);
        nGetTransform(getNativeObject(), ci, out);
        return out;
    }

    /**
     * Returns the local transform of a transform component.
     *
     * @param ci The instance of the transform component to query the local transform from.
     *
     * @return The local transform of the component (i.e. relative to the parent). This always
     *         returns the value set by setTransform().
     *
     * @see #setTransform
     */
    @NonNull @Size(min = 16)
    public double[] getTransformAccurate(@EntityInstance int ci, @Nullable @Size(min = 16) double[] out) {
        out = Asserts.assertMat4d(out);
        nGetTransformAccurate(getNativeObject(), ci, out);
        return out;
    }

    /**
     * Return the world transform of a transform component.
     *
     * @param ci The instance of the transform component to query the world transform from.
     *
     * @return The world transform of the component (i.e. relative to the root). This is the
     *         composition of this component's local transform with its parent's world transform.
     *
     * @see #setTransform
     */
    @NonNull @Size(min = 16)
    public float[] getWorldTransform(@EntityInstance int ci, @Nullable @Size(min = 16) float[] out) {
        out = Asserts.assertMat4f(out);
        nGetWorldTransform(getNativeObject(), ci, out);
        return out;
    }

    /**
     * Return the world transform of a transform component.
     *
     * @param ci The instance of the transform component to query the world transform from.
     *
     * @return The world transform of the component (i.e. relative to the root). This is the
     *         composition of this component's local transform with its parent's world transform.
     *
     * @see #setTransform
     */
    @NonNull @Size(min = 16)
    public double[] getWorldTransformAccurate(@EntityInstance int ci, @Nullable @Size(min = 16) double[] out) {
        out = Asserts.assertMat4d(out);
        nGetWorldTransformAccurate(getNativeObject(), ci, out);
        return out;
    }

    /**
     * Opens a local transform transaction.
     *
     * <p>During a transaction, getWorldTransform() can
     * return an invalid transform until commitLocalTransformTransaction() is called. However,
     * setTransform() will perform significantly better and in constant time.</p>
     *
     * <p>This is useful when updating many transforms and the transform hierarchy is deep (say more
     * than 4 or 5 levels).</p>
     *
     * <p>If the local transform transaction is already open, this is a no-op.</p>
     *
     * @see #commitLocalTransformTransaction
     * @see #setTransform
     */
    public void openLocalTransformTransaction() {
        nOpenLocalTransformTransaction(getNativeObject());
    }

    /**
     * Commits the currently open local transform transaction.
     *
     * <p>When this returns, calls
     * to getWorldTransform() will return the proper value.</p>
     *
     * <p>@attention failing to call this method when done updating the local transform will cause
     *            a lot of rendering problems. The system never closes the transaction
     *            automatically.</p>
     *
     * <p>If the local transform transaction is not open, this is a no-op.</p>
     *
     * @see #openLocalTransformTransaction
     * @see #setTransform
     */
    public void commitLocalTransformTransaction() {
        nCommitLocalTransformTransaction(getNativeObject());
    }

    public long getNativeObject() {
        if (mNativeObject == 0) {
            throw new IllegalStateException("Calling method on destroyed TransformManager");
        }
        return mNativeObject;
    }

    void clearNativeObject() {
        mNativeObject = 0;
    }

    private static native boolean nHasComponent(long nativeTransformManager, @Entity int e);
    @EntityInstance
    private static native int nGetInstance(long nativeTransformManager, @Entity int e);
    @IntRange(from = 0)
    private static native int nGetComponentCount(long nativeTransformManager);
    private static native boolean nEmpty(long nativeTransformManager);
    @Entity
    private static native int nGetEntity(long nativeTransformManager, @EntityInstance int i);
    @NonNull
    private static native int[] nGetAllEntities(long nativeTransformManager, int[] out);
    private static native void nSetAccurateTranslationsEnabled(long nativeTransformManager, boolean enable);
    private static native boolean nIsAccurateTranslationsEnabled(long nativeTransformManager);
    private static native void nCreate(long nativeTransformManager, @Entity int entity, @EntityInstance int parent, @NonNull @Size(min = 16) float[] localTransform);
    private static native void nCreate(long nativeTransformManager, @Entity int entity, @EntityInstance int parent, @NonNull @Size(min = 16) double[] localTransform);
    private static native void nCreate(long nativeTransformManager, @Entity int entity, @EntityInstance int parent);
    private static native void nDestroy(long nativeTransformManager, @Entity int e);
    private static native void nSetParent(long nativeTransformManager, @EntityInstance int i, @EntityInstance int newParent);
    @Entity
    private static native int nGetParent(long nativeTransformManager, @EntityInstance int i);
    @IntRange(from = 0)
    private static native int nGetChildCount(long nativeTransformManager, @EntityInstance int i);
    @IntRange(from = 0)
    private static native int nGetChildren(long nativeTransformManager, @EntityInstance int i, @NonNull @Entity int[] children, @IntRange(from = 0) int count);
    private static native void nSetTransform(long nativeTransformManager, @EntityInstance int ci, @NonNull @Size(min = 16) float[] localTransform);
    private static native void nSetTransform(long nativeTransformManager, @EntityInstance int ci, @NonNull @Size(min = 16) double[] localTransform);
    private static native void nGetTransform(long nativeTransformManager, @EntityInstance int ci, @Size(min = 16) float[] out);
    private static native void nGetTransformAccurate(long nativeTransformManager, @EntityInstance int ci, @Size(min = 16) double[] out);
    private static native void nGetWorldTransform(long nativeTransformManager, @EntityInstance int ci, @Size(min = 16) float[] out);
    private static native void nGetWorldTransformAccurate(long nativeTransformManager, @EntityInstance int ci, @Size(min = 16) double[] out);
    private static native void nOpenLocalTransformTransaction(long nativeTransformManager);
    private static native void nCommitLocalTransformTransaction(long nativeTransformManager);
}

// THIS FILE IS GENERATED BY APIGEN AND MUST NOT BE HAND-MODIFIED.
