/*
 * Copyright (C) 2026 The Android Open Source Project
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *      http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

// THIS FILE IS GENERATED BY APIGEN AND MUST NOT BE HAND-MODIFIED.

package com.google.android.filament;

import androidx.annotation.NonNull;
import androidx.annotation.Nullable;
import androidx.annotation.Size;

/** A frustum defined by six planes */
public class Frustum {
    /* package */ final float[] mPlanes = new float[24];

    public enum Plane {
        LEFT,
        RIGHT,
        BOTTOM,
        TOP,
        FAR,
        NEAR;

        /** @return the value of this enum constant as used by the native Filament engine. */
        public int toFilamentNative() { return ordinal(); }
    }

    public Frustum() {
    }

    /**
     * Creates a frustum from a projection matrix in GL convention (usually the projection * view matrix)
     *
     * @param pv a 4x4 projection matrix in GL convention
     */
    public Frustum(@NonNull @Size(min = 16) float[] pv) {
        setProjection(pv);
    }

    /**
     * Sets the frustum from the given projection matrix
     *
     * @param pv a 4x4 projection matrix
     */
    public void setProjection(@NonNull @Size(min = 16) float[] pv) {
        Asserts.assertMat4fIn(pv);
        nSetProjection(mPlanes, pv);
    }

    /**
     * Returns the plane equation parameters with normalized normals
     *
     * @param plane Identifier of the plane to retrieve the equation of
     *
     * @return A plane equation encoded a float4 R such as R.x<i>x + R.y</i>y + R.z*z + R.w = 0
     */
    @NonNull @Size(min = 4)
    public float[] getNormalizedPlane(@NonNull Plane plane, @Nullable @Size(min = 4) float[] out) {
        out = Asserts.assertFloat4(out);
        nGetNormalizedPlane(mPlanes, plane.toFilamentNative(), out);
        return out;
    }

    /**
     * Returns a copy of all six frustum planes in left, right, bottom, top, far, near order
     *
     * @param out pre-allocated array to receive the data, or null.
     */
    @NonNull @Size(min = 24)
    public float[] getNormalizedPlanes(@Nullable @Size(min = 24) float[] out) {
        if (out == null) {
            out = new float[24];
        }
        System.arraycopy(mPlanes, 0, out, 0, 24);
        return out;
    }

    @NonNull @Size(min = 24)
    public float[] getNormalizedPlanes() {
        return getNormalizedPlanes(null);
    }

    /**
     * Returns whether a box intersects the frustum (i.e. is visible)
     *
     * @param box The box to test against the frustum
     *
     * @return true if the box may intersects the frustum, false otherwise. In some situations
     * a box that doesn't intersect the frustum might be reported as though it does. However,
     * a box that does intersect the frustum is always reported correctly (true).
     */
    public boolean intersects(@NonNull Box box) {
        return nIntersects(mPlanes, box.getCenterX(), box.getCenterY(), box.getCenterZ(), box.getHalfExtentX(), box.getHalfExtentY(), box.getHalfExtentZ());
    }

    /**
     * Returns whether a sphere intersects the frustum (i.e. is visible)
     *
     * @param spherex (x component) A sphere encoded as a center + radius.
     * @param spherey (y component) A sphere encoded as a center + radius.
     * @param spherez (z component) A sphere encoded as a center + radius.
     * @param spherew (w component) A sphere encoded as a center + radius.
     *
     * @return true if the sphere may intersects the frustum, false otherwise. In some situations
     * a sphere that doesn't intersect the frustum might be reported as though it does. However,
     * a sphere that does intersect the frustum is always reported correctly (true).
     */
    public boolean intersects(float spherex, float spherey, float spherez, float spherew) {
        return nIntersects(mPlanes, spherex, spherey, spherez, spherew);
    }

    /**
     * Returns whether a sphere intersects the frustum (i.e. is visible)
     *
     * @param sphere A sphere encoded as a center + radius.
     *
     * @return true if the sphere may intersects the frustum, false otherwise. In some situations
     * a sphere that doesn't intersect the frustum might be reported as though it does. However,
     * a sphere that does intersect the frustum is always reported correctly (true).
     */
    public boolean intersects(@NonNull @Size(min = 4) float[] sphere) {
        return intersects(sphere[0], sphere[1], sphere[2], sphere[3]);
    }

    /**
     * Returns whether the frustum contains a given point.
     *
     * @param px (x component) the point to test
     * @param py (y component) the point to test
     * @param pz (z component) the point to test
     *
     * @return the maximum signed distance to the frustum. Negative if p is inside.
     */
    public float contains(float px, float py, float pz) {
        return nContains(mPlanes, px, py, pz);
    }

    /**
     * Returns whether the frustum contains a given point.
     *
     * @param p the point to test
     *
     * @return the maximum signed distance to the frustum. Negative if p is inside.
     */
    public float contains(@NonNull @Size(min = 3) float[] p) {
        return contains(p[0], p[1], p[2]);
    }


    private static native void nSetProjection(float[] planes, @NonNull @Size(min = 16) float[] pv);
    private static native void nGetNormalizedPlane(float[] planes, int plane, @Size(min = 4) float[] out);
    private static native boolean nIntersects(float[] planes, float boxCenterX, float boxCenterY, float boxCenterZ, float boxHalfExtentX, float boxHalfExtentY, float boxHalfExtentZ);
    private static native boolean nIntersects(float[] planes, float spherex, float spherey, float spherez, float spherew);
    private static native float nContains(float[] planes, float px, float py, float pz);
}

// THIS FILE IS GENERATED BY APIGEN AND MUST NOT BE HAND-MODIFIED.
