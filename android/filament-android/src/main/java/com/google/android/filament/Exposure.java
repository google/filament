/*
 * Copyright (C) 2026 The Android Open Source Project
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *      http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

// THIS FILE IS GENERATED BY APIGEN AND MUST NOT BE HAND-MODIFIED.

package com.google.android.filament;

import androidx.annotation.NonNull;

/** A series of utilities to compute exposure, exposure value at ISO 100 (EV100), luminance and illuminance using a physically-based camera model. */
public class Exposure {
    private Exposure() {
    }

    /** Returns the exposure value (EV at ISO 100) of the specified camera. */
    public static float ev100(@NonNull Camera camera) {
        return nEv100(camera.getNativeObject());
    }

    /** Returns the exposure value (EV at ISO 100) of the specified exposure parameters. */
    public static float ev100(float aperture, float shutterSpeed, float sensitivity) {
        return nEv100(aperture, shutterSpeed, sensitivity);
    }

    /** Returns the exposure value (EV at ISO 100) for the given average luminance (in cd / m<sup>2</sup>). */
    public static float ev100FromLuminance(float luminance) {
        return nEv100FromLuminance(luminance);
    }

    /** Returns the exposure value (EV at ISO 100) for the given illuminance (in lux). */
    public static float ev100FromIlluminance(float illuminance) {
        return nEv100FromIlluminance(illuminance);
    }

    /** Returns the photometric exposure for the specified camera. */
    public static float exposure(@NonNull Camera camera) {
        return nExposure(camera.getNativeObject());
    }

    /**
     * Returns the photometric exposure for the specified exposure parameters.
     *
     * <p>This function is equivalent to calling {@code exposure(ev100(aperture, shutterSpeed, sensitivity))}
     * but is slightly faster and offers higher precision.</p>
     */
    public static float exposure(float aperture, float shutterSpeed, float sensitivity) {
        return nExposure(aperture, shutterSpeed, sensitivity);
    }

    /** Returns the photometric exposure for the given EV100. */
    public static float exposure(float ev100) {
        return nExposure(ev100);
    }

    /** Returns the incident luminance in cd / m<sup>2</sup> for the specified camera acting as a spot meter. */
    public static float luminance(@NonNull Camera camera) {
        return nLuminance(camera.getNativeObject());
    }

    /**
     * Returns the incident luminance in cd / m<sup>2</sup> for the specified exposure parameters of a camera acting as a spot meter.
     *
     * <p>This function is equivalent to calling {@code luminance(ev100(aperture, shutterSpeed, sensitivity))}
     * but is slightly faster and offers higher precision.</p>
     */
    public static float luminance(float aperture, float shutterSpeed, float sensitivity) {
        return nLuminance(aperture, shutterSpeed, sensitivity);
    }

    /**
     * Converts the specified EV100 to luminance in cd / m<sup>2</sup>.
     *
     * <p>EV100 is not a measure of luminance, but an EV100 can be used to denote a
     * luminance for which a camera would use said EV100 to obtain the nominally
     * correct exposure</p>
     */
    public static float luminance(float ev100) {
        return nLuminance(ev100);
    }

    /** Returns the illuminance in lux for the specified camera acting as an incident light meter. */
    public static float illuminance(@NonNull Camera camera) {
        return nIlluminance(camera.getNativeObject());
    }

    /**
     * Returns the illuminance in lux for the specified exposure parameters of a camera acting as an incident light meter.
     *
     * <p>This function is equivalent to calling {@code illuminance(ev100(aperture, shutterSpeed, sensitivity))}
     * but is slightly faster and offers higher precision.</p>
     */
    public static float illuminance(float aperture, float shutterSpeed, float sensitivity) {
        return nIlluminance(aperture, shutterSpeed, sensitivity);
    }

    /**
     * Converts the specified EV100 to illuminance in lux.
     *
     * <p>EV100 is not a measure of illuminance, but an EV100 can be used to denote an
     * illuminance for which a camera would use said EV100 to obtain the nominally
     * correct exposure.</p>
     */
    public static float illuminance(float ev100) {
        return nIlluminance(ev100);
    }


    private static native float nEv100(long nativeCamera);
    private static native float nEv100(float aperture, float shutterSpeed, float sensitivity);
    private static native float nEv100FromLuminance(float luminance);
    private static native float nEv100FromIlluminance(float illuminance);
    private static native float nExposure(long nativeCamera);
    private static native float nExposure(float aperture, float shutterSpeed, float sensitivity);
    private static native float nExposure(float ev100);
    private static native float nLuminance(long nativeCamera);
    private static native float nLuminance(float aperture, float shutterSpeed, float sensitivity);
    private static native float nLuminance(float ev100);
    private static native float nIlluminance(long nativeCamera);
    private static native float nIlluminance(float aperture, float shutterSpeed, float sensitivity);
    private static native float nIlluminance(float ev100);
}

// THIS FILE IS GENERATED BY APIGEN AND MUST NOT BE HAND-MODIFIED.
