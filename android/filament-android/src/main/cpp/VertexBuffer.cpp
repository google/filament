/*
 * Copyright (C) 2026 The Android Open Source Project
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *      http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

// THIS FILE IS GENERATED BY APIGEN AND MUST NOT BE HAND-MODIFIED.

#include "common/CallbackUtils.h"
#include "common/NioUtils.h"

#include <filament/Engine.h>
#include <filament/VertexBuffer.h>

#include <backend/BufferDescriptor.h>

#include <utils/CString.h>
#include <utils/ImmutableCString.h>

#include <common/JniUtils.h>
#include <jni.h>

#include <memory>

using namespace filament;
using namespace filament::android;
using namespace utils;

extern "C" JNIEXPORT jint JNICALL
Java_com_google_android_filament_VertexBuffer_nGetVertexCount(JNIEnv *env, jclass clazz, jlong nativeVertexBuffer) {
    VertexBuffer const * const that = (VertexBuffer const *) nativeVertexBuffer;
    return (jint)that->getVertexCount();
}

extern "C" JNIEXPORT jint JNICALL
Java_com_google_android_filament_VertexBuffer_nSetBufferAt(JNIEnv *env, jclass clazz, jlong nativeVertexBuffer, jlong nativeEngine, jint bufferIndex, jobject buffer, jint remaining, jint destOffsetInBytes, jint count, jobject handler, jobject runnable) {
    VertexBuffer *that = (VertexBuffer *) nativeVertexBuffer;
    Engine *engine = (Engine *) nativeEngine;

    return filament::android::wrapJni<jint>(env, [=]() {
        AutoBuffer nioBuffer(env, buffer, count);
        void* data = nioBuffer.getData();
        size_t sizeInBytes = nioBuffer.getSize();
        if (sizeInBytes > (remaining << nioBuffer.getShift())) {
            // BufferOverflowException
            return -1;
        }

        auto* callback = JniBufferCallback::make(engine, env, handler, runnable, std::move(nioBuffer));

        backend::BufferDescriptor desc(data, sizeInBytes,
                callback->getHandler(), &JniBufferCallback::postToJavaAndDestroy, callback);

        that->setBufferAt(*engine, (uint8_t)bufferIndex, std::move(desc), (uint32_t) destOffsetInBytes);

        return 0;
    });
}

extern "C" JNIEXPORT void JNICALL
Java_com_google_android_filament_VertexBuffer_nSetBufferObjectAt(JNIEnv *env, jclass clazz, jlong nativeVertexBuffer, jlong nativeEngine, jint bufferIndex, jlong nativeBufferObject) {
    VertexBuffer* const that = (VertexBuffer*) nativeVertexBuffer;
    Engine* const engine = (Engine*) nativeEngine;
    BufferObject const* const bufferObject = (BufferObject const*) nativeBufferObject;
    wrapJni(env, [=]() {
        that->setBufferObjectAt(*engine, (uint8_t)bufferIndex, bufferObject);
    });
}

extern "C" JNIEXPORT jboolean JNICALL
Java_com_google_android_filament_VertexBuffer_nIsCreationComplete(JNIEnv *env, jclass clazz, jlong nativeVertexBuffer) {
    VertexBuffer const * const that = (VertexBuffer const *) nativeVertexBuffer;
    return (jboolean)that->isCreationComplete();
}

extern "C" JNIEXPORT jlong JNICALL
Java_com_google_android_filament_VertexBuffer_nCreateBuilder(JNIEnv *env, jclass clazz) {
    return (jlong) new VertexBuffer::Builder{};
}

extern "C" JNIEXPORT void JNICALL
Java_com_google_android_filament_VertexBuffer_nDestroyBuilder(JNIEnv *env, jclass clazz, jlong nativeBuilder) {
    VertexBuffer::Builder* builder = (VertexBuffer::Builder*) nativeBuilder;
    delete builder;
}

extern "C" JNIEXPORT void JNICALL
Java_com_google_android_filament_VertexBuffer_nBuilderBufferCount(JNIEnv *env, jclass clazz, jlong nativeBuilder, jint bufferCount) {
    VertexBuffer::Builder* builder = (VertexBuffer::Builder*) nativeBuilder;
    builder->bufferCount((uint8_t)bufferCount);
}

extern "C" JNIEXPORT void JNICALL
Java_com_google_android_filament_VertexBuffer_nBuilderVertexCount(JNIEnv *env, jclass clazz, jlong nativeBuilder, jint vertexCount) {
    VertexBuffer::Builder* builder = (VertexBuffer::Builder*) nativeBuilder;
    builder->vertexCount((uint32_t)vertexCount);
}

extern "C" JNIEXPORT void JNICALL
Java_com_google_android_filament_VertexBuffer_nBuilderEnableBufferObjects(JNIEnv *env, jclass clazz, jlong nativeBuilder, jboolean enabled) {
    VertexBuffer::Builder* builder = (VertexBuffer::Builder*) nativeBuilder;
    builder->enableBufferObjects((bool)enabled);
}

extern "C" JNIEXPORT void JNICALL
Java_com_google_android_filament_VertexBuffer_nBuilderAttribute(JNIEnv *env, jclass clazz, jlong nativeBuilder, jint attribute, jint bufferIndex, jint attributeType, jint byteOffset, jint byteStride) {
    VertexBuffer::Builder* builder = (VertexBuffer::Builder*) nativeBuilder;
    builder->attribute((VertexBuffer::VertexAttribute)attribute, (uint8_t)bufferIndex, (VertexBuffer::AttributeType)attributeType, (uint32_t)byteOffset, (uint8_t)byteStride);
}

extern "C" JNIEXPORT void JNICALL
Java_com_google_android_filament_VertexBuffer_nBuilderNormalized(JNIEnv *env, jclass clazz, jlong nativeBuilder, jint attribute, jboolean normalized) {
    VertexBuffer::Builder* builder = (VertexBuffer::Builder*) nativeBuilder;
    builder->normalized((VertexBuffer::VertexAttribute)attribute, (bool)normalized);
}

extern "C" JNIEXPORT void JNICALL
Java_com_google_android_filament_VertexBuffer_nBuilderAdvancedSkinning(JNIEnv *env, jclass clazz, jlong nativeBuilder, jboolean enabled) {
    VertexBuffer::Builder* builder = (VertexBuffer::Builder*) nativeBuilder;
    builder->advancedSkinning((bool)enabled);
}

extern "C" JNIEXPORT void JNICALL
Java_com_google_android_filament_VertexBuffer_nBuilderName(JNIEnv *env, jclass clazz, jlong nativeBuilder, jstring name_) {
    VertexBuffer::Builder* builder = (VertexBuffer::Builder*) nativeBuilder;
    char const * const name = env->GetStringUTFChars(name_, nullptr);
    builder->name(utils::ImmutableCString(name));
    env->ReleaseStringUTFChars(name_, name);
}

extern "C" JNIEXPORT jlong JNICALL
Java_com_google_android_filament_VertexBuffer_nBuilderBuild(JNIEnv *env, jclass clazz, jlong nativeBuilder, jlong nativeEngine) {
    VertexBuffer::Builder* builder = (VertexBuffer::Builder*) nativeBuilder;
    Engine* engine = (Engine*) nativeEngine;
    return filament::android::wrapJni<jlong>(env, [=]() {
        return (jlong) builder->build(*engine);
    });
}


// THIS FILE IS GENERATED BY APIGEN AND MUST NOT BE HAND-MODIFIED.
