/*
 * Copyright (C) 2026 The Android Open Source Project
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *      http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

// THIS FILE IS GENERATED BY APIGEN AND MUST NOT BE HAND-MODIFIED.

#include "common/CallbackUtils.h"

#include <filament/Engine.h>
#include <filament/SwapChain.h>

#include <utils/tribool.h>

#include <common/JniUtils.h>
#include <jni.h>

using namespace filament;
using namespace filament::android;
using namespace utils;

extern "C" JNIEXPORT jboolean JNICALL
Java_com_google_android_filament_SwapChain_nIsProtectedContentSupported(JNIEnv *env, jclass clazz, jlong nativeEngine) {
    Engine* const engine = (Engine*) nativeEngine;
    return (jboolean)SwapChain::isProtectedContentSupported(*engine);
}

extern "C" JNIEXPORT jboolean JNICALL
Java_com_google_android_filament_SwapChain_nIsSRGBSwapChainSupported(JNIEnv *env, jclass clazz, jlong nativeEngine) {
    Engine* const engine = (Engine*) nativeEngine;
    return (jboolean)SwapChain::isSRGBSwapChainSupported(*engine);
}

extern "C" JNIEXPORT jboolean JNICALL
Java_com_google_android_filament_SwapChain_nIsMSAASwapChainSupported(JNIEnv *env, jclass clazz, jlong nativeEngine, jint samples) {
    Engine* const engine = (Engine*) nativeEngine;
    return (jboolean)SwapChain::isMSAASwapChainSupported(*engine, (uint32_t)samples);
}

extern "C" JNIEXPORT jboolean JNICALL
Java_com_google_android_filament_SwapChain_nIsFrameRateChangeSupported(JNIEnv *env, jclass clazz, jlong nativeSwapChain) {
    SwapChain const * const that = (SwapChain const *) nativeSwapChain;
    return (jboolean)((that->isFrameRateChangeSupported()).is_true());
}

extern "C" JNIEXPORT void JNICALL
Java_com_google_android_filament_SwapChain_nSetFrameRate(JNIEnv *env, jclass clazz, jlong nativeSwapChain, jfloat frameRate, jint compatibility, jint strategy) {
    SwapChain* const that = (SwapChain*) nativeSwapChain;
    that->setFrameRate(frameRate, (SwapChain::FrameRateCompatibility)compatibility, (SwapChain::ChangeFrameRateStrategy)strategy);
}

extern "C" JNIEXPORT void JNICALL
Java_com_google_android_filament_SwapChain_nSetFrameScheduledCallback(JNIEnv *env, jclass clazz, jlong nativeSwapChain, jobject handler, jobject callback, jlong flags) {
    SwapChain* that = (SwapChain*) nativeSwapChain;
    JniCallback* jniCallback = JniCallback::make(env, handler, callback);
    filament::android::wrapJni<void>(env, [=]() {
        that->setFrameScheduledCallback(jniCallback->getHandler(), [jniCallback](auto&&...) { JniCallback::postToJavaAndDestroy(jniCallback); }, (uint64_t)flags);
    });
}

extern "C" JNIEXPORT jboolean JNICALL
Java_com_google_android_filament_SwapChain_nIsFrameScheduledCallbackSet(JNIEnv *env, jclass clazz, jlong nativeSwapChain) {
    SwapChain const * const that = (SwapChain const *) nativeSwapChain;
    return (jboolean)that->isFrameScheduledCallbackSet();
}

extern "C" JNIEXPORT void JNICALL
Java_com_google_android_filament_SwapChain_nSetFrameCompletedCallback(JNIEnv *env, jclass clazz, jlong nativeSwapChain, jobject handler, jobject callback) {
    SwapChain* that = (SwapChain*) nativeSwapChain;
    JniCallback* jniCallback = JniCallback::make(env, handler, callback);
    that->setFrameCompletedCallback(jniCallback->getHandler(), [jniCallback](auto&&...) { JniCallback::postToJavaAndDestroy(jniCallback); });
}

// THIS FILE IS GENERATED BY APIGEN AND MUST NOT BE HAND-MODIFIED.
