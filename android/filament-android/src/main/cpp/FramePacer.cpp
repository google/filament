/*
 * Copyright (C) 2026 The Android Open Source Project
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *      http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

// THIS FILE IS GENERATED BY APIGEN AND MUST NOT BE HAND-MODIFIED.

#include <filament/Engine.h>
#include <filament/FramePacer.h>

#include <common/JniUtils.h>
#include <jni.h>

#include <chrono>

using namespace filament;
using namespace filament::android;
using namespace utils;

extern "C" JNIEXPORT void JNICALL
Java_com_google_android_filament_FramePacer_nConfigure(JNIEnv *env, jclass clazz, jlong nativeFramePacer, jfloat configTargetFrameRate, jlong configLatency) {
    FramePacer* const that = (FramePacer*) nativeFramePacer;
    FramePacer::Configuration config;
    config.targetFrameRate = configTargetFrameRate;
    config.latency = std::chrono::nanoseconds(configLatency);
    wrapJni(env, [=]() {
        that->configure(config);
    });
}

extern "C" JNIEXPORT jint JNICALL
Java_com_google_android_filament_FramePacer_nSetupFrame(JNIEnv *env, jclass clazz, jlong nativeFramePacer, jlong tickBaseTime, jlong tickVsyncPeriod, jlong tickFrameScheduleTime, jlongArray tickTimelines, jint tickTimelinesCount) {
    FramePacer* const that = (FramePacer*) nativeFramePacer;
    FramePacer::VsyncTick tick;
    tick.baseTime = std::chrono::steady_clock::time_point(std::chrono::nanoseconds(tickBaseTime));
    tick.vsyncPeriod = std::chrono::nanoseconds(tickVsyncPeriod);
    tick.frameScheduleTime = std::chrono::steady_clock::time_point(std::chrono::nanoseconds(tickFrameScheduleTime));
    jlong* tickTimelinesElements = nullptr;
    if (tickTimelines != nullptr && tickTimelinesCount > 0) {
        tickTimelinesElements = env->GetLongArrayElements(tickTimelines, nullptr);
        auto* structs = reinterpret_cast<FramePacer::HardwareTimeline*>(tickTimelinesElements);
        tick.timelines = { structs, (size_t) tickTimelinesCount };
    }
    jint const result = wrapJni<jint>(env, [=]() {
        return (jint)that->setupFrame(tick);
    });
    if (tickTimelinesElements != nullptr) {
        env->ReleaseLongArrayElements(tickTimelines, tickTimelinesElements, JNI_ABORT);
    }
    return result;
}

extern "C" JNIEXPORT jboolean JNICALL
Java_com_google_android_filament_FramePacer_nSetupExtraFrame(JNIEnv *env, jclass clazz, jlong nativeFramePacer) {
    FramePacer* const that = (FramePacer*) nativeFramePacer;
    return (jboolean)that->setupExtraFrame();
}

extern "C" JNIEXPORT jboolean JNICALL
Java_com_google_android_filament_FramePacer_nHasGpuFallenBehind(JNIEnv *env, jclass clazz, jlong nativeFramePacer, jlong nativeRenderer) {
    FramePacer* const that = (FramePacer*) nativeFramePacer;
    Renderer* const renderer = (Renderer*) nativeRenderer;
    return wrapJni<jboolean>(env, [=]() {
        return (jboolean)that->hasGpuFallenBehind(renderer);
    });
}

extern "C" JNIEXPORT void JNICALL
Java_com_google_android_filament_FramePacer_nApplyPresentationTime(JNIEnv *env, jclass clazz, jlong nativeFramePacer, jlong nativeRenderer) {
    FramePacer* const that = (FramePacer*) nativeFramePacer;
    Renderer* const renderer = (Renderer*) nativeRenderer;
    wrapJni(env, [=]() {
        that->applyPresentationTime(renderer);
    });
}

extern "C" JNIEXPORT jlong JNICALL
Java_com_google_android_filament_FramePacer_nGetExpectedPresentationTime(JNIEnv *env, jclass clazz, jlong nativeFramePacer) {
    FramePacer const * const that = (FramePacer const *) nativeFramePacer;
    return (jlong)that->getExpectedPresentationTime().time_since_epoch().count();
}

extern "C" JNIEXPORT jlong JNICALL
Java_com_google_android_filament_FramePacer_nGetRenderingDeadline(JNIEnv *env, jclass clazz, jlong nativeFramePacer) {
    FramePacer const * const that = (FramePacer const *) nativeFramePacer;
    return (jlong)that->getRenderingDeadline().time_since_epoch().count();
}

extern "C" JNIEXPORT jlong JNICALL
Java_com_google_android_filament_FramePacer_nGetEffectiveLatency(JNIEnv *env, jclass clazz, jlong nativeFramePacer) {
    FramePacer const * const that = (FramePacer const *) nativeFramePacer;
    return (jlong)that->getEffectiveLatency().count();
}

extern "C" JNIEXPORT jint JNICALL
Java_com_google_android_filament_FramePacer_nGetPacingStatus(JNIEnv *env, jclass clazz, jlong nativeFramePacer) {
    FramePacer const * const that = (FramePacer const *) nativeFramePacer;
    return (jint)that->getPacingStatus();
}

extern "C" JNIEXPORT void JNICALL
Java_com_google_android_filament_FramePacer_nResetPacing(JNIEnv *env, jclass clazz, jlong nativeFramePacer) {
    FramePacer* const that = (FramePacer*) nativeFramePacer;
    that->resetPacing();
}

extern "C" JNIEXPORT jfloat JNICALL
Java_com_google_android_filament_FramePacer_nGetSelectedFrameRate(JNIEnv *env, jclass clazz, jlong nativeFramePacer) {
    FramePacer const * const that = (FramePacer const *) nativeFramePacer;
    return (jfloat)that->getSelectedFrameRate();
}

extern "C" JNIEXPORT jboolean JNICALL
Java_com_google_android_filament_FramePacer_nIsExactFrameRateAchieved(JNIEnv *env, jclass clazz, jlong nativeFramePacer) {
    FramePacer const * const that = (FramePacer const *) nativeFramePacer;
    return (jboolean)that->isExactFrameRateAchieved();
}

extern "C" JNIEXPORT jlong JNICALL
Java_com_google_android_filament_FramePacer_nCreateBuilder(JNIEnv *env, jclass clazz) {
    return (jlong) new FramePacer::Builder{};
}

extern "C" JNIEXPORT void JNICALL
Java_com_google_android_filament_FramePacer_nDestroyBuilder(JNIEnv *env, jclass clazz, jlong nativeBuilder) {
    FramePacer::Builder* builder = (FramePacer::Builder*) nativeBuilder;
    delete builder;
}

extern "C" JNIEXPORT void JNICALL
Java_com_google_android_filament_FramePacer_nBuilderTargetFrameRate(JNIEnv *env, jclass clazz, jlong nativeBuilder, jfloat fps) {
    FramePacer::Builder* builder = (FramePacer::Builder*) nativeBuilder;
    builder->targetFrameRate(fps);
}

extern "C" JNIEXPORT void JNICALL
Java_com_google_android_filament_FramePacer_nBuilderLatency(JNIEnv *env, jclass clazz, jlong nativeBuilder, jlong latency) {
    FramePacer::Builder* builder = (FramePacer::Builder*) nativeBuilder;
    builder->latency(std::chrono::nanoseconds(latency));
}

extern "C" JNIEXPORT void JNICALL
Java_com_google_android_filament_FramePacer_nBuilderLatencyFrames(JNIEnv *env, jclass clazz, jlong nativeBuilder, jint frames) {
    FramePacer::Builder* builder = (FramePacer::Builder*) nativeBuilder;
    builder->latencyFrames((uint32_t)frames);
}

extern "C" JNIEXPORT jlong JNICALL
Java_com_google_android_filament_FramePacer_nBuilderBuild(JNIEnv *env, jclass clazz, jlong nativeBuilder, jlong nativeEngine) {
    FramePacer::Builder* builder = (FramePacer::Builder*) nativeBuilder;
    Engine* engine = (Engine*) nativeEngine;
    return filament::android::wrapJni<jlong>(env, [=]() {
        return (jlong) builder->build(*engine);
    });
}


// THIS FILE IS GENERATED BY APIGEN AND MUST NOT BE HAND-MODIFIED.
