/*
 * Copyright (C) 2026 The Android Open Source Project
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *      http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

// THIS FILE IS GENERATED BY APIGEN AND MUST NOT BE HAND-MODIFIED.

#include "common/NioUtils.h"

#include <filament/Engine.h>
#include <filament/RenderableManager.h>
#include <filament/SkinningBuffer.h>

#include <utils/CString.h>
#include <utils/ImmutableCString.h>
#include <utils/Slice.h>

#include <math/mat4.h>

#include <common/JniUtils.h>
#include <jni.h>

using namespace filament;
using namespace filament::android;
using namespace filament::math;
using namespace utils;

extern "C" JNIEXPORT jint JNICALL
Java_com_google_android_filament_SkinningBuffer_nSetBonesAsQuaternions__JJLjava_nio_Buffer_2III(JNIEnv *env, jclass clazz, jlong nativeSkinningBuffer, jlong nativeEngine, jobject transforms_, jint remaining, jint count, jint offset) {
    SkinningBuffer* const that = (SkinningBuffer*) nativeSkinningBuffer;
    Engine* const engine = (Engine*) nativeEngine;
    return filament::android::wrapJni<jint>(env, [=]() {
        AutoBuffer nioBuffer(env, transforms_, count * 8);
        void* data = nioBuffer.getData();
        size_t sizeInBytes = nioBuffer.getSize();
        if (sizeInBytes > (remaining << nioBuffer.getShift())) {
            // BufferOverflowException
            return -1;
        }
        that->setBones(*engine, utils::Slice<RenderableManager::Bone const>(static_cast<RenderableManager::Bone const *>(data), (size_t)count), (size_t)offset);
        return 0;
    });
}

extern "C" JNIEXPORT void JNICALL
Java_com_google_android_filament_SkinningBuffer_nSetBonesAsQuaternions__JJ_3FIII(JNIEnv *env, jclass clazz, jlong nativeSkinningBuffer, jlong nativeEngine, jfloatArray transforms_, jint count, jint offset, jint arrayOffset) {
    SkinningBuffer* const that = (SkinningBuffer*) nativeSkinningBuffer;
    Engine* const engine = (Engine*) nativeEngine;
    jfloat const * const transforms = env->GetFloatArrayElements(transforms_, nullptr);
    wrapJni(env, [=]() {
        that->setBones(*engine, utils::Slice<RenderableManager::Bone const>(reinterpret_cast<RenderableManager::Bone const *>(transforms) + arrayOffset, (size_t)count), (size_t)offset);
    });
    env->ReleaseFloatArrayElements(transforms_, const_cast<jfloat *>(transforms), JNI_ABORT);
}

extern "C" JNIEXPORT jint JNICALL
Java_com_google_android_filament_SkinningBuffer_nSetBonesAsMatrices__JJLjava_nio_Buffer_2III(JNIEnv *env, jclass clazz, jlong nativeSkinningBuffer, jlong nativeEngine, jobject transforms_, jint remaining, jint count, jint offset) {
    SkinningBuffer* const that = (SkinningBuffer*) nativeSkinningBuffer;
    Engine* const engine = (Engine*) nativeEngine;
    return filament::android::wrapJni<jint>(env, [=]() {
        AutoBuffer nioBuffer(env, transforms_, count * 16);
        void* data = nioBuffer.getData();
        size_t sizeInBytes = nioBuffer.getSize();
        if (sizeInBytes > (remaining << nioBuffer.getShift())) {
            // BufferOverflowException
            return -1;
        }
        that->setBones(*engine, utils::Slice<math::mat4f const>(static_cast<math::mat4f const *>(data), (size_t)count), (size_t)offset);
        return 0;
    });
}

extern "C" JNIEXPORT void JNICALL
Java_com_google_android_filament_SkinningBuffer_nSetBonesAsMatrices__JJ_3FIII(JNIEnv *env, jclass clazz, jlong nativeSkinningBuffer, jlong nativeEngine, jfloatArray transforms_, jint count, jint offset, jint arrayOffset) {
    SkinningBuffer* const that = (SkinningBuffer*) nativeSkinningBuffer;
    Engine* const engine = (Engine*) nativeEngine;
    jfloat const * const transforms = env->GetFloatArrayElements(transforms_, nullptr);
    wrapJni(env, [=]() {
        that->setBones(*engine, utils::Slice<math::mat4f const>(reinterpret_cast<math::mat4f const *>(transforms) + arrayOffset, (size_t)count), (size_t)offset);
    });
    env->ReleaseFloatArrayElements(transforms_, const_cast<jfloat *>(transforms), JNI_ABORT);
}

extern "C" JNIEXPORT jint JNICALL
Java_com_google_android_filament_SkinningBuffer_nGetBoneCount(JNIEnv *env, jclass clazz, jlong nativeSkinningBuffer) {
    SkinningBuffer const * const that = (SkinningBuffer const *) nativeSkinningBuffer;
    return (jint)that->getBoneCount();
}

extern "C" JNIEXPORT jlong JNICALL
Java_com_google_android_filament_SkinningBuffer_nCreateBuilder(JNIEnv *env, jclass clazz) {
    return (jlong) new SkinningBuffer::Builder{};
}

extern "C" JNIEXPORT void JNICALL
Java_com_google_android_filament_SkinningBuffer_nDestroyBuilder(JNIEnv *env, jclass clazz, jlong nativeBuilder) {
    SkinningBuffer::Builder* builder = (SkinningBuffer::Builder*) nativeBuilder;
    delete builder;
}

extern "C" JNIEXPORT void JNICALL
Java_com_google_android_filament_SkinningBuffer_nBuilderBoneCount(JNIEnv *env, jclass clazz, jlong nativeBuilder, jint boneCount) {
    SkinningBuffer::Builder* builder = (SkinningBuffer::Builder*) nativeBuilder;
    builder->boneCount((uint32_t)boneCount);
}

extern "C" JNIEXPORT void JNICALL
Java_com_google_android_filament_SkinningBuffer_nBuilderInitialize(JNIEnv *env, jclass clazz, jlong nativeBuilder, jboolean initialize) {
    SkinningBuffer::Builder* builder = (SkinningBuffer::Builder*) nativeBuilder;
    builder->initialize((bool)initialize);
}

extern "C" JNIEXPORT void JNICALL
Java_com_google_android_filament_SkinningBuffer_nBuilderName(JNIEnv *env, jclass clazz, jlong nativeBuilder, jstring name_) {
    SkinningBuffer::Builder* builder = (SkinningBuffer::Builder*) nativeBuilder;
    char const * const name = env->GetStringUTFChars(name_, nullptr);
    builder->name(utils::ImmutableCString(name));
    env->ReleaseStringUTFChars(name_, name);
}

extern "C" JNIEXPORT jlong JNICALL
Java_com_google_android_filament_SkinningBuffer_nBuilderBuild(JNIEnv *env, jclass clazz, jlong nativeBuilder, jlong nativeEngine) {
    SkinningBuffer::Builder* builder = (SkinningBuffer::Builder*) nativeBuilder;
    Engine* engine = (Engine*) nativeEngine;
    return filament::android::wrapJni<jlong>(env, [=]() {
        return (jlong) builder->build(*engine);
    });
}


// THIS FILE IS GENERATED BY APIGEN AND MUST NOT BE HAND-MODIFIED.
