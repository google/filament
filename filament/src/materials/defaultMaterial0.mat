material {
    name : "Filament Default Material",
    shadingModel : unlit,
    featureLevel: 0,
    variantFilter : [ stereo ]
}

fragment {
    void material(inout MaterialInputs material) {
        prepareMaterial(material);
        material.baseColor.rgb = vec3(0.8);
    }
}
