/*
 * Copyright (C) 2026 The Android Open Source Project
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *      http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

// THIS FILE IS GENERATED BY APIGEN AND MUST NOT BE HAND-MODIFIED.

package com.google.android.filament;

import java.lang.IllegalStateException;

import java.nio.Buffer;
import java.nio.BufferOverflowException;
import java.nio.ReadOnlyBufferException;

import androidx.annotation.IntRange;
import androidx.annotation.NonNull;
import androidx.annotation.Nullable;
import androidx.annotation.RestrictTo;

public class PixelBufferDescriptorTest {
    private long mNativeObject;

    PixelBufferDescriptorTest(long nativeObject) {
        mNativeObject = nativeObject;
    }

    @NonNull
    @RestrictTo(RestrictTo.Scope.LIBRARY_GROUP)
    public static PixelBufferDescriptorTest wrap(long nativeObject) {
        return new PixelBufferDescriptorTest(nativeObject);
    }

    public void readPixels(@IntRange(from = 0) int xoffset, @IntRange(from = 0) int yoffset, @IntRange(from = 0) int width, @IntRange(from = 0) int height, @NonNull PixelBufferDescriptor buffer) {
        if (buffer.storage.isReadOnly()) {
            throw new ReadOnlyBufferException();
        }
        int type = (buffer.type != null) ? buffer.type.ordinal() : 0;
        int format = (buffer.type == Texture.Type.COMPRESSED) ?
                (buffer.compressedFormat != null ? buffer.compressedFormat.ordinal() : 0) :
                (buffer.format != null ? buffer.format.ordinal() : 0);
        int stride = (buffer.type == Texture.Type.COMPRESSED) ? buffer.compressedSizeInBytes : buffer.stride;
        nReadPixels(getNativeObject(), xoffset, yoffset, width, height, buffer.storage, buffer.storage.remaining(),
                buffer.left, buffer.top, type, buffer.alignment,
                stride, format,
                buffer.handler, buffer.callback);
    }

    public void readPixels(@NonNull RenderTarget renderTarget, @IntRange(from = 0) int xoffset, @IntRange(from = 0) int yoffset, @IntRange(from = 0) int width, @IntRange(from = 0) int height, @NonNull PixelBufferDescriptor buffer) {
        if (buffer.storage.isReadOnly()) {
            throw new ReadOnlyBufferException();
        }
        int type = (buffer.type != null) ? buffer.type.ordinal() : 0;
        int format = (buffer.type == Texture.Type.COMPRESSED) ?
                (buffer.compressedFormat != null ? buffer.compressedFormat.ordinal() : 0) :
                (buffer.format != null ? buffer.format.ordinal() : 0);
        int stride = (buffer.type == Texture.Type.COMPRESSED) ? buffer.compressedSizeInBytes : buffer.stride;
        nReadPixels(getNativeObject(), renderTarget.getNativeObject(), xoffset, yoffset, width, height, buffer.storage, buffer.storage.remaining(),
                buffer.left, buffer.top, type, buffer.alignment,
                stride, format,
                buffer.handler, buffer.callback);
    }

    public void setImage(@NonNull Engine engine, @IntRange(from = 0) int level, @IntRange(from = 0) int xoffset, @IntRange(from = 0) int yoffset, @IntRange(from = 0) int width, @IntRange(from = 0) int height, @NonNull PixelBufferDescriptor buffer) {
        int type = (buffer.type != null) ? buffer.type.ordinal() : 0;
        int format = (buffer.type == Texture.Type.COMPRESSED) ?
                (buffer.compressedFormat != null ? buffer.compressedFormat.ordinal() : 0) :
                (buffer.format != null ? buffer.format.ordinal() : 0);
        int stride = (buffer.type == Texture.Type.COMPRESSED) ? buffer.compressedSizeInBytes : buffer.stride;
        nSetImage(getNativeObject(), engine.getNativeObject(), level, xoffset, yoffset, width, height, buffer.storage, buffer.storage.remaining(),
                buffer.left, buffer.top, type, buffer.alignment,
                stride, format,
                buffer.handler, buffer.callback);
    }

    public long getNativeObject() {
        if (mNativeObject == 0) {
            throw new IllegalStateException("Calling method on destroyed PixelBufferDescriptorTest");
        }
        return mNativeObject;
    }

    void clearNativeObject() {
        mNativeObject = 0;
    }

    private static native void nReadPixels(long nativePixelBufferDescriptorTest, int xoffset, int yoffset, int width, int height, Buffer storage, int remaining, int left, int top, int type, int alignment, int stride, int format, Object handler, Runnable callback);
    private static native void nReadPixels(long nativePixelBufferDescriptorTest, long nativeRenderTarget, int xoffset, int yoffset, int width, int height, Buffer storage, int remaining, int left, int top, int type, int alignment, int stride, int format, Object handler, Runnable callback);
    private static native void nSetImage(long nativePixelBufferDescriptorTest, long nativeEngine, int level, int xoffset, int yoffset, int width, int height, Buffer storage, int remaining, int left, int top, int type, int alignment, int stride, int format, Object handler, Runnable callback);
}

// THIS FILE IS GENERATED BY APIGEN AND MUST NOT BE HAND-MODIFIED.
