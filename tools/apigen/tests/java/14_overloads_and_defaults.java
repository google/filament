/*
 * Copyright (C) 2026 The Android Open Source Project
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *      http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

// THIS FILE IS GENERATED BY APIGEN AND MUST NOT BE HAND-MODIFIED.

package com.google.android.filament;

import java.lang.IllegalStateException;

import androidx.annotation.NonNull;
import androidx.annotation.Nullable;
import androidx.annotation.RestrictTo;
import androidx.annotation.Size;

public class OverloadTest {
    private long mNativeObject;

    public enum Mode {
        FAST,
        ACCURATE;

        /** @return the value of this enum constant as used by the native Filament engine. */
        public int toFilamentNative() { return ordinal(); }
    }

    OverloadTest(long nativeObject) {
        mNativeObject = nativeObject;
    }

    @NonNull
    @RestrictTo(RestrictTo.Scope.LIBRARY_GROUP)
    public static OverloadTest wrap(long nativeObject) {
        return new OverloadTest(nativeObject);
    }

    /**
     * Configures the system with optional mode.
     *
     * @param a first parameter
     * @param b second parameter
     */
    public void configure(int a, float b) {
        configure(a, b, Mode.FAST);
    }

    /**
     * Configures the system with optional mode.
     *
     * @param a first parameter
     * @param b second parameter
     * @param mode operating mode
     */
    public void configure(int a, float b, @NonNull Mode mode) {
        nConfigure(getNativeObject(), a, b, mode.toFilamentNative());
    }

    /**
     * Computes projection matrix with default far plane.
     *
     * @param fov field of view
     * @param aspect aspect ratio
     * @param near near plane distance
     * @param out optional array to store the result, or null to allocate a new one
     *
     * @return projection matrix
     */
    @NonNull @Size(min = 16)
    public static double[] projection(double fov, double aspect, double near, @Nullable @Size(min = 16) double[] out) {
        return projection(fov, aspect, near, Double.POSITIVE_INFINITY, out);
    }

    /**
     * Computes projection matrix with default far plane.
     *
     * @param fov field of view
     * @param aspect aspect ratio
     * @param near near plane distance
     * @param far far plane distance
     *
     * @return projection matrix
     */
    @NonNull @Size(min = 16)
    public static double[] projection(double fov, double aspect, double near, double far, @Nullable @Size(min = 16) double[] out) {
        out = Asserts.assertMat4d(out);
        nProjection(fov, aspect, near, far, out);
        return out;
    }

    /**
     * Sets light intensity using simple intensity.
     *
     * @param intensity luminous intensity
     */
    public void setIntensity(float intensity) {
        nSetIntensity(getNativeObject(), intensity);
    }

    /**
     * Sets light intensity using physical power and efficiency.
     *
     * @param watts power in watts
     * @param efficiency luminous efficiency
     */
    public void setIntensity(float watts, float efficiency) {
        nSetIntensity(getNativeObject(), watts, efficiency);
    }

    /**
     * Computes inverse of double-precision matrix.
     *
     * @param m input matrix
     *
     * @return inverted matrix
     */
    @NonNull @Size(min = 16)
    public static double[] inverse(@NonNull @Size(min = 16) double[] m, @Nullable @Size(min = 16) double[] out) {
        Asserts.assertMat4dIn(m);
        out = Asserts.assertMat4d(out);
        nInverse(m, out);
        return out;
    }

    /**
     * Computes inverse of single-precision matrix.
     *
     * @param m input matrix
     *
     * @return inverted matrix
     */
    @NonNull @Size(min = 16)
    public static float[] inverse(@NonNull @Size(min = 16) float[] m, @Nullable @Size(min = 16) float[] out) {
        Asserts.assertMat4fIn(m);
        out = Asserts.assertMat4f(out);
        nInverse(m, out);
        return out;
    }

    public long getNativeObject() {
        if (mNativeObject == 0) {
            throw new IllegalStateException("Calling method on destroyed OverloadTest");
        }
        return mNativeObject;
    }

    void clearNativeObject() {
        mNativeObject = 0;
    }

    private static native void nConfigure(long nativeOverloadTest, int a, float b, int mode);
    private static native void nProjection(double fov, double aspect, double near, double far, @Size(min = 16) double[] out);
    private static native void nSetIntensity(long nativeOverloadTest, float intensity);
    private static native void nSetIntensity(long nativeOverloadTest, float watts, float efficiency);
    private static native void nInverse(@NonNull @Size(min = 16) double[] m, @Size(min = 16) double[] out);
    private static native void nInverse(@NonNull @Size(min = 16) float[] m, @Size(min = 16) float[] out);
}

// THIS FILE IS GENERATED BY APIGEN AND MUST NOT BE HAND-MODIFIED.
