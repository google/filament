/*
 * Copyright (C) 2026 The Android Open Source Project
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *      http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

// THIS FILE IS GENERATED BY APIGEN AND MUST NOT BE HAND-MODIFIED.

package com.google.android.filament;

import java.lang.IllegalStateException;

import java.nio.Buffer;
import java.nio.BufferOverflowException;

import androidx.annotation.IntRange;
import androidx.annotation.NonNull;
import androidx.annotation.Nullable;
import androidx.annotation.RestrictTo;
import androidx.annotation.Size;

public class MathParams {
    private long mNativeObject;

    MathParams(long nativeObject) {
        mNativeObject = nativeObject;
    }

    @NonNull
    @RestrictTo(RestrictTo.Scope.LIBRARY_GROUP)
    public static MathParams wrap(long nativeObject) {
        return new MathParams(nativeObject);
    }

    /**
     * @param i index
     * @param vx (x component) value
     * @param vy (y component) value
     */
    public void setFloat2(int i, float vx, float vy) {
        nSetFloat2(getNativeObject(), i, vx, vy);
    }

    /**
     * @param i index
     * @param v value
     */
    public void setFloat2(int i, @NonNull @Size(min = 2) float[] v) {
        setFloat2(i, v[0], v[1]);
    }

    /**
     * @param vx (x component)
     * @param vy (y component)
     * @param vz (z component)
     */
    public void setFloat3(int i, float vx, float vy, float vz) {
        nSetFloat3(getNativeObject(), i, vx, vy, vz);
    }

    /**
     * @param i
     * @param v
     */
    public void setFloat3(int i, @NonNull @Size(min = 3) float[] v) {
        setFloat3(i, v[0], v[1], v[2]);
    }

    /**
     * @param vx (x component)
     * @param vy (y component)
     * @param vz (z component)
     * @param vw (w component)
     */
    public void setFloat4(int i, float vx, float vy, float vz, float vw) {
        nSetFloat4(getNativeObject(), i, vx, vy, vz, vw);
    }

    /**
     * @param i
     * @param v
     */
    public void setFloat4(int i, @NonNull @Size(min = 4) float[] v) {
        setFloat4(i, v[0], v[1], v[2], v[3]);
    }

    /**
     * @param vx (x component)
     * @param vy (y component)
     */
    public void setDouble2(int i, double vx, double vy) {
        nSetDouble2(getNativeObject(), i, vx, vy);
    }

    /**
     * @param i
     * @param v
     */
    public void setDouble2(int i, @NonNull @Size(min = 2) double[] v) {
        setDouble2(i, v[0], v[1]);
    }

    /**
     * @param vx (x component)
     * @param vy (y component)
     * @param vz (z component)
     */
    public void setDouble3(int i, double vx, double vy, double vz) {
        nSetDouble3(getNativeObject(), i, vx, vy, vz);
    }

    /**
     * @param i
     * @param v
     */
    public void setDouble3(int i, @NonNull @Size(min = 3) double[] v) {
        setDouble3(i, v[0], v[1], v[2]);
    }

    /**
     * @param vx (x component)
     * @param vy (y component)
     * @param vz (z component)
     * @param vw (w component)
     */
    public void setDouble4(int i, double vx, double vy, double vz, double vw) {
        nSetDouble4(getNativeObject(), i, vx, vy, vz, vw);
    }

    /**
     * @param i
     * @param v
     */
    public void setDouble4(int i, @NonNull @Size(min = 4) double[] v) {
        setDouble4(i, v[0], v[1], v[2], v[3]);
    }

    public void setMat2f(int i, @NonNull @Size(min = 4) float[] v) {
        Asserts.assertMat2fIn(v);
        nSetMat2f(getNativeObject(), i, v);
    }

    public void setMat3f(int i, @NonNull @Size(min = 9) float[] v) {
        Asserts.assertMat3fIn(v);
        nSetMat3f(getNativeObject(), i, v);
    }

    public void setMat4f(int i, @NonNull @Size(min = 16) float[] v) {
        Asserts.assertMat4fIn(v);
        nSetMat4f(getNativeObject(), i, v);
    }

    public void setMat2d(int i, @NonNull @Size(min = 4) double[] v) {
        Asserts.assertMat2dIn(v);
        nSetMat2d(getNativeObject(), i, v);
    }

    public void setMat3d(int i, @NonNull @Size(min = 9) double[] v) {
        Asserts.assertMat3dIn(v);
        nSetMat3d(getNativeObject(), i, v);
    }

    public void setMat4d(int i, @NonNull @Size(min = 16) double[] v) {
        Asserts.assertMat4dIn(v);
        nSetMat4d(getNativeObject(), i, v);
    }

    public void setFloat4ptr_nullable(@Nullable @Size(min = 4) float[] p) {
        if (p != null) {
            Asserts.assertFloat4In(p);
        }
        nSetFloat4ptr_nullable(getNativeObject(), p);
    }

    public void setFloat4ptr_nonnull(@NonNull @Size(min = 4) float[] p) {
        Asserts.assertFloat4In(p);
        nSetFloat4ptr_nonnull(getNativeObject(), p);
    }

    public void setFloat4ptr(@Size(min = 4) float[] p) {
        if (p != null) {
            Asserts.assertFloat4In(p);
        }
        nSetFloat4ptr(getNativeObject(), p);
    }

    /**
     * @param count number of elements (structured element count) in <code>p</code>
     * @param p buffer containing p data
     */
    public void setMat4Array(@IntRange(from = 0) int count, @NonNull Buffer p) {
        int result = nSetMat4Array(getNativeObject(), p, p.remaining(), count);
        if (result < 0) {
            throw new BufferOverflowException();
        }
    }

    /**
     * @param p array containing p data
     * @param offset offset in elements (structured element count) in <code>p</code> to skip
     * @param count number of elements (structured element count) in <code>p</code>
     */
    public void setMat4Array(@NonNull @Size(min = 16) double[] p, @IntRange(from = 0) int offset, @IntRange(from = 0) int count) {
        if (p.length < (offset + count) * 16) {
            throw new ArrayIndexOutOfBoundsException("Array length must be at least " + ((offset + count) * 16));
        }
        nSetMat4Array(getNativeObject(), p, count, offset);
    }

    /**
     * @param p array containing p data
     * @param count number of elements (structured element count) in <code>p</code>
     */
    public void setMat4Array(@NonNull @Size(min = 16) double[] p, @IntRange(from = 0) int count) {
        setMat4Array(p, 0, count);
    }

    /** @param p array containing p data */
    public void setMat4Array(@NonNull @Size(min = 16) double[] p) {
        setMat4Array(p, 0, p.length / 16);
    }

    /**
     * @param count number of elements (structured element count) in <code>p</code>
     * @param p buffer containing p data
     */
    public void setMat3fArray(@IntRange(from = 0) int count, @NonNull Buffer p) {
        int result = nSetMat3fArray(getNativeObject(), p, p.remaining(), count);
        if (result < 0) {
            throw new BufferOverflowException();
        }
    }

    /**
     * @param p array containing p data
     * @param offset offset in elements (structured element count) in <code>p</code> to skip
     * @param count number of elements (structured element count) in <code>p</code>
     */
    public void setMat3fArray(@NonNull @Size(min = 9) float[] p, @IntRange(from = 0) int offset, @IntRange(from = 0) int count) {
        if (p.length < (offset + count) * 9) {
            throw new ArrayIndexOutOfBoundsException("Array length must be at least " + ((offset + count) * 9));
        }
        nSetMat3fArray(getNativeObject(), p, count, offset);
    }

    /**
     * @param p array containing p data
     * @param count number of elements (structured element count) in <code>p</code>
     */
    public void setMat3fArray(@NonNull @Size(min = 9) float[] p, @IntRange(from = 0) int count) {
        setMat3fArray(p, 0, count);
    }

    /** @param p array containing p data */
    public void setMat3fArray(@NonNull @Size(min = 9) float[] p) {
        setMat3fArray(p, 0, p.length / 9);
    }

    /**
     * @param i the i
     * @param s the s
     * @param count number of elements (structured element count) in <code>d</code>
     * @param d buffer containing d data
     */
    public void cvtVecsArray(int i, @NonNull float[][] s, @IntRange(from = 0) int count, @NonNull Buffer d) {
        int result = nCvtVecsArray(getNativeObject(), i, s, d, d.remaining(), count);
        if (result < 0) {
            throw new BufferOverflowException();
        }
    }

    /**
     * @param i the i
     * @param s the s
     * @param d array containing d data
     * @param offset offset in elements (structured element count) in <code>d</code> to skip
     * @param count number of elements (structured element count) in <code>d</code>
     */
    public void cvtVecsArray(int i, @NonNull float[][] s, @NonNull @Size(min = 3) float[] d, @IntRange(from = 0) int offset, @IntRange(from = 0) int count) {
        if (d.length < (offset + count) * 3) {
            throw new ArrayIndexOutOfBoundsException("Array length must be at least " + ((offset + count) * 3));
        }
        nCvtVecsArray(getNativeObject(), i, s, d, count, offset);
    }

    /**
     * @param i the i
     * @param s the s
     * @param d array containing d data
     * @param count number of elements (structured element count) in <code>d</code>
     */
    public void cvtVecsArray(int i, @NonNull float[][] s, @NonNull @Size(min = 3) float[] d, @IntRange(from = 0) int count) {
        cvtVecsArray(i, s, d, 0, count);
    }

    /**
     * @param i the i
     * @param s the s
     * @param d array containing d data
     */
    public void cvtVecsArray(int i, @NonNull float[][] s, @NonNull @Size(min = 3) float[] d) {
        cvtVecsArray(i, s, d, 0, d.length / 3);
    }

    public long getNativeObject() {
        if (mNativeObject == 0) {
            throw new IllegalStateException("Calling method on destroyed MathParams");
        }
        return mNativeObject;
    }

    void clearNativeObject() {
        mNativeObject = 0;
    }

    private static native void nSetFloat2(long nativeMathParams, int i, float vx, float vy);
    private static native void nSetFloat3(long nativeMathParams, int i, float vx, float vy, float vz);
    private static native void nSetFloat4(long nativeMathParams, int i, float vx, float vy, float vz, float vw);
    private static native void nSetDouble2(long nativeMathParams, int i, double vx, double vy);
    private static native void nSetDouble3(long nativeMathParams, int i, double vx, double vy, double vz);
    private static native void nSetDouble4(long nativeMathParams, int i, double vx, double vy, double vz, double vw);
    private static native void nSetMat2f(long nativeMathParams, int i, @NonNull @Size(min = 4) float[] v);
    private static native void nSetMat3f(long nativeMathParams, int i, @NonNull @Size(min = 9) float[] v);
    private static native void nSetMat4f(long nativeMathParams, int i, @NonNull @Size(min = 16) float[] v);
    private static native void nSetMat2d(long nativeMathParams, int i, @NonNull @Size(min = 4) double[] v);
    private static native void nSetMat3d(long nativeMathParams, int i, @NonNull @Size(min = 9) double[] v);
    private static native void nSetMat4d(long nativeMathParams, int i, @NonNull @Size(min = 16) double[] v);
    private static native void nSetFloat4ptr_nullable(long nativeMathParams, @Size(min = 4) float[] p);
    private static native void nSetFloat4ptr_nonnull(long nativeMathParams, @NonNull @Size(min = 4) float[] p);
    private static native void nSetFloat4ptr(long nativeMathParams, @Size(min = 4) float[] p);
    private static native int nSetMat4Array(long nativeMathParams, @NonNull Buffer p, int remaining, @IntRange(from = 0) int count);
    private static native void nSetMat4Array(long nativeMathParams, @NonNull @Size(min = 16) double[] p, @IntRange(from = 0) int count, @IntRange(from = 0) int offset);
    private static native int nSetMat3fArray(long nativeMathParams, @NonNull Buffer p, int remaining, @IntRange(from = 0) int count);
    private static native void nSetMat3fArray(long nativeMathParams, @NonNull @Size(min = 9) float[] p, @IntRange(from = 0) int count, @IntRange(from = 0) int offset);
    private static native int nCvtVecsArray(long nativeMathParams, int i, float[][] s, @NonNull Buffer d, int remaining, @IntRange(from = 0) int count);
    private static native void nCvtVecsArray(long nativeMathParams, int i, float[][] s, @NonNull @Size(min = 3) float[] d, @IntRange(from = 0) int count, @IntRange(from = 0) int offset);
}

// THIS FILE IS GENERATED BY APIGEN AND MUST NOT BE HAND-MODIFIED.
