/*
 * Copyright (C) 2026 The Android Open Source Project
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *      http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

// THIS FILE IS GENERATED BY APIGEN AND MUST NOT BE HAND-MODIFIED.

#include <utils/Slice.h>

#include <math/mat4.h>
#include <math/vec4.h>

#include <common/JniUtils.h>
#include <jni.h>
#include <tools/apigen/tests/java/25_tagged_array_buffers.h>

#include <string_view>

using namespace filament;
using namespace filament::android;
using namespace filament::math;
using namespace utils;

extern "C" JNIEXPORT void JNICALL
Java_com_google_android_filament_TaggedArrayBuffersTest_nSetBufferFloatArray(JNIEnv *env, jclass clazz, jlong nativeTaggedArrayBuffersTest, jstring name_, jint element, jfloatArray values_, jint offset, jint count) {
    TaggedArrayBuffersTest* const that = (TaggedArrayBuffersTest*) nativeTaggedArrayBuffersTest;
    const char* const name = env->GetStringUTFChars(name_, nullptr);
    jfloat* values = env->GetFloatArrayElements(values_, nullptr);

    wrapJni(env, [=]() {
        switch (element) {
            case 0: // FLOAT
                that->setBuffer(name, utils::Slice<const float>(((const float*) values) + offset, (size_t)count));
                break;
            case 1: // FLOAT2
                that->setBuffer(name, utils::Slice<const math::float2>(((const math::float2*) values) + offset, (size_t)count));
                break;
            case 2: // FLOAT4
                that->setBuffer(name, utils::Slice<const math::float4>(((const math::float4*) values) + offset, (size_t)count));
                break;
            case 3: // MAT4
                that->setBuffer(name, utils::Slice<const math::mat4f>(((const math::mat4f*) values) + offset, (size_t)count));
                break;
        }
    });

    env->ReleaseFloatArrayElements(values_, values, JNI_ABORT);
    env->ReleaseStringUTFChars(name_, name);
}

extern "C" JNIEXPORT void JNICALL
Java_com_google_android_filament_TaggedArrayBuffersTest_nSetBufferIntArray(JNIEnv *env, jclass clazz, jlong nativeTaggedArrayBuffersTest, jstring name_, jint element, jintArray values_, jint offset, jint count) {
    TaggedArrayBuffersTest* const that = (TaggedArrayBuffersTest*) nativeTaggedArrayBuffersTest;
    const char* const name = env->GetStringUTFChars(name_, nullptr);
    jint* values = env->GetIntArrayElements(values_, nullptr);

    wrapJni(env, [=]() {
        switch (element) {
            case 0: // INT
                that->setBuffer(name, utils::Slice<const int32_t>(((const int32_t*) values) + offset, (size_t)count));
                break;
            case 1: // INT4
                that->setBuffer(name, utils::Slice<const math::int4>(((const math::int4*) values) + offset, (size_t)count));
                break;
        }
    });

    env->ReleaseIntArrayElements(values_, values, JNI_ABORT);
    env->ReleaseStringUTFChars(name_, name);
}

extern "C" JNIEXPORT void JNICALL
Java_com_google_android_filament_TaggedArrayBuffersTest_nSetBufferBooleanArray(JNIEnv *env, jclass clazz, jlong nativeTaggedArrayBuffersTest, jstring name_, jint element, jbooleanArray values_, jint offset, jint count) {
    TaggedArrayBuffersTest* const that = (TaggedArrayBuffersTest*) nativeTaggedArrayBuffersTest;
    const char* const name = env->GetStringUTFChars(name_, nullptr);
    jboolean* values = env->GetBooleanArrayElements(values_, nullptr);

    wrapJni(env, [=]() {
        switch (element) {
            case 0: // BOOL
                that->setBuffer(name, utils::Slice<const bool>(((const bool*) values) + offset, (size_t)count));
                break;
            case 1: // BOOL4
                that->setBuffer(name, utils::Slice<const math::bool4>(((const math::bool4*) values) + offset, (size_t)count));
                break;
        }
    });

    env->ReleaseBooleanArrayElements(values_, values, JNI_ABORT);
    env->ReleaseStringUTFChars(name_, name);
}

// THIS FILE IS GENERATED BY APIGEN AND MUST NOT BE HAND-MODIFIED.
